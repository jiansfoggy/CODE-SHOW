# Architect Output — Phase 3

Project: JudgeE2 (Phase 3: Tap-to-Segment)
Target: iPhone 11
Phase 2 Frozen Baseline: shared_v2/architect_output.md §5

---

# Phase 3 — Day 1 (Architect) Interaction Contract

日期：2026-07-19
依据：tasks.md Phase 3 Day 1 Architect 任务 + Phase 2 冻结契约 + 代码审查（SAMDecoder.swift / PromptBuilder.swift / TemporalManager.swift）

---

## 1) Tap-to-Segment 模式插入点（锁定）

### 1.1 模式并存规则

```
AppMode.detectionOnly   → Phase 1 路径（YOLO bbox-only）
AppMode.segmentation    → Phase 2 路径（YOLO + SAM 自动分割）
AppMode.tapToSegment    → Phase 3 路径（YOLO 保持运行，SAM 由用户 tap 驱动）
```

**关键规则：**
- YOLO Detection Pipeline **永远运行**（三种模式均如此）
- `.tapToSegment` 模式下，YOLO 输出仅用于维持 FrameGeometry 元数据，**不用于驱动 SAM**
- Phase 2 `.segmentation` 路径的调度代码（TemporalManager 主目标选择 + 每 12 帧 encode）在 `.tapToSegment` 模式下**必须静默**（不触发）
- 两种模式**不得同时激活**；模式切换时清空当前模式的所有 mask 实例

### 1.2 插入点位置

```
[Camera Capture] → [Preprocess] → [YOLO Detection]
                                        │
                          ┌─────────────┴──────────────┐
                          │ .segmentation               │ .tapToSegment
                          ▼                             ▼
               [TemporalManager]              [TouchHandler]
               [SAMEncoder (auto)]            [TapInstanceManager]
               [SAMDecoder (bbox)]            [SAMEncoder (on-tap)]
               [MaskRenderer (Phase 2)]       [SAMDecoder (point)]
                                              [TapMaskRenderer]
```

- 插入点：Camera Capture 完成后，两条路径共享同一个 `CVPixelBuffer` 和 `FrameGeometry`
- Phase 3 路径**不接触** TemporalManager；TapInstanceManager 是独立新模块
- Phase 3 有独立的渲染层（`TapMaskRenderer` 或在 MaskRenderer 内新增 tap 渲染分支），位于 Phase 2 mask layer **之上**

### 1.3 渲染层次（Z 轴）

```
最上层：TouchHandler 手势识别层（透明，仅捕获手势）
       ↑
       TapMaskRenderer（tap 实例 mask 叠加，多色）
       ↑
       Phase 2 MaskRenderer（YOLO 驱动 mask，.segmentation 模式独用）
       ↑
       BBox Overlay（Phase 1，始终显示）
       ↑
最底层：AVCaptureVideoPreviewLayer（相机预览）
```

**约束：** TouchHandler 的手势识别层不得拦截 Phase 2 渲染层的触摸事件；两者使用不同的 `UIGestureRecognizer` 实例，不共享 target-action。

---

## 2) Tap 坐标变换契约（锁定）

### 2.1 完整变换链

```
UIKit tap point
(CGPoint in UIView.bounds, points)
        │
        ▼ Step 1: AspectFill 逆映射
AVCaptureVideoPreviewLayer
  .captureDevicePointConverted(fromLayerPoint: tapPoint)
        │ → CGPoint 归一化传感器坐标 [0,1]×[0,1]
        │   （此 API 内部处理 aspectFill 裁剪偏移，Apple 官方支持）
        ▼ Step 2: 传感器坐标 → Canonical 像素坐标
  normalizedPoint.x × FrameGeometry.origW
  normalizedPoint.y × FrameGeometry.origH
        │ → CGPoint in Canonical pixel space (origW × origH)
        ▼ Step 3: 方向 & 镜像修正
  复用 Phase 1/2 orientation + mirror 变换逻辑
  （与 CameraManager.preprocessFrame 中使用的同一逻辑）
        │ → CGPoint in corrected Canonical pixel space ✅
        ▼ Step 4: 越界裁剪
  x = clamp(x, 0, origW - 1)
  y = clamp(y, 0, origH - 1)
        │ → 最终 Canonical 像素坐标，送入 PointPromptBuilder
```

### 2.2 实现要求

- **Builder 新增方法**：`FrameGeometry.invertViewPoint(_ point: CGPoint, viewSize: CGSize, previewLayer: AVCaptureVideoPreviewLayer) -> CGPoint`
  - 封装上述 Step 1–4
  - 内部调用 `captureDevicePointConverted` + orientation/mirror 修正 + clamp
  - **不得**在此方法外重复以上任何步骤
- **验证 Log（Builder 必须输出）**：
  `[TAP] view=(x,y) normalized=(nx,ny) canonical=(cx,cy) orientation=N mirrored=B`
- **禁止**：TouchHandler 内部独立计算坐标变换；所有变换必须通过 `FrameGeometry.invertViewPoint`

### 2.3 不变式

| 不变式 | 说明 |
|--------|------|
| 坐标系唯一 | Canonical = 原始 camera px（origW × origH），与 Phase 1/2 YOLO 输出坐标系完全一致 |
| 归一化位置 | 归一化（÷ 1024.0）**仅在 PointPromptBuilder 内部**执行，不在调用层 |
| 变换路径唯一 | 只允许 `FrameGeometry.invertViewPoint` 一条路径，禁止多处各自实现 |
| 越界不排除 | tap 越界后裁剪到边界内继续处理，不丢弃 tap |

---

## 3) Multi-Mask 管理模型（锁定）

### 3.1 TapInstance 数据结构契约

```swift
struct TapInstance {
    let id: UUID
    let canonicalPoint: CGPoint         // Canonical 像素坐标
    let createdAt: Date                 // FIFO 排序依据
    var mask: MLMultiArray?             // 解码后的 mask（256×256 Float32）
    var maskTimestamp: Date?            // mask 生成时间（遥测口径 maskAgeMs；不参与有效性判定，见 §11）
    var iouPred: Float                  // 上次解码的 iou_pred（0 = 尚未解码）
    let color: UIColor                  // 实例颜色（从色板分配，固定不变）
    var isPrimary: Bool                 // true = 最近一次 tap
}
```

**Mask 生命周期（Day 5 修订，取代原「Mask TTL = 2000 ms」条款）：**

> per-instance mask **不设时间型 TTL**。已解码成功的 tap mask **常驻**，直到被 §3.2 的某个**事件**移除。
> 原条款「复用 Phase 2 mask TTL = 2000 ms（与 TemporalManager 独立计算，不共享状态）」自 **Day 5 起正式作废**，撤销理由见 §11.2–§11.3。
> `maskTimestamp` 字段**保留**，语义降级为**遥测时间戳**：仅用于计算 `maskAgeMs` 日志、与 `cacheAge` 做「mask 年龄 × 质量」相关性分析。它**不得**出现在任何显示、过滤或有效性判定路径上。
> Phase 3 内**唯一**的时间型失效时钟是 embedding TTL（8000 ms，见 §11.6）；mask 侧不存在第二个时钟。

### 3.2 实例池管理规则

| 操作 | 规则 |
|------|------|
| 新 tap | 创建新 TapInstance，设为 primary，旧 primary 降为 secondary |
| 超出 N=3 | FIFO 删除 `createdAt` 最早的实例（不论是否 primary）|
| 双击已有 mask | 清空全部实例（`clearAll()`） |
| 点击已有 mask 内部 | 将该实例提升为 primary（不重新 decode） |
| 模式切换 | 退出 `.tapToSegment` 时清空全部实例 |

#### 3.2.1 mask 移除条件全集（Day 5 新增，封层）

下表是 Phase 3 内 tap mask 消失的**穷尽列表**。不在表内的任何原因导致 mask 消失，都按 bug 处理。

| # | 触发 | 作用范围 | 性质 | 用户可见语义 |
|---|------|---------|------|-------------|
| C1 | **双击屏幕** → `clearAll()` | 全部实例 | 用户显式 | 唯一的「我要清干净」入口 |
| C2 | **第 4 次 tap** → FIFO 淘汰 `createdAt` 最早者 | 单个实例 | 容量约束（N=3）| 最老的那个让位给新的 |
| C3 | **退出 `.tapToSegment` 模式** | 全部实例 | 生命周期 | 换模式即重来 |
| C4 | **几何签名变化**（`tapGeometryChanged == true`，如旋转 / 预览尺寸变化）→ 清空实例池 | 全部实例 | **正确性**（旧 mask 属于已失效的坐标空间）| 画面构图变了，选择重置 |
| C5 | **同一实例的新请求取代旧请求**（per-instance supersession，§10.5 R4）| 单个实例的**在途请求** | 调度 | 不是清除，是替换：旧 decode 结果被丢弃，新结果就位 |
| C6 | **长按单个 mask 删除**（`removeInstance(id:)`，Day 6 接手势）| 单个实例 | 用户显式 | 精确删掉一个 |

- **时间流逝不在表内。** 无论 mask 存在多久，只要 C1–C6 均未发生，它必须保持可见。
- C5 属「替换」而非「移除」：在途请求被注销时不得触发失败提示（§10.4 要求 C 的「静默清除加载指示禁令」只约束**真失败**，用户自己替换掉的请求应静默注销）。
- C4 是唯一由系统自动触发的全清。它是**事件驱动**的正确性保护，与被撤销的时间型 TTL 有本质区别：它对应一个可观测的失效事实，而非一个墙钟阈值。

### 3.3 呈现色彩规格（Day 5 追加修订 2026-08-11，取代原「颜色色板（固定顺序，循环分配）」）

> **原文（正式作废）：** `实例 1：systemBlue (alpha 0.55 primary / 0.35 secondary) / 实例 2：systemGreen / 实例 3：systemOrange`。
> 作废理由见 §12.2–§12.3：该色板把「mask 是否看得见」整个压在「填充色 vs 物体本色的对比」上，而**物体本色是不可控输入** ⇒ 撞色不是偶发事故，是这套规格的必然产物。2026-08-11 录像实证：蓝色保温杯被涂 systemBlue、绿色键盘被涂 systemGreen、橙色文件架被涂 systemOrange，三块 mask **同时隐身**。

#### 3.3.1 呈现的三层职责模型（新增；本节其余条款均由此推出）

| 层 | 它回答的问题 | 承载者 | 允许依赖不可控输入？ |
|---|---|---|---|
| **L1 可见性** | 「屏幕上存在一块被选中的区域」 | **双色轮廓描边** | ❌ **绝不允许** |
| **L2 归属** | 「这块是我最近那一下点出来的」 | primary / secondary 的 alpha 分级 + tap 锚点标记 | 允许 |
| **L3 区分** | 「这三块分别来自三次不同点击」 | 描边样式分级 + tap 锚点编号（**不由色相承担**） | 允许 |

> **裁决：L1 不得由色相承担。** 色相只服务 L3，而且只有在 L1 已被描边独立保证之后才有意义。
> 任何「把可见性交给填充色」的规格都是有缺陷的——它把正确性押在用户拍什么上。

#### 3.3.2 填充色约束原则（C-1 … C-7；**换色时必须逐条检验并把数值写回本表**）

| # | 约束 | 判据 | 现行/历史色的检验结果 |
|---|---|---|---|
| **C-1** | **稀有色带**：填充色相必须落在日常物体低发色带内 | **允许带（唯一）：H ∈ [160°, 194.78°]，宽 34.8°**（青–蓝绿）。上界不是 200°，而是 C-2 在 S=1.00/V=1.00 时的截断点 H=194.78°（Y 恰为 0.450）；把 S 放到 C-3 下限 0.85 最远可延到 H≈197.6°，**该 2.8° 不计为可用余量**（要靠掉饱和度换，与 C-3 的立意相反）。**禁用带**：[0°,60°] 红/橙/棕/木/肤色；[60°,90°] 黄/纸/暖光；[90°,155°] 绿植与绿色制品；[195°,260°] 蓝色塑料/牛仔布/天空/屏幕蓝。❌ **原「品红备选带 H ∈ [280°,330°]」已删除（2026-08-11 勘误，见 §13.2.3）—— 它在 C-2∩C-3 下是空集**，该带内满足 S≥0.85 且 V≥0.90 的颜色最高只能到 **Y = 0.2988**（H=300°, S=0.85, V=1.00），永远够不到 C-2 的 0.45。留着会误导下一次换色 | slot 0 **188.94° ✅** ／ slot 1 **176.94° ✅** ／ slot 2 **160.00° ✅（零余量，见 C-1a）** ／ systemBlue **211° ❌** ／ systemGreen **135° ❌** ／ systemOrange **35° ❌** |
| **C-1a** | **边界余量（新增 2026-08-11）**：允许带的两个端点**不是安全线** | 推荐取色区间 **H ∈ [165°, 190°]**（两端各留 ≥5° 余量）。落在余量区内的色值属**例外**，须在 C-7 记录中单独标注风险并在 (b) 目视测试里**专门覆盖**相邻禁用带的代表物 | slot 2 = 160.00° 是**已批准的例外**：距 [90°,155°] 绿色禁用带仅 5°、零余量；放行条件 = C-7(b) 必须包含薄荷/春绿实物 |
| **C-2** | **相对亮度下界** Y ≥ 0.45（sRGB relative luminance） | 保证半透明叠加把区域**提亮**而不是压暗；压暗的覆盖层在暗物体/阴影里等于不存在 | 青 **0.569 ✅** ／ systemBlue **0.211 ❌** ／ systemGreen **0.428 ❌** ／ systemOrange **0.425 ❌** |
| **C-3** | **人造信号特征**：HSV **S ≥ 0.85 且 V ≥ 0.90** | 真实光照下的自然表面普遍被去饱和；「高饱和 + 高明度」本身就是「这是叠加图层，不是物体」的信号 | 青 S=1.00 V=1.00 ✅ |
| **C-4** | **L1 不得由色相承担** | 任何被显示的 mask（含 secondary）必须带 §3.4 的双色描边 | 规格要求；实现已从 Day 6 提前至 Day 5（§12.6） |
| **C-5** | **描边在屏幕坐标系生成** | 线宽以 **pt** 定义；**不得**烧进 256×256 的 alpha 位图——否则线宽会随 mask 一起被放大 4–8×，且与 D-13「让 GPU 直接缩放 256×256」的重构方向不兼容 | 约束（非现状） |
| **C-6** | **单变量回退能力** | 描边必须能由**单点开关**整体关闭，以便与 Day 4 的「无描边」视觉条件做单变量对照评分 | 约束 |
| **C-7** | **换色准入程序** | 任何填充色变更须同时满足：**(a)** 通过 C-1/C-1a/C-2/C-3/C-8 的算术检验，并把数值补进本表；**(b)** 在**至少一个自身即为该色系的真实物体**上做目视「撞色测试」——多色板须**逐槽位各测一个**，且**必须包含一次同屏三实例**、判「能否看出这三块来自三次不同点击」。**两项缺一不得合入。** 失败判据是「连轮廓都找不到」（有描边兜底后，「填充看不清但轮廓仍在」**不算失败**）| 程序（本次事故的防复发机制）。⚠️ 现行三槽色板**只通过了 (a)**，(b) 未执行 ⇒ 属程序违例，处置见 §13.2.5 W-2 |
| **C-8** | **混合封闭性（新增 2026-08-11）**：多色板在 `compositeLayers` 的 source-over 叠放下必须**封闭** | 穷举全部叠放组合（N=3 ⇒ 2 层与 3 层、各种顺序、alpha 0.40/0.40/0.60，共 **12 种**），**每一种重叠产物自身都必须通过 C-1/C-2/C-3**。充分构造技巧：让全部槽位共享一个恒为 0 的分量（本色板 R≡0 ⇒ 任意凸组合仍 R=0 ⇒ S 恒等于 1.000）| 现行三槽色板：H ∈ [163.57°, 186.48°]、S = 1.000、V ∈ [0.930, 1.000]、Y ∈ [0.5997, 0.7706] ⇒ **12/12 全部通过 ✅**（Architect 独立复算确认）。旧三色板 ❌：蓝+橙的混合物掉出整个约束集 |

#### 3.3.3 Phase 3 色板（**Day 5 追加三修订 2026-08-11：恢复按实例配色，条件性追认**）

> **上一版（2026-08-11 追加二）「全实例统一青、按实例配色撤销」正式被本节取代。**
> 取代的合法性见 §13.2.2：撤销按实例配色的推理前提是「色相在承担 L1 可见性」，
> 而**双色描边落地后 L1 已由描边独立承担**，色相退回 §3.3.1 的 L3 ⇒ 原推理的前提消失，结论随之失效。
> 这不是反复横跳：被推翻的是我的**一条论据（允许带装不下三个色相）**，被前提变更作废的是**结论**，两者各有各的原因。

```
三槽位色板（全部 S = 1.00 / V = 1.00，C-7 (a) 算术检验已过；Architect 独立复算逐项吻合）

  slot 0   青   (0, 217, 255)    H = 188.94°   Y = 0.5685   ← 保留 Day 4 的青
  slot 1   水青 (0, 255, 242)    H = 176.94°   Y = 0.7793
  slot 2   春青 (0, 255, 170)    H = 160.00°   Y = 0.7442   ← C-1a 例外（零余量）

  alpha    primary 0.60 / secondary 0.40（**未变**，与追加二一致）
  描边     双色轮廓，primary 2.0/1.5 pt、secondary 1.5/1.0 pt（**未变**）
  槽位分配 新实例取当前未占用的槽位；FIFO 淘汰释放槽位 ⇒ 两个存活实例不可能同色
```

**追认状态：🟢 FINAL（2026-08-16 升级）。** C-7 (b) 真机目视测试已完成（§13.2.5 W-2 已结案）。结果：三槽各测同色系实物，全部「填充看不清但轮廓仍在」或更好；同屏三实例可区分（W-1 PASS）。详见 debug_report.md §28。

**四条封层性质的结论（逐条给出，含两处对上一版的勘误）：**

1. ❌ **上一版理由 1「C-1 允许色带装不下三个彼此可区分的色相」经算术核对不成立，予以撤回。** 在修订后的允许带 H ∈ [160°, 194.78°] 内、slot 0 钉死 188.94° 的前提下，(160.0°, 177.5°, 188.94°) 的最小 CIEDE2000 = 17.4；实际发布的 (160°, 176.94°, 188.94°) = **17.1**，高于「类别可辨识」常用的 ΔE00 ≈ 10。**约束集容得下三个可区分色相；容不下的是「散布在整个色环上的三个」** —— 旧色板 ΔE00_min = 50.2，正是那种散布把两个色相拖进了禁用带。原表述把「色相间距不够」与「色相位置越界」混为一谈了。
2. ❌ **上一版理由 2「三色重叠会混出第四种颜色」对本色板不成立，但其成立条件已提炼为 C-8。** 本色板在 source-over 下**封闭**（R ≡ 0 ⇒ 任意凸组合 S = 1.000），12 种叠放组合全部通过 C-1/C-2/C-3。旧色板不成立的原因不是「三色」而是「未检验混合封闭性」：蓝 + 橙的混合物掉出了整个约束集。⇒ 该理由不再作为反对按实例配色的论据，而是作为**每次换色都必须跑的检验（C-8）**保留。
3. ✅ **理由 3 仍然成立，但方向反了过来用 —— 色相是 L3 的「载体之一」，不是充分载体。** 见 §13.2.5 W-1：alpha 衰减后 secondary 之间的合成 ΔE00 只有 6.8–10.6，多数背景低于 10。**在 Day 6 的 tap 锚点编号落地之前，L3 处于已知欠载状态**（primary/secondary 的区分由描边分档承担，属 L2；两个 secondary 之间目前只有色相一个载体）。
4. ✅ **理由 4 仍然成立，且被 slot 0 的选择精确保住：** slot 0 = Day 4 的青 (0,217,255) ⇒ N = 1 时合成输出与 Day 4 `renderMask` 的字面量 (0,217,255,153) **逐位相同**，44/50 = 88% 人工评分基线在**单 tap** 上完全不受影响（多实例场景本就不在该基线的采样协议内）。

`TapInstance.color` 字段语义**回升**为「实例区分色（L3 载体之一）」；`palette` 三个槽位为 plain sRGB 字面量，不得使用 dynamic system color（后台线程解析问题，D-15）。


---

### 3.4 呈现层次规则（Day 5 追加修订 2026-08-11，取代原「Primary 渲染规则」）

```
Primary 实例（最近一次 tap）：
    填充   该实例的槽位色（slot 0 青 / slot 1 水青 / slot 2 春青，§3.3.3）@ alpha 0.60
    描边   双色轮廓：外层 2.0 pt 近黑 (0,0,0) @0.85  →  内层 1.5 pt 近白 (255,255,255) @0.95
Secondary 实例：
    填充   该实例的槽位色 @ alpha 0.40
    描边   双色轮廓（减档）：外层 1.5 pt 近黑 @0.70  →  内层 1.0 pt 近白 @0.70
实例已创建但 mask 尚未就位（mask == nil）：
    不显示该实例的 mask，在其 tap 位置显示等待指示
    （§10.3 D2 的脉冲确认动画；语义 =「已收到点击」，非进度）
实例池为空：
    maskImage = nil，回退「无 mask」显示状态
绘制顺序：
    按 createdAt 升序 —— secondary 在下、primary 最后（压最上层）；与 Builder Day 5 实现一致，不变
```

**双色描边为什么是背景无关的（这是本次修订的实质）：** 单色白描边在白墙上消失，单色黑描边在暗物体上消失——单色描边只是把「押注」从填充色换到了描边色，仍然依赖不可控输入。「深色外圈 + 浅色内线」则保证无论背景亮度落在量程哪一端，两条线中**至少一条**与背景存在足够的亮度阶跃。这是字幕、地图注记、医学影像叠加层的通行做法。**它把 L1 从「与不可控输入比对比度」改造成「呈现元素自带对比度」。**

**primary / secondary 的区分不再依赖色相后的载体清单（按强度排序）：**
1. 描边档位（2.0/1.5 pt 全强度 vs 1.5/1.0 pt 减档）——**最强，且背景无关**；
2. 填充 alpha 0.60 vs 0.40（比值 1.5:1，secondary 仍在「自然场景上可见」的下限之上）；
3. Z 序（primary 永远压在最上层）；
4. tap 锚点标记 + 实例编号（Day 6）—— **L3 的必需载体**，定级 P1；在它落地前，两个 secondary 之间只有色相一个 L3 载体，且其合成 ΔE00 多数背景 < 10（§13.2.5 W-1）；
5. 色相（槽位色，§3.3.3）—— **L3 的载体之一，不是充分载体，且永远不承担 L1**。

**保留条款（Day 5 原文，本次未被推翻，继续有效）：**

> **「不显示」的唯一条件是 `mask == nil`（尚未解码完成 / 解码失败）。**
> **已解码成功的 mask 不存在「失效」状态**——它不会因为变旧而变得不可显示，只会被 §3.2.1 的 C1–C6 事件移除。
> 渲染层因此不需要、也**不得**持有任何「mask 是否仍然有效」的时间判定；`drawableInstances()` 的正确行为就是「返回所有已有 alpha 的实例」。
> （原修订背景：旧表述中的「无有效 mask」曾被合理地读作「TTL 未过期」，与 §3.1 的 2000 ms 叠加后推出「tap 后 2 秒自动消失」的错误行为，见 §11。）

---

## 4) Encoder 触发逻辑（锁定）

### 4.1 encoderQueue 共享规则

```
encoderQueue = 单一 serial DispatchQueue（Phase 2 已有，Phase 3 复用）
SAMEncoder   = 单一实例（Phase 2 已有，Phase 3 复用，禁止加载第二份）
isEncoding   = 已有 Bool flag（Phase 2 已有，Phase 3 共用，不得新建独立 flag）
```

### 4.2 Tap 模式触发逻辑

```
用户 tap
    │
    ▼
embedding 是否有效？
（TTL 未过期 AND geometry 未变化）
    │
    ├── YES → 直接进入 decode（point prompt）
    │          显示短暂波纹动画，mask 通常在 ~61 ms 内出现
    │
    └── NO →
          isEncoding == true？
              ├── YES → 显示持续脉冲动画（等待 encode 完成）
              │          encode 完成后自动触发 decode（point prompt）
              │
              └── NO → 立即触发 encode
                        encode 完成 → 触发 decode（point prompt）
                        decode 完成 → 更新 TapInstance.mask，刷新渲染
```

### 4.3 Phase 2 与 Phase 3 模式交互规则

| 场景 | 行为 |
|------|------|
| `.segmentation` 模式下 encode 进行中，用户切换到 `.tapToSegment` | 等待当前 encode 完成后 Phase 2 回调被忽略（mode check），下一次 tap 触发新 encode |
| `.tapToSegment` 模式下，Phase 2 每 12 帧调度 encode | **Phase 2 调度必须 no-op**（mode != .segmentation 时 TemporalManager 不得触发 encode）|
| 两模式均需 encode，encoderQueue 有任务 | 串行排队，后者等待，不并发 |
| encode 失败（任何原因） | `isEncoding = false` 复位，tap 实例保持 iouPred=0，显示 fallback 状态 |

### 4.4 Phase 2 TemporalManager 静默规则（重要）

在 `.tapToSegment` 模式下，`CameraManager` 必须在调用 TemporalManager 前检查 `currentMode`：

```swift
// CameraManager 中的检查（伪代码）
if currentMode == .segmentation {
    temporalManager.update(detections: detections, ...)
    // 后续 encode/decode 调度由 TemporalManager 驱动
}
// .tapToSegment 模式：TemporalManager 静默，不调用
```

---

## 5) SAMDecoder 加法重载设计（审批）

### 5.1 现有 API（冻结，不得修改）

```swift
// SAMDecoder.swift — 现有方法，Phase 2 使用，不得改动签名或方法体
func decode(embedding: MLMultiArray,
            prompt: PromptBuilder.BoxPrompt) -> MLMultiArray?
```

### 5.2 新增重载（Phase 3 — 加法 ONLY）

```swift
// 新增：点提示重载，与上方方法并存
// 文件：SAMDecoder.swift（在现有 decode 方法之后追加）
func decode(embedding: MLMultiArray,
            pointPrompt: PointPromptBuilder.PointPrompt) -> (mask: MLMultiArray, iouPred: Float)?
```

**约束：**
- 此重载**不得修改**现有 `decode(embedding:prompt:)` 的任何一行
- 内部仅调用 `model.prediction(from:)`，输入 tensor 来自 `PointPromptBuilder.PointPrompt`
- 输出增加 `iouPred`（点提示质量评估依赖 iou_pred，bbox 路径不返回此值是已知遗留，Phase 3 新增）
- 返回可选 tuple：decode 失败时返回 `nil`，调用方必须处理 nil

### 5.3 PointPromptBuilder.PointPrompt 契约

```swift
// 新文件：Interaction/PointPromptBuilder.swift
struct PointPromptBuilder {
    struct PointPrompt {
        let pointCoords: MLMultiArray    // shape 由 ML_Vision Day 1 确认（[1,1,2] 或 padding 到 [1,2,2]）
        let pointLabels: MLMultiArray    // 对应 shape，foreground label = 1
        let maskInput: MLMultiArray      // shape [1,1,256,256]，全零
        let hasMaskInput: MLMultiArray   // shape [1]，值 0.0
    }

    /// 将 Canonical 像素坐标转为 SAM 点提示 tensor
    /// - canonicalPoint: CGPoint in original camera px space
    /// - origSize: CGSize(width: origW, height: origH)
    /// - inputSize: SAM encoder input size（default 1024，Phase 3 优化后可改）
    static func buildPointPrompt(canonicalPoint: CGPoint,
                                 origSize: CGSize,
                                 inputSize: Int = 1024) -> PointPrompt?
}
```

**归一化规则（与 PromptBuilder 一致）：**
1. Apply ResizeLongestSide(inputSize) + centered padding（与 PromptBuilder.buildBoxPrompt 相同的变换）
2. 结果：SAM 空间坐标（0 ~ inputSize）
3. **不得**在此函数外部预先归一化

> ⚠️ tensor shape（[1,1,2] vs [1,2,2]）待 ML_Vision Day 1 确认模型实际接受的 point_coords 形状后，Builder Day 2 方可实现。

---

## 6) isolate_phase2.md 剩余 Checklist 裁决

| 项 | 裁决 | 说明 |
|----|------|------|
| SAMDecoder 加法重载设计 Architect 审批 | ✅ **批准** | 见 §5，加法设计符合冻结契约，现有方法不受影响 |
| 坐标变换链契约确认 | ✅ **锁定** | 见 §2，完整变换链已定义，Builder 按此实现 |
| Agent 分工对齐 | ✅ **确认** | 见下表 |

### Agent 分工（Phase 3 Day 2 起生效）

| Agent | Phase 3 职责 |
|-------|-------------|
| **Architect** | 契约冻结、SAMDecoder 重载审批（已完成）、Day 4 分辨率裁决、Day 7 架构冻结 |
| **ML_Vision** | 点提示 tensor shape 确认、ANE 修复方案（Day 1）、分辨率评估（Day 3） |
| **Builder** | Interaction/ 全部模块实现、FrameGeometry.invertViewPoint、PointPromptBuilder、TapInstanceManager |
| **Debugger** | tap 坐标误差验证（Day 2）、端到端延迟测量（Day 4）、多实例内存压测（Day 5）、性能回归监控 |

---

## 7) Deliverable 确认

Phase 3 Day 1 Architect 契约已锁定，以下内容均已定义：

- [x] Tap-to-Segment 插入点（§1）
- [x] 坐标变换契约（§2）
- [x] Multi-Mask 管理模型（§3）
- [x] Encoder 触发逻辑（§4）
- [x] SAMDecoder 加法重载设计审批（§5）
- [x] Agent 分工对齐（§6）

**Phase 2 pipeline 保护状态：**
- YOLO 路径不受影响 ✅
- TemporalManager 不被 Phase 3 代码调用 ✅（mode check 保护）
- SAMEncoder / SAMDecoder 实例不重复加载 ✅
- encoderQueue 共享但串行，无并发风险 ✅

可进入 Day 2（ML_Vision ANE 修复交付 + Builder TouchHandler 实现）。

---

END OF PHASE 3 DAY 1 ARCHITECT OUTPUT

---

# Phase 3 — Day 4 (Architect) 降编码分辨率裁决

日期：2026-07-23  
依据：ML_Vision Day 3 评估报告（shared/resolution_eval_report.md）+ 原始 JSON 数据（shared/resolution_eval_results.json）+ model_plan.md §C + Phase 3 冻结契约  

---

## 8) 降编码分辨率裁决（Day 4）

### 8.1 裁决结论

| 分辨率 | 裁决 | 说明 |
|--------|------|------|
| **1024×1024** | ✅ 保留为当前默认 | Phase 3 主路径，质量基准，不变 |
| **768×768** | ✅ **批准 Builder 展开 AB 测试** | 有条件接受；AB 人工评审是主门控 |
| **512×512** | ❌ **Phase 3 拒绝** | 保留给 Phase 4 fine-tuning 后评估 |

---

### 8.2 评估数据质量审查（⚠️ 重要）

在审阅 ML_Vision Day 3 评估报告时，Architect 发现报告结论与原始 JSON 数据之间存在**关键不一致**，须在裁决前正式记录：

| 数据项 | 报告声称 | JSON 原始数据 | 差异 |
|--------|---------|--------------|------|
| 768 IoU vs 1024（均值） | 0.581 | **0.0（所有样本）** | ⚠️ 完全矛盾 |
| 512 IoU vs 1024（均值） | 0.592 | **0.0（所有样本）** | ⚠️ 完全矛盾 |
| 有效测试样本数 | 「20 样本（4 图×5 点）」 | **2 张图像（n_images=2）** | ⚠️ 样本不足 |
| 768 iou_pred（内部评分） | — | 0.664 / 0.697（两图均值 ~0.68） | ✅ 与报告相符 |
| 512 iou_pred（内部评分） | — | 0.716 / 0.850（两图均值 ~0.78） | ✅ 与报告相符 |

**根因分析（Architect 判断）：**

- `iou_vs_1024 = 0.0` 意味着 768/512 encoder 产生的 mask 与 1024 mask 在像素层面**完全不重叠**
- 像素数对比支持这一结论：good1.PNG 中 1024 mask 有 8319 px，而 768 mask 仅 1186 px（14%）
- 根本原因：TinyViT-5M 从未在 768/512 上训练，降分辨率后模型在不同语义尺度上产生分割，与 1024 输出在空间上不对齐
- 报告中的 IoU=0.581 数字来源不明（可能为理论估算或脚本 bug），**不应作为裁决依据**
- 模型内部的 `iou_pred` (~0.68–0.78) 表明模型仍在分割某个有意义的区域，但不保证与 1024 语义一致

**裁决影响：** 上述发现使 768 的批准条件更为严格。AB 测试中的**人工视觉评估**成为唯一可信的质量门控（不可依赖 iou_vs_1024 数字）。

---

### 8.3 768×768 批准依据与约束条件

**批准理由：**
1. **延迟收益显著且数据可靠**：Mac 实测比例法预测 iPhone 11 encoder 从 857ms 降至 ~555ms（×1.54），该数字来自可重复的 Mac CPU 计时，具有参考价值
2. **实现可逆**：通过 `encoderInputSize` 配置项控制，1024 路径不受任何修改，失败时立即回退
3. **内部 iou_pred 尚可**：~0.68，高于 Phase 3 质量门控下限（0.5），说明模型在低分辨率下仍在有效分割，但语义可能与 1024 有偏差
4. **AB 测试可验证**：真机上的人工评审将给出最终答案；Phase 3 尚有时间在 Day 5 撤销

**批准约束（Builder 必须严格遵守）：**

| 约束编号 | 约束内容 | 理由 |
|---------|---------|------|
| C-1 | Phase 3 **默认分辨率保持 1024**；768 仅通过 `SAMConfiguration.encoderInputSize = 768` 显式启用 | 1024 为质量基准，768 为待验证优化 |
| C-2 | `PointPromptBuilder` 中点坐标**永远在 1024 空间计算**，不随 encoderInputSize 变化 | Decoder 固定以 1024 归一化，坐标混用会导致 mask 完全错位（见 model_plan §C.4）|
| C-3 | 768 encoder 输出 `[1,256,48,48]`，**必须**在 Swift 层双线性上采样至 `[1,256,64,64]` 后再传入 Decoder | Decoder 固定接受 64×64 embedding |
| C-4 | 768 模型导出**必须**同步应用 milfix（`export_encoder_fp16_milfix.py` + trace 尺寸改为 768）| 保持 ANE 对齐修复，维持与 1024-milfix 相同的性能基线 |
| C-5 | AB 测试**必须包含人工视觉评分**（1–5 分，5=视觉上与 1024 无区别）| iou_vs_1024 数值不可信，人工评审是唯一可靠质量门控 |
| C-6 | AB 测试数据（延迟 + iou_pred + 人工评分）必须写入 `builder_progress.md` | 供 Day 5 Architect 审批 |

---

### 8.4 512×512 拒绝依据

| 拒绝理由 | 具体数据 |
|---------|--------|
| 质量无优于 768 | iou_pred ~0.78，与 768 (~0.68) 接近；iou_vs_1024 同样为 0.0 |
| 极端退化风险更高 | 512 时 1 特征单元对应 32px，tap 精度粗糙；mask 极端失真风险更大 |
| 强依赖 fine-tuning | zero-shot 可用性无法保证；需专项训练才有意义 |
| Phase 3 范畴外 | Phase 3 聚焦 UX 正确性与流水线稳定性；精度优化属 Phase 4 |

**保留入口点（Phase 4）：** 512 fine-tuning 后可重新评估；导出脚本与 768 相同，仅修改 trace 尺寸为 `(1,3,512,512)`。

---

### 8.5 Phase 3 冻结参数表（Day 4 中间态 — **已被 §9.5 取代，勿引用**）

| 参数 | 当前状态 | Day 5 目标 |
|------|---------|----------|
| Encoder 输入分辨率 | ~~**1024（默认）；768（AB 测试中）**~~ | ✅ 已裁决 — **封层 1024，撤销 768**（见 §9.1）|
| Decoder 输入 embedding | `[1, 256, 64, 64]`（固定） | 不变 |
| Point prompt 坐标空间 | **永远 1024 空间**（锁定） | 不变 |
| Encoder 模型系列 | milfix fp16（已锁定）| ✅ 已裁决 — 768 变体撤销，milfix fp16 单模型封层（见 §9.5）|
| Embedding TTL | 8000ms（Phase 2 继承）| 不变（Tap 路径 TTL 修复在 Day 6）|
| Mask TTL | 2000ms（Phase 2 继承）| 不变 |
| 最大实例数 N | 3（Day 1 锁定）| 不变 |
| fine-tuning | Phase 4 范畴 | 不变 |

---

### 8.6 Builder Day 4 AB 测试规格（正式授权）

Builder 按以下规格展开，结果写入 `builder_progress.md`：

**模型导出（如未完成）：**
1. 修改 `shared/export_encoder_fp16_milfix.py`：将 dummy_trace 改为 `torch.randn(1, 3, 768, 768)` + monkeypatch `forward_features` 动态化（`feat_size = img_size // 16`）
2. 输出 `MobileSAM_ImageEncoder_fp16_milfix_768.mlpackage`（~14 MB）
3. Decoder mlpackage 不变

**Swift 适配（3 处）：**
1. `SAMConfiguration.encoderInputSize: Int`（default=1024，AB 测试时设为 768）
2. Encoder 预处理 resize 适配 encoderInputSize
3. Encoder 输出上采样桥接（`[1,256,48,48]` → `[1,256,64,64]`，双线性插值）

**AB 采样要求：**
- 同场景同目标，分别运行 1024 和 768 各 5 次
- 记录：encoder latency mean + p95、iou_pred 均值、人工视觉评分（1–5）
- 人工评分标准：5=mask 完整覆盖目标对象；3=大致覆盖但有缺口；1=mask 完全错位或无意义

---

### 8.7 Day 5 Architect 最终封层条件（预告）

| AB 结果 | Day 5 决定 |
|---------|----------|
| 768 latency 降幅 ≥ **150ms** 且人工评分均值 ≥ **3.5** | ✅ 封层 768 为 Phase 3 默认 encoder 分辨率 |
| 768 latency 降幅 < 100ms 或人工评分均值 < 3.0 | ❌ 撤销 768，封层 1024 为唯一分辨率 |
| 介于两者之间（降幅 100–150ms 或评分 3.0–3.5）| Architect 主观裁量，可能封层为可选「速度模式」 |

---

### 8.8 Deliverable 确认

- [x] 768×768 批准 Builder AB 测试（见 §8.3，含严格约束）
- [x] 512×512 Phase 3 拒绝（见 §8.4）
- [x] 评估数据质量差异正式记录（见 §8.2）
- [x] Phase 3 冻结参数表部分更新（见 §8.5）
- [x] Builder AB 测试规格授权（见 §8.6）
- [x] Day 5 封层条件预告（见 §8.7）

**Phase 2 pipeline 保护状态：** 无变化，所有 Phase 2 路径不受影响 ✅

---

END OF PHASE 3 DAY 4 ARCHITECT OUTPUT

---

# Phase 3 — Day 5 (Architect) 分辨率封层 + 延迟展示策略裁决

日期：2026-08-09
依据：Debugger Day 4 真机实测（`shared/debug_report.md` §10.1–10.4、§13）+ Session A 良品率评测（`shared/phase3_day4_test_checklist.md` §2–§3）+ Day 4 tap 延迟全量采样（24 次）+ §8.7 预告的封层条件
适用设备：iPhone 11 / A13（唯一实测平台）

---

## 9) Encoder 分辨率最终封层裁决

### 9.1 裁决结论

| 分辨率 | Day 4 状态 | **Day 5 最终裁决** |
|--------|-----------|-------------------|
| **1024×1024** | 默认 | ✅ **封层为 Phase 3 唯一 encoder 分辨率** |
| **768×768** | AB 测试中 | ❌ **撤销**（§8.3 的批准正式作废，约束 C-1…C-6 随之失效）|
| **512×512** | Phase 3 拒绝 | ❌ 维持拒绝（不变，Phase 4 重评入口保留）|

**该行落入 §8.7 门控表的第二行（撤销分支）。裁决即刻生效，Day 5 起 `encoderInputSize` 不再作为可调优化项进入任何后续排期。**

---

### 9.2 撤销依据（实测，同 session 配对设计）

| 指标 | 1024（n=3 warm） | 768（n=4 warm） | 差值 |
|------|-----------------|-----------------|------|
| mean | **970.6 ms** | **1036.0 ms** | **+65.4 ms（768 更慢）** |
| p95 | 1006.1 ms | 1126.5 ms | +120.4 ms |

§8.7 要求「降幅 ≥150 ms」。实测不仅无降幅，方向相反。差距达 215 ms（门控线 −150 与实测 +65 之间），**不在测量噪声可解释的范围内**，无需补采即可裁决。

**理论预测被证伪的原因**（采纳 debug_report §10.3 的机制解释）：

1. ML_Vision Day 3 的 ×1.54 加速预测建立在 **Mac CPU 线性缩放假设**上。A13 的 ANE 并非按像素数线性计费：768 与 1024 两份权重文件同为 14.1 MB，ANE 的图编译与调度开销占比高，计算量下降 44% 并未按比例转化为墙钟时间下降。
2. 768 路径**额外**背负 C-3 要求的 Swift 层 48×48 → 64×64 双线性上采样，把节省的一部分又还了回去，并推迟了 decoderQueue 的入队时刻。

**架构层教训（写入长期约定）：** 跨硬件后端（Mac CPU → iPhone ANE）的比例外推**不得**作为架构裁决的充分依据，只能作为「值得做 AB」的立项依据。今后任何涉及 ANE 的性能提案，门控必须绑定目标设备真机配对实测。

---

### 9.3 门控口径缺陷两则（如实记录，不得美化）

#### 缺陷 1：人工评分维度**从未在 768 上采集**

- §8.7 门控的撤销分支写法为「降幅 <100 ms **或** 评分 <3.0」，而批准分支为「降幅 ≥150 ms **且** 评分 ≥3.5」。
- 事实：C-5/C-6 要求的 768 人工评分表**未产出**。Session A 的 50 次评分**全部在 1024 上**完成（良品率 44/50 = 88%），与 768 无关。debug_report §13 记为「无法评分（mask 滞后不可见）」。
- **对本次裁决的影响：无。** 撤销分支的两个条件是**析取**关系，延迟维度单独越线（+65 ms，远差于 <100 ms 的触发线）已足以触发撤销。评分维度缺失不改变结论。
- **但门控表述必须修订，供未来复用：**

> **门控语义修订（自 Day 5 生效）**
> 「批准」需**全部**条件满足（合取）；「撤销」**任一**条件触发即可（析取）；仅当所有条件均落在中间带时才进入主观裁量。
> 推论：一旦某一维度已单独触发撤销，**不要求**补齐其余维度的数据。反之，若仅有部分维度数据且这些维度全部指向「批准」，**不得**据此封层——缺失维度视为未通过。

#### 缺陷 2：热态口径未标注，970.6 ms 曾有被误用为绝对指标的风险

同一份 1024 encoder，在两轮不同会话中给出显著不同的绝对数值：

| 数据来源 | 会话条件 | 实测 | n |
|---------|---------|------|---|
| debug_report §10.1（2026-07-23）| **AB 交替会话**：同一进程内先后加载 1024 与 768 两份 encoder，反复切换分辨率触发 counter 重置；warm 样本取自冷启动之后 | mean **970.6** ms / p95 1006.1 ms | 3 |
| Day 4 Session B（2026-08-01/02）| **单模型稳态会话**：仅 1024 一份 encoder，tapToSegment 空闲态 background refresh，无模型切换 | **573–755 ms，mean ≈648 ms** | 14 |

两者相差约 33%。

> **封层文本标注（必须随 1024 一起引用）**
> **970.6 ms 是 AB 配对比较量，不是 Phase 3 的 encoder 绝对性能指标。** 它仅在其采集会话内部有效（1024 与 768 共处同一热态与同一模型切换负载，配对设计保证了**内部效度**，因此 §9.2 的相对结论稳健），但**不具备跨会话的外部效度**。
> **Phase 3 引用 encoder 绝对延迟时，一律使用 648 ms（n=14, warm, 单模型稳态, tapToSegment 空闲 background refresh, iPhone 11）。**
> 任何后续文档、验收指标、Phase 4 对比基线若引用 970.6 ms 作绝对值，视为口径错误。

**附带要求：** 自 Day 5 起，任何写入 debug_report / builder_progress 的延迟数字**必须**同时标注：(a) 冷/热态及 warm 样本的判定方式；(b) 同会话内是否发生过模型切换；(c) 并发负载（YOLO 是否在跑、模式为何）；(d) n。缺任一项的数字不得进入封层裁决。

---

### 9.4 768 资产处置

- 已导出的 `MobileSAM_ImageEncoder_fp16_milfix_768.mlpackage` **不删除**，归档保留，作为「ANE 非线性缩放」这一结论的实证物。
- `SAMConfiguration.encoderInputSize` 配置项**保留但冻结**为常量语义：Phase 3 内不得再被任何代码路径设为 1024 以外的值；C-3 要求的 48→64 上采样桥接代码若已实现，允许保留为死代码，但**不得**在主路径被调用。是否物理删除交由 Day 7 冻结时一并处理（属清理，不属功能）。
- Phase 4 若重启低分辨率议题，**入口条件**为：先在目标设备上取得 ANE 逐层 profile（证明计算量确实是瓶颈），再谈导出与 AB。仅有 FLOPs 下降不构成立项理由。

---

### 9.5 Phase 3 冻结参数表（**Day 5 最终封层**）

本表取代 §8.5。Day 7 架构冻结时直接继承，除非有新的真机数据触发复议。

| 参数 | **封层值** | 状态 | 依据 |
|------|-----------|------|------|
| Encoder 输入分辨率 | **1024×1024** | 🔒 **FINAL（Day 5）** | §9.1–9.3；768 撤销、512 拒绝 |
| Encoder 模型系列 | **milfix fp16 单一模型** | 🔒 FINAL | 768 变体撤销后不再有第二份 encoder |
| Decoder 输入 embedding | `[1, 256, 64, 64]` | 🔒 FINAL | 与 1024 encoder 原生匹配，无桥接层 |
| Point prompt 坐标空间 | **恒为 1024 空间** | 🔒 FINAL | C-2 保留；768 撤销后该约束自动恒成立 |
| Encoder 绝对延迟基线 | **≈648 ms**（warm / 单模型稳态 / n=14）| 🔒 FINAL（口径见 §9.3 缺陷 2）| Day 4 Session B |
| Decode 延迟基线 | **≈61 ms**（warm, n=24）| 🔒 FINAL | Day 4 Session B 全量 tap 采样 |
| Embedding TTL | 8000 ms | ⚠️ 未封层 | **Phase 3 内唯一的时间型 TTL**。数值复议留到 Day 6（见 §10.5 风险 R2、§11.6）|
| ~~Mask TTL~~ | ~~2000 ms~~ | ❌ **作废（Day 5，§11）** | 时间型 mask TTL 整体撤销，本行不再有值可填 |
| Tap mask 生命周期 | **事件驱动，无时钟**（清除条件全集 = §3.2.1 C1–C6）| 🔒 **FINAL（Day 5）** | §11.1–§11.4；用户实测确认「双击才消失」符合预期 |
| 最大实例数 N | **3** | 🔒 FINAL | Day 1 锁定，Day 5 多实例照此实现 |
| Tap 延迟展示策略 | **脉冲确认动画，不设进度条** | 🔒 FINAL（快路径）/ ⚠️ 慢路径待 Day 7 数据 | §10 |
| Tap mask 填充色 | **三槽色板** slot0 青 (0,217,255) / slot1 水青 (0,255,242) / slot2 春青 (0,255,170) | 🔒 **FINAL（Day 7 C-7(b) 通过，2026-08-16）** | §3.3.3；变更须过 §3.3.2 C-1/C-1a/C-2/C-3/C-8 + C-7 |
| Tap mask alpha | **primary 0.60 / secondary 0.40** | 🔒 **FINAL（Day 5 追加二）** | §3.4；恢复 Day 4 逐位一致，保住 88% 基线可比性 |
| Tap mask 描边 | **双色轮廓**（primary 2.0/1.5 pt、secondary 1.5/1.0 pt，屏幕坐标系生成）| 🔒 **FINAL（Day 5 追加二）** | §3.4；L1 唯一承载者，实现从 Day 6 提前到 Day 5 |
| 按实例配色（per-instance hue）| **Phase 3 内恢复**（`TapInstance.color` 语义回升为「实例区分色」，仅承担 L3）| 🔒 **FINAL（与色板同步升级，2026-08-16）** | §3.3.3 / §13.2；前提变更 = 双色描边已独立承担 L1 |
| fine-tuning | Phase 4 范畴 | 不变 | §8.4 |

---

## 10) tap-to-mask 延迟展示策略裁决

### 10.1 数据审查

**Day 4 实测（24 次全量采样，iPhone 11，全部为 decode-only 快路径）：**

| 分量 | 实测 |
|------|------|
| tap → mask 端到端 | **429–1030 ms，中位 ≈620 ms** |
| 其中 decode 计算 | **48.9–75.6 ms，mean ≈61 ms** |
| **无法解释的差值** | **约 400–960 ms** |

**差值已定位，且不是计算开销：** `handleTap` 把 tap 派发到 `videoQueue`，而 `videoQueue` 同时是 `AVCaptureVideoDataOutput` 的 delegate 队列，正在跑 YOLO（单帧占用 400–670 ms）。tap 排在当前帧之后。差值区间 400–960 ms 与 YOLO 单帧区间 400–670 ms 高度吻合（上界处叠加了排队中的第二帧），**归因成立**。

**结论性判断：快路径 620 ms 的中位延迟里，约 90% 是调度排队，只有约 10%（61 ms）是真实计算。**

### 10.2 三个口径缺陷（裁决必须带着走，不得当作干净数据）

| 编号 | 缺陷 | 对裁决的影响 |
|------|------|-------------|
| **G-1** | `e2eMs` 计时终点落在**主线程发布 mask 之前**（`CameraManager.swift:761`），不满足「手指离开 → 看到 mask」的定义 | 429–1030 ms 是**下界估计**，真实感知延迟更高。因此本裁决的方向（延迟确实需要治理）**更强**而非更弱；但任何「已达标」的判断不得基于该口径 |
| **G-2** | 本轮**零个** encode+decode 慢路径样本（唯一一次 parked tap 被丢弃）| 慢路径**无实测基线**。慢路径的 UI 策略**不予封层**，只给临时规则（见 §10.4 Tier 1）|
| **G-3** | encoder 冷启动 warmup 实测 **1283 ms ~ 8605 ms**（跨度 6.7 倍），而 `scheduleTapBusyTimeout` 兜底仅 **3 秒** | 8.6 秒那一次：超时先清掉加载指示，用户看到「点了没反应」。**这是当前 tap 路径最严重的用户可见失败模式**，且是纯逻辑不一致（兜底值与实测分布不相容），不是调参问题 |

---

### 10.3 裁决结论

> **D1 —— 不引入进度条，不引入「裁剪 / 分段等待」UI。**
>
> 理由：快路径的真实计算量是 61 ms。用 UI 去粉饰一个 90% 由错误队列归属造成的排队延迟，是把架构缺陷转译成永久性 UI 债；同时进度条会向用户植入「这个功能本来就慢」的错误心智模型，而事实是它本来很快。**先修调度，再谈 UI。**

> **D2 —— 保留 `TapLoadingIndicator`（脉冲圆环）作为唯一等待反馈，并明确其语义为「已收到点击」的确认，而非进度指示。**
>
> 它不表达剩余时间，不分段，不带百分比。`@Published tapProcessing` 保持为布尔量，**不得**扩展为进度值 —— 一旦有了进度数值，就会有人去画进度条。

> **D3 —— 授权并要求 Builder 将 tap 快路径从 `videoQueue` 上摘除（架构级，Day 5 内完成）。**
>
> 这是本次裁决的**主要交付物**，UI 策略只是其推论。

> **D4 —— `scheduleTapBusyTimeout` 的 3 秒固定兜底作废，改为按分支分级，且超时必须显式告知失败。**

> **D5 —— `e2eMs` 计时口径修正为架构级验收定义；Day 4 的 429–1030 ms 降级为「下界参考」，不得作为 Day 7 验收基线。**

---

### 10.4 对 Builder 的具体要求（接口级，不含实现）

#### 要求 A（对应 D3）— 快路径去队列化

**契约：** tap 的**快路径不需要 camera pixel buffer**，只需要 letterbox 几何 + 已缓存的 embedding 引用。因此它没有任何理由排在 YOLO 帧后面。

```
现状（错误）:
  手势线程 → videoQueue.async { handleTap }      ← 排在 YOLO 帧后（400–670 ms）
              ├─ stateLock 读 embedding / geometry
              └─ decoderQueue.async { decode }    ← 61 ms

要求（Day 5）:
  手势线程 → stateLock 快照 { FrameGeometry, embedding 引用, TTL/geoChanged 判定 }
              ├─ 判定 = 快路径 → decoderQueue.async { decode }   ← 直达，不经 videoQueue
              └─ 判定 = 慢路径 → videoQueue.async { 取 pixelBuffer → encode → decode }
```

- 快照必须在 `stateLock` 保护下完成，且**只做读取与判定**，不做任何模型调用（锁内不得跨队列等待）。
- 慢路径（`canReuse == false`）路径**不变**，仍需最新 pixel buffer，仍走 videoQueue。
- §4.2 的触发逻辑语义**不变**（TTL + geometry 判定、encode/decode 顺序、`isEncoding` 共享 flag 全部保持）。本要求只改**判定发生在哪个队列**，属调度层细化，不构成契约变更。
- 不得为快路径新建第三个队列；`decoderQueue` 仍为唯一 serial decode 队列。

**目标：** 快路径 tap→mask（修正口径）**p95 ≤ 200 ms**。

#### 要求 B（对应 D5）— 计时口径修正

- `e2eMs` 终点必须移到**主线程 mask 实际提交渲染之后**（`maskImage` 赋值 / layer contents 更新所在的 main-thread 块尾）。
- 起点保持在手势 `.ended`。
- 日志必须区分快/慢路径标签，并附带 `cacheAge`（见 R2）。
- Day 7 的 tap→mask 指标**必须**用修正口径重采；Day 4 数字在 Day 7 报告中只能以「旧口径下界」形式出现。

#### 要求 C（对应 D4）— 超时分级与失败可见化

| 分支 | 兜底超时 | 超时后行为 |
|------|---------|-----------|
| 快路径（decode-only）| **1.5 s** | 清指示 + 显式失败提示（tap 无效） |
| 慢路径（encode 在途，encoder 已 warm）| **12 s**（覆盖实测 8605 ms 上界 + 余量）| 清指示 + 显式失败提示 |
| **冷启动 warmup 未完成期间的 tap** | 不适用 —— 见下 | —— |

- **静默清除加载指示是被禁止的行为。** 任何超时路径都必须给用户一个可见的失败信号（文案 / toast / 指示环变色皆可，由 Builder 选形式），否则失败模式退化为「点了没反应」，这是最坏的一种。
- **冷启动（1283–8605 ms）的正解不是加长超时，而是不让 tap 掉进一个会超时的等待。** 要求：warmup 未完成时，UI 呈现明确的「初始化中」状态（可复用脉冲指示但语义不同），tap 要么被显式排队一次（parked tap，warmup 完成后立即执行），要么被明确拒绝并提示。**不得**沉默丢弃 —— Day 4 唯一一次 parked tap 就是被丢弃的（G-2 的成因）。
- `.tapToSegment` 模式**必须**触发 warmup（debug_report §254 记录 `warmupSegmentationIfPossible()` 当前只在 `.segmentation` 下调用）。修复后冷启动窗口应从「首次 tap 时」前移到「进入模式时」，用户可见延迟随之消失。此项归 Builder，Day 5 内完成。

---

### 10.5 保留项与风险（不封层，明确列出）

| 编号 | 保留项 | 处置 |
|------|-------|------|
| **R1** | ✅ **已结案（2026-08-16）。** 慢路径 e2eMs：mean=822.9ms / p95=915.1ms（n=5，debug_report §29）。Tier 1 慢路径 UI 临时规则留至 Phase 4 裁决（sample size n=5，p95 估计精度低，Phase 4 Day 1 建议扩充至 n≥20）<br>➡️ **最终处置见 §15.6.1（2026-08-16）：**n 已补至 22（mean 804.5 / p95 931.0，p95 首次脱离 max），Tier 1 慢路径 UI 转 🔒 FINAL（§15.3 D-15.1）⇒ **R1 两部分全部清偿，正式除名。** | ✅ 完全结案（§15.6.1）|
| **R2** | **embedding 复用过于激进**：TTL 8000 ms + 刷新阈值 5000 ms，tap 可能命中最长 8 秒前的 embedding；手持抖动下 tap 点与 embedding 语义错位，**可能是过分割的隐藏贡献者** | 当前日志不打 cache age，**无法证实也无法排除**。要求 Builder 在要求 B 的日志中补 `cacheAge` 字段（只打日志，不改行为）；TTL 数值本身留在 Day 6 复议（故 §9.5 中 TTL 标为未封层）|
| **R3** | **低对比失败率约 12%（6/50）在 Day 5 多实例下可见度被放大**：最多 3 个实例并存，单次会话中至少出现一个坏 mask 的概率由 12% 升至约 1−0.88³ ≈ **32%** | 不改 Day 5 范围。按 Day 4 保留项的三步走执行：(1) Builder 打 stability score 日志、**不参与决策**（保护 88% 基线）；(2) 定向复采带 `candidates` 日志；(3) ML_Vision 裁决。Day 5 期间**禁止**在多实例逻辑里夹带任何 mask 质量过滤——两个变量同时动会毁掉归因 |
| **R4** | 要求 A 落地后，多个实例的 decode 请求会更密集地进入 `decoderQueue`（serial）| Day 5 的 TapInstanceManager 必须按 §3.2 顺序入队、不并发；若出现 decode 排队堆积（debug_report §264 记录 `tapDecodeWithPoint` 无 `isDecoding` 检查），按「同一实例的新 tap 取代旧请求」处理，不做无界排队<br>➡️ **状态更新见 §15.6.2（2026-08-16）：**实测 `qwait` mean 0.14–0.25 ms / p95 0.60 ms，堆积**未发生**；但测量协议为「逐次 tap、间隔 ≥2 s」，**R4 所描述的密集节奏从未出现过** ⇒ **判定为「未触发」而非「已排除」，R4 保持 OPEN**；Phase 4A re-anchor 是第一个会制造该节奏的改动，处置见裁决 D-15.2（负载自适应节流）<br>➡️ **状态更新见 §34.3（2026-08-17）：CONFIRMED**（qwait max 189.90 ms，自变量 = 批内实例数 ≥2）<br>➡️ **最终处置见 §20.4.2（2026-08-17）：重新划范围结项。** R4-a（re-anchor↔re-anchor 累积）与 R4-b（re-anchor 让路给 tap）**关闭**；R4-c（tap 排在 re-anchor 之后）**从未在被专门制造的条件下测到**，但已由构造定界（单在途 + 批次恒为 1 ⇒ 最坏 ≈61 ms）并被 D-7 的 195 ms 预算覆盖。⛔ **不得表述为「R4 已排除」**；四条重开触发条件见 §20.4.2 第 4 点<br>➡️ **状态更新见 §21.2（2026-08-18）：R4-c 首次获得测量值，R4 状态不变、不关闭。** §36.4 在「三实例在屏 + 刻意高频 tap」下取样 n=32：max **4.9 ms** / p95 3.5 / mean 0.45；max 那一条经时间戳反推确为 R4-c 形态本身（tap 排在 re-anchor 之后），**距重开触发条件 (iii) 的 > 5 ms 线仅 0.1 ms —— 按实测记录余量，⛔ 不表述为「通过」**。⚠️ 该轮期望碰撞仅 ≈0.31（低于 §35 的 ≈0.95，因高频 tap 抑制 re-anchor 4.9×）⇒ **4.9 ms 不是尾部估计**；该 tap 等的是 re-anchor 单元的 post-decode 尾段（≈10 ms）而非 decode（≈55 ms）⇒ **构造界 ≈61 ms 不变，仍被 D-7 的 195 ms 预算覆盖**。⚠️ 证据来源为粘贴转录、无归档日志（§21.1）。⛔ 仍不得表述为「R4 已排除」 |
| **R5** | 本裁决全部数据来自 iPhone 11 / A13 单一设备 | A16/A17 上排队占比与 encoder 绝对值都会变。§9.5 的绝对值列为 A13 专属；跨设备结论不得外推（同 §9.2 教训）|

---

### 10.6 Day 7 复审门控（预告）

要求 A–C 落地后，Day 7 用**修正口径**重测，按下表复审 §10.3 的 D1：

| 快路径 tap→mask（修正口径，p95） | Day 7 决定 |
|---------------------------------|-----------|
| ≤ 200 ms | ✅ D1 维持；脉冲确认动画可进一步简化为一次性波纹 |
| 200–500 ms | ✅ D1 维持；保留持续脉冲确认 |
| > 500 ms 且已排除排队因素 | ⚠️ 重开进度语义 UI 议题（此时才成立） |

（门控语义按 §9.3 修订后的规则理解：批准取合取，撤销取析取。）

---

### 10.7 Deliverable 确认

- [x] Encoder 分辨率最终封层：**1024**，768 撤销、512 维持拒绝（§9.1–9.4）
- [x] §8.7 门控口径缺陷两则如实记录并给出修订规则（§9.3）
- [x] 970.6 ms 的热态与会话条件标注完成，绝对基线改为 648 ms（§9.3 缺陷 2）
- [x] Phase 3 冻结参数表最终封层（§9.5，取代 §8.5）
- [x] tap 延迟展示策略裁决：**不设进度条**，保留脉冲确认动画（§10.3 D1/D2）
- [x] 授权 Builder 快路径去队列化 + 计时口径修正 + 超时分级（§10.4 要求 A/B/C）
- [x] 保留项 R1–R5 明确列出，慢路径 UI 不封层（§10.5）
- [x] Day 7 复审门控预告（§10.6）

**Phase 2 pipeline 保护状态：** 无变化。要求 A 只改 tap 快路径的队列归属，`.segmentation` 路径与 YOLO delegate 回调不受影响 ✅

---

END OF PHASE 3 DAY 5 ARCHITECT OUTPUT（§9–§10）


---

# Phase 3 — Day 5 追加 (Architect) per-instance mask 生命周期裁决

日期：2026-08-09
依据：Builder 上交的规格自相矛盾（`builder_progress.md` Day 5「交给 Architect 的未决问题」）+ 用户真机实测反馈 + Session A 人工评分方法论（`shared/phase3_day4_test_checklist.md` §3.4）+ Day 4 良品率基线（44/50 = 88%）
触发方式：Builder 发现 §3.1 与 §3.4 冲突后**未擅自决定行为**，如实上交。此为正确处置，记录在案。

---

## 11) per-instance mask 生命周期裁决（TTL 撤销）

### 11.1 裁决结论

| 问题 | **裁决** |
|------|---------|
| **Q1 — per-instance mask TTL 在 Phase 3 的最终语义** | ❌ **取消**。时间型 mask TTL（2000 ms）整体撤销，不保留为「内部状态」，不延长到任何其它数值，**不改作他用**。`maskTimestamp` 保留但降级为纯遥测字段 |
| **Q2 — mask 的正式清除条件** | **事件驱动，穷尽列表 = §3.2.1 的 C1–C6**：C1 双击 `clearAll()` / C2 第 4 次 tap 的 FIFO 淘汰 / C3 退出 `.tapToSegment` / C4 几何签名变化清空 / C5 同实例请求取代（替换非清除）/ C6 长按删除单实例（Day 6）。**时间流逝不是清除条件** |
| **Q3 — §3.1 / §3.4 原文修订** | 已直接改写，见本文件 §3.1「Mask 生命周期」段、新增 §3.2.1、§3.4「修订说明」段。§9.5 冻结参数表同步：`Mask TTL` 行作废，新增 `Tap mask 生命周期 = 事件驱动，无时钟` 🔒 FINAL |
| **Q4 — 与 embedding TTL 8000 ms 的关系** | 见 §11.6。**Phase 3 内只保留一个时间型 TTL：embedding TTL。** mask 侧不存在第二个时钟，术语「mask TTL」自 Day 5 起在本项目中**停用** |
| **Q5 — 对 Day 5 Debugger 五条验收的影响** | 见 §11.7。第 1 条的**前提被修复**（原字面规格下不可能通过），第 4 条的**测量前提被改变**（mask 常驻 ⇒ 内存不再被 TTL 自动回收所掩盖）。第 2/3/5 条不受影响。**本裁决不代 Debugger 勾选、不改写其条目** |

**Builder Day 5 的既有实现（TTL 状态维护但不接显示、`drawableInstances()` 不过滤过期）与本裁决的行为结论一致。因此 Day 5 内不需要任何行为改动，也不需要重新出包**——Debugger 可直接在当前构建上执行验收。命名与死代码清理归 Day 6（§11.8）。

---

### 11.2 冲突的根因：把「刷新驱动管线」的 TTL 复制到了「一次性用户产物」上

原 §3.1 的写法是「**复用** Phase 2 mask TTL = 2000 ms」。复用二字暴露了问题所在——两个管线的 mask 有本质不同的生命周期性质：

| 维度 | Phase 2（`.segmentation` 自动分割） | Phase 3（`.tapToSegment`） |
|------|-----------------------------------|---------------------------|
| mask 的来源 | 机器选择：YOLO 检出 → TemporalManager 调度 decode | **用户显式选择**：用户点了这个点 |
| 是否有刷新源 | **有**。每帧 / 每 12 帧的持续调度会不断产生新 mask | **无**。tap 是一次性事件，没有任何机制会自动重新生成它 |
| 过期的代价 | **短暂空窗**，下一轮调度即补上 | **永久丢失**，且用户无法「让它自己回来」，只能重新 tap |
| 陈旧的危害 | 高。物体在动，旧 mask 会漂移到错误位置并误导 | 中。但真正的失效事实是**几何/坐标空间变化**，已由 C4 事件精确捕获 |

> **根因判定：TTL 是一种「等下一次刷新」的兜底机制，它的正确性前提是「存在刷新循环」。Phase 3 的 tap 路径没有刷新循环，把 TTL 搬过来是一次类别错误（category error），不是参数选错。**
> 因此正确处置是**撤销机制本身**，而不是把 2000 ms 调大。调大只是把「多久之后无理由地删掉用户的选择」这个问题推迟，性质不变。

**代价的不对称性（本裁决的核心论据）：**
- TTL 的收益：避免一段时间之后 mask 可能已经不贴合物体所带来的误导。
- TTL 的代价：**删除用户显式产生的结果，且不可自动恢复**。
- 用户**已经拥有**清除手段（双击，C1），而且是零成本、随时可用的。
- 用户**没有**恢复手段。
> 在「用户能自己清除」与「用户无法自己恢复」并存的情况下，把清除权交给墙钟是错误的权力分配。**清除权归用户，失效权归事件。**

---

### 11.3 原 2000 ms 设定的理由复核（逐条，不做简单删除）

按要求，对当初设定 2000 ms 可能的三条理由逐一复核其在当前证据下是否仍成立：

| 原理由 | 在 Phase 2 是否成立 | 在 Phase 3 tap 路径是否**仍**成立 | 结论 |
|--------|-------------------|--------------------------------|------|
| **R-a 避免陈旧 mask 误导**（物体已移动，旧 mask 贴错位置）| ✅ 成立。Phase 2 的目标是移动中的自动检出物，且有刷新循环兜底 | ⚠️ **部分成立，但工具选错**。真实失效事实是坐标空间变化（旋转 / 预览尺寸变化），已由 **C4 事件**精确、无延迟地捕获；墙钟 2000 ms 既**误杀**（机位不动时 mask 依然正确却被删）又**漏杀**（1.9 s 内快速甩动相机，mask 已经错了 TTL 却还没到）。**墙钟与失效事实不相关，是一个坏的代理变量** | 撤销 TTL，保留 C4 |
| **R-b 限制内存**（不让 mask 无限堆积）| ✅ 在 Phase 2 成立（检出数无硬上限）| ❌ **不成立**。Phase 3 实例池有硬上限 **N=3**，FIFO（C2）已经封顶。量级：mask 256×256 Float32 = 256 KiB + alpha 256×256 UInt8 = 64 KiB，**3 实例合计 < 1 MB**，相对 Debugger 的 +30 MB 预算差 30 倍以上 | 该理由在 N=3 下失效 |
| **R-c 与 Phase 2 保持参数一致性**（「复用」）| —— | ❌ **不成立**。一致性本身不是理由；两条管线的刷新性质不同（§11.2 表），强行对齐参数只会把一个管线的假设偷渡进另一个管线 | 术语与数值一并停用 |

**新增的、当初未纳入考虑的约束（本次裁决的决定性输入）：**

1. **用户真机实测反馈**：当前构建中 mask 在**双击之后才消失**，用户明确表示「这样很适合测试阶段」。这是对既有行为的直接确认。
   - ⚠️ 如实记录措辞边界：用户说的是「**适合测试阶段**」，**不是**「永久正确」。因此本裁决对 Phase 3 封层，对 Phase 4 不封层（入口条件见 §11.9 R8）。
2. **Session A 人工评分方法论依赖 mask 常驻**（`phase3_day4_test_checklist.md` §3.4 标准流程）：
   ```
   点击 → 等 mask 出现（约 1 秒）→ 停 2 秒（录屏拍清楚）→ 双击清除 → 再停 2 秒
   ```
   - 该流程要求单次 tap 的 mask 至少常驻 **≈3 秒**（出现 1 s + 停留 2 s），2000 ms TTL 会在录屏拍清楚之前把它删掉。
   - 更严重的是：TTL 会让「**双击清除**」这个**受控动作失去意义**——mask 早已自行消失，观察者无法区分「双击生效了」与「TTL 到了」，采样流程的可控性被破坏。
   - **已建立的基线**：Session A 用该流程产出 **50 次评分，良品率 44/50 = 88%**（基线 15/24 = 62%）。后续的 stability 定向复采、以及任何改动前后的 A/B 对照**仍将复用同一流程**。
   > **裁决含义：mask 存续语义是评分方法论的一部分。改变它 = 使已建立的 88% 基线不可比。** 这条约束单独就足以否决 2000 ms TTL，与前述架构论证相互独立且方向一致。
3. **相对 Day 4 的行为回退**：Day 4 已交付「tap mask 常驻直到双击清除」。TTL 上线属**用户可见的功能倒退**，且无任何用户价值支撑。

---

### 11.4 长期约定（写入架构原则，与 §9.2 的教训并列）

> **约定 A-1（TTL 适用性判据）：**
> **时间型 TTL 只允许作用于「过期后可被自动重建」的资源。** 对于「过期后无法自动重建的用户产物」，**禁止**使用时间型 TTL，只能使用**事件型失效**（有可观测的失效事实作为触发）。
> 自检问法：*「它过期之后，谁会把它变回来？」* —— 答不出具体机制，就不该有 TTL。
> - embedding：过期 → 下一次 encode 重建，代价是**延迟** ⇒ 允许 TTL ✅
> - tap mask：过期 → **无人重建**，代价是**内容丢失** ⇒ 禁止 TTL ❌

> **约定 A-2（跨模式参数复用）：**
> 「复用 Phase N 的某参数」不构成设计理由。跨模式复用参数前必须先确认**两个模式的资源生命周期性质相同**（刷新源、过期代价、失效事实是否可观测）。性质不同则参数不可搬运，**术语也不应搬运**。

（§9.2 的既有教训「跨硬件后端的比例外推不构成裁决依据」与本条并列；二者同属「相似不等于可迁移」这一类错误。）

---

### 11.5 §3.1 / §3.4 修订后原文（本次已直接改写，此处为索引）

| 位置 | 原文 | 修订后 |
|------|------|--------|
| §3.1 | `**Mask TTL：** 复用 Phase 2 mask TTL = **2000 ms**（与 TemporalManager 独立计算，不共享状态）` | 改为「**Mask 生命周期**」段：per-instance mask **不设时间型 TTL**，已解码成功的 mask 常驻至被 §3.2 事件移除；原条款正式作废；`maskTimestamp` 保留为遥测时间戳，不得进入显示 / 过滤 / 有效性判定路径 |
| §3.1 结构体注释 | `var maskTimestamp: Date? // mask 生成时间（用于 TTL 判断）` | `var maskTimestamp: Date? // mask 生成时间（遥测口径 maskAgeMs；不参与有效性判定，见 §11）` |
| §3.2 | （无清除条件全集）| 新增 **§3.2.1 mask 移除条件全集**（C1–C6，穷尽列表；表外原因导致 mask 消失按 bug 处理）|
| §3.4 | `无有效 mask：不显示 mask，保留 tap 位置波纹动画指示` | `实例已创建但 mask 尚未就位（mask == nil）：不显示该实例的 mask，在其 tap 位置显示等待指示（§10.3 D2 脉冲确认）`；并补「实例池为空 → maskImage = nil」；附修订说明：**「不显示」的唯一条件是 `mask == nil`；已解码成功的 mask 不存在「失效」状态** |
| §9.5 | `\| Mask TTL \| 2000 ms \| ⚠️ 未封层 \| Day 6 \|` | 该行作废（删除线保留痕迹）；新增 `Tap mask 生命周期 \| 事件驱动，无时钟（= §3.2.1 C1–C6）\| 🔒 FINAL（Day 5）`；`Embedding TTL` 行补注「Phase 3 内唯一的时间型 TTL」|

（§8.5 的 `Mask TTL | 2000ms` 行位于**已被 §9.5 取代**的历史表中，按既定处理方式不回改，历史文本保持原样。）

---

### 11.6 与 embedding TTL（8000 ms）的关系 —— 术语纪律

本裁决**刻意避免**留下两个语义相近、数值不同的 TTL。处置如下：

| 对象 | Phase 3 内的失效机制 | 数值 | 状态 | 过期后谁重建 | 过期代价 |
|------|--------------------|------|------|------------|---------|
| **embedding** | **时间型 TTL** + 几何签名事件（双重）| **8000 ms** | ⚠️ 未封层，数值复议留 Day 6（R2）| 下一次 encode（约 648 ms）| **延迟**，无内容丢失 |
| **tap mask** | **纯事件型**（§3.2.1 C1–C6）| —— **无时钟** | 🔒 FINAL（Day 5）| **无人重建** | **内容丢失，不可恢复** |

**术语纪律（自 Day 5 生效）：**

1. **「Mask TTL」这一术语在 JudgeE2 项目中停用。** 后续文档、代码注释、日志字段名一律不得再出现。需要表达 mask 何时消失时，写「**mask 移除条件（§3.2.1 C1–C6）**」。
2. **项目中「TTL」一词自此专指 embedding TTL**，单一所指，无需再加限定词。看到 TTL = 8000 ms，不存在第二个候选。
3. `maskTimestamp` 派生量的命名必须是**年龄**而非**有效性**：允许 `maskAgeMs`（遥测），**不允许** `isMaskValid` / `maskTTL` / `maskExpiry` 这类**判定式**命名——一个休眠的 `isMaskValid(now:)` 是个地雷，早晚有人把它接到 `drawableInstances()` 上。命名即防呆。
4. R2（embedding 复用过激，可能命中 8 秒前的 embedding）**不受本裁决影响，仍完整保留**，Day 6 复议。注意区分：R2 讨论的是「mask 是用**多旧的 embedding** 解出来的」（生成时刻的输入陈旧度），本裁决讨论的是「**mask 生成之后**能存活多久」（产物的存续期）。两者是不同的量，**不得**互相引用为依据。
   - 顺带明确一条可能被误解的推论：mask 常驻**不会**加剧 R2。R2 的风险在解码那一刻就已经全部兑现，之后 mask 存续多久都不改变它的质量。

---

### 11.7 对 Day 5 Debugger 五条验收的影响（**不代勾选，不改写条目**）

| # | Debugger 条目（原文，不动）| 本裁决的影响 | 需要注意什么 |
|---|--------------------------|-------------|-------------|
| **1** | 分别 tap 1、2、3 个不同位置，确认多 mask 同时显示 | ✅ **前提被修复**。在原字面规格（2000 ms TTL + 「无有效 mask 不显示」）下，该动作**在物理上不可能通过**——依次点三下必然超过 2 s，第一个在第三个出现前已过期。本裁决使该验收成为**可执行且有意义**的检验 | 无需刻意加快点击节奏；三次 tap 之间可以正常停顿。若仍出现「先点的 mask 自己消失」，那**不再有任何合规解释**，应按 bug 上报（唯一合法的自动全清是 C4 几何变化，需同时伴随构图/旋转变化）|
| **2** | 确认插入第 4 个 tap 时最老实例被删除（FIFO）| ⚪ 不受影响，且**归因变干净了**：撤销 TTL 后 FIFO（C2）成为「最老的那个消失」的**唯一**解释。原设计下无法区分「FIFO 淘汰」与「TTL 过期」，验收结论会有歧义 | —— |
| **3** | 确认同一 geometry 下各个实例共享 embedding，不重复 encode | ⚪ 不受影响（该条归 embedding TTL 8000 ms + `tapGeometryChanged` 管辖，与 mask 生命周期无关）| 前提澄清：三次 tap 的**间隔总和须在 8 s 内**，否则触发的 re-encode 是 embedding TTL 的正常行为、**不是** bug。测量时把 burst 控制在 8 s 内 |
| **4** | 确认内存在 3 个实例同时活跃时无明显升高（< +30 MB）| ⚠️ **测量前提被本裁决改变**。mask 现在**常驻**，不会被 TTL 自动回收，因此这条从「顺带看看」变成了**真正的泄漏检验**——原设计下 TTL 会周期性释放 mask，可能**掩盖**实例池的泄漏 | 建议（不改条目，供 Debugger 自行取舍）：(a) 在「3 个实例都已持有 mask 且常驻」的稳态下读数，而非 tap 之后立即读；(b) 做 ≥12 次 tap（触发 ≥4 轮 FIFO）后再读一次，检验 FIFO 淘汰是否**真正释放**；(c) 量级预期：3 实例 mask+alpha 合计 **< 1 MB**，若实测接近 +30 MB，那**不是 mask 本身**（应是 CoreML / CIImage / 合成缓冲），属需要单独报告的发现，不应因为「仍在 30 MB 预算内」而放过 |
| **5** | 确认对齐告警在当前测试中已消失或显著减少 | ⚪ 不受影响 | —— |

**上述五条的勾选权、措辞与判定标准全部归 Debugger。本节只说明哪些条目的前提被本裁决改变，不构成对结果的预设。**

---

### 11.8 对 Builder 的处置要求

| 项 | 要求 | 时点 |
|----|------|------|
| **B-1** | **Day 5 内不做任何代码改动。** 既有实现（TTL 不接显示）与本裁决行为一致；此刻改代码会作废 Debugger 即将验收的构建 | Day 5：**不动** |
| **B-2** | 移除 `TapInstance.maskTTL` 派生字段与 `isMaskValid(now:)` **判定式** API（休眠谓词属防呆隐患，见 §11.6 纪律 3）；`maskTimestamp` **保留**，改提供 `maskAgeMs(now:)` 之类的**年龄**读数供日志使用 | Day 6（**非行为改动**，纯命名与死代码清理）|
| **B-3** | tap 日志建议附带 `maskAgeMs`（与要求 B 的 `cacheAge` 并列，两者含义不同见 §11.6-4），为 Day 6 的「mask 年龄 × 质量」相关性分析留数据。**只打日志，不参与任何决策** | Day 6，随 R2 一并 |
| **B-4** | 沿用 §10.5 R3 禁令：**Day 5/Day 6 期间不得在多实例逻辑中夹带 mask 质量过滤**。本裁决同样不得被当作引入「低质量 mask 自动消失」的授权——那是质量过滤，不是生命周期 | 持续 |

**Builder 上交决策而非擅自决定的做法记录在案**：per-instance mask 是否自动消失属**用户可见行为**，行为语义的变更权归 Architect / 用户，实现方保留状态但不接显示是本次的正确处置。此模式作为后续遇到规格冲突时的推荐处理方式。

---

### 11.9 保留项与风险

| 编号 | 保留项 | 处置 |
|------|-------|------|
| **R6** | **场景运动导致 mask 与物体脱节，当前无任何机制捕获**。C4 只覆盖几何/坐标空间变化（旋转、预览尺寸），不覆盖「机位不动但物体走了」或「手持缓慢平移」。撤销 TTL 后，这类脱节的 mask 会**一直显示**直到用户双击 | **Phase 3 不处理，明确接受。** 依据：(a) Session A 流程要求「手机架好后全程不动」，测试期该风险被设计消除；(b) 正确的解法是**事件型**的漂移检测（tap 点邻域光流 / detection box IoU 变化），而不是墙钟；(c) 引入漂移检测属新功能，需用户显式立项。Day 7 冻结时如实记录为已知限制 |
| **R7** | **前后台切换 / camera session 中断恢复后是否应清空实例池**，当前未定义（不在 C1–C6 内）| Day 6 待议。倾向：若中断恢复会改变几何签名，则 C4 已自动覆盖；若不改变，则属 R6 的一个特例。**Day 5 不新增行为**，先由 Debugger/用户观察实际表现再定 |
| **R8** | 用户反馈的措辞是「**适合测试阶段**」，不是「产品形态最终结论」 | Phase 3 封层常驻语义。**Phase 4 若重开「mask 自动消失」议题，入口条件为三条同时满足**：(1) 有客观的失效度量（不是墙钟）；(2) 先建立 tap mask 的**刷新循环**（让 mask 能自动重解码跟随目标），使「消失」的代价从「内容丢失」降为「短暂空窗」——即先补上 §11.4 A-1 缺的那个前提；(3) 评分方法论同步修订并**重建基线**（因 88% 不再可比）。仅凭「久了可能不准」不构成立项理由 |
| **R9** | 本裁决未改变任何 mask **质量**相关逻辑（候选选择、stability、flood fill 全部原样）| 与 §10.5 R3 的三步走互不干扰；Day 4 的 88% 良品率基线在本裁决下**保持可比** |

---

### 11.10 Deliverable 确认

- [x] Q1 裁决：per-instance mask **时间型 TTL 取消**，不延长、不改作他用；`maskTimestamp` 降级为遥测（§11.1）
- [x] Q2 裁决：mask 清除条件正式定义为事件驱动穷尽列表 C1–C6（§3.2.1）
- [x] Q3：§3.1 / §3.4 原文已直接改写并自洽；新增 §3.2.1；§9.5 冻结表同步（§11.5）
- [x] Q4：与 embedding TTL 8000 ms 的关系厘清 + 术语纪律（「Mask TTL」停用，TTL 一词专指 embedding）（§11.6）
- [x] Q5：对 Day 5 Debugger 五条验收的影响逐条说明，**未代勾选、未改写条目**（§11.7）
- [x] 原 2000 ms 的三条理由（陈旧误导 / 内存 / 一致性）逐条复核，说明何者仍成立、何者失效（§11.3）
- [x] 长期约定 A-1（TTL 适用性判据）/ A-2（跨模式参数复用）写入（§11.4）
- [x] 保留项 R6–R9 明确列出（§11.9）

**Phase 2 pipeline 保护状态：** 无变化。本裁决只作用于 `.tapToSegment` 的 per-instance mask 生命周期；Phase 2 的 mask TTL（isolate_phase2.md 冻结表，2000 ms）**原样保留、不受影响**——它有刷新循环，符合 §11.4 A-1 ✅

**Day 5 构建状态：** 本裁决**不要求任何 Day 5 代码改动**，Debugger 可在当前构建上直接验收 ✅

---

END OF PHASE 3 DAY 5 ARCHITECT OUTPUT（§11 追加裁决）

---
---

# Phase 3 — Day 5 追加二 (Architect) 呈现规格复议裁决
# 2026-08-11
# 触发：用户主诉「app 分割物体的精确度比 Day 4 下降很多」
# 输入：debug_report.md §22（补测，Release）、§23、§25.1 的 A-2 / A-3；录像 shared/simu_record_0811.MP4
# 范围：**只裁 §3.3/§3.4 呈现规格**。候选选择规则（A-1）不在本次范围，见 §12.8

---

## 12) 呈现规格复议裁决（撞色 / 亮度 / 描边缺席）

### 12.1 裁决结论

| # | 裁决 | 状态 |
|---|---|---|
| **Q1** | **`systemBlue / systemGreen / systemOrange` 三色板正式作废。** Phase 3 全部实例统一使用青 **(0,217,255)**，primary alpha **0.60**、secondary **0.40**（§3.3.3） | ✅ 封层 |
| **Q2** | **建立呈现的三层职责模型 L1/L2/L3，并裁决「L1 可见性不得由色相承担」**（§3.3.1） | ✅ 封层，架构原则 |
| **Q3** | **建立填充色约束原则 C-1…C-7**，含稀有色带 / 亮度下界 / 饱和度下界 / 描边强制 / 屏幕坐标系描边 / 单变量回退 / **换色准入程序**（§3.3.2） | ✅ 封层 |
| **Q4** | **描边由「白色 1pt」升级为「双色轮廓（深外圈 + 浅内线）」**，且 **primary 与 secondary 都要有**（分档，§3.4） | ✅ 封层 |
| **Q5** | **描边任务从 Day 6 提前到 Day 5**，定级 **P0**（§12.6） | ✅ 已改 tasks.md |
| **Q6** | **按实例配色（per-instance hue）在 Phase 3 内撤销**，`TapInstance.color` 字段保留但语义降级为「呈现槽位」 | ✅ 封层 |
| **Q7** | **R6（场景运动导致 mask 脱节）复议：维持「Phase 3 不做自动失效」的原裁决，但优先级从「已接受的功能限制」上调为「已确认的呈现风险」**，并给出两条不违反 §11 的呈现侧缓解（§12.7） | ✅ 复议完成，裁决未变、定级已变 |

**一句话：** 用户看到的「变差」是**呈现层的三处叠加回退**，几何零变更（Debugger 已逐行核对）；而三处里最致命的一处（撞色）不是实现错误，是**我在 §3.3 写下那套色板时就已经注定的规格缺陷**——因为我把「看不看得见」押在了「用户拍的物体是什么颜色」上。

---

### 12.2 定性：这是一个规格缺陷，不是实现缺陷，也不是参数没调好

Debugger 已用两条互相独立的方法（Builder 的留存基准文本比对、Debugger 的代码路径追踪 + 常量清点）确认：

> `buildTapAlpha` 函数体只有一行，直接转调 `buildAlpha`；候选选择整段（`MaskRenderer.swift:402-483`）连同 `minComponentPx=30` / `minComponentSidePx=3` / `minComponentFill=0.05` / `maxPlausibleLogit=500` / cap60 / cap85 / `iou_pred>=0.1` 与 Day 4 逐项一致。
> ⇒ **同一帧、同一 embedding 下，Day 5 与 Day 4 的二值 alpha 逐位相同。**

且 2026-08-11 录像目视确认：mask 覆盖的是**完整物体**（保温杯、键盘、文件架、显示器文档区、瓶子），**不是碎片**。§22.5 提出的「并排录像决定性实验」至此**已执行完毕，落在「主诉 = 感知变化」分支**。

所以问题的定性是清楚的：

> **规格 §3.3 把「L1 可见性」这一必须无条件成立的性质，交给了「填充色与物体本色的对比度」这一不可控输入。**
> 在这种规格下，撞色不是概率事件，而是**遍历事件**——只要用户拍的东西足够多，就一定会撞上。
> systemBlue / systemGreen / systemOrange 恰恰是日常物体**最常见**的三个色系，于是「足够多」变成了「一个桌面」。

**Builder 没有做错任何事**：他照着 §3.3/§3.4 实现，甚至保留了 `renderMask` 的青色字面量以求兼容。修复路径在规格侧。

---

### 12.3 三个叠加成因逐条复核（不做合并，逐条给结论）

#### 成因 1 —— 亮度下降 2.7 倍：✅ 成立，已由 C-2 封堵

sRGB 相对亮度：青 (0,217,255) **Y = 0.569** → systemBlue (0,122,255) **Y = 0.211**，降至 **37%**。
叠加 alpha 0.60 → 0.55（着色强度 −8%），secondary 0.35 只剩 Day 4 的 **58%**。

- 关键不只是「暗」，而是**方向反了**：Y=0.569 的青压在多数室内表面上是**提亮**，Y=0.211 的 systemBlue 在中等以上亮度的表面上是**压暗**。一个提亮的覆盖层与一个压暗的覆盖层，在「这块被选中了」的可读性上不是程度差别，是性质差别。
- 处置：C-2 设 **Y ≥ 0.45** 硬下界。该下界同时判掉 systemGreen（0.428）与 systemOrange（0.425）——它们表面上「够亮」，实际都在线下。

#### 成因 2 —— 调色板与物体本色撞色：✅ 成立，且**判定为本次的主成因**，已由 C-1 + C-4 + C-7 封堵

这是本次新增的、此前分析未纳入的一条，也是唯一能解释「为什么是**很多**、而不是**有点**」的一条：

| 物体 | 物体本色 | 被涂上的 mask 色 | 结果 |
|---|---|---|---|
| 保温杯 | 蓝 | systemBlue H=211° | 等于隐身 |
| 键盘 | 绿 | systemGreen H=135° | 等于隐身 |
| 文件架 | 橙 | systemOrange H=35° | 等于隐身 |

**三块 mask 全部撞上物体自身颜色。** 用户看到的不是「mask 画歪了」，而是「点了没反应 / 只有一点点」——而这正是**几何完全正确**的三块 mask。

- 为什么 Day 4 的青不会这样：(0,217,255) 是自然物体与常见工业配色里**几乎不出现**的信号色，压在任何物体上都跳得出来。
- 为什么这不是「换一套更好的三色」能解决的：见 §3.3.3 的四条理由。核心是**允许色带装不下三个可区分色相**。
- 处置：C-1 明确禁用带（把 systemBlue/Green/Orange 三者一次性判掉）+ C-7 换色准入程序（**必须在同色系真实物体上做撞色测试才能合入**）。C-7 是这条的防复发机制——没有它，下一次换色照样会撞。

#### 成因 3 —— 白色 1pt 轮廓缺席：✅ 成立，且定性被**升级**

原 §3.4 把描边写成「Day 6 实现」，隐含把它当作锦上添花。**本次裁决推翻这个定性：**

> 描边不是装饰，它是 **L1 可见性的承重结构**，是整套呈现里**唯一**不依赖用户内容的可见性来源。
> 把它推迟，等于让可见性预算**长期缺一块承重墙**，而缺口的大小由用户拍什么决定。

同时，原规格的「白色 1pt」本身也不够：单色白在白墙 / 浅色桌面上消失。**升级为双色轮廓**（深外圈 + 浅内线），使其在亮度量程的两端都有对比（§3.4）。

---

### 12.4 修订后的 §3.3 / §3.4（本次已直接改写正文，此处仅索引）

| 节 | 变更 |
|---|---|
| **§3.3** | 整节重写：新增 §3.3.1 三层职责模型、§3.3.2 约束原则 C-1…C-7、§3.3.3 Phase 3 封层色板。原三色板正式作废 |
| **§3.4** | 整节重写：填充色/alpha 改为青 0.60 / 0.40；描边升级为双色轮廓并**扩展到 secondary**（分档）；补 primary/secondary 区分载体清单；绘制顺序不变；Day 5 的「`mask == nil` 是唯一不显示条件」条款**原样保留** |
| **§9.5 冻结参数表** | 新增两行（见下） |

**§9.5 冻结参数表增补（Day 5 追加二）：**

| 参数 | 值 | 状态 |
|---|---|---|
| Tap mask 填充色 | 青 (0,217,255)，全实例统一 | **FINAL**（变更须过 §3.3.2 C-1…C-3 + C-7） |
| Tap mask alpha | primary 0.60 / secondary 0.40 | **FINAL** |
| Tap mask 描边 | 双色轮廓，primary 2.0/1.5 pt、secondary 1.5/1.0 pt，屏幕坐标系生成 | **FINAL**（实现 Day 5） |
| 按实例配色 | Phase 3 内**撤销** | FINAL（Phase 4 可重开） |

---

### 12.5 与既有裁决的一致性检查（自检，避免架构漂移）

| 既有裁决 | 是否受本次影响 | 说明 |
|---|---|---|
| §11 mask 无时间型 TTL | ❌ 不受影响 | 本次只改「怎么画」，不改「画多久」。C1–C6 清除条件全集不变 |
| §11.9 R3「Day 5/6 禁止在多实例里夹带 mask 质量过滤」 | ❌ 不受影响 | 本次**没有**引入任何以 mask 质量为条件的显示分支；描边对所有 mask 无差别施加 |
| §10 不设进度条 | ❌ 不受影响 | 等待指示语义不变 |
| §11.9 R9「88% 基线保持可比」 | ✅ **被本次修复** | 填充恢复到与 Day 4 逐位一致；描边是**加法**（只增不减可辨识度）且按 C-6 可单点关闭，故仍能做单变量对照 |
| §1.3 渲染层次（Z 轴） | ❌ 不受影响 | 描边与填充同属 mask 层，不新增层级 |
| D-13（把光栅化交给 GPU，256×256 + `contentsRect`） | ⚠️ **有耦合，已前置约束** | C-5 要求描边在屏幕坐标系生成、线宽以 pt 计。若把描边烧进 256×256 位图，1px 会被放大成 4–8px 且随缩放抖动，同时锁死 D-13。**这条约束必须在实现描边之前就位，否则 Day 6/7 会返工** |

---

### 12.6 描边任务提前的裁决（Q5）

**裁决：把「Mask 轮廓描边」从 Day 6 提前到 Day 5，定级 P0。**

依据：

1. 它是 §3.3.1 里 **L1 的唯一承载者**。在它就位之前，可见性仍然押在填充色上——即使换回青色，也只是把撞色概率从「很高」降到「较低」，**没有改变押注这一性质**。
2. 用户主诉的三条成因里，成因 1 与成因 2 都只能靠「选一个更好的色」缓解，**只有成因 3 能从机制上消除**。
3. 成本低、无算法风险：它不触碰 `buildAlpha` 的任何决策代码，因此**不威胁 §11.9 R3 的禁令，也不影响 88% 基线的几何可比性**。
4. Day 6 的原条目本就存在，提前不是新增范围，是**改排期**。

**tasks.md 的具体改动（本次已执行，改动范围见 §12.10）：**
- **Day 5 / Builder** 新增一条 `- [ ] **Mask 呈现规格修订 + 轮廓描边（P0，从 Day 6 提前）**`，含 4 个子项（色板回退、alpha、双色描边分档、C-5/C-6 约束）。
- **Day 6 / Builder** 原「Mask 高亮 + 轮廓描边」条目**保留但改写**：描边部分标注「已提前至 Day 5」，本条只余「点击已有实例 mask 内部 → 提升为 primary」；并在「Tap 指示动画」下增列 tap 锚点标记（L3 的编号载体）。
- **不新增任何 Day 7 之后的范围。**

---

### 12.7 R6 复议：「mask 永不过期 + 同屏三张不同龄 mask」（Q7）

用户要求显式复议，不要求改变裁决。**复议结论：裁决不变，定级改变。**

#### 12.7.1 复议输入（本次新增，§11.9 写 R6 时尚不存在）

- §16.3 / §23.3：同一 pool 内三个实例的 embedding 最多相差 **8 秒**（embedding TTL 上界）。
- §11 撤销时间型 mask TTL 后，mask 存续时间**无上界**。
- 2026-08-11 录像：屏幕上同时贴着 3 张锚定在不同时刻的 mask，其中部分已从物体上漂走。
- Debugger §22.6 判断：这可能是「最可能被读成不准」的一条（标为推测）。

#### 12.7.2 我在 §11.9 低估了什么（如实记录）

低估的**不是** R6 的正确性风险，而是它的**感知放大效应**：

> 一张漂走的 mask 只是「这一次不准」；**三张里有两张漂走，会被读成「这个功能不准」。**
> 而且 Day 4 的组合（同屏恒为 1 张、每次 tap 整体替换）使陈旧度被**下一次 tap 自动截断**；
> Day 5 的组合（N=3 + FIFO + 永不过期）第一次让**陈旧度变成无上界量**。
> ⇒ R6 不是被 §11 引入的，但**是被 §11 与多实例的组合放大的**。我在 §11.9 只单独评估了 TTL 撤销，没有评估「TTL 撤销 × 多实例」的乘积效应。这是一处真实的分析缺口。

#### 12.7.3 但裁决维持不变，理由四条（逐条独立）

1. **不改变 §11 的核心论证。** mask 是「过期后无法重建的用户产物」这一事实不因漂移而改变；给它加回墙钟 TTL，仍然是用「误杀 + 漏杀」的工具去抓一个它抓不住的事实（机位不动时 mask 仍然正确却被删；1.9 s 内甩动相机已错却未到期）。**漂移的正确捕获器是事件型漂移检测，不是时钟。**
2. **测量协议中漂移为零。** Session A 评分流程要求手机架好不动，88% 基线不受污染；本次主诉的实证材料（录像）也已定性为呈现问题而非漂移问题——录像里 mask 覆盖的是完整物体。
3. **引入自动失效会撞上 §11.9 R8 的三条件**（需客观失效度量、需先建立刷新循环、需同步修订评分方法论并重建基线），三条一条都不满足。
4. **它是新功能，需用户显式立项。** 我不在无指示的情况下扩大范围。

#### 12.7.4 定级变更与呈现侧缓解（不违反 §11，本次即生效）

R6 由「Phase 3 不处理、明确接受」调整为：**「Phase 3 不做自动失效；但按呈现风险处理，并在呈现侧做部分缓解」**。

| 缓解 | 内容 | 是否违反 §11 | 何时 |
|---|---|---|---|
| **M1 陈旧性可见化** | secondary 的呈现权重进一步下压：填充 0.40（低于 primary 的 0.60）+ **减档描边**。语义从「我现在断言这就是物体」降为「这里曾经被选中过」。**旧 mask 不再冒充当前断言。** | ❌ 不违反——没有任何 mask 被自动移除，只是分层呈现 | 已写入 §3.4，**Day 5 生效** |
| **M2 清除手段更易达** | C1 双击（已有）+ C6 长按删除单实例（Day 6 已在排期）。**清除权仍完全归用户**（§11 的核心原则） | ❌ 不违反 | Day 6 |

#### 12.7.5 Phase 4 入口点（新增候选，写入 Day 7 的 Phase 4 定义）

> **「mask 重锚定（re-anchor）」**：事件型漂移检测 + 用同一 `canonicalPoint` 在新帧上重新 decode。
> 这条之所以值得记：**一旦存在 re-anchor 循环，tap mask 就从「一次性产物」变成「有刷新源的资源」，§11.4 A-1 的自检问法「它过期之后谁把它变回来？」第一次有了答案** ⇒ 那时时间型刷新重新变得合法。
> **顺序不能颠倒：先有刷新循环，才谈得上过期。** 这正是 §11.9 R8 第二条件的具体形态。

---

### 12.8 对 Debugger §25.1 四条移交项的处置

| 编号 | 事项 | 本次处置 |
|---|---|---|
| **A-1** 🔴 | 候选选择规则复议（「取最小」已退化为恒取 ch0 = SAM token 1 = 最细子部件，10/10） | ⏸ **不在本次范围，不裁决。** 但记录两条对它的定级修正：(a) §22.5 的并排录像**决定性实验已执行**，2026-08-11 录像显示 mask 覆盖完整物体 ⇒ **A-1 不是本次主诉的成因，不得作为主诉的修复路径**；(b) A-1 仍是独立且真实的质量议题（#3/#4 两例 ch1 的 iou/stability 同向大幅更优），但其裁决**依赖 §25.3-6 的 stability 定向复采**，该数据仍缺。**在数据到位前动候选规则，会重蹈「从太大荡到太小」的覆辙。** 归口 ML_Vision，Day 6 之后 。🆕 **2026-08-11 已由 §13.3 裁决方向**：候选集扩充（token 0 / 4 候选 / fallback）正式关闭，方向收敛为「在 token 1–3 内改进选择」；**规则本身仍不得改动**，须先满足 §13.3.4 的 E-1…E-4 |
| **A-2** 🟠 | §3.3/§3.4 呈现规格复议 | ✅ **本次裁决完成**（§12.1–§12.6） |
| **A-3** 🟠 | mask 永不过期 + 三张不同龄 mask | ✅ **本次复议完成**（§12.7）：裁决维持，定级上调，呈现侧缓解 M1 已写入 §3.4 并 Day 5 生效 |
| **A-4** 🟡 | 双击失败窗口 ~300 ms 的口径裁决（D-8 提级至 P2） | ⏸ **本次不裁决，挂账。** 它是延迟口径议题，不是呈现议题；且它与「是否保留双击清除」这一**交互权衡**绑定（双击是 §3.2.1 C1 的唯一入口、也是 §11 的「清除权归用户」的实现）。归 Day 7 冻结前一并裁 |

---

### 12.9 长期约定 A-3（写入架构原则，与 §9.2 / §11.4 A-1、A-2 并列）

> **A-3（覆盖层可见性判据）：任何叠加在用户内容之上的呈现元素，其「可见性」不得依赖它与用户内容的对比度。**
> 用户内容是**不可控输入**；把可见性押在不可控输入上，等价于把正确性押在运气上——失败不是概率事件，是遍历事件。
> 可见性必须由**呈现元素自带的对比**保证（双色描边 / 阴影 / 外发光 / 图案），色相与 alpha 只能承担**次级**职责（归属、区分、层次）。
> **自检问法：「如果用户把镜头对准一块和我这个颜色一模一样的东西，会发生什么？」**——答案若是「看不见了」，规格就是错的。

三条约定的共同母题（值得一并记住）：

| 约定 | 出处 | 母题 |
|---|---|---|
| 跨硬件后端的比例外推不构成裁决依据 | §9.2 | **相似 ≠ 可迁移** |
| A-1 时间型 TTL 只允许作用于可自动重建的资源 | §11.4 | **机制的适用前提必须随机制一起搬运** |
| A-2「复用 Phase N 的参数」不构成设计理由 | §11.4 | 同上 |
| **A-3 覆盖层可见性不得依赖不可控输入** | **§12.9** | **别把正确性押在你控制不了的量上** |

---

### 12.10 本次对 tasks.md 的改动范围（可 diff 核对）

| 小节 | 改动 |
|---|---|
| **Day 5 / Debugger** | 条目 1–4 勾选 [x] 并补依据；条目 5 **保持 [ ]**，补根因说明 |
| **Day 5 / Architect** | 新增 1 条已完成条目（本裁决） |
| **Day 5 / Builder** | 新增 1 条未勾选条目：Mask 呈现规格修订 + 轮廓描边（P0，从 Day 6 提前） |
| **Day 6 / Builder** | 「Mask 高亮 + 轮廓描边」改写（描边部分标注已提前）；「Tap 指示动画」增列锚点标记子项 |
| **ML_Vision 任何小节** | ❌ **未改动**（本次约束） |
| **Day 7 / Architect** | Phase 4 入口候选增列「mask 重锚定」1 行 |

---

### 12.11 保留项与风险（不封层，明确列出）

| # | 保留项 | 性质 |
|---|---|---|
| **R10** | **单一青色的撞色风险未归零，只是变小。** 青色物体（青色马克杯、青绿封面、部分屏幕内容）仍会撞。这正是 C-4 双色描边必须落地的原因——**描边就位之后，撞色的后果从「隐身」降级为「填充看不清但轮廓仍在」**。若真机上仍出现「青物体上完全找不到 mask」，说明描边实现有问题，不是色板问题 | 已知残余，有兜底 |
| **R11** | **Day 4 逐位可比性只在「描边关闭」时严格成立。** 描边是加法，理论上只增不减可辨识度，但它也可能轻微改变边界的主观判读（描边会让边缘看起来更"硬"）。Day 7 若要与 44/50 = 88% 严格对照，须用 C-6 的开关做单变量回退 | 方法学 |
| **R12** | **描边的实现路径未指定，但其架构约束已封层（C-5）。** 矢量轮廓（marching squares → path → CAShapeLayer）与全分辨率上下文内描边都可接受，Builder 自选；**唯一不可接受的是把描边烧进 256×256 位图**（线宽失控 + 锁死 D-13） | 接口级约束 |
| **R13** | **R6 的呈现侧缓解（M1）是缓解不是修复。** 相机大幅移动后，三张 mask 仍然全部锚定在旧帧上。Day 7 冻结时须**如实记为已知限制**（§11.9 原承诺不变），并把「mask 重锚定」列入 Phase 4 入口候选 | 已知限制 |
| **R14** | **本次全部依据来自 2026-08-11 单次录像 + 代码/色度算术。** 色度检验（C-1/C-2/C-3）是确定性算术，不受样本量影响；但「撞色是主成因」这一**归因**目前只有一次录像的三个物体作为证据。修订后须复录一次同场景做确认——若换回青色 + 描边后主观质量未回到 Day 4 水平，则归因需重开 | 证据强度 |

---

### 12.12 Deliverable 确认

- [x] Q1 三色板作废，青色单色板封层（§3.3.3）
- [x] Q2 三层职责模型 L1/L2/L3 + 「L1 不得由色相承担」裁决（§3.3.1）
- [x] Q3 填充色约束原则 C-1…C-7，**给出的是约束原则而不只是色值**，含换色准入程序 C-7（§3.3.2）
- [x] Q4 描边升级为双色轮廓、扩展到 secondary、分档（§3.4）
- [x] Q5 描边提前到 Day 5 定级 P0，tasks.md 已改（§12.6）
- [x] Q6 按实例配色 Phase 3 撤销，字段保留（§3.3.3）
- [x] Q7 R6 显式复议：裁决维持、定级上调、缓解 M1/M2、Phase 4 入口点（§12.7）
- [x] 三个成因逐条复核，不合并、不美化（§12.3）
- [x] 与既有裁决（§10 / §11 / §1.3 / D-13）的一致性自检（§12.5）
- [x] Debugger A-1 / A-2 / A-3 / A-4 逐条处置，明确哪两条**不裁决**（§12.8）
- [x] 长期约定 A-3 写入（§12.9）
- [x] 保留项 R10–R14（§12.11）

**Phase 2 pipeline 保护状态：** 无变化。`renderMask`（`MaskRenderer.swift:114-140`）是 Phase 2 box/segmentation 路径的唯一入口，青色字面量 (0,217,255,153) 原样保留，本裁决**不触碰该函数**。tap 路径的 alpha 0.60 与它的 153/255 = 0.600 一致，属巧合性一致，**不得因此把两条路径合并** ✅

**几何保护状态：** 本裁决**不触碰** `buildAlpha` 的任何决策代码（阈值 / 候选选择 / cap60 / cap85 / flood fill / stability / 数值哨兵），§11.9 R3 禁令继续有效，88% 基线的几何可比性不受影响 ✅

---

（§12 追加二裁决结束 —— 后续见 §13 追加三）

---

# Phase 3 — Day 5 追加三 (Architect) 三项裁决
# 2026-08-11
# 触发：(1) Debugger 第 5 条的立项前提被对照实验证伪；(2) 用户要求恢复按实例配色，Builder 已实施并指出 §3.3.2 两处错误；(3) ML_Vision 的 token 0 离线评估完成（model_plan.md §D.0–D.7）
# 输入：debug_report.md §16.5 / §20.1 / §23.4 / §25.1–25.2、builder_progress.md「Day 5（追加二）」、model_plan.md §D.0–D.7
# 范围：本节裁三件事，**不触碰** §9 分辨率封层、§10 延迟策略、§11 mask 生命周期

---

## 13) Day 5 追加三裁决

### 13.1 裁决一：Debugger 第 5 条重新表述 + 「Phase 3 优化入口点 1」处置

#### 13.1.1 结论（先给判定）

| 项 | 裁决 |
|---|---|
| **Day 5 Debugger 第 5 条** | **重新表述，保持未勾选 [ ]。** 验收目标由「对齐告警消失或显著减少」改为「**`.tapToSegment` 路径不加载用不到的模型**」；告警计数**降级为观测指标**，不再是目的 |
| **「Phase 3 优化入口点 1：ANE 对齐修复」** | **重新界定（re-scope），不撤销、不移交 Phase 4。** 更名为「**模型加载卫生（model-load hygiene）**」；原目标「消除对齐告警」正式作废 |
| **milfix encoder 本身** | **保留，冻结不动。** 作废的是它的**立项理由**，不是它的**部署** |
| **decoder 的 channel-1 / 64-byte 对齐问题** | **降级为观测项，不立项。** 重开条件见 §13.1.5 |

#### 13.1.2 为什么原表述从一开始就是错的

原条目：「确认对齐告警在当前测试中已消失或显著减少（如 ANE 修复已应用）」。它有两处结构性缺陷，与 Debugger 判 FAIL 的字面结果无关：

1. **它把一个观测指标写成了验收目标。** 告警计数是**日志文本的出现次数**，它同时受三个与正确性无关的量支配：某个模型被加载了几次（3 → 6 就是这么来的，`testMobileSAMLoad()` + `SAMDecoder.init` 各一次）、构建配置、SDK 版本。计数下降既可能是「问题解决了」，也可能是「触发路径没跑到」。**一个既不能证明成功也不能证明失败的判据，不是判据。**
2. **它追错了组件。** Debugger 的两次孤立加载对照是决定性的：模式切换时孤立加载 milfix encoder ⇒ **0 条**；TAP#1 孤立加载 `MobileSAM_PromptMaskDecoder` ⇒ **3 条**。`Invalid layer … must be aligned on 64 bytes` **从来不是 encoder 发的**。`ModelLoader.swift:55-58` 的注释属误归因，而「优化入口点 1」正是照着那条注释立的项 ⇒ **该优化至今没有消除过任何一条告警，因为它压根不在告警的因果链上。**

**误归因是怎么产生的（值得记，因为它会复发）：** 注释写在**被改动的组件旁边**（ModelLoader 里 encoder 换用那一段），于是「改了 X，日志里有 Y」被读成「X 产生 Y」。真正把它切开的是**隔离实验**——让两个模型在时间上分开加载，看告警跟着谁走。⇒ **归因必须由隔离实验建立；代码注释不是证据，时间上的相邻也不是证据。**

#### 13.1.3 真正的架构缺陷（这才是应该验收的东西）

`SAMDecoder.init` **无条件**用调用方传入的 `computeUnits`（真机为 `.all` ⇒ 允许 ANE）加载 **box decoder**，而在 `.tapToSegment` 下 box decoder **从头到尾不会被调用一次**（tap 路径走 `pointModel`，已显式强制 `.cpuAndGPU`）。这条的性质是：

- **它是架构缺陷，不是噪声问题。** 代价落在用户的**第一次 tap 的关键路径**上（§17.4 超时的直接助推、§20.1 的 IMPACT），并额外占用内存与启动时间。
- **告警只是它的副产品。** box decoder 是全工程**唯一**用 ANE-enabled compute units 加载的 decoder ⇒ 它一惰性化，tap 路径上那 3 条告警会自然归零。**修好架构，指标自己会动；反过来盯着指标修，就会去改 encoder（已经浪费过一轮了）。**

⇒ 验收对象应当是**「不加载用不到的模型」这一行为断言**，告警计数只作为**旁证**记录。

#### 13.1.4 修订后的验收标准与所需数据

**条目新表述（写入 tasks.md，保持 `[ ]`）：**

> 确认 `.tapToSegment` 路径不加载用不到的模型（box decoder 惰性加载生效）；对齐告警计数作为副产品指标一并记录

**判定标准（P = 必过，S = 旁证，G = 回归护栏）：**

| # | 类别 | 判据 | 判定方式 |
|---|---|---|---|
| **P-1** | 必过 | 从进入 `.tapToSegment` 到 TAP#N 全程，**box decoder 未被构造** | 真机日志：`SAMDecoder.init` 只打印 point decoder 加载；box decoder 的加载日志在整段 tap 会话中**零次** |
| **P-2** | 必过 | box decoder 只在 **`.segmentation` 需要它时**才构造，且 Phase 2 行为无变化（仍走 ANE、仍能正常出 mask）| 切到 `.segmentation` 跑一段：出现**恰好一次** box decoder 加载 + mask 正常渲染 + box decode 延迟与 Day 4 同量级 |
| **P-3** | 必过 | 冷加载成本被**移动**而不是被消除，必须被**测量**而不是被隐藏 | 记录 box decoder 首次构造耗时与它落在哪个阶段。Builder 的实现把它留在 **Phase 2 warmup 的排练 decode** 上（与改动前同一队列、同一时点）⇒ 预期**不落在用户可见路径**；**须实测确认这一点**，若它实际落到 `.segmentation` 首帧且用户可感（>1 s），作为新条目上报，**不得当作已解决** |
| **P-4** | 必过 | **warmup 排练的必须是当前模式真正会用的那个 decoder** | `.tapToSegment` 下 warmup 日志须显示 **point prompt** 排练（`[SAM][warmup] … point prompt`）。若仍用 box prompt 排练，box decoder 会在 warmup 阶段被造出来 —— **告警只是从 TAP#1 挪到 warmup，一条都不会少，而惰性化等于白做**。这是本条最容易被「计数看起来没变」掩盖的失败模式 |
| **S-1** | 旁证 | 告警计数：TAP#1 期间 **3 → 0**；全 session 6 → 3（仅惰性化）或 6 → 0（若 D-5 同时移除 `testMobileSAMLoad()` 的重复加载） | 与实测数不符时**必须解释差值**，不得只报「减少了」 |
| **G-1** | 护栏 | decode 数值行为零变化：`Mask logits` 量级正常（参照 min=-9.74 / max=3.60）、`iou_pred` ⊂ [0,1]、数值哨兵触发 **0** 次 | 惰性化只应改变「什么时候加载」，**不应改变任何输出** |
| **G-2** | 护栏 | `ModelLoader.swift:55-58` 的误归因注释已订正（D-4） | 代码审查；留着它下一个人还会照它立项 |

**所需数据（缺一不可判定）：** 一份**惰性加载落地之后**的 **Release 构建**真机日志，含：① 启动段；② `MODE SWITCH → tapToSegment`；③ ≥3 次 tap（覆盖 TAP#1 冷路径）；④ 切回 `.segmentation` 跑若干帧；⑤ 全段的 `Invalid layer` 计数并**按阶段分列**（启动 / 模式切换 / warmup / TAP#1 / Phase 2 首次 box decode）；⑥ 两个模式各自的 `SAM decoder warmup latency` 行（含 `origin` 与 `point|box prompt` 标注）。

**明确不要求的：** 不要求告警归零才能勾选。若 P-1…P-4 全过而 S-1 仍有残余（例如启动期 3 条来自其它加载点），**照勾**，把残余计数如实记为观测项。**这正是本次重新表述的意义：验收绑定行为，不绑定日志字面。**

#### 13.1.5 「优化入口点 1」的处置理由（为什么是重新界定，不是撤销、不是移交）

**不撤销的三条理由：**

1. **撤销会丢掉教训。** 这个条目现在是项目里最有价值的**反面案例**：一个基于代码注释、未经隔离实验的归因，驱动了一整轮工作。删掉它，下一次还会有人照着某条注释立项。
2. **撤销有被误读为「回滚 milfix」的实际风险。** milfix encoder 已在 §9.5 冻结为「Encoder 模型系列 = milfix fp16 单一模型 FINAL」，Day 4/Day 5 的全部延迟基线（648 ms / 970.6 ms 配对量）与 44/50 = 88% 的评分基线**都是在 milfix 上测的**。换回原 encoder 将使冻结参数表与全部基线一次性失效，而**没有任何证据表明原 encoder 更好**。⇒ **作废理由，保留部署。**
3. 该入口点下真实存在的问题（§13.1.3）**就在 Phase 3 内、就在 Day 6 的可修范围内**，没有任何理由推到 Phase 4。

**重新界定后的定义（取代 tasks.md 第 386 行的「1. ANE 对齐告警消解」）：**

> **入口点 1 —— 模型加载卫生（model-load hygiene）**
> (a) 不加载用不到的模型：box decoder 惰性化（D-5）、`testMobileSAMLoad()` 移除或限 DEBUG；
> (b) 冷加载成本不落在用户的第一次交互上：warmup 归属修正（D-1 / D-2）；
> (c) 订正 `ModelLoader.swift:55-58` 的误归因注释（D-4）。
> **观测指标（非目标）：** `Invalid layer … 64 bytes` 的分阶段计数。

**「真·ANE 对齐」议题的处置：降级为观测项，Phase 3 内不立项。** 依据两条：① 实质风险实测为零（logits 量级正常、`iou_pred` ⊂ [0,1]、哨兵 0 次）；② point decoder 已强制 `.cpuAndGPU`，**根本不上 ANE**，box decoder 惰性化后 tap 路径上无 ANE 加载 ⇒ 问题在当前配置下**不可达**。
**重开三条件（合取，缺一不可）：** (i) 有明确的延迟收益目标（例如「把 point decoder 放回 ANE 以省 X ms」）；(ii) 目标设备真机配对实测（§9.2 约定：跨后端外推只构成立项理由，不构成裁决依据）；(iii) 数值哨兵 + logits 量级作为**验收门**而非事后检查——A13 fp16 LayerNorm 崩坏是这条路径的已知失败模式。

#### 13.1.6 长期约定 A-4（写入架构原则）

> **A-4（验收断言的绑定对象）：验收标准必须写成对*系统行为*的断言，不得写成对*日志文本*的断言。**
> 日志计数受「触发路径跑没跑到」「加载了几次」「构建配置」共同支配，因此它既不能证明成功、也不能证明失败；把它当目标会导致**优化指标而不是优化系统**。
> 日志只能作**旁证**，且引用时必须分阶段计数并解释差值。
> **推论（归因纪律）：因果归因必须由*隔离实验*建立。** 代码注释不是证据，时间上的相邻不是证据，「我改了 X 之后 Y 变了」也不是证据——除非 X 能被单独开关。
> **自检问法：「如果这条日志明天被人删掉，我的验收还成立吗？」** 答「不成立」⇒ 验收标准写错了对象。

---

### 13.2 裁决二：三槽色板追认（**条件性**）+ §3.3.2 两处勘误

#### 13.2.1 结论（先给判定）

| 项 | 裁决 |
|---|---|
| **三槽色板 slot0 (0,217,255) / slot1 (0,255,242) / slot2 (0,255,170)** | ✅ **条件性追认（PROVISIONAL）**，写入 §3.3.3 与 §9.5；转 FINAL 的唯一条件 = 完成 C-7 (b) |
| **§12.1 Q6「按实例配色 Phase 3 撤销」** | ❌ **正式取代。** 撤销的前提（色相承担 L1）已被双色描边落地消灭 |
| **§3.3.2 C-1 的品红备选带 [280°,330°]** | ❌ **删除**（在 C-2∩C-3 下是空集，Architect 独立复算确认 max Y = 0.2988） |
| **§3.3.2 C-1 的允许带上界 200°** | ❌ **订正为 194.78°**（宽 34.8°） |
| **§3.3.3 上一版理由 1「装不下三个色相」** | ❌ **撤回**（算术不成立） |
| **§3.3.3 上一版理由 2「会混出第四种颜色」** | ⚠️ **对本色板不成立**，其成立条件提炼为新约束 **C-8 混合封闭性** |
| **新增 C-1a（边界余量）/ C-8（混合封闭性）** | ✅ 写入 §3.3.2 |

#### 13.2.2 前提变更的合法性检查（这不是反复横跳，理由要讲清楚）

一个规格在 8 小时内被改了两次，必须先回答「你到底在按什么原则做事」。我的回答是：**两次改动推翻的不是同一个东西。**

| 上一版的内容 | 本次处置 | 原因类型 |
|---|---|---|
| **结论**：Phase 3 内撤销按实例配色 | 取代 | **前提变更** —— 撤销的推导是「色相在承担 L1 ⇒ 色相必须与用户内容竞争 ⇒ 三个色相里必有一个输」。双色描边落地后 **L1 由描边独立承担**，色相退回 L3。前提没了，结论跟着没 |
| **论据 1**：允许带装不下三个可区分色相 | 撤回 | **我算错了** —— 与前提变更无关，即使 L1 仍由色相承担，这条论据也是错的 |
| **论据 2**：三色重叠混出第四色 | 限定 | **过度一般化** —— 对旧色板成立，对任何在混合下封闭的色板不成立 |
| **原则**：L1 不得由色相承担（§3.3.1、A-3） | **完全不变** | 本次裁决恰恰是它的**正确应用**：正因为 L1 已被描边接管，色相才被允许回来 |

**⇒ 判据是「L1 的承载者是谁」，不是「用几种颜色」。** 上一版之所以要撤销按实例配色，是因为当时描边还没落地、L1 无人承担；现在有人承担了。真机复录已确认呈现回退解决（R14 的复录要求已满足），前提变更是**实证的**，不是推测的。

#### 13.2.3 §3.3.2 两处勘误（我的错误，已独立复算确认）

Builder 指出的两处**全部成立**。我用独立实现（sRGB 线性化 + WCAG 相对亮度 + CIEDE2000）重算，逐项吻合：

**勘误 1 —— 品红备选带 H ∈ [280°,330°] 在 C-2∩C-3 下是空集。**
在 S ≥ 0.85 且 V ≥ 0.90 的约束内穷举该色带，相对亮度最大值 **Y = 0.2988**（H = 300.0°, S = 0.85, V = 1.00），距 C-2 的门槛 0.45 差 **0.15**，且**无论如何取值都够不到**。原因是物理的：品红 = 红 + 蓝，而 Y 的系数是 R 0.2126 / G 0.7152 / B 0.0722 —— **亮度几乎全在绿通道上**，一个绿分量被压到接近 0 的色相不可能同时高饱和与高亮度。
⇒ **删除，且必须写明删除理由**。留着它，下一次换色的人会以为自己有两个色带可选，然后在品红里挑一个通不过 C-2 的色，重演一遍本次事故。

**勘误 2 —— 首选带被 C-2 截断在 H = 194.78°。**
在 S = 1.00 / V = 1.00 上做色相扫描，Y 在 **H = 194.78°** 处恰好穿过 0.45；H = 195° 时 Y = 0.4459 已越线，H = 200° 时只剩 0.3597。⇒ 可行弧段是 **H ∈ [160°, 194.78°]，宽 34.8°**，而不是原写的 [160°, 200°]。
补充一处我在复算中发现、Builder 未提的细节：若把 S 放宽到 C-3 的下限 0.85，可行上界能延到 **H ≈ 197.6°**（掉饱和度 = 掺白 = 提亮）。**我判定这 2.8° 不计入可用余量**——用掉饱和度换亮度与 C-3「人造信号特征」的立意直接冲突，写进规格只会诱导下一个人去踩。已在 C-1 中明确标注。

**由此，我上一版的判断「允许色带装不下三个可区分色相」经验算不成立：** 在 34.8° 弧内、slot 0 钉死 188.94° 的条件下，最优三元组 (160.0°, 177.5°, 188.94°) 的 ΔE00_min = 17.4，实际发布的 (160°, 176.94°, 188.94°) = **17.1**（我独立复算：三对分别为 18.0 / 35.3 / 17.1），高于「类别可辨识」常用的 10。
**装不下的是「散布在整个色环上的三个」** —— 旧色板 ΔE00_min = 50.2，那种散布正是把两个色相拖进禁用带的原因。**我把「色相间距不够」和「色相位置越界」混成了一件事**：真实约束只作用在**位置**上，间距在剩下的弧段里是够用的。

#### 13.2.4 新增 C-8（混合封闭性）—— 从被撤回的论据里救出来的东西

上一版「三色重叠会混出第四种颜色」的错误在于把一个**依赖具体色板**的性质说成了普遍性质。正确的表述是一个**检验**而不是一个**结论**：

> **C-8：多实例色板必须在 `compositeLayers` 的 source-over 叠放下封闭** —— 穷举全部叠放组合（N=3 ⇒ 12 种），每一种重叠产物**自身**都必须通过 C-1/C-2/C-3。

本色板通过的机制值得记下来，因为它是可复用的构造技巧：**三色的 R 分量恒为 0** ⇒ 任意凸组合仍 R = 0 ⇒ **S 恒等于 1.000**，色相被锁在 G/B 两分量张成的一维子空间里。我独立复算 12 种组合：**H ∈ [163.57°, 186.48°]、S = 1.000、V ∈ [0.930, 1.000]、Y ∈ [0.5997, 0.7706]**，与 Builder 的数字逐位一致，**12/12 全部通过**。
旧色板则是反例：蓝 + 橙的混合物掉出了整个约束集 —— 它失败的原因不是「三色」，而是**从来没有人检验过混合产物**。

#### 13.2.5 两条削弱项（原样纳入裁决，不美化）

**W-1 —— secondary 之间的可辨识度不足（已确认，非推测）。**

上面那些 ΔE00 是**不透明色块**的值。在 7 种代表性背景上实算**合成之后**的最小两两 ΔE00（我独立复算，与 Builder 结论一致）：

| alpha | 合成后 ΔE00_min 范围 | 判定 |
|---|---|---|
| **0.60（primary）** | **10.2 – 13.5**（全部背景 ≥ 10）| 可辨识 |
| **0.40（secondary）** | **6.8 – 10.7**（多数背景 **< 10**）| ⚠️ **欠载** |

**架构含义（这是本条真正的产出）：色相是 L3 的载体之一，但不是充分载体。** primary/secondary 的区分由描边分档承担，那是 **L2**；**两个 secondary 彼此之间**目前只有色相一个 L3 载体，而它在 0.40 alpha 下多数背景达不到可辨识门槛。
⇒ **裁决：Day 6 的「tap 锚点常驻标记 + 实例编号」从增强项升格为 L3 的必需载体，定级 P1。** 在它落地之前，L3 处于**已知欠载状态**，Day 7 冻结时须如实记录。
⚠️ 该条目已在 tasks.md Day 6 Builder 小节存在（§12 已增列），**本次不改写 Builder 小节**（本轮授权范围外）；定级要求记录在此与 Architect 小节，Day 6 排期时生效。
⚠️ **不接受的两条"便宜解法"**：① 把 secondary alpha 提回 0.60 —— 会削弱 L2 的归属分级，也违背 §12.7.4 的 M1 缓解（旧 mask 不该冒充当前断言）；② 把三个色相拉得更开 —— 弧段只有 34.8°，17.1 已接近该弧内的上界 17.4，**没有空间可挖**。⇒ **只能靠增加一个新载体（编号），不能靠调参。**

**W-2 —— C-7 (b) 真机目视撞色测试：✅ 已结案（2026-08-16）。**

C-7 的字面要求是「两项缺一不得合入」，而代码**已经合入**。我不做追溯性否决（用户显式要求 + 描边兜底已把撞色后果从「隐身」降为「填充看不清但轮廓仍在」+ slot 0 保住了 Day 4 逐位一致），但**违例必须记录在案，否则 C-7 这条防复发机制第一次被援引就被架空了**。

处置三条：
1. **色板状态标记为 🟡 PROVISIONAL，不是 FINAL**（§3.3.3、§9.5 均已标注）。**C-7 (b) 完成前，不得在本色板基础上再做任何换色。**
2. **补测是阻塞项，不是可选项。** 测试内容：三个测试物（一个青色物体、一个水青/蓝绿物体、一个薄荷或春绿物体，**slot 2 = 160° 是三者中风险最高的**——正压在允许带下沿、距绿色禁用带只有 5°、零余量），逐槽位各测一个；**外加一次同屏三实例**，判「能否看出这三块来自三次不同点击」（这一项直接检验 W-1）。
3. **失败判据要写死，避免补测时口径漂移**：失败 = **连轮廓都找不到**。「填充看不清但轮廓仍在」**不算失败**——那正是 R10 预期的残余风险形态，也正是双色描边的设计目的。若出现「连轮廓都找不到」，问题在描边实现（R10），不在色板。

#### 13.2.6 与既有裁决的一致性自检

| 既有裁决 | 本次是否冲突 | 说明 |
|---|---|---|
| §3.3.1 三层职责 / A-3「L1 不得依赖不可控输入」 | ❌ 不冲突 | 本次是它的正确应用：L1 仍**只**由描边承担，色相只回到 L3 |
| §3.4 alpha 分级 0.60/0.40、描边分档 | ❌ 不冲突 | **一个字未动** |
| §11.9 R3 禁令 / 88% 几何基线 | ❌ 不冲突 | 本次只改色相；`compositeLayers` 的 `accA` 只由 `alpha[i]>0` 与 `opacity` 决定、**与颜色无关** ⇒ 二值覆盖与不透明度分布逐位不变 |
| Day 4 逐位可比性（R11） | ⚠️ 收窄但保住 | 现在只在 **N=1（slot 0）且描边关闭** 时严格成立。多实例场景本就不在 44/50 基线的采样协议内 |
| Phase 2 `renderMask` 保护 | ❌ 不冲突 | 未触碰；其青色字面量与 slot 0 的一致仍属巧合性一致，**不得据此合并两条路径** |

---

### 13.3 裁决三：A-1 方向收敛 —— 候选集扩充关闭，只在 token 1–3 内改进选择

#### 13.3.1 结论（先给判定）

| 项 | 裁决 |
|---|---|
| **是否导出 token 0 / 改为 4 候选 / token 0 作 fallback** | ❌ **正式关闭。** 采纳 ML_Vision（§D.5）的推荐 |
| **是否退回 Day 3 的纯 token 0** | ❌ **正式关闭。** 这条退路此前一直隐含存在，本次被数据切断 |
| **A-1 的方向** | ✅ **收敛为「在现有 token 1–3 内改进选择」** |
| **是否现在就可以动选择规则** | ❌ **否。R3 禁令继续有效。** 动规则需先满足 §13.3.4 的 E-1…E-4（**合取**）|
| **「改为取 iou_pred 最大」** | ❌ **不批准**，即使 §D.6 的数据表面上支持它（理由见 §13.3.3）|

#### 13.3.2 依据

四条理由互相独立（D.2–D.4），其中 **D.3 是决定性的**：

- **D.3（零影响）：** A4 vs 现状 20 次中 19 次选择完全相同，唯一"差异"两者面积/bbox/fill 全同；FB vs 现状 **20/20 相同**，fallback **从未触发**。机制清楚：token 0 面积在 19/20 上 ≥ token 1，而规则取最小 ⇒ **token 0 永远选不上**。⇒ 导出 4 候选要付出模型重导出 + decoder 输出契约变更（通道数）+ 全链路 shape 变更的成本，**换来一个已被观测为 ≤ 1/20 的效应**。
- **D.2（不是新粒度档）：** token 0 ↔ token 2 中位 mask IoU ≈ 0.94（vs token 1 的 0.70、token 3 的 0.44）⇒ 它落在「部件」档，与 token 2 高度重合，**不提供新的粒度层级**。候选集的价值在于覆盖不同粒度，一个重复档没有覆盖价值。
- **D.4（自评量上也不占优）：** 四个 token 的 iou_pred 最大者分布为 token 2 占 10/20、token 1 占 5、token 3 占 4、**token 0 占 0**。
- **D.4（退路被切断）：** 纯 token 0 替换在**退化提示**上明显更差 —— 点裸大理石溢出 8.4% → **26%** 内容区，点裸泡沫 16% → **25%**。⇒ 「Day 3 那时候更准」这个一直悬着的假说，现在有了反证。结合 §12 的结论（用户主诉是呈现问题，不是几何问题），**两条独立证据同时指向「不要回滚几何/候选集」**。

**一条方法学上的说明（它让 n=20 的结论仍然可用）：**
> **证明一个改动*无效*所需的证据强度，低于证明它*有效*。** 前者的效应量上界已被直接观测到（19/20 或 20/20 相同 ⇒ 效应 ≤ 1/20）；后者要求估计一个未知的正效应，需要的样本量高得多。
> ⇒ D.7 的 R-T3（n=20、集中于 4 场景）**足以支撑「关闭一个方向」，不足以支撑「开启一个方向」，更不足以支撑任何阈值设定**。本裁决严格停在前者。

#### 13.3.3 明确不批准的路径：「改为取 iou_pred 最大」

§D.6 指出 token 2 在 10/20 上 iou_pred 最高、且常 iou 与 stability 双高，而现行规则（取最小）几乎从不选它。这是**真实且有价值的观察**，但它**不构成一个可以直接实施的规则**，理由三条：

1. **iou_pred 是未经验证的代理量。** 全部指标（iou_pred / stability / fill）都是模型自评量，且 Debugger 已证三者共线（stab~iou 0.945、iou~fill 0.924）——**三个共线的自评量提供的是一个维度的信息，不是三个**。「哪个 token 更接近用户意图」**从来没有被测量过**（D.7 R-T2）。照着一个未验证的代理量重排序，等于把选择规则从一个有已知缺陷的锚（面积）换到另一个**缺陷未知**的锚。
2. **它是「选择式」改动，会让 88% 基线整体不可比。** 取最大 iou_pred 会在**几乎每个样本上**改变输出（现行规则 ≈ 恒取 token 1，而 token 1 只在 5/20 上 iou_pred 最高）⇒ 直接撞 §11.9 R3 与 R9。
3. **Debugger §19.4 的既有结论仍然有效：** stability **只能否决、不能选择**；高 stability ≠ 好 mask。iou_pred 与 stability 共线 ⇒ 该结论顺延到 iou_pred。

#### 13.3.4 动选择规则所需的证据（E-1…E-4，**合取**；沿用 §9.3 修订后的门控口径：批准取合取、撤销取析取）

| # | 要求 | 为什么必需 |
|---|---|---|
| **E-1** | **人工评分配对数据（最高优先）。** 对同一批 tap 点做**盲评强制选择**：呈现候选 mask（顺序随机化、**不显示** iou/stability/面积），由人标注「哪一个最接近我点这一下时想选的东西」。**设计必须是配对的**：离线重放两条规则，**只对「两条规则选择不同」的样本子集做人工评分**（McNemar 设计）。判据：discordant 子集上新规则更优的次数 b 与更差的次数 c 满足 b > c 且在 b+c ≥ 10 上显著 | 这是**唯一**能把模型自评量与用户意图挂钩的数据。D.7 R-T2 明说该量未被测量。配对 + 只评 discordant 子集能把样本需求压到极低（D.3 显示多数样本两规则同选），**成本不是借口** |
| **E-2** | **设备端定向复采**，能**复现**那三个灾难性失败（S3 电脑盖 / S5 插头 / S2 显示器底座），并同时导出四个候选的 candidates 日志 | D.7 R-T1：重建帧**没能复现**设备端的灾难性失败（四个 token 面积均在 0.3%–3%，无一是「整张桌子」）。如果规则改动的主要卖点是「修掉灾难性溢出」，而灾难性溢出在离线介质上根本不出现，那**离线重放无法证明改动有效** |
| **E-3** | **规则改动的形式必须是「否决式」而非「选择式」** —— 须能写成「先用阈值否决掉部分候选，再在剩余候选里**沿用现行的『面积最小且 ≤cap60』**」 | 三个好处：① 承接 §19.4「stability 只能否决不能选择」；② 改动**单调收缩**，可用单开关回退（C-6 同款）；③ **在「无候选被否决」的样本上输出逐位不变** ⇒ 88% 基线的可比性由**构造**保证，而不是靠事后统计辩护 |
| **E-4** | **副作用指标必须一并采集**：退化提示（点裸大理石 / 裸泡沫等裸表面）上的溢出率**不得上升** | D.4 给出的最有价值的副作用指标。选择规则一旦"变松"（倾向更大的候选），第一个坏掉的就是裸表面点击 —— 这正是把 mask 荡回「整张桌子」的机制。**没有这一项，就会重演"从太大荡到太小再荡回太大"** |

**未满足 E-1…E-4 之前：** ML_Vision 可以继续采数据、继续离线重放、继续打日志；**不得改动 `MaskRenderer` 中任何参与选择的代码**（§11.9 R3 禁令原样有效）。

#### 13.3.5 三条保留项如何限制结论 + 一个必须命名的分叉点

D.7 的三条保留项**原样纳入本裁决**，并逐条给出它限制了什么：

| 保留项 | 限制了什么 |
|---|---|
| **R-T1** 重建帧未复现设备端灾难性失败 | 「不导出 token 0」的结论对**整体趋势**成立；对**那三个失败案例**结论强度低。⇒ 本裁决只关闭候选集方向，**不声称三个失败案例已被解释** |
| **R-T2** 无人工评分配对 | 「token 2 更好」严格来说**未被测量**。⇒ §D.6 只能作为 E-1 的**假设来源**，不能作为规则改动的依据 |
| **R-T3** n=20、集中于 4 场景 | 不得用于任何**阈值级**结论（cap 值、stability 门槛、面积比 K、Δiou δ 一律不得由本批数据定）|

**必须命名的分叉点（这决定了 A-1 到底是什么问题）：**

> 若 E-2 的设备端定向复采显示：灾难性失败发生时**四个 token 全部不可用**（都是「整张桌子」或都是碎片），
> 则问题**既不在选择规则、也不在候选集**，而在**提示/embedding 本身**（低对比度下 logit 场变平 —— 与 Day 4 归因「局部对比度是唯一失败预测因子」同源）。
> ⇒ 那时 **A-1 应被重新界定为「提示质量问题」**，候选方向变成 detect→crop→re-encode / 多点提示，而不是在三个都坏的候选里挑一个。
> **顺序不可颠倒：先确认『存在一个好候选』，再谈『怎么把它选出来』。** 现行 A-1 的全部提法都默认了前者，而那个默认**至今未被验证**。

#### 13.3.6 长期约定 A-5（写入架构原则）

> **A-5（代理量禁令）：当一个决策规则的全部输入都是模型自评量、且这些自评量彼此共线时，不得在没有外部标注的情况下改变该规则的判据。**
> 共线的 k 个自评量提供的是**一个**维度的信息，不是 k 个；用其中一个替换另一个，只是换了个投影方向，不增加任何关于「用户想要什么」的信息。
> **自检问法：「这个量被验证过与我真正关心的东西相关吗？」** —— 答不出验证在哪，就说明你在优化一个从未被校准过的仪表。
> 与 §12.9 A-3 的关系：A-3 管「别把正确性押在你控制不了的量上」，A-5 管「别把正确性押在你没校准过的量上」。

**五条长期约定汇总：**

| 约定 | 出处 | 母题 |
|---|---|---|
| 跨硬件后端的比例外推不构成裁决依据 | §9.2 | 相似 ≠ 可迁移 |
| A-1 时间型 TTL 只允许作用于可自动重建的资源 | §11.4 | 机制的适用前提必须随机制一起搬运 |
| A-2「复用 Phase N 的参数」不构成设计理由 | §11.4 | 同上 |
| A-3 覆盖层可见性不得依赖不可控输入 | §12.9 | 别把正确性押在你控制不了的量上 |
| **A-4 验收断言绑定行为、不绑定日志字面；归因须由隔离实验建立** | **§13.1.6** | **别把指标当目的** |
| **A-5 代理量禁令：未经外部标注校准的自评量不得驱动规则变更** | **§13.3.6** | **别把正确性押在你没校准过的量上** |

---

### 13.4 保留项与风险（不封层，明确列出）

| # | 保留项 | 性质 |
|---|---|---|
| **R15** | **色板 C-7 (a)+(b) 均通过，状态升 FINAL（2026-08-16）。** slot 2 的 160.00° 零余量已在真机测试中以绿色物体验证（「填充看不清但轮廓仍在」PASS）。今后任何换色仍须过完整 C-7 (a)+(b) 程序 | 已结案 |
| **R16** | **L3 欠载已确认：** 两个 secondary 之间合成 ΔE00 只有 6.8–10.6（多数背景 < 10），且**没有调参空间**（弧段 34.8°、17.1 已近上界 17.4）。唯一解法是 Day 6 的锚点编号 | 已知限制，有明确解法 |
| **R17** | **Debugger 第 5 条的新判据尚无数据。** 惰性加载正在由 Builder 并行实施，判定须等修订后的 Release 真机日志（§13.1.4 五项）。**在此之前该条保持 [ ]，且不得因「告警变少了」被勾选** | 待数据 |
| **R18** | **box decoder 的冷加载成本是被*移动*而不是被*消除*的。** 按 Builder 的实现它落在 Phase 2 warmup 的排练 decode 上（与改前同队列同时点）⇒ 预期不可见，但**未经真机确认**。另有两处需一并观察：① `setMode` 复位 `decoderWarmupDecodeDone` 会带来至多一次多余排练 decode；② `.tapToSegment` 的 warmup 现在改排练 point decoder（P-4），这是新路径 | 已知副作用，须测量 |
| **R19** | **A-1 的三个灾难性失败案例至今没有被任何介质复现过**（设备端有、重建帧无）。在 E-2 复现之前，**任何声称"修好了失败案例"的改动都无法被验证**；且 §13.3.5 的分叉点意味着 A-1 有可能根本不是选择规则问题 | 证据缺口，含方向性风险 |

---

### 13.5 本次对 tasks.md 的改动范围（可 diff 核对）

| 小节 | 改动 |
|---|---|
| **Day 5 / Debugger 条目 5** | **仅表述修订 + 追加裁决说明，保持 `[ ]` 未勾选**（本轮授权范围内的唯一例外）|
| **Day 5 / Architect** | 新增 1 条已完成条目（本次三项裁决）|
| **Phase 3 优化入口点列表（第 386 行）** | 第 1 项重新界定为「模型加载卫生」，并标注原目标作废 |
| **Day 5 / Builder** | ❌ **未改动** |
| **Day 6 / Builder、Day 7 / *、ML_Vision 任何小节** | ❌ **未改动**（W-1 对锚点编号的 P1 定级记录在 Architect 小节与 §13.2.5，Day 6 排期时生效）|

### 13.6 Deliverable 确认

- [x] 任务一 (1) Debugger 第 5 条重新表述，验收目标从「告警减少」改为「不加载用不到的模型」，**保持未勾选**（§13.1.4）
- [x] 任务一 (2)「优化入口点 1」处置裁决：**重新界定为「模型加载卫生」**，不撤销、不移交，理由三条；milfix 部署保留、立项理由作废（§13.1.5）
- [x] 任务一 (3) 修订后的验收标准 P-1/P-2/P-3 + S-1 + G-1/G-2 与所需数据清单（§13.1.4）
- [x] 任务二 追认三槽色板（**条件性 PROVISIONAL**），§3.3.3 / §9.5 已改写（§13.2.1）
- [x] 任务二 §3.3.2 两处错误已核实并修订：品红带删除、上界订正 194.78°；**Architect 独立复算确认**（§13.2.3）
- [x] 任务二 上一版理由 1 撤回、理由 2 限定并提炼为新约束 C-8；新增 C-1a（§13.2.4、§3.3.2）
- [x] 任务二 两条削弱项 W-1（secondary 可辨识度欠载）/ W-2（C-7 (b) 程序违例）**原样写入，未美化**（§13.2.5）
- [x] 任务三 A-1 方向收敛为「在 token 1–3 内改进选择」，候选集扩充与"退回 Day 3"双双关闭（§13.3.1）
- [x] 任务三 明确**不批准**「取 iou_pred 最大」，理由三条（§13.3.3）
- [x] 任务三 动规则所需证据 E-1…E-4（合取）+ 门控口径（§13.3.4）
- [x] 任务三 三条保留项逐条纳入并说明其限制 + 命名分叉点「A-1 可能根本是提示质量问题」（§13.3.5）
- [x] 长期约定 A-4 / A-5 写入，五条汇总（§13.1.6、§13.3.6）
- [x] 保留项 R15–R19（§13.4）

**几何保护状态：** 本裁决**不触碰** `buildAlpha` 的任何决策代码。§11.9 R3 禁令继续有效；A-1 的裁决明确**不授权**任何选择规则改动 ✅
**Phase 2 pipeline 保护状态：** 无变化。`renderMask` 未触碰；box decoder 惰性化要求 P-2 明确把「Phase 2 仍能正常出 mask」写成必过项 ✅

---

END OF PHASE 3 DAY 5 ARCHITECT OUTPUT（§13 追加三裁决）

---

# Phase 3 — Day 7 (Architect) 架构冻结 + Phase 4 规划

日期：2026-08-15
依据：debug_report.md §26（静态分析）+ §27（真机测量）+ tasks.md Phase 3 Day 7 Debugger/Builder 交付物 + architect_output.md §9.5/§10.6/§11.6/§12.8/§13

---

## §14 Phase 3 架构冻结 + Phase 4 规划（Day 7，2026-08-15）

---

### 14.1 Day 7 Debugger 结果审阅

#### 14.1.1 已通过项（全部来自 §27 真机实测）

| 项目 | 结果 | 数据 |
|------|------|------|
| 构建（Release，零 warning）| ✅ PASS | §26.1 |
| 快路径 tap-to-mask mean | ✅ 73.9 ms | n=17，decode-only |
| 快路径 tap-to-mask p95 | ✅ **94.7 ms** | §10.6 门控 ≤200 ms，通过 |
| FIFO 压力测试（快速连点 5 个）| ✅ PASS | 6 次 eviction，pool 恒定 n=3 |
| Encoder 忙碌期间 tap（进度动画）| ✅ PASS | 脉冲动画持续至 mask 出现 |
| 旋转 90° 后 tap（几何传递）| ✅ PASS（修复后，§27.10）| 旋转时 mask 立即清除 |
| 边缘/角落 tap（8 个位置）| ✅ PASS | 无崩溃，clamp 正常 |
| Phase 2/3 切换 ×5 内存 | ✅ PASS | ~590 MB ±15 MB，无单调增长 |
| 多实例内存 N=0→3 | ✅ +31 MB | 略超 +30 MB 设计预期，有合理解释（见 §14.1.2）|

**ISSUE-D7-3 修复（已验收）：** 旋转后旧 mask 不自动消失的问题由 Builder 在 `applyCaptureRotation(_:)` 中修复（sessionQueue 调用，三条件门控：角度变化 + `.tapToSegment` 模式 + 实例池非空）。用户真机验证通过（"解决了"）。此修复归档为 Phase 3 最终变更，不影响任何冻结参数。

#### 14.1.2 仍为开放项（不阻塞冻结，明确记录）

| 项目 | 状态 | 处置 |
|------|------|------|
| **慢路径（encode+decode）样本** | n=0（R1 保留）| 用户协议，不阻塞冻结；纳入 Phase 4 首批任务 |
| **ISSUE-D7-1**（P2）：`maskTTL`/`isMaskValid` 死代码 | Builder B-2 未完成 | 纯清理，行为零变化；不阻塞冻结，作为 Phase 4 进入前清单项 |
| **ISSUE-D7-2**（P3）：`cacheLogKey` 日志标注 | 低优先级 | 可在 Phase 4 任意时点修正 |
| **C-7(b) 撞色目视测试** | ✅ FINAL（2026-08-16） | 三色板已升 FINAL；Phase 4 进入条件已满足 |

**多实例内存 +31 MB 的可接受性判定：**
+31 MB 略超 Day 5 验收门控 +30 MB，但测量条件偏高（本 session 共 7 次冷启动使 CoreML 运行时缓存累积；基线 484 MB 而非 Day 5 的约 304 MB）。增量结构符合架构预期（每实例约 10 MB，3 实例共 31 MB），无泄漏信号（FIFO 7 轮淘汰后内存无增长）。**判定：可接受；+31 MB 作为 Phase 3 实测值记录，不触发架构复议。**

---

### 14.2 A-4 裁决：手势识别延迟口径

#### 14.2.1 问题定义

`TouchHandler` 中 `singleTap.require(toFail: doubleTap)` 导致单击手势在手指抬起后**等待约 300 ms** 确认无双击后才触发 `handleTap` 回调。当前 `tapStartMs` 在 `handleTap` 内取，因此 `e2eMs` **不含**这 300 ms 系统等待。

**问题：** 是否应将约 300 ms 手势识别等待纳入 `e2eMs` 口径定义？

#### 14.2.2 裁决结论

> **裁决：~300 ms 手势识别等待不纳入 `e2eMs`（pipeline metric）。`e2eMs` 口径维持现定义：手势回调触发（`handleTap` 入口）→ 主线程 `maskImage` 赋值完成。**

#### 14.2.3 裁决依据（逐条独立）

1. **性质不同，不可混入同一口径。** `e2eMs` 度量的是「系统知道有一次有效 tap 后，到 mask 出现需要多长时间」——即计算管线延迟。~300 ms 等待是 OS 手势消歧窗口（让系统区分单击与双击），是**手势识别层的输入延迟**，不是管线的计算开销。混入会使 `e2eMs` 无法反映任何管线优化的实际收益。

2. **不可消除，不应被优化目标覆盖。** 双击是 §3.2.1 C1「清除全部实例」的**唯一**入口，也是 §11 裁决「清除权归用户」原则的实现形态。要消除 ~300 ms 等待，唯一路径是放弃双击手势语义。这是交互权衡，不是性能优化。**不应把一个设计决策的固定成本计入性能 SLO。**

3. **§10.6 门控对 pipeline metric 成立。** `e2eMs p95 = 94.7 ms`，远低于 ≤200 ms 的门控值。如果把 ~300 ms 纳入，数字变为约 395 ms，§10.6 设计时的 ≤200 ms 目标与「快路径本来很快」的论据（§10.3 D1）全部失效——但两者的原始立意是「pipeline 计算本来只有 61 ms，别让 UI 框架把它说成慢」，与手势层无关。口径改变会扭曲论据与结论之间的因果。

4. **与行业惯例一致。** 手势识别延迟（tap-to-callback）与 CPU/GPU 渲染延迟（callback-to-pixel）在 Apple 的性能分析工具（Instruments / MetricKit）中分属不同层级，不合并计量。本项目遵循同一分层原则。

#### 14.2.4 口径规范（封层，Day 7 起生效）

| 口径 | 定义 | 引用时标注要求 |
|------|------|--------------|
| `e2eMs`（pipeline metric）| `handleTap` 入口 → 主线程 `maskImage = composed` 赋值完成 | 必须标注「不含手势识别等待」|
| `userPerceivedMs`（感知延迟参考）| ~300 ms 手势等待 + `e2eMs` | 仅作为用户体验参考值，不作为架构 SLO |

**实测数据（Day 7）：**
- `e2eMs` mean = 73.9 ms，p95 = 94.7 ms（n=17，快路径）
- `userPerceivedMs` 参考估算 = ~374 ms median，~395 ms p95

**§10.6 门控继续以 `e2eMs` 为基准。** Phase 4 若引入替代手势（无双击等待，例如长按替代双击清除），可在新条件下重新建立 `userPerceivedMs` 基线，但不得追溯修改本次封层的 pipeline metric。

---

### 14.3 Phase 3 冻结决定

#### 14.3.1 结论

> **Phase 3 正式冻结（有条件冻结，R1 以显式保留项形式进入 Phase 4）。**

**冻结即刻生效（2026-08-15）。**

#### 14.3.2 冻结的充分条件

| 条件 | 满足状态 |
|------|---------|
| 快路径 e2e p95 ≤ 200 ms（§10.6 门控）| ✅ 实测 94.7 ms |
| 所有压力测试通过 | ✅ FIFO / encoder 忙碌 tap / 旋转 / 边缘 / 切换内存 |
| 几何正确性 | ✅ ISSUE-D7-3 修复，旋转后 tap 位置准确 |
| 无已知正确性 bug | ✅（死代码不影响行为，ISSUE-D7-1/D7-2 均非正确性问题）|
| Phase 2 pipeline 保护 | ✅ 所有改动均保护了 Phase 2 路径 |
| R3 禁令参数未被修改 | ✅ `minComponentPx=30` 等全部未动 |

#### 14.3.3 接受零慢路径样本的依据（如实记录，不美化）

R1（慢路径无样本）是已记录的已知缺口。接受它的理由有三，**三条相互独立，任意一条单独不足以支撑接受，合取则足够：**

1. **无慢路径证据 ≠ 有问题。** 慢路径难以触发（embedding TTL=8000 ms，background refresh 持续刷新）在正常使用中意味着该路径稀少，不意味着它有缺陷。Architecture 理解是完整的：慢路径 = encode(~648 ms) + decode(~61 ms)，UI 策略为脉冲动画 + 12 s 超时（§10.4 C Tier 1）。

2. **UI 策略「临时规则」的含义是「不封层」而非「不安全」。** §10.5 R1 的「临时规则」标注意味着：若日后数据揭示慢路径体验不可接受，才触发 UI 策略复议。它不预设问题存在。**「临时」修饰的是封层状态，不是正确性。**

3. **慢路径是 Phase 4 首批任务之一。** 接受 R1 的前提是 Phase 4 第一天即把慢路径采样列为强制任务（见 §14.5）。如果 Phase 4 数据揭示问题，Phase 4 架构文档写入复议结论；Phase 3 不需要因此推迟冻结。

#### 14.3.4 不阻塞冻结的开放项（承诺处置）

| 开放项 | 为何不阻塞 | Phase 4 承诺 |
|--------|----------|-------------|
| R1 慢路径 n=0 | 无正确性问题，见 §14.3.3 | Phase 4 首批任务强制采样 ≥10 次 |
| C-7(b) 色板 | ✅ FINAL（2026-08-16）| Phase 4 进入条件已满足 |
| ISSUE-D7-1 死代码 | 纯清理，行为零变化，无运行期影响 | Phase 4 进入前必须完成（Builder B-2）|
| ISSUE-D7-2 日志标注 | P3，无分析影响 | Phase 4 任意时点 |

---

### 14.4 Phase 3 最终封层表（FINAL）

本表取代 §9.5 作为 Phase 3 最终基准，并纳入 Day 7 新增实测数据。§9.5 内容仍有效，本表在其基础上补充 Day 7 条目。

**引用本表时，以本表为准（§9.5 同值行不重复引用）。**

| 参数 | 封层值 | 状态 | 依据 |
|------|--------|------|------|
| Encoder 输入分辨率 | **1024×1024** | 🔒 FINAL（Day 5）| §9.1–9.3 |
| Encoder 模型系列 | **milfix fp16 单一模型** | 🔒 FINAL（Day 5）| 768 撤销后唯一 encoder |
| Decoder 输入 embedding | `[1, 256, 64, 64]` | 🔒 FINAL（Day 5）| 与 1024 encoder 原生匹配 |
| Point prompt 坐标空间 | **恒为 1024 空间** | 🔒 FINAL（Day 5）| C-2 永成立 |
| Encoder 绝对延迟基线 | **648 ms（warm / 单模型稳态 / n=14）** | 🔒 FINAL（Day 5）| §9.3 口径标注 |
| Decode 延迟基线 | **61 ms（warm，n=24）** | 🔒 FINAL（Day 5）| Day 4 Session B |
| Embedding TTL | **8000 ms** | 🔒 **FINAL（Day 7）** | Day 6 日志（8 次 background refresh 均走 `ttl_approaching`，数值无需修订）|
| Tap mask 生命周期 | **事件驱动，无时钟（§3.2.1 C1–C6）** | 🔒 FINAL（Day 5）| §11 |
| 最大实例数 N | **3** | 🔒 FINAL（Day 1）| FIFO 上限 |
| Tap 延迟展示策略（快路径）| **脉冲确认动画，无进度条** | 🔒 FINAL（Day 5 + Day 7 复审通过）| §10.3 D1/D2；p95=94.7ms |
| Tap 延迟展示策略（慢路径）| **持续脉冲 + 12 s 超时（§10.4 C Tier 1）** | ⚠️ 临时规则（R1 保留）| 0 慢路径样本，Phase 4 补测 |
| `e2eMs` 口径定义 | `handleTap` 入口 → 主线程 `maskImage` 赋值完成 | 🔒 **FINAL（Day 7）**（A-4 裁决，§14.2）| 不含 ~300 ms 手势等待 |
| 手势识别等待 | **~300 ms，固定系统开销，不纳入 `e2eMs`** | 🔒 **FINAL（Day 7）** | §14.2；双击 C1 不可撤销 |
| 快路径 e2e p95（修正口径）| **94.7 ms**（n=17，iPhone 11，Release）| 🔒 **FINAL（Day 7）** | §27.1；通过 §10.6 ≤200 ms 门控 |
| 快路径 e2e mean（修正口径）| **73.9 ms**（n=17）| 🔒 **FINAL（Day 7）** | §27.1 |
| 多实例内存 delta（N=0→3）| **+31 MB**（tapToSegment 稳态，本 session）| ✅ **FINAL（Day 7 实测）** | §27.5；略超 +30 MB 预期，可接受 |
| Phase 2/3 切换内存平台 | **~590 MB ±15 MB，无单调增长** | ✅ **FINAL（Day 7 实测）** | §27.6 ×5 切换 |
| Tap mask 填充色 | **三槽色板** slot0 青(0,217,255) / slot1 水青(0,255,242) / slot2 春青(0,255,170) | 🔒 **FINAL**（C-7(b) 通过，2026-08-16）| §3.3.3 |
| Tap mask alpha | **primary 0.60 / secondary 0.40** | 🔒 FINAL（Day 5）| §3.4 |
| Tap mask 描边 | **双色轮廓**（primary 2.0/1.5 pt，secondary 1.5/1.0 pt，屏幕坐标系）| 🔒 FINAL（Day 5）| §3.4 L1 唯一承载者 |
| 按实例配色（per-instance hue）| **Phase 3 内恢复**（L3，仅区分用途）| 🔒 **FINAL**（与色板同步升级，2026-08-16）| §3.3.3 / §13.2 |
| R3 禁令参数 | `minComponentPx=30` / `minComponentSidePx=3` / `minComponentFill=0.05` / `maxPlausibleLogit=500.0` / `stabilityDelta=1.0` / `cap60` / `cap85` / 候选选择规则 | 🔒 FINAL（**INVIOLABLE**）| Phase 3 全程未动，§11.9 R3 |
| Fine-tuning | Phase 4 范畴 | 不变 | §8.4 |

**冻结参数修改禁令（持续到 Phase 4 明确决定）：** 上表所有 🔒 FINAL 行不得被任何 PR 修改，包括「小调参」。PROVISIONAL 行的变更须过 §3.3.2 C-7 完整准入程序。SAMDecoder.swift 与 MaskRenderer.swift 继续作为不可触碰文件。

---

### 14.5 Phase 4 规划

#### 14.5.1 Phase 4 进入条件（前置阻塞项，缺一不可进入）

| 条件 | 负责人 | 说明 |
|------|--------|------|
| **ISSUE-D7-1 清理完成**（`maskTTL`/`isMaskValid` 删除）| Builder | 纯死代码，行为零变化。留着是防呆隐患（§11.6 纪律 3）|
| **C-7(b) 撞色目视测试完成** | Builder + Debugger | 三槽位各测一个同色系真实物体 + 一次同屏三实例。通过后 §9.5 色板行由 PROVISIONAL 改 FINAL |

#### 14.5.2 Phase 4 目标

Phase 4 的核心主题是**从「功能正确」到「体验精良」**：mask 不再是一次性静态叠加，而是能跟随场景、支持更丰富交互、且可导出的活动对象。

#### 14.5.3 Phase 4 入口点（候选，按优先级排序）

**入口点 1 — 慢路径基线补全 + UI 策略封层（P0，Phase 4 首批任务）**

- 强制触发 ≥10 次 encode+decode 慢路径（方法：在 tapToSegment 模式下静止 ≥9 s 使 TTL 超时，或旋转设备触发 geometryChanged）
- 采集 mean + p95（修正口径 `e2eMs`）
- 根据数据决定：慢路径 UI 策略是否需要不同于快路径的语义（§10.5 R1 最终处置）
- 如数据显示慢路径 UX 可接受（e2eMs ≤ 800 ms p95），Tier 1（持续脉冲 + 12 s 超时）转 FINAL；否则触发 UI 策略复议

**入口点 2 — mask 重锚定（re-anchor）（P1）**

来源：§12.7.5（R6 呈现侧缓解的根本修复路径）。

> 事件型漂移检测 + 用同一 `canonicalPoint` 在新帧上重新 decode。

这是 Phase 3 的所有已知限制（R6 场景运动 mask 脱节、R13 三张不同龄 mask 同屏）的根本修复路径。它的架构价值超出视觉质量：**一旦有了 re-anchor 循环，tap mask 从「一次性产物」变成「有刷新源的资源」**，§11.4 A-1 的「自检问法」首次有了答案，时间型刷新机制才在逻辑上重新成立。顺序不可颠倒：先有刷新循环，再谈时间型刷新语义。

实现条件：(1) embedding TTL 刷新已工作（✅ Phase 3 已验证）；(2) 旋转检测 C4 已工作（✅ ISSUE-D7-3 修复）；(3) re-anchor 循环独立于 embedding TTL，使用同一 `canonicalPoint` 在新 embedding 可用后触发 decode。

**入口点 3 — C-7(b) 转 FINAL 后的 tap anchor 编号优化（P2）**

- Day 6 的 tap 锚点编号（① ② ③）是 §13.2.5 W-1 的唯一解法（两个 secondary 之间 ΔE00 < 10，色相不是充分 L3 载体）
- Phase 4 可在此基础上优化：编号的字体/对比度/动画，以及长按单实例删除（C6，已在 §3.2.1 中定义但 Day 6 只实现了框架）

**入口点 4 — A-1 候选选择规则改进（P2，须满足 §13.3.4 E-1…E-4 全部前置条件）**

- 方向已收敛：在 token 1–3 内改进选择（§13.3）
- 第一步必须是 E-2 设备端定向复采（复现三个灾难性失败案例）
- R3 禁令（INVIOLABLE）在 E-1…E-4 全部满足前持续有效
- 注意 §13.3.5 分叉点：若复采显示灾难性失败时四个 token 全部不可用，则问题在提示/embedding 层而非候选选择，须重新界定 A-1

**入口点 5 — 交互模式扩展（P3）**

- 双指拉框选区（替代 point prompt，使用 box prompt，可复用 Phase 2 box decoder）
- 长按单实例删除（C6 已定义，需完整手势实现）
- 模式 UI 改进（tap/box/segmentation 三模式切换，更清晰的视觉状态）

**入口点 6 — 分割结果导出（P3）**

- 屏幕截图 + mask 抠图（合成透明 PNG 或分层图）
- 实例池状态导出（坐标、mask、实例编号）
- 设计约束：导出不得持有 CVPixelBuffer（相机帧生命周期短），须在 decoderQueue 完成时即时复制 mask 数据

**入口点 7 — 模型升级评估（P3，后期）**

- SAM 2（比 MobileSAM 精度更高，但 iPhone 11 上资源需求须先评估）
- 512×512 fine-tuning 重评（Phase 3 已拒绝 zero-shot，fine-tuning 后可重测；入口条件：先取 ANE 逐层 profile 确认计算量是瓶颈，见 §9.4）

#### 14.5.4 Phase 4 首批任务（Day 1 启动）

| Agent | 任务 | 优先级 |
|-------|------|--------|
| **Builder** | 完成 ISSUE-D7-1（maskTTL 死代码清理）| P0（进入条件）|
| **Builder + Debugger** | 完成 C-7(b) 撞色目视测试，转 FINAL 或触发色板复议 | P0（进入条件）|
| **Debugger** | 采集 ≥10 次慢路径样本（mean + p95），写入 debug_report | P0（入口点 1）|
| **Architect** | 根据慢路径数据裁决慢路径 UI 策略（R1 最终处置）| P0（入口点 1）|
| **Architect** | 定义 Phase 4 模块边界（re-anchor 新模块 vs TapInstanceManager 扩展）| P1（入口点 2 前置）|
| **ML_Vision** | 启动 E-2 设备端定向复采（三个灾难性失败案例）| P2（入口点 4 前置）|

#### 14.5.5 Phase 4 架构约束（继承自 Phase 3，不得更改）

1. **SAMDecoder.swift 和 MaskRenderer.swift 不得修改**（现有方法签名和方法体均冻结）。
2. **R3 禁令参数（见 §14.4 最后一行）不得修改**，直到 §13.3.4 E-1…E-4 全部满足。
3. **几何链（CanonicalFrame / FrameGeometry / LetterboxTransform）不得引入第二条变换路径**，所有坐标变换仍通过 `FrameGeometry.invertViewPoint` 唯一入口。
4. **encoderQueue 和 SAMEncoder 实例单一**，Phase 4 的任何新模块不得加载第二份 encoder。
5. **Phase 2（.segmentation）路径保护**：Phase 4 改动须维持 Phase 2 bbox→mask 行为不变（TemporalManager、box decoder、renderMask 路径均受保护）。
6. **§11.4 A-1（TTL 适用性判据）在 re-anchor 循环落地之前继续有效**：Phase 4 的 mask 生命周期改动须先证明刷新源存在，再引入时间型失效。

---

### 14.6 Deliverable 确认

- [x] Day 7 Debugger 结果审阅：已通过项、仍开放项、ISSUE-D7-3 修复确认（§14.1）
- [x] A-4 裁决：手势识别 ~300 ms 不纳入 `e2eMs`，口径封层 FINAL（§14.2）
- [x] Phase 3 冻结决定：正式冻结，R1 以显式保留项进入 Phase 4（§14.3）
- [x] 接受零慢路径样本的三条独立依据（§14.3.3）
- [x] Phase 3 最终封层表（§14.4），含 Day 7 新增实测数据
- [x] Embedding TTL 8000 ms 封层为 FINAL（Day 6 数据无修订触发）
- [x] Phase 4 进入条件（两项，§14.5.1）
- [x] Phase 4 七个入口点（含优先级和前置条件，§14.5.3）
- [x] Phase 4 首批任务表（§14.5.4）
- [x] Phase 4 架构约束（§14.5.5）

**Phase 2 pipeline 保护状态：** 无变化。本节所有裁决和规划均未触碰 Phase 2 路径。✅
**R3 禁令状态：** 全部六项参数（minComponentPx/minComponentSidePx/minComponentFill/maxPlausibleLogit/stabilityDelta/cap 系列/候选选择规则）在本文件自 Phase 3 Day 1 至 Day 7 全程未被任何裁决授权修改。✅

---

END OF PHASE 3 DAY 7 ARCHITECT OUTPUT（§14 架构冻结 + Phase 4 规划）


---

# Phase 4 — Day 1 (Architect) 慢路径 UI 最终裁决

日期：2026-08-16
依据：Debugger 六段埋点（D-7'）真机补测数据（iPhone 11 / Release，慢路径合并 n=22、快路径 n=49）+ debug_report §29/§31/§32 + Builder Phase 4 Day 1 交付（锚点编号 + D-7' 埋点）
本节裁决的对象：`architect_output.md §10.4 要求 C / §14.4` 中标注为「⚠️ 临时规则（R1 保留）」的慢路径 UI 策略（Tier 1）。

---

## §15 Phase 4 Day 1 裁决：慢路径 UI 封层 + 门控线修订 + Day 1 数据审阅

---

### 15.0 本节所依据的数据（口径先声明，再用）

全部数据：iPhone 11 / A13 / Release 构建 / `.tapToSegment` / 修正口径 `e2eMs`（§14.2 FINAL，不含 ~300 ms 手势消歧窗口）。跨设备不得外推（§10.5 R5 持续有效）。

**慢路径（`encode=own`），合并 n=22（本轮 17 + §29 的 5）：**

| 指标 | 值 |
|---|---|
| mean | **804.5 ms** |
| median | 788.0 ms |
| **p95** | **931.0 ms** |
| max | 1105.4 ms |
| min | 663.4 ms |
| sd（样本）| ≈ 106.6 ms |
| > 1000 ms 的样本 | **1 / 22 = 4.5%** |
| > 800 ms 的样本 | 10 / 22 = 45.5% |

p95 用工程内既有算法 `ceil(0.95×n) − 1` = 索引 20（第 21 小）。**n=22 ≥ 20，满足 §32.5 的结构性要求，这是本项目第一个「p95 ≠ max」的慢路径估计**（max = 1105.4 被排除在 p95 之外）。我独立复核了 mean：22 个值求和 = 17698.7，÷22 = 804.49 → 804.5 ✅。

**慢路径六段（n=17）：**

| 段 | mean (ms) | 占比 |
|---|---|---|
| lock | 0.00 | ~0% |
| decide（吸收 encode）| **748.2** | **93.9%** |
| qwait | 0.14 | ~0% |
| decode | 36.4 | 4.6% |
| post | 14.4 | 1.8% |

**快路径六段（n=49）：**

| 段 | mean (ms) | p95 (ms) | 占比 |
|---|---|---|---|
| lock | 0.00 | 0.00 | ~0% |
| decide | 0.31 | 0.70 | 0.4% |
| qwait | 0.25 | 0.60 | 0.3% |
| **decode** | **63.70** | 79.90 | **78.7%** |
| **post** | **16.69** | 27.50 | **20.6%** |
| **total** | **80.97** | **97.30** | 100% |

---

### 15.1 首先要解决的不是数据，是两条互相矛盾的规则

本次裁决被两条规则同时管辖，且它们给出相反的结论：

| 出处 | 门控线 | 实测 931.0 ms 的判定 |
|---|---|---|
| `architect_output.md §14.5.3 入口点 1` | **p95 ≤ 800 ms** ⇒ Tier 1 转 FINAL；否则触发 UI 策略复议 | ❌ **不满足**（超出 131 ms）|
| `tasks.md` Phase 4 Day 1 Architect 条目 | **p95 < 1000 ms** ⇒ 维持波纹动画，Tier 1 转 FINAL | ✅ **满足**（余量 69 ms）|

#### 15.1.1 不能用「文档层级」解决

一般规则应当是：**`architect_output.md` 是规范文档，`tasks.md` 是排期文档；二者冲突时以 `architect_output.md` 为准。** 我在此把这条确立为长期文档纪律（见 §15.10 D-1）。

但它**在本例中不足以解决问题**，理由必须如实记录：**这两个数字是我在同一天（Day 7，2026-08-15）写进两个文件的，出自同一次规划、同一个作者。** 这不是「规范被排期文档挑战」，而是**我自己的输出内部不自洽**。用文档层级去裁自己的笔误，等于用形式规则掩盖一个实质错误。⇒ **必须回到实质：这两个数字分别在回答什么问题，哪一个的推导站得住。**

#### 15.1.2 回到实质：800 与 1000 在回答**两个不同的问题**

这是本次裁决的核心发现。

**1000 ms 的来源**：Miller (1968) / Card-Robertson-Mackinlay (1991) / Nielsen (1993) 的三段式响应时间阈值中的第二条边界（0.1 s / 1 s / 10 s）。它是一条**感知学**的线：1 s 以下用户的思维流不中断，处方是「无进度语义的活动指示器」；1 s 以上思维流开始中断，处方是「进度或剩余时间信息」。它回答的问题是：**「这个等待需要什么形态的 UI？」**

**800 ms 的来源**：§14.5.3 **没有给出任何推导**（Debugger §31.4 已如实指出这一点，我确认属实）。但它并非凭空而来 —— 把 Phase 3 的封层基线代进去，它的来路是清楚的：

```
§14.4 FINAL: Encoder 绝对延迟基线 648 ms（warm / background refresh / n=14）
§14.4 FINAL: Decode 延迟基线        61 ms（warm / n=24）
架构预测的慢路径 e2e = 648 + 61 = 709 ms
800 ≈ 709 × 1.13（约 13% 余量）
```

即：**800 是「管线按设计跑应该花多少钱」的和，加一点余量。** 它回答的问题是：**「慢路径的实际开销和架构预测对得上吗？」**

**⇒ 一个由各分量延迟相加得到的数字，无法回答一个感知学问题。** 把它当作 UI 门控，是把**管线预算符合性**误当成**用户体验可接受性** —— 这是与 §11（时间型 TTL 用错在「无刷新源的用户产物」上）同族的**类别错误**：工具本身没错，只是被指向了它无法度量的对象。

**一个直接的反证（reductio）**：如果 800 ms 真是一条 UX 线，那么本批数据里 **45.5% 的慢路径 tap 都属于「用户体验不可接受」** —— 而与此同时 mean = 804.5 ms 稳稳落在文献支持的「100 ms – 1 s：思维流不中断」区间内，离上界还有 196 ms。一条会把近一半样本判为不可接受、却与该区间的全部感知学处方相容的线，**不是在度量 UX**。

---

### 15.2 修订：§14.5.3 入口点 1 门控线（正式修正案）

> **修正案 AMD-15.1（2026-08-16 生效）**
>
> **§14.5.3 入口点 1 中「如数据显示慢路径 UX 可接受（e2eMs ≤ 800 ms p95），Tier 1 转 FINAL」一句，其中的 800 ms 作为 UI 门控的用途 —— ⛔ SUPERSEDED（作废）。**
>
> **取而代之的 UI 门控（判据 U）：**
>
> | # | 判据 | 阈值 | 实测 | 判定 |
> |---|---|---|---|---|
> | **U-1** | 慢路径 `e2eMs` **p95** | **< 1000 ms** | 931.0 ms | ✅ 余量 69 ms（7.4%）|
> | **U-2** | 慢路径 `e2eMs` **超限率** P(e2e > 1000 ms) | **≤ 10%** | 4.5%（1/22）| ✅ |
> | **U-3** | 样本量足以让 p95 脱离 max | **n ≥ 20** | n = 22 | ✅ |
>
> 三条**取合取**（§9.3 修订口径：批准取合取）。三条同时满足 ⇒ 慢路径不需要区别于快路径的 UI 语义。

#### 15.2.1 U-1 的推导

1000 ms 是**有文献支撑的区间边界**（Miller 1968 三阈值之一），800 ms 没有任何推导。在两条候选线之间，选有推导的那条不是「选方便的那条」—— 判据是**推导强度**，而结论方向只是碰巧。

场景条件也支持采用 1 s 带的标准处方：本场景是**用户主动发起的离散动作**、**有即时确认反馈**（tap 落下即出脉冲）、**无数据丢失风险**，这是 1 s 带里最有利的一类情形，而非边缘情形。

⚠️ 诚实标注：文献阈值是**通用交互任务**的经验值，未在本 App 上校准；它对本项目的效力来自「感知阈值是人的属性、不是设备的属性」，因此 §10.5 R5（跨设备不外推）**不适用于 U-1**，但**适用于 931.0 这个测量值本身**。

#### 15.2.2 U-2 的推导（以及为什么 p95 单独不够）

1000 ms 是**逐事件**阈值（每一次 tap 要么中断思维流，要么不中断），所以正确的检验量不只是分位数，还有**超限率**。设定 10% 的依据（锚定在本项目自身的量纲上，不是文献值）：

- 本项目的采样协议与真实使用中，一次连续 tap 会话规模约 20 次。
- 超限率 10% ⇒ 用户单次会话平均遭遇约 2 次 >1 s；这足以形成「这个功能有时候会卡」的可学习模式。
- 超限率 5% ⇒ 约 1 次/会话，读作偶发事件，不形成模式。
- ⇒ **10% 是「偶发事件」与「可学习模式」的分界，按本项目自身的会话规模定标。**

🟡 **U-2 的阈值是工程判断，不是文献值，如实标注。** 它比 800 ms 强的地方在于：它有推导、量纲对得上、且**可证伪**。

#### 15.2.3 800 ms 不作废，改指向它能回答的问题（判据 P）

> **800 ms 保留，重新界定为「慢路径管线预算符合性线（pipeline conformance）」**，负责人转为性能工作（Debugger / 后续优化），**不再触发任何 UI 决策**。
>
> - 定义：慢路径 `e2eMs` p95 ≤ 800 ms ⟺ 管线开销与 §14.4 的分量封层基线（648 + 61）相符。
> - **实测 p95 = 931.0 ms ⇒ 判据 P ❌ 未通过，超出 131 ms（+16.4%）；mean 804.5 亦超预测 709 ms 达 +95.5 ms（+13.5%）。**
> - **本条不因裁决而关闭。** 它作为 Phase 4 的一个**已确认的性能缺口**记录在案，归因见 §15.5。

这一点必须讲清楚，否则本裁决就只是「把不方便的线挪走」：**我没有删掉 800 ms，我把它指向它真正能回答的问题，而它在那个问题上照样不通过，并因此产生了一个真实的、有归属的工作项。** 我拒绝的只是「管线超预算 16%」自动推导出「脉冲动画是错的 UI」—— 这条因果链不存在。

---

### 15.3 裁决 D-15.1：Tier 1 转 FINAL

> **D-15.1 —— 慢路径 UI 策略维持 Tier 1（持续脉冲 + 12 s 超时 + 显式失败提示），不引入独立的慢路径 UI 语义，不引入进度条。状态由「⚠️ 临时规则」转为 🔒 FINAL。**

#### 15.3.1 批准所依据的合取条件（六条，全部成立）

按 §9.3 口径「批准取合取」：

| # | 条件 | 实测 / 事实 | 状态 |
|---|---|---|---|
| **G-1** | U-1：p95 < 1000 ms | 931.0 ms | ✅ |
| **G-2** | U-2：超限率 ≤ 10% | 4.5%（1/22）| ✅ |
| **G-3** | U-3：n ≥ 20，p95 脱离 max | n=22，p95 为第 21 小 | ✅ |
| **G-4** | **不存在可诚实呈现的进度量** | decide 段占 93.9%，整段落在**单次不可分割的 CoreML encode 调用**内部；无中间进度可读 | ✅（见 §15.3.3）|
| **G-5** | 自然发生率极低 | 两次完整 session 自然发生 **0** 次；全部 22 个样本均需 `forceSlowPath` 调试开关强制触发 | ✅ |
| **G-6** | 快/慢路径**已自带区分度**，无需第二套语义 | 同一套脉冲在 81 ms 下播不完一个周期，在 804 ms 下循环约 8–10 次；形态差异由延迟本身产生 | ✅ |

#### 15.3.2 冻结的参数（FINAL，明确边界）

| 项 | 冻结值 | 状态 |
|---|---|---|
| 慢路径等待反馈形态 | **持续脉冲圆环，无进度语义** | 🔒 FINAL |
| `@Published tapProcessing` 类型 | **布尔量**，不得扩展为进度值（§10.3 D2 原文） | 🔒 FINAL |
| 慢路径兜底超时 | **12 s**（§10.4 要求 C） | 🔒 FINAL |
| 快路径兜底超时 | **1.5 s**（§10.4 要求 C） | 🔒 FINAL |
| 超时行为 | **必须给出可见失败信号；静默清除加载指示为禁止行为** | 🔒 FINAL |
| 是否存在独立的慢路径 UI 分支 | **否** —— UI 不得读取 `TapPath` 来选择呈现形态 | 🔒 FINAL |

⚠️ **不在冻结范围内的**（明确写出，避免冻结被过度解读）：指示器的**文案措辞**、脉冲的周期/半径/配色等纯视觉参数。这些不是架构参数，Phase 4 可在不触发复议的前提下调整，前提是不引入进度语义、不引入路径分支。

#### 15.3.3 G-4 是本裁决中最强的一条，且它独立于阈值之争

即使 p95 是 1105 而不是 931，**进度条依然是错的**，理由与延迟大小无关：

慢路径 93.9% 的时间落在一次 `SAMEncoder` 的 CoreML 推理调用内部。**CoreML 不暴露单次推理的中间进度。** 因此任何进度条上的「50%」都只能是按经验时长动画出来的**编造值** —— 它在 encode 提前完成时会跳到 100%，在 encode 慢于预期时会停在 95% 不动，这两种形态都比没有进度条更伤信任。

> **⇒ 长期约定 A-6（进度语义准入）：**
> **进度语义（进度条 / 百分比 / 剩余时间）的准入条件是「存在一个可观测的进度量」，不是「延迟够长」。**
> 自检问法：**「如果我要画这个进度条，第 50% 是从哪里读出来的？」** 若答案是「按经验时间估的」，则进度语义被禁止，无论延迟多长。此时正确的升级方向是**把不透明的长操作拆成可观测的阶段**，而不是画一个假的进度。
>
> 与既有约定的关系：A-3 管「不要把正确性押在你控制不了的量上」，A-5 管「不要用没校准过的代理量改判据」，**A-6 管「不要呈现你没有的信息」**。三者同族：都禁止用一个你实际没有的东西去支撑一个断言。

#### 15.3.4 撤销条件（析取，任一触发即重开）

按 §9.3 口径「撤销取析取」。**FINAL 不等于停止观察，等于在下列任一条件触发前停止重新裁决**：

| # | 撤销触发条件 |
|---|---|
| **V-1** | 参考设备（iPhone 11 / Release）上任一次 n ≥ 20 的慢路径复测出现 **p95 ≥ 1000 ms** |
| **V-2** | 任一次 n ≥ 20 复测的 **>1000 ms 超限率 > 10%** |
| **V-3** | **自然发生率上升**：在**未开启** `forceSlowPath` 的单次正常会话中出现 **≥ 5 次** 慢路径 tap（此条直接针对 G-5 —— Phase 4A re-anchor 有可能改变 encode 的触发节奏）|
| **V-4** | 管线结构性变更使 encode 不再是单次不可分割调用（例如分块 encode / 流式 encode）⇒ G-4 失效，进度语义首次成为可选项 |

V-3 是这四条里最可能被触发的一条，也是我认为 Day 2–3 必须留意的：**本裁决有一条支柱（G-5）建立在「用户基本遇不到慢路径」之上，而 re-anchor 正是可能改变这一点的改动。**

---

### 15.4 1105.4 ms 那一个样本：它改变答案吗？

**结论：不改变。四条独立理由。**

1. **量化定位。** 1105.4 位于 mean + **2.82 sd**；它与次大值 931.0 之间隔了 **174.4 ms**，而排名第 2–6 的五个样本（869.9 / 884.2 / 889.1 / 915.1 / 931.0）总跨度只有 **61.1 ms**。这个间隔是上尾密集区跨度的 **2.85×** ⇒ 形态上更像一次**独立事件**（一次调度/热管理打嗝），而不是同一分布的自然尾部。
   ⚠️ **但我没有任何独立证据可以把它剔除**（无对应的热状态或调度记录），因此 **§15.0 的全部统计量都包含它**，包括 mean 和超限率。定位归定位，不做数据裁剪。
2. **它已经被判据吸收了。** U-2 存在的目的就是处理这类逐事件越线：4.5% ≤ 10%，通过。用「有一个样本越线」去否决一个**已经把越线率作为判据**的裁决，是重复计数。
3. **越线之后的正确处方，我们已经在做了。** 1 s 以上的失效模式是「用户注意力转移」，其对策是**保持一个可信的『还在工作』信号**。Tier 1 恰好就是这个信号，且它在 12 s 内不会消失（§10.4 要求 C）。1105 ms 时用户看到的是脉冲多转两圈，不是指示器消失。**Tier 1 在 >1 s 区间里缺的不是「进度」，是「措辞升级」** —— 而措辞不在冻结范围内（§15.3.2）。
4. **复合暴露率。** 用户真正遭遇一次 >1 s 的 tap，需要「自然进入慢路径」×「该次越线」两件事同时发生。前者在两次完整 session 中发生 **0** 次。为一个复合概率极低的事件设计第二套 UI 语言，正是 §10.3 D1 与 §31.3 都指出的**反向风险**（教会用户「这个 App 有时候会变慢」）。

#### 15.4.1 已批准但**不排期**的可选增强（不构成 scope 变更）

Debugger §31.4 提出的低成本增强 —— **脉冲持续超过约 300 ms 后，文案由「已收到点击」切换为「正在分割…」** —— 我**在设计上批准**：

- 它不是进度语义：不分段、无百分比、`tapProcessing` 仍是布尔量 ⇒ 满足 §10.3 D2 与 A-6。
- 300 ms 阈值有推导：快路径 p95 = 97.3 ms，3× 余量后仍绝不会在快路径触发 ⇒ **天然只在慢路径可见，无需 UI 读取 `TapPath`** ⇒ 不违反 §15.3.2 的「不得有 UI 路径分支」。
- 它落在「文案措辞」上，**不在冻结范围内**，因此实施它**不需要撤销 D-15.1**。

⚠️ **但我不把它排进 Phase 4 Day 1–3。** 理由：(a) 本轮授权范围是裁决，新增 Builder 任务属 scope 变更，需用户显式指示；(b) Day 2–3 的 re-anchor 是核心基础设施，不应被非阻塞的呈现改进挤占。**处置：作为「已批准、待排期」的设计存档；若 V-3 被触发（慢路径变常见），本项自动升为 P1。**

---

### 15.5 慢路径延迟账目 100% 闭合 —— 并且发现了一个新的条件性基线

判据 P（§15.2.3）没过，超预算 +95.5 ms（mean）。六段数据让这笔账**完全对上了**，零残差：

```
架构预测（§14.4 分量基线相加）              648.0 (encode) +  61.0 (decode)          = 709.0 ms
  ① tap 触发的 encode 溢价                  748.2 − 648.0                            = +100.2 ms
  ② 慢路径 decode 折价                      36.4 − 61.0                              =  −24.6 ms
  ③ 预测中**根本没有**的 post 项            + post                                    =  +14.4 ms
  ④ lock + qwait                                                                      =  + 0.1 ms
─────────────────────────────────────────────────────────────────────────────────────────────────
合计                                                                                  ≈ 799.1 ms
实测慢路径 n=17 总均值（由 93.9% 占比反推 ≈ 796.8；由六段均值直接相加 = 799.1）
差 2.3 ms = 各段均值的舍入误差
```

三条结论：

**(1) 新发现：tap 触发的 encode 比 background refresh 的 encode 贵约 100 ms（+15.5%），且可复现。**
§29.2 在完全独立的一轮测量里得到 +114 ms（762 vs 648）。两轮独立会话方向一致、量级一致 ⇒ **这不是噪声。**
⇒ **§14.4 的「Encoder 绝对延迟基线 648 ms」没有错，但它是条件受限的**：它标注的条件是 `warm / 单模型稳态 / n=14`，实际采自 **background refresh** 路径。tap 触发的 encode 是另一个工况。
按 §9.3 确立的纪律（延迟数字必须标注冷/热态、模型切换、并发负载、n），本项**新增一行条件性基线，不修改 648 那一行**（见 §15.9）。

**(2) 判据 P 未通过的成因已完全归因，不是未知风险。** 超预算的构成是 ①+③ 减去 ②，其中 ③（post 项）是**静态预测的漏项**：§26.2.4 的 720–760 ms 估算里根本没有后处理项。
> **方法学记录 M-15.1：静态预测漏项造成的是系统性低估，而低估量恰好会落进门控的余量里。** ⇒ 今后由分量相加得到的门控线，必须先列出「分量清单是否穷尽」，未穷尽的清单不得直接当门控。

**(3) 慢路径的可优化面在 encode，且只在 encode。** decide 段 93.9%，其余五段合计 6.1%。任何针对 lock / qwait / post 的慢路径优化，收益上界都小于 15 ms。**明确预先否决**：慢路径的调度层优化不予立项。

---

### 15.6 保留项状态更新

#### 15.6.1 R1（慢路径无基线 / Tier 1 临时规则）—— ✅ **完全结案**

R1 此前已在 2026-08-16 由 §29（n=5）「结案」，但残留了一条尾巴：「Tier 1 慢路径 UI 临时规则留至 Phase 4 裁决」。**本节 D-15.1 清偿了这条尾巴。**

| R1 的两个部分 | 状态 |
|---|---|
| 慢路径缺少实测基线 | ✅ 已关闭（n=22，mean 804.5 / p95 931.0，且 p95 首次脱离 max）|
| 慢路径 UI 策略未封层 | ✅ **本节关闭**（D-15.1，Tier 1 → 🔒 FINAL）|

**R1 自 §10.5 起存在 7 天，现正式除名。** 值得记的是 §14.3.3 当时接受「零慢路径样本」的三条理由中，第 3 条是「Phase 4 第一天即把慢路径采样列为强制任务」—— 这条承诺**已兑现**，接受 R1 的前提没有落空。**「有条件冻结」这个做法在本项目上被验证为有效**，条件是承诺必须写成可执行的排期项而不是意向。

#### 15.6.2 R4（decoderQueue decode 堆积）—— ⚠️ **保持 OPEN，不得关闭**

实测 `qwait`：慢路径 mean 0.14 ms；快路径 mean 0.25 ms / p95 0.60 ms。堆积**没有发生**。

**但这不构成关闭 R4 的理由，理由必须写清楚**：R4 描述的风险是「**多个实例的 decode 请求更密集地进入串行 decoderQueue**」。当前测量协议是**逐次 tap、间隔 ≥ 2 秒**（§32.3 步骤 5 明文要求），**这个密集节奏从未出现过**。

> **在一个从未发生过的节奏下测不到堆积，不是「堆积不会发生」的证据。**（与 §13.3.2 的方法学互补：那里说「证明无效比证明有效容易」，这里说「无效的证明只在被测过的条件域内有效」。）

⇒ R4 状态：**未触发（NOT-OBSERVED at Phase-3 cadence），非已排除。** 而 **Phase 4A re-anchor 正是第一个会制造该节奏的改动**（见 §15.7.2）。qwait 埋点已就位 ⇒ **R4 在 Day 2–3 首次成为可直接观测量**，这是 D-7' 的一个额外红利。

#### 15.6.3 W-1（secondary 之间 L3 欠载）—— ✅ **关闭，并升格为冻结载体**

Builder 的锚点编号已落地并在**两段真机录像**上通过目视验收：编号 1/2/3 清晰，primary（大号 + 白色描边环）可辨，FIFO 编号回收正确，复杂背景下可读。

**关闭 W-1 的同时，本轮数据反过来验证了 §13.2.5 的判断是必要的而非保守**：三个 FINAL 色相（160°–189°，可行弧仅 34.8°）在真机上确实**不足以单独承担实例区分**，现在这个负载由数字承担 —— 这是 W-1 当初「色相是 L3 的载体之一但不是充分载体」结论的**直接现场证据**，不再是推算。

> **⇒ 由此产生一条冻结条款：锚点编号自即日起是 L3 的承载者，进入冻结集。**
> **在没有提供等效替代 L3 载体的前提下，不得移除编号、不得把编号降级为「可选装饰」。** 移除编号 = W-1 重新打开。
> 与 §13.2.5 已列的两条「不接受的便宜解法」（提 secondary alpha、拉开色相）合并为同一条纪律：**L3 只能靠增加载体解决，不能靠调参解决，也不能靠删载体回避。**

#### 15.6.4 A-1（候选选择规则）—— 状态不变，仅增加机制层旁证

数据：慢路径 **17/17 taps 选中 `sel=ch0`**。

**这与 §13.3 的既有机制描述一致**：`ch0` 是导出的三个 token（token 1–3）中的第一个，即 token 1；现行规则「面积最小且 ≤cap60」在 §13.3.2 已被观测为 ≈ 恒取 token 1（token 1 只在 5/20 上 iou_pred 最高，却几乎总被选中）。⇒ **17/17 是对该机制的旁证。**

**明确它不是什么**（三条限制，缺一不可省）：
1. **只覆盖慢路径**，且慢路径的 decode 剖面与快路径显著不同（36.4 vs 63.7 ms，见 §15.7）⇒ 不能外推到快路径。
2. **17 个样本来自同一会话、同一组场景** ⇒ 观测之间高度相关，**不是 17 个独立观测**，实际证据量远小于 n=17。
3. **它是「规则退化」的证据，不是「规则错误」的证据。** 从「规则近乎恒定」推出「应当换规则」正是 §13.3.3 已经封堵的推理：换成取 iou_pred 最大是**选择式**改动，会在几乎每个样本上改变输出，直接毁掉 44/50 = 88% 基线的可比性，且 iou_pred 是未经外部标注校准的共线代理量（约定 A-5）。

⇒ **A-1 状态不变：方向仍收敛在 token 1–3 内改进选择；R3 禁令（INVIOLABLE）继续有效；E-1…E-4 一条都没有被本轮数据推进。**

一条**次要的正面影响**（不构成门控松动）：既然现行规则的输出近乎恒定，那么 E-3 要求的「否决式」改动在分析上更干净 —— 基线输出是一个近乎常数的参照，discordant 子集会很小且边界清楚。**这降低了 E-1 的执行成本，但不降低 E-1 的必要性。**

⚠️ **文档卫生提示**：本项目中 `A-1` 被两个不同对象占用 —— **长期约定 A-1**（时间型 TTL 适用性判据，§11.4）与**保留项 A-1**（候选选择规则，§12/§13）。今后引用必须带限定词（「约定 A-1」/「A-1 候选选择」），不得裸写。本节全部裸写的 `A-1` 均指**保留项 A-1（候选选择）**；新增的进度语义约定编号为 **A-6**，避开冲突。

---

### 15.7 新增架构关注项：decode 段异常（ISSUE-P4-DECODE）

> 对应 Debugger 在 debug_report §33 中新提的 ISSUE（本节不引用其编号以避免与我未见到的正文冲突；架构侧统一以 **ISSUE-P4-DECODE** 指代）。

#### 15.7.1 事实

| | mean | sd | n |
|---|---|---|---|
| 快路径 decode | **63.7 ms** | 9.3 | 50 |
| 慢路径 decode | **36.4 ms** | 7.3 | 17 |

差 27.5 ms；Welch t = 12.42；Cohen d = 3.28。热节流已被排除（更快的那批在更热的会话里、且时间更晚）。**这是一个效应量极大、方向反直觉的差异**（同一份 decoder、同一个 `.cpuAndGPU` 配置、同样形状的输入，慢路径反而快 43%）。

#### 15.7.2 架构侧的判断：候选假说不完整，缺第三条

Debugger 列了两条：(a) background refresh 造成的 ANE/GPU 争用；(b) embedding 内存局部性。**我认为缺了一条，而且它是与观测方向最相符的一条：**

> **(c) GPU/ANE 的 DVFS 时钟爬坡（power-state ramp）。**
> - 慢路径的 decode 紧跟在一次刚刚跑完 **748 ms 连续推理**的 encode 之后 ⇒ GPU/ANE 处在高频稳态、缓存已热。
> - 快路径的 decode 复用的是**最多 8000 ms 之前**由 background refresh 产生的 embedding，其间 GPU 可能已降频 ⇒ 这次 decode 要付爬坡的钱。
> - 该假说同时解释**符号**（慢路径更快）与**量级**（数十 ms 的爬坡在移动 GPU 上是常见量级），而 (a) 预测的方向相反（争用应当让**有 refresh 的那侧**更慢，即快路径更慢 —— 这一点与观测相符，故 (a) 也未被排除），(b) 只能解释缓存命中差异，较难独立解释 27.5 ms。

⚠️ **实验设计权仍归 Debugger。** 我在此只提供两点架构输入：**(1) 假说集必须扩充到三条**；**(2) (a) 与 (c) 有一个共同的判别维度 —— 「decode 之前 GPU 是否刚刚被连续占用」，二者可用同一个操纵变量区分（在 decode 前插入一段可控的 GPU 预热负载）。**

**如果 (c) 成立，则这 30 ms 的「可恢复性」需要重新表述**：它不是靠改代码免费拿回的，而是要用一份**常驻预热负载**去换，代价是功耗与发热。那时它就从「性能 bug」变成「功耗/延迟权衡」，需要单独立项而不是顺手修掉。

#### 15.7.3 优先级裁决：P1，但**不排在 Day 2–3 之前**，且必须**在 re-anchor 之后重新定基线**

Debugger 的观点是：若 ~30 ms 可恢复，会实质性放宽 re-anchor 的帧预算。**我确认这个判断在算术上是对的**：

| 每次漂移事件的 decode 批次成本（串行 `decoderQueue`）| decode = 63.7 ms | decode = 36.4 ms |
|---|---|---|
| N=1 实例 | 63.7 ms ✅ 装得下 100 ms 节流窗 | 36.4 ms ✅ |
| N=2 实例 | 127.4 ms ❌ 溢出 | 72.8 ms ✅ |
| N=3 实例 | 191.1 ms ❌ 溢出 91 ms | 109.2 ms ❌ 溢出 9 ms |

⇒ 这 30 ms 的价值是把「可持续的实例数」从 **1 抬到 2**。确实是实质性的。

**但它改变不了一个更重要的事实：无论 30 ms 是否可恢复，N=3 在 100 ms 固定节流下都装不下。** ⇒ 真正的架构缺陷不在 decode 的绝对值，而在**「固定间隔节流」这个设计本身对每次 decode 成本敏感**。

> **⇒ 裁决 D-15.2（Day 2–3 re-anchor 契约的预置约束，正式契约将在 Day 2 的 Architect 条目中给出）：**
> **re-anchor 的节流必须是「负载自适应」而非「固定间隔」** —— 下一批 re-anchor 的准入条件是「上一批已完成」（**同时最多一个在途批次，超出的漂移事件丢弃而非排队**），固定间隔只作为**下界**（最小间隔），不作为唯一闸门。
> 这样，无论 decode 是 36 还是 64 ms，循环都以**降低刷新频率**降级，而不是**累积队列深度**。⇒ **把 decode 绝对值从「正确性问题」降级为「性能问题」**，也正面覆盖 R4（§15.6.2）。

**由此得出优先级：**

| 工作项 | 优先级 | 排序 |
|---|---|---|
| re-anchor 负载自适应节流契约（D-15.2）| **P0** | Day 2，Architect 契约内，**先于** Builder 实现 |
| Day 2–3 re-anchor 实现 | **P0** | 不被 ISSUE-P4-DECODE 阻塞 |
| ISSUE-P4-DECODE 判别实验 | **P1** | **排在 re-anchor 落地之后，并在其后重新定基线** |

**为什么判别实验要排在 re-anchor 之后（这是本条最重要的理由）：**
假说 (a) 与 (c) 都以「GPU 的占用/功耗状态」为自变量，而 **re-anchor 循环本身就会持续占用 GPU，从而改变这个自变量**。在 re-anchor 落地之前测得的结论，可能描述的是一个 Day 3 之后**不再存在的系统状态**。反过来说：**re-anchor 循环本身就是对 (a)/(c) 的一次粗粒度实验** —— 若 (c) 成立，快路径 decode 会在 re-anchor 上线后**自发**向 36 ms 靠拢。D-7' 埋点已就位，这份数据**不额外花钱**。

⚠️ **对该排序的诚实反论**：如果先不查，Builder 在 Day 2–3 会拿着一个可能错的 decode 值去定阈值。**D-15.2 正是为了消解这条反论而设** —— 负载自适应节流让阈值不再依赖 decode 的绝对值。若 D-15.2 因故未被采纳，则本优先级排序**必须重新裁决**。

#### 15.7.4 Day 2 契约还必须预算 `post` 段（当前未被任何人纳入）

`post` = 16.69 ms/次（快路径 mean，p95 27.5 ms），**占快路径的 20.6%，是仅次于 decode 的第二大项**。N=3 的一次 re-anchor 批次会产生 3 × 16.69 ≈ **50 ms** 的后处理 + 主线程提交工作。

⚠️ 当前 `post`（T5→T6）**同时包含 decoderQueue 上的后处理和主线程跳转**，二者未分离 ⇒ **其中有多少落在主线程上，目前无人知道**。这是 re-anchor 帧预算里唯一一个连量级都没有约束的项。**Day 2 契约必须为 post 显式留预算；若 Day 2–3 出现掉帧或主线程卡顿，post 的主/非主分解是第一个该查的地方**（形式上等价于 §30.5 的 D-7'-ext 扩展，但针对 T5→T6 而非 T2）。

---

### 15.8 Day 1 埋点数据的其余架构结论

#### 15.8.1 §24.3 的 280–310 ms 未归因残差：已不存在，(a)/(b)/(c) 三分叉结案

post = 16.69 ms（快路径 mean）⇒ §24.3 在 Day 5 口径下记录的 280–310 ms 残差**已被消除**，且是被一个**为别的目的做的改动**（§10.4 要求 A 快路径去队列化）顺带消除的。

**⇒ 三个互相排斥的候选（(a) GPU→CPU 回读阻塞 / (b) 主队列派发等待 / (c) `UIGraphicsImageRenderer` 非主线程构造）全部结案，无需任何一方被证实。**

这件事有两个值得写进纪律的产出：

1. **§30.0 的排序纪律被证实是对的，且这是它的第一笔直接收益。** Debugger 坚持「D-7' 埋点先于任何延迟优化」，并在 §30.0 里预先写明「D-7' 的第一个产出可能是『残差已不存在』」。**结果正是如此。** 如果按原计划直接从 (a)/(b)/(c) 里三选一动手，代价是三分之二的概率改错地方 + 污染归因基线，**而正确答案是「三个都不用改」**——这个答案只有测量能给出。
   > **⇒ 流程规则确立：任何「未归因残差」都必须先埋点复测，再立项修复。「先测再修」在本项目已有实证收益，不再是原则性主张。**
2. **归因账目有保质期。**
   > **方法学记录 M-15.2：跨口径、跨版本的残差量不得直接沿用。** §24.3 的残差是 Day 5 口径（e2e ≈ 466 ms）下的量，到 Day 7/Phase 4 口径下它已经蒸发。**引用一个旧的性能残差之前，必须先确认它在当前口径下仍然存在。** 与约定 A-4（验收断言绑定系统行为、归因必须由隔离实验建立）同族。

#### 15.8.2 `lock` 段：stateLock 无竞争 —— 并预先否决对它的优化

`lock` 段在 **66 个样本（49 快 + 17 慢）** 上全部打印为 `0.0`。

⚠️ **精度声明**：`[D7']` 用 `%.1f` 打印 ⇒ `0.0` 的真实含义是 **< 0.05 ms**，不是「恒等于零」。本文档中不得把它表述为「零」。

两条结论：

1. **§10.4 要求 A 的设计得到验证**：「在 `stateLock` 保护下只做读取与判定、锁内不得跨队列等待、不做任何模型调用」—— 这条约束把锁持有时间压到了 50 µs 以下。**这是 Phase 3 调度设计中被实测确认最干净的一条。**
2. > **预先否决**：任何「把 `stateLock` 换成无锁结构 / 读写锁 / actor」的提案，**其收益上界已被直接观测为 < 0.05 ms/次**。此类提案在 Phase 4 内不予立项，除非先提出一个与延迟无关的理由（例如正确性或可维护性），且该理由不得引用性能。

#### 15.8.3 快路径新基线：**不覆盖** §14.4 的 FINAL 行，另立条目

| | Day 7 §14.4 FINAL | Phase 4 Day 1 本轮 |
|---|---|---|
| n | 17 | **49** |
| mean | 73.9 ms | **80.97 ms** |
| p95 | 94.7 ms | **97.30 ms** |

处置：**不修改 §14.4 的 FINAL 行**（§14.2 已确立「不得追溯修改已封层的 pipeline metric」；§14.4 亦已确立「本表在 §9.5 基础上补充而非改写」的先例）。本轮数据作为**独立出处、独立条件标注**的新行记入 §15.9。

- 两者**不冲突**：不同会话、不同 n、且可能落在 ISSUE-P4-DECODE 的不同 decode 工况上。
- **§10.6 门控（p95 ≤ 200 ms）在新基线下依然通过，余量 2.06×。** D1 维持无异议。
- ⚠️ **mean +7.1 ms（+9.6%）的来源未归因**，如实记录，并给出我能确定的边界：
  - **可排除：D-7' 埋点本身。** 新增 5 次 `PerfLogger.nowMs()`（单调时钟读取，纳秒级）+ 每次 tap 一行 `os_log`（微秒到百微秒级）⇒ **量级上不可能解释 7 ms**。
  - **不可排除：Builder 本轮的 `TapAnchorMarkerView` 三层构造**（黑色底盘 + 槽位色圆盘 + 数字，随实例发布到主线程）。它若有成本，会落在 `post` 段内 ⇒ **上界被 post = 16.69 ms 约束**。
  - ⇒ 这是把 ISSUE-P4-DECODE 定为 **P1 而非 P2** 的第二条理由：快路径 mean 存在一个方向不利、成因未定的漂移，而它与 decode 异常共用同一段测量。

---

### 15.9 Phase 4 Day 1 封层增量表

**本表在 §14.4 基础上增量，不改写 §14.4 任何一行。** 引用时：同名项以本表为准；本表未列的项仍以 §14.4 为准。

| 参数 | 值 | 状态 | 依据 |
|---|---|---|---|
| **Tap 延迟展示策略（慢路径）** | **持续脉冲（无进度语义）+ 12 s 超时 + 显式失败提示** | 🔒 **FINAL（Phase 4 Day 1）** ——「⚠️ 临时规则（R1 保留）」**已取代** | §15.3 D-15.1 |
| **慢路径 UI 门控线** | **p95 < 1000 ms ∧ 超限率 ≤ 10% ∧ n ≥ 20**（判据 U-1/U-2/U-3）| 🔒 FINAL | §15.2 AMD-15.1 |
| **§14.5.3「p95 ≤ 800 ms」作为 UI 门控** | ⛔ **SUPERSEDED（作废）** | 已作废 | §15.1.2 / §15.2 |
| **800 ms 的新用途：慢路径管线预算符合性线** | p95 ≤ 800 ms | ⚠️ **未通过（实测 931.0，+16.4%）**，作为性能缺口保持开放 | §15.2.3 / §15.5 |
| 慢路径 `e2eMs` mean | **804.5 ms**（n=22，iPhone 11 / Release / `forceSlowPath`）| ✅ FINAL（实测）| §15.0 |
| 慢路径 `e2eMs` p95 | **931.0 ms**（n=22，首个 p95 ≠ max 的估计）| ✅ FINAL（实测）| §15.0 |
| 慢路径 >1000 ms 超限率 | **4.5%（1/22）** | ✅ FINAL（实测）| §15.0 |
| **Encoder 延迟基线（tap 触发工况）** | **≈ 748 ms**（n=17；§29.2 独立复现 ≈762）| ✅ **新增条件性基线**（**不修改** §14.4 的 648 ms 行 —— 后者的工况是 background refresh）| §15.5 |
| Decode 延迟（快路径工况）| **63.7 ms**（n=50，sd 9.3）| ⚠️ 与下一行并存，差异见 ISSUE-P4-DECODE | §15.7 |
| Decode 延迟（慢路径工况，紧随 encode）| **36.4 ms**（n=17，sd 7.3）| ⚠️ 同上 | §15.7 |
| 快路径 `e2eMs` mean / p95（Phase 4 Day 1 口径）| **80.97 / 97.30 ms**（n=49）| ✅ 实测新行，**§14.4 的 73.9 / 94.7（n=17）保持不变** | §15.8.3 |
| `lock` 段（stateLock 持有）| **< 0.05 ms**（66 样本全部低于打印精度）| ✅ FINAL（实测）| §15.8.2 |
| `qwait` 段（decoderQueue 排队）| **0.14–0.25 ms mean / 0.60 ms p95**（Phase 3 节奏下）| ⚠️ **条件受限** —— re-anchor 节奏下须重测 | §15.6.2 |
| **tap 锚点编号（1/2/3）** | **L3 必需载体** | 🔒 **FINAL（冻结载体）** —— 无等效替代载体前不得移除 | §15.6.3 |
| `@Published tapProcessing` | **布尔量，不得扩展为进度值** | 🔒 FINAL（§10.3 D2 延续）| §15.3.2 |
| UI 是否按 `TapPath` 分支 | **否** | 🔒 FINAL | §15.3.2 |
| 指示器文案措辞 / 脉冲视觉参数 | **不在冻结范围内** | 明示排除 | §15.3.2 |
| R3 禁令参数 | 全部七项 | 🔒 FINAL（**INVIOLABLE**）| **本节未授权任何修改** |

---

### 15.10 本节确立的长期约定与纪律（汇总）

| 编号 | 内容 | 自检问法 |
|---|---|---|
| **A-6** | **进度语义准入**：进度条 / 百分比 / 剩余时间的准入条件是「存在可观测的进度量」，不是「延迟够长」。无进度量时，正确方向是把不透明的长操作拆成可观测阶段，而不是画一个估出来的进度 | **「如果我要画这个进度条，第 50% 是从哪里读出来的？」** 答「按经验估的」⇒ 禁止 |
| **D-1** | **文档层级**：`architect_output.md`（规范）> `tasks.md`（排期）。二者冲突以前者为准 —— **但若两者出自同一作者同一次输出，层级规则不适用，必须回到实质裁决**（§15.1.1） | 「这两个数字是两个人写的，还是我自己写了两遍？」 |
| **D-2** | **门控线必须带推导。** 无推导的整数阈值不得作为门控。（800 ms 的教训：它其实有来路 —— 分量相加 —— 但因为没写出来，它被指向了一个它无法回答的问题） | 「这条线是从哪个量推出来的？它推出的是哪个问题的答案？」 |
| **M-15.1** | 由分量相加得到的门控线，必须先证明**分量清单穷尽**；漏项造成系统性低估，且低估量恰好落进余量里 | 「这个和里少了什么段？」 |
| **M-15.2** | **归因账目有保质期。** 跨口径 / 跨版本的性能残差不得直接沿用，引用前必须在当前口径下复测其仍存在 | 「这个残差是哪个口径下的？现在还在吗？」 |
| **M-15.3** | **无效的证明只在被测过的条件域内有效。** 在从未出现过的节奏 / 负载下测不到某个失效，不构成该失效不会发生的证据（R4 的处置依据） | 「我测的时候，触发它的条件出现过吗？」 |
| **M-15.4** | **先测再修**：未归因残差必须先埋点复测再立项。本项目已有直接收益（§15.8.1，三分叉全部结案且无需任何修改）| 「我要修的这个东西，我直接测到过它吗？」 |

---

### 15.11 Deliverable 确认

- [x] 识别并解决 §14.5.3（800 ms）与 tasks.md（1000 ms）两条门控线的冲突，**在实质层面而非文档层级层面裁决**（§15.1）
- [x] 修正案 AMD-15.1：800 ms 的 **UI 门控用途作废并标记 SUPERSEDED**；新门控 U-1/U-2/U-3 给出推导（§15.2）
- [x] 800 ms **不删除**，重新界定为管线预算符合性线；**该线仍未通过（931.0 vs 800）并保持开放**（§15.2.3）
- [x] 裁决 D-15.1：Tier 1 → 🔒 **FINAL**，六条合取批准条件 + 四条析取撤销条件，冻结参数与非冻结项明确划界（§15.3）
- [x] 1105.4 ms 单样本处置：四条独立理由，**不改变裁决**；统计量不做裁剪（§15.4）
- [x] 长期约定 **A-6**（进度语义准入）确立（§15.3.3）
- [x] 慢路径延迟账目 **100% 闭合**；新发现 tap 触发 encode 溢价 ≈ +100 ms（两轮独立会话复现）（§15.5）
- [x] **R1 完全结案并除名**（基线 + UI 封层两部分均已清偿）（§15.6.1）
- [x] **R4 保持 OPEN**（在从未出现的节奏下未观测 ≠ 已排除）（§15.6.2）
- [x] **W-1 关闭**，锚点编号升格为**冻结的 L3 载体**（§15.6.3）
- [x] **A-1（候选选择）状态不变**；17/17 `sel=ch0` 记为机制旁证，三条限制写明；E-1…E-4 未被推进（§15.6.4）
- [x] **ISSUE-P4-DECODE** 立为 Phase 4 架构关注项；补充第三条假说（DVFS 时钟爬坡）；定级 **P1，排在 Day 2–3 之后并须重新定基线**，含反论与其消解条件（§15.7）
- [x] 裁决 **D-15.2**：re-anchor 节流必须**负载自适应**，正式契约 Day 2 给出（§15.7.3）
- [x] Day 2 契约必须为 `post` 段留预算（当前 20.6%，主/非主未分解）（§15.7.4）
- [x] §24.3 的 280–310 ms 残差结案，(a)/(b)/(c) 三分叉全部关闭（§15.8.1）
- [x] `stateLock` 优化提案**预先否决**（收益上界 < 0.05 ms）（§15.8.2）
- [x] 快路径新基线**另立条目、不覆盖 §14.4 FINAL 行**；mean +7.1 ms 漂移如实记录并给出可排除/不可排除边界（§15.8.3）
- [x] Phase 4 Day 1 封层增量表（§15.9）
- [x] 长期约定与纪律汇总：A-6 / D-1 / D-2 / M-15.1…M-15.4（§15.10）

**Phase 2 pipeline 保护状态：** 无变化。本节全部内容为裁决与文档，未触碰任何源文件、任何 `.segmentation` 路径。✅
**R3 禁令状态：** `minComponentPx=30` / `minComponentSidePx=3` / `minComponentFill=0.05` / `maxPlausibleLogit=500.0` / `stabilityDelta=1.0` / `cap60` / `cap85` / 候选选择规则 —— **本节未授权任何修改**。✅
**冻结文件状态：** `SAMDecoder.swift` / `MaskRenderer.swift` 未涉及、未授权修改。✅

---

END OF PHASE 4 DAY 1 ARCHITECT OUTPUT（§15 慢路径 UI 最终裁决）


---

# Phase 4 — Day 2–3 (Architect) Re-anchor 架构契约

日期：2026-08-16
依据：tasks.md Phase 4 Day 2–3 Architect 任务 + architect_output.md §14.4（冻结参数）+ §15（Day 1 裁决：D-15.2 / ISSUE-P4-DECODE / R4 / post 预算）+ debug_report.md §33（快路径 decode mean=63.70ms p95=79.90ms、慢路径 decode mean=36.4ms、post mean=16.69ms p95=27.50ms、qwait mean=0.25ms max=0.8ms）

---

## §16 Phase 4 Day 2–3 Re-anchor 架构契约

---

### 16.1 DriftDetector 接口契约

#### 16.1.1 协议定义（伪代码，接口级，非可编译 Swift）

```
// 新文件：Interaction/DriftDetector.swift
struct DriftDetector {

    // --- 可调参常量（Builder 以 var 暴露，供真机调参，不得硬编码魔数）---
    static var translationThresholdPt: CGFloat = 10.0   // 初始值，待真机调参
    static var rotationThresholdDeg: Double     = 3.0   // 初始值，待真机调参

    // --- 主接口 ---
    // 输入：两帧 FrameGeometry 快照（来自同一 AVCaptureSession run）
    // 输出：Bool —— true = 漂移量超过任意一个分量的阈值
    // 注：两分量独立判定，取 OR（任一超阈即为漂移）
    static func hasDrifted(from anchor: FrameGeometry, to current: FrameGeometry) -> Bool {
        let translationDrift = euclideanDistance(anchor.letterboxOffset, current.letterboxOffset)
        let rotationDrift    = abs(current.videoRotationAngle - anchor.videoRotationAngle)
        return translationDrift > translationThresholdPt
            || rotationDrift    > rotationThresholdDeg
    }

    // 辅助：欧氏距离
    private static func euclideanDistance(_ a: CGPoint, _ b: CGPoint) -> CGFloat {
        let dx = b.x - a.x
        let dy = b.y - a.y
        return sqrt(dx * dx + dy * dy)
    }
}
```

#### 16.1.2 阈值初始值与选择依据

| 分量 | 初始值 | 依据 | 待调参方向 |
|------|--------|------|-----------|
| 平移 `translationThresholdPt` | **10.0 pt** | 手持抖动约 1–3 pt/帧；刻意慢速平移约 15–30 pt/s（30fps 下约 0.5–1 pt/帧）；10 pt 在当前帧与上次 anchor 帧之间提供约 10× 的抖动余量，同时能被 1 s 内的有意平移触发 | 若误触发频繁（手持抖动触发）→ 上调；若 mask 跟踪明显滞后 → 下调 |
| 旋转 `rotationThresholdDeg` | **3.0°** | 设备方向噪声约 0.1–0.5°；有意微调约 5–15°/s；3° 比噪声高 6× 以上，且 10× 以下于实测 Debugger 测试（缓慢平移）的典型旋转分量 | 同上逻辑类推 |

**重要：两条均标注为「初始值，待真机调参」。** Builder 不得把数值烧进业务逻辑；它们必须是具名常量（见 §16.1.1），Debugger 在 Day 2–3 验收期间可直接改数值重测，无需改其它代码。

#### 16.1.3 OR 条件的架构含义

两分量取 OR 而非 AND，理由：
- 旋转和平移各自都足以让当前 mask 与物体明显错位（分量独立失效）
- AND 会让实现在「大幅旋转 + 小平移」场景下不触发，产生遗漏
- 若将来真机调参发现某分量过于敏感，只需单独调高该分量的阈值，不影响另一分量

---

### 16.2 ReAnchorLoop 设计契约

#### 16.2.1 触发条件（全部为 AND 关系）

Re-anchor 批次仅在以下**全部**条件满足时才启动：

| # | 条件 | 检查位置 | 说明 |
|---|------|---------|------|
| 1 | `currentMode == .tapToSegment` | 视频帧回调入口 | 其他模式不得触发 re-anchor |
| 2 | `tapInstances.count > 0` | 读取实例池 | 空池无需 re-anchor |
| 3 | `embeddingCache != nil` | 检查缓存指针 | nil 时 re-anchor 无意义（见 §16.4） |
| 4 | `isReAnchoring == false` | stateLock 保护读取 | D-15.2 节流：已有在途批次时丢弃（见 §16.2.3） |
| 5 | `DriftDetector.hasDrifted(from: lastAnchorGeometry, to: currentFrameGeometry)` | 调用漂移检测 | 无漂移时不触发 |

#### 16.2.2 调用位置（在视频帧回调链中的位置）

```
视频帧回调（sessionQueue / videoQueue）：
  Step 1: preprocess(pixelBuffer) → FrameGeometry
  Step 2: YOLO detection（不变）
  Step 3: [现有逻辑] refreshTapEmbeddingIfNeeded(...)
  Step 4: [NEW] ReAnchorLoop.checkAndFire(currentGeometry, embeddingCache, tapInstances)
                ↑ 仅在条件 1–5 全满足时才有实际操作
```

- `checkAndFire` 必须在 `refreshTapEmbeddingIfNeeded` **之后**调用，以确保读到最新的 `embeddingCache`
- `checkAndFire` 本身**不阻塞**调用线程：全部 decode 通过 `decoderQueue.async` 派发后立即返回

#### 16.2.3 节流策略（D-15.2 合规）

**规则：同时最多一个在途批次；新的漂移事件若在批次未完成时到达，直接丢弃（不排队、不取消已有批次）。**

算术依据（§15.7.3）：

| decode 取值 | N=3 串行批次成本 | 与 100ms 固定节流窗的关系 |
|------------|----------------|------------------------|
| 63.7 ms（快路径实测）| 191.1 ms | ❌ 超出 91.1 ms |
| 36.4 ms（慢路径实测）| 109.2 ms | ❌ 超出 9.2 ms |

结论：无论 decode 落在哪个工况，固定 100ms 间隔对 N=3 都是欠额的。正确设计是让批次天然自定步调。

**实现机制（伪代码）：**

```
// ReAnchorLoop 状态（由 stateLock 保护）
var isReAnchoring: Bool = false
var reAnchorBatchPendingCount: Int = 0

// checkAndFire 的节流检查片段：
stateLock {
    guard !isReAnchoring else {
        // 丢弃：上一批尚未完成
        return
    }
    isReAnchoring = true
    reAnchorBatchPendingCount = tapInstances.count
    // snapshot lastAnchorGeometry = currentFrameGeometry（批次开始即更新基准）
    lastAnchorGeometry = currentFrameGeometry
}
// 然后派发各实例的 decode（见 §16.2.4）
```

当全部实例 decode 完成（无论成功或失败）：

```
stateLock {
    reAnchorBatchPendingCount -= 1
    if reAnchorBatchPendingCount == 0 {
        isReAnchoring = false   // 批次完成，下一次漂移事件可启动新批次
    }
}
```

⚠️ C4（几何签名变化 → 清空实例池）发生时：`tapInstances` 被清空，`isReAnchoring` 必须同步复位为 `false`，否则下一次进入 `.tapToSegment` 后节流永久阻塞。**在 `clearAll()` 的实现中，必须同时将 `isReAnchoring = false` 和 `reAnchorBatchPendingCount = 0`。**

#### 16.2.4 每实例 decode 调度

对每个 `TapInstance`（在上面 stateLock 块之外，批次开始后）：

```
// 批次开始时，在 stateLock 外快照以下内容：
let capturedEmbedding   = embeddingCache        // 强引用，闭包持有
let capturedGen         = instance.decodeRequestId  // 生成计数器快照
let capturedInstanceId  = instance.id
let capturedPoint       = instance.canonicalPoint
let dispatchTime        = PerfLogger.nowMs()    // T3：派发时刻

decoderQueue.async { [weak self, capturedEmbedding, capturedGen, ...] in
    guard let self = self else { return }

    // T4：decode 开始
    let t4 = PerfLogger.nowMs()
    let qwait = t4 - dispatchTime               // qwait = T3 → T4

    // 成功/失败 decode（不得调用 SAMEncoder；见 §16.7）
    let result = SAMDecoder.shared.decode(
        embedding: capturedEmbedding,
        pointPrompt: buildPointPrompt(canonicalPoint: capturedPoint)
    )

    // T5：decode 结束
    let t5 = PerfLogger.nowMs()
    let decodeMs = t5 - t4

    // 日志（必须输出；格式见 §16.2.5）
    let driftPt  = translationDrift  // 从批次开始时记录的漂移量传入
    let driftDeg = rotationDrift
    os_log("[REANCHOR][inst#%d] drifted %.1fpt/%.1fdeg → qwait: %.1fms decode: %.1fms",
           instanceIndex, driftPt, driftDeg, qwait, decodeMs)

    // 主线程更新（§16.6 成功/失败分支）
    DispatchQueue.main.async {
        // 生成计数器检查（§16.5 优先级规则）
        guard let inst = self.findInstance(id: capturedInstanceId),
              inst.decodeRequestId == capturedGen else {
            // 被新 tap 取代或实例已删除 → 静默丢弃
            return
        }
        if let (mask, _) = result {
            inst.maskImage = mask    // 成功：更新 mask
        } else {
            // 失败：保留旧 mask（§16.6.1）
            os_log("[REANCHOR][inst#%d] decode failed — keeping stale mask", instanceIndex)
        }
    }

    // 批次计数器递减
    self.stateLock {
        self.reAnchorBatchPendingCount -= 1
        if self.reAnchorBatchPendingCount == 0 {
            self.isReAnchoring = false
        }
    }
}
```

#### 16.2.5 日志格式（INVIOLABLE，Debugger 直接从该格式提取数据）

```
[REANCHOR][inst#N] drifted %.1fpt/%.1fdeg → qwait: %.1fms decode: %.1fms
```

字段定义：

| 字段 | 含义 | 对应 D-7' 段 | 用途 |
|------|------|------------|------|
| `inst#N` | 实例槽位索引（0-based）| — | 区分同批次不同实例 |
| `drifted Xpt` | 触发本批次的平移漂移量（pt）| — | 调参参考 |
| `Ydeg` | 触发本批次的旋转漂移量（°）| — | 调参参考 |
| `qwait` | 从 `.async` 调用到 decode 实际开始的等待时间 | I3（T3→T4）| R4 验证（§15.6.2）|
| `decode` | SAMDecoder.decode 执行时间 | I4（T4→T5）| ISSUE-P4-DECODE 被动采集（§16.8）|

**R4 验收判据（§15.6.2）：** qwait 在 `[REANCHOR]` 日志中的 max 值必须 < 50 ms。若实测 max ≥ 50 ms，证实 R4「decode 堆积」，须立即上报并暂停 re-anchor 合入。

#### 16.2.6 post 段预算（§15.7.4 要求）

`post` 段（T5→T6，后处理 + 主线程 maskImage 赋值）在快路径实测 mean=16.69ms / p95=27.50ms，占总延迟 20.6%。**主线程与非主线程的分解比例目前未测量。**

re-anchor 路径与 tap 路径走**相同的 post 步骤**（`DispatchQueue.main.async` 中的 CIImage 合成 + maskImage 赋值 + 触发渲染），因此 post 成本假设相近。

N=3 一次 re-anchor 批次的 post 影响估算：

- 3 个独立的 `DispatchQueue.main.async` 调用（串行执行于主线程）
- 最坏情况：3 × 27.5 ms p95 ≈ **82.5 ms** 主线程时间（连续触发时）
- 实际情况：每个实例 decode 在串行 `decoderQueue` 上先后完成，main.async 的触发时间间隔 ≥ decode 间隔（≥30ms），因此 3 个 post 不会同时压主线程

**监控要求：** 若 Day 2–3 出现主线程掉帧或卡顿，re-anchor 的 post 分量是**第一优先排查点**。Builder 应在 `[REANCHOR]` 日志之外额外检查主线程的帧率日志，以便 Debugger 定位。

---

### 16.3 FrameGeometry drift computation

#### 16.3.1 参与漂移计算的字段

| 字段 | 类型 | 漂移分量 | 计算方式 |
|------|------|---------|---------|
| `letterboxOffset` | `CGPoint`（pt）| 平移漂移 | Euclidean distance：`sqrt((Δx)² + (Δy)²)` |
| `videoRotationAngle` | `Double`（度）| 旋转漂移 | `abs(current - anchor)` |
| `scale` | `CGFloat` | **不参与 DriftDetector** | 见 §16.3.3 |

#### 16.3.2 坐标系说明

`letterboxOffset` 在 **view 坐标系（pt，UIKit 布局空间）** 中定义。它表示当相机帧被 letterbox 处理后，图像内容在 preview layer 中的居中偏移量。

**与 canonicalPoint 的坐标系关系（显式澄清）：**

- `canonicalPoint` 在 **canonical 像素坐标系**（origW × origH 像素）中定义，是 tap 时刻通过 `FrameGeometry.invertViewPoint` 从 UIKit 坐标系反变换得到的结果（§2 坐标变换契约）。
- `letterboxOffset` 在 **view 坐标系（pt）** 中，与 canonical pixel 坐标系通过 `scale` 因子相关联，但**不是同一个空间**。

因此，`DriftDetector.hasDrifted` 中计算的平移量（pt）是 view 层面的几何偏移代理量，**不是 canonical 像素层面的物体位移量**。这是设计上的有意选择：view 层的几何变化（letterboxOffset delta > 10 pt）是一个可靠的代理，表明相机的物理姿态相对于上次 anchor 帧发生了有意义的变化。

#### 16.3.3 scale 字段的处置决定

`FrameGeometry.scale` 的变化意味着编码器输入的缩放比例改变，会导致 `canonicalPoint` 与 SAM 输入空间的映射关系变化。

**决定：`scale` 不作为 DriftDetector 的第三分量，原因如下：**

1. **Encoder 输入分辨率已冻结为 1024×1024 FINAL（§14.4）**，在当前架构下 `scale` 在正常运行中不会变化。
2. `scale` 的实质性变化（例如 display 层尺寸改变）会同时触发 **C4（几何签名变化 → 清空实例池）**，实例池清空后不再有任何 TapInstance 需要 re-anchor，DriftDetector 根本不会被执行到。
3. 因此 scale delta 已被 C4 完整覆盖，DriftDetector 中添加 scale 分量是冗余的且不必要。

**Phase 4 约定：** 若 Phase 4B 或之后引入用户可调的 zoom，scale 变化可能不再触发 C4（取决于实现），届时须重新评估是否将 scale 加入 DriftDetector。

---

### 16.4 Embedding cache 有效性契约

#### 16.4.1 re-anchor 启动条件（与 §16.2.1 条件 3 呼应）

re-anchor 批次启动前必须验证：

```
guard embeddingCache != nil else {
    // 静默跳过，记录一行日志（§16.6.3）
    return
}
```

**不需要**检查 "是否来自当前 AVCaptureSession run"，原因：
- `embeddingCache` 在 `AVCaptureSession` 停止时由现有代码清空（设为 nil）
- session 重启后 background refresh 会产生新 embedding
- `nil` 检查已完整覆盖「embedding 不属于当前 session」的情况

#### 16.4.2 anchor frame 快照时机

**批次开始即更新 `lastAnchorGeometry`**（在 `isReAnchoring` 置 true 的同一个 stateLock 块内）：

```
stateLock {
    isReAnchoring = true
    lastAnchorGeometry = currentFrameGeometry   // ← 批次开始即快照
    ...
}
```

**选择「批次开始时」而非「批次完成时」的理由：**
- 批次开始时，已知当前几何是触发漂移的那帧；下次比较应以此为基准，而非旧的 anchor 帧
- 若等批次完成再更新，中间帧的累积漂移会再次触发额外批次（无必要）
- 若批次过程中发生 C4（实例池清空），`lastAnchorGeometry` 的值不影响任何存活实例（池为空）

#### 16.4.3 embedding TTL 到期 mid-batch 的处理

| 场景 | 处理 |
|------|------|
| embedding TTL 到期（`embeddingCache = nil`），**批次已在途** | 各实例的 decode 闭包已通过 `capturedEmbedding` 强持有 embedding 引用，可正常完成本次批次；不受 TTL 影响 |
| embedding 被 background refresh 替换为新值，**批次已在途** | 各实例闭包持有旧 embedding 的强引用；本次批次用旧 embedding 完成；下一次漂移事件触发时用新 embedding |
| embedding TTL 到期，**尚未派发批次** | `embeddingCache == nil`，`checkAndFire` 条件 3 不满足，静默跳过（§16.6.3）|
| background refresh 生成新 embedding，**DriftDetector 未检测到漂移** | 不触发 re-anchor（re-anchor 依赖漂移事件，不依赖 embedding 刷新事件）|

**最后一行的架构含义：** 若相机完全静止（letterboxOffset 和 videoRotationAngle 均无变化），`DriftDetector.hasDrifted` 将始终返回 false，re-anchor 不会触发，即使 background refresh 产生了新 embedding。这是预期行为：相机静止时旧 mask 仍然正确，不需要 re-anchor。

---

### 16.5 Re-anchor vs tap 交互优先级

#### 16.5.1 Per-instance 生成计数器机制

每个 `TapInstance` 持有一个 `decodeRequestId: UInt64`，语义如下：

| 操作 | 对 decodeRequestId 的影响 |
|------|--------------------------|
| 用户 tap → 派发新 decode | `instance.decodeRequestId += 1`，然后 dispatch（tap decode 增计数）|
| re-anchor → 派发 decode | **不修改** `instance.decodeRequestId`（re-anchor 仅读取，不增加）|
| re-anchor 闭包：dispatch 时快照 capturedGen | `capturedGen = instance.decodeRequestId` |
| re-anchor 闭包完成：写 maskImage 前检查 | `guard inst.decodeRequestId == capturedGen else { return }` |

**结果：**
- 新 tap 到来 → `decodeRequestId += 1` → 所有在途 re-anchor 闭包写 maskImage 时检查不通过 → **tap 结果永远覆盖 re-anchor 结果**
- 两个 re-anchor 批次相互之间不冲突（都读同一个 decodeRequestId，不增加）
- 实例被 FIFO 淘汰后，`findInstance(id:)` 返回 nil → 闭包静默退出

#### 16.5.2 tap 与 re-anchor 在 decoderQueue 上的交错

`decoderQueue` 是 serial queue（现有，Phase 3 已建立）。tap decode 与 re-anchor decode 都通过 `.async` 进入同一 queue，按**入队顺序**执行：

- 若 re-anchor 批次中有 N 个实例正在排队，用户此时 tap：tap 的 decode 排在 re-anchor 之后
- 这意味着 tap 结果到达时，对应实例的 `decodeRequestId` 已经增加（tap dispatch 时即增加），re-anchor 的旧结果被计数器检查拦截
- **tap 结果在语义上总是最新的**，即使 re-anchor 结果先写入（re-anchor 结果在 tap 之前 dispatch 但之后完成 → tap 的 decodeRequestId 更高 → re-anchor 结果被拦截）

**结论：无需在 decoderQueue 层面取消 re-anchor 任务；生成计数器机制在写入层面保证 tap 优先，实现成本最低。**

#### 16.5.3 discardAllTapWork 调用（C4 场景）

C4（几何签名变化，如旋转）触发时，`clearAll()` 被调用。**以下操作必须在 `stateLock` 内以原子方式完成：**

```
func clearAll() {
    stateLock {
        tapInstances.removeAll()      // 清空实例池
        isReAnchoring = false         // 节流标志复位（§16.2.3）
        reAnchorBatchPendingCount = 0 // 批次计数器复位
        // lastAnchorGeometry 可留为旧值（池为空时不再触发 re-anchor）
    }
    // 在 stateLock 外更新主线程 UI（现有逻辑）
}
```

**在途的 re-anchor 闭包的处理：**
- 闭包在 decoderQueue 上继续执行（无法强制取消），但：
  - `findInstance(id:)` 在 main.async 中将返回 nil（实例池已清空）
  - `guard let inst = ...` 失败 → 静默退出
- **不会写入任何已删除实例的 maskImage**（因为实例对象本身已从池中移除）

---

### 16.6 失败与降级策略

#### 16.6.1 decode 失败（SAMDecoder 返回 nil 或抛出）

```
// 在 re-anchor 闭包的 DispatchQueue.main.async 中：
if let (mask, _) = result {
    inst.maskImage = mask    // 成功：更新
} else {
    // 失败：保留旧 mask，不清除，静默降级
    os_log("[REANCHOR][inst#%d] decode failed — keeping stale mask", instanceIndex)
}
// 在两种情况下都必须递减批次计数器（见 §16.2.4）
```

**「保留旧 mask」而非「清除 mask」的依据：**
- 旧 mask 可能已经略有偏差，但它是用户显式触发的结果
- 清除 mask 会触发视觉闪烁（mask 消失 → 重现），体验比保留旧帧更差
- 失败通常是短暂的（embedding 切换期间、低内存时），下一次成功 re-anchor 会自动替换
- 与 §3.2.1 C1–C6 穷尽列表一致：decode 失败不是 mask 清除的合法理由

#### 16.6.2 重度漂移（超过 2× 阈值）的 mask 清除决定

| 选项 | 决定 |
|------|------|
| 超过 2× 阈值时清除 mask（"heavy drift mask-clear"）| **不实现** |
| 保留旧 mask 同时触发 re-anchor | **✅ 当前策略** |

**不实现 heavy drift mask-clear 的理由：**
1. **正确性保证来自 C4，不来自墙钟或距离阈值。** 若漂移大到坐标系已实质性失效（如旋转 90°），C4 会清空整个池；DriftDetector 的 threshold 是漂移的「触发线」，不是「放弃线」。
2. **重度漂移下 re-anchor 可能仍然有效**（物体仍在 canonicalPoint 附近的语义区域内），强制清除是过早放弃。
3. 清除 mask 会让「快速甩动相机」的 Debugger 验收（见 §16.9）失败——验收要求「mask 不崩溃（降级为旧帧而非消失）」。
4. Phase 4A 范围内已接受「物体漂走时 mask 不跟随」的已知限制（R6 定级：Phase 3 不做自动失效，Phase 4A re-anchor 是有限修复）。

**若未来需要 heavy drift mask-clear，必须同时满足 §11.9 R8 三条件（客观失效度量 + 先建立刷新循环 + 重建基线），不得在无决策的情况下加入。**

#### 16.6.3 embeddingCache = nil 时的批次中止

```
// 在 checkAndFire 的条件检查中：
guard embeddingCache != nil else {
    os_log("[REANCHOR] skipped — no embedding")
    // 不更新 isReAnchoring（维持 false，下次漂移可重试）
    // 不更新 lastAnchorGeometry（漂移未被处理，下次仍应触发）
    return
}
```

注意：`lastAnchorGeometry` 在此场景下**不更新**，确保下一次帧回调再次检测到漂移时可以重试（一旦 embedding 恢复，立即 re-anchor）。

---

### 16.7 re-anchor 明确禁止事项（INVIOLABLE）

| 禁止 | 说明 |
|------|------|
| **禁止调用 SAMEncoder** | re-anchor 仅 decode；encoder 仍由 background refresh（现有 `refreshTapEmbeddingIfNeeded`）管理，不受 re-anchor 控制 |
| **禁止修改 `canonicalPoint`** | canonicalPoint 在 tap 时刻固定，re-anchor 在同一点重新 decode；修改 canonicalPoint 会破坏 tap 语义 |
| **禁止调用 `refreshTapEmbeddingIfNeeded` 或任何 encoder 路径** | 即使 embeddingCache 过期，也不在 re-anchor 内启动 encode；让 background refresh 自行处理 |
| **禁止新增 DispatchQueue 或锁对象** | 使用现有 `decoderQueue`（serial）和 `stateLock`；新 queue 会引入调度开销并破坏串行保证 |
| **禁止修改 SAMDecoder.swift** | Phase 3 冻结文件，不得触碰（§14.4 FINAL，§14.5.5 约束 1）|
| **禁止修改 MaskRenderer.swift** | 同上 |
| **禁止修改 R3 禁令参数** | `minComponentPx=30` / `minComponentSidePx=3` / `minComponentFill=0.05` / `maxPlausibleLogit=500.0` / `stabilityDelta=1.0` / `cap60` / `cap85` / 候选选择规则（§14.4 INVIOLABLE）|
| **禁止新增第二份 SAMEncoder 实例** | Single encoder instance 约束（§14.5.5 约束 4）|

---

### 16.8 ISSUE-P4-DECODE 被动数据采集规范

**目标：** `[REANCHOR]` 日志中的 decode 时间（§16.2.5）可直接被 Debugger 提取，与 `[D7']` 快路径 decode（63.70ms）对比，无需额外测量 session。

#### 16.8.1 三条假说与判别逻辑

| 假说 | 预测 | 判别方式 |
|------|------|---------|
| **(a) background-refresh 与 re-anchor decode 的 ANE/GPU 争用** | re-anchor decode（background refresh 同时运行）≈ 快路径 decode（63.7ms）；无争用时慢路径 decode ≈ 36.4ms | 比较 `[REANCHOR]` decode 均值与 63.7ms vs 36.4ms |
| **(b) embedding 内存局部性** | re-anchor decode 接近慢路径（36.4ms），因为 embedding 数据在 re-anchor 时刚被刷新（局部性好）| 同上比较 |
| **(c) GPU/ANE DVFS 时钟爬坡（Architect 补充，§15.7.2）** | re-anchor decode 若在 background refresh encode 之后立即执行，decode 接近 36.4ms（GPU 已被 encode 预热）；若 encode 空闲期间执行，接近 63.7ms | 观察 `[REANCHOR]` decode 与 background refresh 时序的相关性 |

#### 16.8.2 判别结论（Debugger 在 Day 2–3 后执行）

| `[REANCHOR]` decode 均值 | 结论方向 |
|------------------------|---------|
| ≈ 36.4 ms | **支持假说 (a)**：background refresh 争用是快路径倒挂的主因；re-anchor 无争用时恢复正常 |
| ≈ 63.7 ms | **排除假说 (a)**；指向 (b) 或 (c)；需进一步判别实验 |
| 呈双峰分布（部分 ≈36ms，部分 ≈63ms）| **支持假说 (c)**：DVFS 爬坡；双峰与 background refresh 时序相关 |

**数据采集要求（无需额外 session）：** Debugger 从现有 `[REANCHOR]` 日志中提取 decode 字段，计算 mean + sd + 分位数，与 §33.2.1 快路径 decode（63.70ms, sd=9.3, n=50）和慢路径 decode（36.4ms, sd=7.3, n=17）对照。**n ≥ 20 `[REANCHOR]` 行即可支撑初步判断；**如果结果模糊（中间值），记为「未能区分 (a)/(b)/(c)」，ISSUE-P4-DECODE 的正式判别实验按 §15.7.3 排期安排。

---

### 16.9 Debugger 验收准则

#### 16.9.1 tasks.md Day 2–3 Debugger 三条验收（原文不变，此处给出判定依据）

| # | 原文条目 | 判定依据与边界 |
|---|---------|--------------|
| **D-1** | 缓慢平移 3 秒，mask 随目标物体移动，偏差 ≤ 15 px | 允许量化误差：测量 mask 中心与物体真实中心的像素距离（取 mask centroid 与目视目标中心对比）；15 px 在 1920×1080 帧上约占 0.8%，为合理的实用门控 |
| **D-2** | 快速甩动相机时 mask 不崩溃（降级为旧帧而非消失）| 判定：甩动后 mask 仍然可见（即使位置偏离目标）；mask 完全消失视为 FAIL；旧位置 mask 保留视为 PASS（§16.6.1）|
| **D-3** | re-anchor 运行 30 秒无内存累积（decode 复用现有路径，无新分配）| 使用 Xcode Memory Report 或 Instruments；re-anchor decode 走现有 SAMDecoder 路径，不应新增任何堆分配；30s 内内存应与 Phase 3 `~590 MB ±15 MB` 基线一致 |

#### 16.9.2 额外验收条件（Architect 增补，§16.2.5 / §15.6.2 依据）

| # | 条件 | 判据 |
|---|------|------|
| **D-4** | `[REANCHOR]` 日志中 `qwait` max < 50 ms | 提取所有 `[REANCHOR]` 行的 qwait 字段；若 max ≥ 50 ms → **R4 证实，须上报**（§15.6.2 / §16.2.5）|
| **D-5** | 无 V-3 触发（自然使用中不出现 ≥5 次慢路径）| re-anchor 循环不得意外改变 encoder 触发节奏，使用户在未 force 的情况下频繁命中慢路径（§15.3.4 V-3 撤销触发条件）|

#### 16.9.3 日志提取协议（Debugger 直接使用，无需写脚本）

```bash
# 从设备日志提取 [REANCHOR] 行
grep '\[REANCHOR\]' device_log.txt

# 提取 qwait 值
grep '\[REANCHOR\]\[inst' device_log.txt | grep -o 'qwait: [0-9.]*' | awk -F': ' '{print $2}'

# 提取 decode 值
grep '\[REANCHOR\]\[inst' device_log.txt | grep -o 'decode: [0-9.]*' | awk -F': ' '{print $2}'
```

---

### 16.10 Phase 2 保护 + 冻结文件状态（本节）

**Phase 2 pipeline 保护状态：** re-anchor 仅作用于 `.tapToSegment` 模式。`.segmentation` 路径（TemporalManager / box decoder / renderMask）**不受影响**。`checkAndFire` 的第一条件（`currentMode == .tapToSegment`）是 Phase 2 的保护门。✅

**R3 禁令状态：** 本节所有伪代码均不触碰 `buildAlpha` 的任何决策代码（阈值 / 候选选择 / cap60 / cap85 / flood fill / stability / 数值哨兵）。`§14.4 R3 禁令（INVIOLABLE）`继续有效。✅

**冻结文件状态：** `SAMDecoder.swift` / `MaskRenderer.swift` 不涉及、不授权修改。✅

---

### 16.11 Deliverable 确认

- [x] §16.1 DriftDetector 接口契约：协议定义（伪代码）+ 阈值初始值（10 pt / 3°）+ 选择依据 + OR 条件架构含义
- [x] §16.2 ReAnchorLoop 设计契约：5 条触发条件（AND）+ 调用位置 + D-15.2 节流策略（in-flight flag + 丢弃而非排队）+ per-instance decode 调度伪代码 + 必须输出的日志格式（qwait + decode 字段）+ post 段预算分析
- [x] §16.3 FrameGeometry drift computation：参与字段（letterboxOffset / videoRotationAngle）+ 坐标系澄清（view pt vs canonical px）+ scale 不参与 DriftDetector 的决定（C4 已覆盖）
- [x] §16.4 Embedding cache 有效性契约：启动条件（nil check 已足够）+ anchor frame 快照时机（批次开始时）+ mid-batch TTL 到期处理
- [x] §16.5 Re-anchor vs tap 交互：per-instance `decodeRequestId` 机制（tap 增计数 / re-anchor 仅读取）+ decoderQueue 交错语义（tap 语义优先）+ discardAllTapWork / C4 的原子性要求
- [x] §16.6 失败与降级：decode 失败保留旧 mask + heavy drift mask-clear 决定（不实现）+ embeddingCache = nil 静默跳过
- [x] §16.7 re-anchor 禁止事项（INVIOLABLE，共 8 条）
- [x] §16.8 ISSUE-P4-DECODE 被动数据采集：三条假说（补充 (c) DVFS 时钟爬坡）+ 判别逻辑 + `[REANCHOR]` 日志可直接对比，无需额外 session
- [x] §16.9 Debugger 验收准则：原文三条（D-1/D-2/D-3）+ 额外两条（D-4 qwait < 50ms / D-5 无 V-3）+ 日志提取协议

---

END OF PHASE 4 DAY 2–3 ARCHITECT OUTPUT（§16 Re-anchor 架构契约）

---

# Phase 4 — Day 2–3 (Architect) §17 Re-anchor 漂移信号修订

日期：2026-08-17
触发：Builder 在实现 §16 后上交阻塞性缺陷（tasks.md Day 2–3 Builder 第 1 条 ⚠️ 条目），经 Architect 独立读源码核实成立。
性质：**修订而非改写**。§16 全文保留，仅对被证伪的条款加 SUPERSEDED 标记（§15 确立的纪律：裁决与封层表不得原地改写）。

---

## §17 Re-anchor 漂移信号修订（取代 §16.3.1）

### 17.1 缺陷陈述

#### 17.1.1 ⛔ SUPERSEDED 标记

> **⛔ §16.3.1「参与漂移计算的字段」整表 SUPERSEDED by §17.3。**
> **⛔ §16.1.1 中 `hasDrifted` 的实现体（`letterboxOffset` 欧氏距离 / `videoRotationAngle` 绝对差）SUPERSEDED by §17.5。**
> **⛔ §16.1.2 阈值表（10.0 pt / 3.0°）SUPERSEDED by §17.4**（单位改变，数值不可迁移）。
> **⛔ §16.3.2 关于「letterboxOffset delta 是相机物理姿态变化的可靠代理量」的论断 —— 撤回，该论断为假。**
>
> §16.2（节流状态机）、§16.4（embedding 有效性）、§16.5（tap 优先级 / 生成计数器）、§16.6（失败降级）、§16.7（禁止事项）、§16.8（ISSUE-P4-DECODE 被动采集）**全部继续有效，一字未改**。§16.3.3（scale 不参与）继续有效。§16.9（验收准则）见 §17.8。

#### 17.1.2 已核实的证据

`letterboxToSquare`，`JudgeE2/Detection/CameraManager.swift:3280`：

```
let image  = CIImage(cvPixelBuffer: pixelBuffer)
let width  = image.extent.width, height = image.extent.height
let scale  = min(CGFloat(size)/width, CGFloat(size)/height)
let padX   = (CGFloat(size) - width  * scale) / 2.0
let padY   = (CGFloat(size) - height * scale) / 2.0
```

`width` / `height` 取自 `image.extent`，即**相机 buffer 的尺寸**。会话运行期内该尺寸恒定，`size` 是常量 640。因此：

| 量 | 自变量 | 相机平移时 |
|----|--------|-----------|
| `scale` | (bufW, bufH, 640) | 不变 |
| `padX` / `padY` = `letterboxOffset` | 同上 | **逐位不变** ⇒ `translationDrift ≡ 0` |
| `videoRotationAngle` | `AVCaptureDeviceRotationCoordinator` | 量化到 {0,90,180,270}；一旦改变先触发 C4（`applyCaptureRotation → discardAllTapWork`）清空实例池，re-anchor 条件 2 立即不满足 |

⇒ **按 §16.3.1 字面实现的 `hasDrifted`，在自然使用中恒返回 false。** Builder 的上交与我的复核结论一致。

#### 17.1.3 根因（我的错误，记录在案）

我在写 §16.3.1 时**没有读 `letterboxToSquare` 的实现**，而是从字段名推断语义：「letterbox 偏移」听起来像是画面内容在视图里的位置偏移，于是被当成了姿态代理量。实际它是「固定尺寸输入的居中填充量」，是一个**标定量**，不是**观测量**。

这与 A-3（不得把正确性押在你控制不了的量上）/ A-5（不得用没校准过的代理量）同族，但它是一个新的失效形态，值得单列：

> **长期约定 A-7（信号存在性核验）**：把某个量指定为**信号**（用于触发行为）之前，必须读到它的**赋值点**，并回答「它的自变量是什么？我关心的物理量在不在这些自变量里？」
> 自检问法：**「这个量是被测出来的，还是被算出来的？」** 若答「算出来的」，则它只能携带其输入已有的信息 —— 输入里没有相机姿态，输出里就不会有。
> 与 A-3 / A-5 的分工：A-3 管「量的行为不受我控制」，A-5 管「量与目标的相关性未校准」，**A-7 管「量与目标根本不相关，且这一点从赋值点一眼可见」**。A-7 的检验成本最低，因此必须最先做。

#### 17.1.4 第二个、更深的缺陷（Builder 未报，我在复核中发现）

§16 还有一个比信号选择更根本的问题，必须在同一次修订里说清楚，否则换了信号也到不了 D-1：

**§16.7 冻结 `canonicalPoint`（re-anchor 不得移动锚点），而 D-1 要求「mask 随目标物体移动」。这两条互相矛盾。**

推理：`canonicalPoint` 定义在 canonical 像素空间 = 相机 buffer 像素空间（显示方向）。相机平移时，物体在 buffer 内的像素位置**改变**，而 `canonicalPoint` 按定义**不变**。于是 re-anchor 在旧点上用新 embedding 重新 decode，得到的是「当前帧中该屏幕位置上的东西」的 mask —— 相机平移足够多时，那是背景/桌面，不是用户点的物体。

⇒ **无论选什么漂移信号，Phase 4A 的 re-anchor 都不可能让 mask 跟随平移中的物体。** 跟随需要锚点更新（tracking），而锚点更新被 §16.7 明令禁止，且本次修订的约束条件里再次禁止。

⇒ 因此本次修订**同时是一次范围重划**，见 §17.2.0。

---

### 17.2 范围重划与候选信号评估

#### 17.2.0 范围重划（先于选信号）

**Phase 4A 的 re-anchor 在 `canonicalPoint` 冻结的前提下，能且只能交付一件事：把 mask 的陈旧度变成有界的。它不能交付目标跟随。**

回到立项动机（§12 保留项 R6 / §11 约定 A-1）：re-anchor 被写进 Phase 4 入口，理由是「TTL 撤销 × 多实例」让 mask 陈旧度**无上界**，而 A-1 的自检问法「它过期之后谁把它变回来？」在 Phase 3 没有答案。re-anchor 就是那个答案 —— **它是刷新循环，不是跟踪器**。tasks.md 里「mask 随目标物体移动，偏差 ≤ 15 px」这一条是**在立项动机之外新增的、从未经过架构裁决的目标**，而且与同一份契约里的 §16.7 直接冲突。

据此重划：

| 能力 | 归属 | 依据 |
|------|------|------|
| **A. 有界刷新**：当锚点处的画面内容变了，用最新 embedding 在同一锚点重新 decode，使 mask 是对**当前帧**的断言而非对若干秒前的断言 | ✅ Phase 4A（本次交付） | 不动 `canonicalPoint`，不调 encoder，§16.7 全数满足 |
| **B. 语义保持**：当锚点处已经换了东西时，**拒绝**更新、保留旧 mask，避免把用户选中的杯子悄悄换成桌面 | ✅ Phase 4A（本次新增，见 §17.3.3） | A 若无 B 则是净退化；B 是 A 的安全条件，不是独立特性 |
| **C. 目标跟随**：锚点随物体在画面中移动，mask 跟着物体走 | ❌ **推迟，另立条目** | 需要修改 `canonicalPoint`，与 §16.7 冲突；需要 tracker（VNTrackObjectRequest / 光流 / YOLO 框关联），是独立立项 |

**C 的归属裁决：不塞进 Phase 4A，也不占用 Phase 4B（Pin & Annotation）的排期，另立为 `ISSUE-P4-TRACK`，优先级 P2，重开条件见 §17.8.4。** 理由：C 的三条候选实现（Vision 跟踪请求 / 光流 / 检测框关联）都要新增每帧计算并与 encoder/decoder 争 ANE-GPU，而 Phase 4A 的延迟账目（§33：快路径 total 80.97 ms，其中 decode 63.70）尚无余量结论；且 C 一旦引入就要重新定义「tap 语义」（用户点的是一个点还是一个物体），这是比信号选择更大的一次架构决定，不该夹带在一次缺陷修订里。

**因此，tasks.md 中以下条目的目标陈述本身有误，需要改写（我只指出，不代改、不动复选框）：**

1. Day 2–3 **Debugger** 第 1 条「进入 tapToSegment → tap 一个物体 → 缓慢平移相机 → 观察 mask 是否随物体移动」—— 描述的是能力 C。
2. Day 2–3 **Debugger** 第 2 条「连续走动 3 秒，mask 随目标物体移动，偏差 ≤ 15 px」—— 同上，且 15 px 判据在能力 A 下无意义（A 不产生位移）。
3. Day 2–3 **Debugger** 第 4 条「无内存累积」与第 3 条「快速甩动不崩溃」**不受影响，继续有效**。

替代验收准则见 §17.8。

#### 17.2.1 候选信号评估（六条判据）

判据：① 是否真随相机位姿变化 ② 每帧成本（对照 §33 预算，且 `.tapToSegment` 下 2/3 的帧走 `% 3 != 0` 提前返回，`CameraManager.swift:2691`）③ 是否需要新依赖 / 新队列 / 新锁（§16.7 禁止后两者）④ 能否产出可与阈值比较的标量 ⑤ 手抖误触发 ⑥ 不可用时能否优雅降级。

| 候选 | ① 随位姿 | ② 成本 | ③ 依赖 | ④ 标量 | ⑤ 手抖 | ⑥ 降级 | 裁决 |
|------|---------|-------|-------|-------|-------|-------|------|
| **§16.3.1 原方案**（letterboxOffset + videoRotationAngle）| ❌ 恒定 | 0 | 无 | ✅ | — | — | ⛔ **已证伪** |
| **YOLO 检测中心 / 框漂移** | ✅ | 0（已在跑）| 无新依赖 | ⚠️ 需跨帧关联 | 中 | ❌ 差 | ❌ **否决** |
| **CoreMotion 设备姿态** | ⚠️ 只测旋转 | ~0 | ➕ 新框架 | ✅ | 好（可低通）| ✅ | ❌ **否决** |
| **Vision 图像配准 / 光流** | ✅ 直接测 | ➕ 每帧 ANE/GPU | ➕ 新框架 | ✅ | 中 | ✅ | ❌ **否决（本阶段）** |
| **锚点邻域内容变化** | ✅ | ~50 µs/帧 | 无 | ✅ | 可控 | ✅ | ✅ **采纳** |

**逐条否决理由：**

**YOLO 检测中心 / 框漂移 —— 否决。**
核实结果（`CameraManager.swift:2691` 起）：`.tapToSegment` 下 YOLO 确实在跑，但只跑 **1/3 帧**（≈10 Hz），且 `detections` / `nmsDetections` 是 `runDetectionPipeline` 的**函数局部变量**，跑完即弃，全项目没有任何跨帧检测状态。要用它必须新增：跨帧检测存储 + 实例↔检测关联 + 关联丢失处理。三条硬伤：
1. **覆盖率不可控。** YOLO 是 COCO 80 类。用户点的对象只要不在 80 类里（墙面、纸张、零件、宠物玩具），就永远没有可关联的框 ⇒ 信号在**恰好最需要它的场景**里缺席，这正是 A-3 的形态。
2. **它测的是能力 C 的量。** 检测框位移是「物体在画面里移动了多少」——这是跟踪信号。在锚点冻结的 Phase 4A 里，我们**没有能力消费它**：知道物体移动了 40 px，却不许把锚点移过去，那这 40 px 唯一的用法就是触发一次注定错位的 decode。**用跟踪信号驱动一个不能跟踪的循环，是把 §17.1.4 的矛盾放大而不是解决。**
3. 10 Hz 且与 re-anchor 的每帧检查节奏不对齐，需要额外的插值/保持逻辑。

**CoreMotion 设备姿态 —— 否决（保留为未来的抑制门）。**
核实结果：全项目 `grep -r "CoreMotion\|CMMotionManager"` **零命中**，是纯新增依赖。三条理由：
1. **它看不见我们关心的一半事件。** 设备完全静止而物体自己移动/变形（人转身、手里的杯子被拿走）时，CoreMotion 输出零变化，而 mask 已经失效。刷新循环恰恰要在这种情况下工作。
2. 平移只能靠加速度二次积分，秒级即漂移不可用；只有旋转可靠 —— 而旋转一旦跨过 90° 量化边界就是 C4，池已清空。剩下的可用区间是「小角度旋转」，信息量太窄。
3. 它需要一个 `CMMotionManager` + 更新回调（`startDeviceMotionUpdates(to:)` 要一个 `OperationQueue`），这在事实上引入第三个执行上下文，与「不新增队列」的约束擦边。
**保留用途**：若 §17.4 的阈值在真机上被手抖误触发困扰，CoreMotion 的角速度可以作为**抑制门**（角速度高于阈值时判定为甩动，跳过 re-anchor），届时再行立项。它适合当否决项，不适合当触发项。

**Vision 图像配准 / 光流（`VNTranslationalImageRegistrationRequest`）—— 本阶段否决。**
核实结果：全项目 `import Vision` **零命中**（YOLO/SAM 走的是 CoreML 直调，不经 Vision）。理由：
1. **成本落在最贵的资源上。** 配准请求走 ANE/GPU，而 §33 的 ISSUE-P4-DECODE 尚未结案 —— 该 issue 的三条假说（(a) 争用 / (b) 局部性 / (c) DVFS 爬坡）**自变量全是 GPU/ANE 占用状态**。在 issue 未结案前引入一个新的每帧 ANE 负载，会同时污染 re-anchor 的 decode 测量和 §16.8 的被动采集设计 —— 那是本阶段唯一免费的数据来源。
2. 它同样测的是能力 C 的量（全局平移），消费不了（同 YOLO 第 2 条）。
3. 它是本次候选里唯一在**将来**真正有用的：`ISSUE-P4-TRACK` 立项时，它和 `VNTrackObjectRequest` 是首选实现路径。**记录为推迟，不是排除。**

**锚点邻域内容变化 —— 采纳，详见 §17.3。**
一句话理由：**它是唯一测量「mask 还成不成立」而不是「相机动了多少」的候选。** 在锚点冻结的架构下，「相机动了多少」是一个我们无权消费的量；「锚点下面的东西还是不是原来那个」才是刷新循环的正确输入。这条判断也解释了为什么前三个候选各自都比它「更准确地测量运动」却都不合用 —— **它们精确地测量了错误的东西。**

---

### 17.3 采纳的漂移信号：锚点邻域内容散度（Anchor-Region Content Divergence）

#### 17.3.1 精确定义

**信号名**：锚点邻域内容散度 `divergence`，**单位：8-bit 亮度级（luma level, 0–255 标度）**。

**它测的是什么**：以某个 TapInstance 的 `canonicalPoint` 为中心的一个固定大小窗口内，**当前帧的像素内容**与**该实例上次 re-anchor 基线帧的像素内容**之间的平均绝对差（去均值后）。

**为什么它随位姿变化（这是 §16.3.1 栽跟头的地方，必须给出赋值点级别的论证）**：

它的输入是 `latestCameraBuffer`，赋值点在 `CameraManager.swift:2645`：

```
latestCameraBuffer = pixelBuffer          // 来自 CMSampleBufferGetImageBuffer(sampleBuffer)
```

即 `AVCaptureVideoDataOutput` 每帧交付的原始采集缓冲区本身。它的内容**就是相机此刻看到的东西**，是观测量而非标定量（A-7 自检：「被测出来的」）。相机平移 ⇒ 缓冲区像素内容改变 ⇒ 窗口内采样值改变 ⇒ 散度上升。相机静止而物体移动 ⇒ 同样上升。相机静止且场景静止 ⇒ 只剩传感器噪声。**这三条正是刷新循环需要区分的三种状态。**

#### 17.3.2 标量的计算方式

已核实的前提条件（全部无需新增几何链）：

| 前提 | 核实结果 |
|------|---------|
| 采集缓冲区像素格式 | `kCVPixelFormatType_32BGRA`（`CameraManager.swift:2593`）⇒ 单平面、CPU 可直接读、无 YUV 平面解析 |
| 采集缓冲区坐标系 与 canonical 空间的关系 | **同一个空间，1:1，无变换**。`LetterboxInfo.origW/origH` 由 `image.extent` 得到（`:3280`），而 `image` 就是 `latestCameraBuffer`；旋转已由 `AVCaptureConnection.videoRotationAngle` 在交付前烘焙进 buffer（FrameGeometry 头注释）。⇒ `canonicalPoint` 可以**直接当作该 buffer 的像素下标**使用，只需取整与边界钳制 |
| 该 buffer 是否被 GPU 占用 | 否。它是采集直出的 IOSurface，不是 `letterboxOutputBuffer`（后者是 `CIContext` 渲染产物，锁基址会强制等待 GPU）⇒ **必须采样 `latestCameraBuffer`，禁止采样 `latestInputBuffer`** |
| 读取所在队列 | `checkAndFireReAnchor` 在 videoQueue（`CameraManager.swift:1943`，Builder 已注明 videoQueue only），`latestCameraBuffer` 也是 videoQueue 所有 ⇒ **同队列读，无跨队列同步，无新锁** |

**计算步骤（伪代码，接口级）：**

```
signature(buffer, canonicalPoint) -> AnchorSignature? :
    W  = anchorWindowPx                       // 窗口边长，canonical px
    G  = anchorGridSide                       // 每边采样点数 (= 8)
    step = W / G
    x0 = canonicalPoint.x - W/2 ; y0 = canonicalPoint.y - W/2
    lock buffer (readOnly)
    for j in 0..<G, i in 0..<G:
        px = clamp(round(x0 + (i + 0.5) * step), 0, bufW-1)
        py = clamp(round(y0 + (j + 0.5) * step), 0, bufH-1)
        // 3×3 盒平均，抗混叠（见下）
        acc = 0
        for dy in -1...1, dx in -1...1:
            (b,g,r) = BGRA at (clamp(px+dx), clamp(py+dy))
            acc += lumaApprox(b,g,r)
        samples[j*G + i] = acc / 9
    unlock buffer
    return AnchorSignature(samples)           // G×G = 64 个 UInt8

divergence(anchorSig, currentSig) -> Double :   // 单位：luma level
    mA = mean(anchorSig.samples)
    mB = mean(currentSig.samples)
    // 去均值：自动曝光 / 整体明暗变化不应被判为内容变化
    return (1/K) * Σ_k | (anchorSig[k] - mA) - (currentSig[k] - mB) |
```

**三个设计要点各自的理由：**

1. **3×3 盒平均（抗混叠）。** 8×8 稀疏采样在纹理表面上对 1–2 px 的手抖会产生巨大的采样值跳变（空间混叠），那不是内容变化而是采样伪影。盒平均是最便宜的低通。成本：64×9 = 576 次像素读/实例/帧，N=3 时 1728 次 —— 相对于每帧已有的 640×640 letterbox（409 600 像素的 CI 渲染）可忽略。
2. **去均值。** 自动曝光在平移时会缓慢爬坡，整幅画面亮度整体抬升/压低。不去均值的话 AE 爬坡会被判成漂移，产生与真实运动无关的持续触发。去均值使信号对**加性亮度偏移**免疫，仍对**内容替换**敏感。
3. **`lumaApprox` 不要求精确的 Rec.709 系数。** 这是一个用于阈值比较的相对量，不是色度学量；使用整数近似（如 `(r + 2g + b) / 4`）即可，Builder 自选，但**必须在整个实例生命周期内保持同一实现**（比较的两侧必须同口径）。

**成本结论**：整条路径无浮点矩阵、无框架调用、无分配（见 §17.6 关于复用缓冲的要求），估算 < 50 µs/帧（N=3）。对照 §33 预算（快路径 total 80.97 ms、post mean 16.69 ms），占比 < 0.1%，且 §17.4 的时间下界会让绝大多数帧**连采样都不做**。

#### 17.3.3 配套的接受门：mask 一致性否决（能力 B）

**这是本次修订中唯一超出 §16 表面的新增机制，也是采纳该信号的必要条件。** 没有它，新信号会让产品变差（§17.2.0 能力 B 一栏）。

**规则**：re-anchor 的 decode 完成、`buildTapAlpha` 产出新 alpha 之后、写回实例之前，计算新 alpha 与该实例**原有 alpha** 的 IoU。若 `IoU < reAnchorAcceptIoU`，**否决本次更新，走 §16.6.1 已有的「保留旧 mask」分支**。

```
// 在 reAnchorDecode 的 buildTapAlpha 之后、tapInstances.updateMask 之前
iou = stridedIoU(previousAlpha, built.alpha, stride: 4)
if reAnchorConsistencyGateEnabled && iou < reAnchorAcceptIoU {
    reAnchorKeepStaleMask(slot: slot, reason: "IoU \(iou) < gate")   // 复用既有分支
    return
}
```

**为什么这不违反 R3 禁令（必须逐条说清）：**

| 质疑 | 答复 |
|------|------|
| 它是不是「mask 质量逻辑」？ | 不是。它不评价 mask 好坏，不比较候选，不读 `iou_pred`、`stability`、面积占比。它比较的是**两个已经各自通过全部 R3 门控的产物之间的几何一致性**。 |
| 它是不是改动了候选选择？ | 否。`MaskRenderer.buildTapAlpha` 一字未动，候选选择规则、cap60/cap85、flood fill、形状门全部原样执行且结论被完整接受。本门控作用在 `buildTapAlpha` **之外、之后**。 |
| 它会不会影响 tap 路径 / Phase 2？ | 不会。它只存在于 `reAnchorDecode` 中。tap 路径（`tapDecodeWithPoint`）与 `.segmentation` 路径均不经过。88% 良品率基线的产生路径逐位不变。 |
| 它是不是 A-5 禁止的「未校准代理量」？ | 不是。A-5 禁止的是「模型自评量（iou_pred / stability）共线且无外部标注时改判据」。此处的 IoU 是**两个产物之间的几何量**，不是模型自评；且**它的作用方向只能是否决**，落回一个已批准的既有行为（保留旧 mask），无法产生任何在本门控不存在时不会出现的结果 —— 这正是 §13 为 A-1 定的 **E-3「否决式改动」** 形态，其基线可比性由构造保证。 |
| 它会不会破坏 §16.8 的被动数据采集？ | 不会。**否决发生在 decode 之后**，`[REANCHOR]` 日志行（含 qwait / decode 字段）在任何门控之前就已发出（Builder 已如此实现，`CameraManager.swift:2100` 附近）。ISSUE-P4-DECODE 的数据一条不少。 |

**IoU 的计算口径**：`built.alpha` 与 `previousAlpha` 都是 origW×origH 的 0/255 字节数组（约 2.07 M 元素 @1920×1080），全量遍历约 1–2 ms。改用**步长 4 的二维子采样**（每 16 个像素取 1，约 130 k 采样点，≈0.1 ms）。IoU 是比值，均匀子采样是其无偏估计，而判据阈值是粗粒度的 0.5 ⇒ 子采样引入的误差远小于判据分辨率。计算发生在 decoderQueue，不在主线程。

---

### 17.4 修订后的阈值（取代 §16.1.2）

**单位改变，§16.1.2 的 10.0 pt / 3.0° 不可迁移，直接作废。** Builder 建立的「具名 `static var` + 真机可直接改数值重测、无需改其它代码」的调参接口形态**完整保留**，只换常量本身。

| 常量名 | 类型 | 初始值 | 单位 | 依据 | 调参方向 |
|--------|------|--------|------|------|---------|
| `contentThresholdLuma` | `Double` | **8.0** | luma level（去均值 MAD）| A13 良好光照下传感器噪声 σ ≈ 1–3 级 ⇒ 去均值 MAD 噪声底 ≈ 1–2；平移一个窗口宽度的纹理内容通常给出 20–60。8.0 ≈ 4× 噪声底，且远低于真实运动量级 | 误触发频繁（静止时反复 fire）→ 上调；内容明显变了却不刷新 → 下调 |
| `anchorWindowPx` | `CGFloat` | **96.0** | canonical px | 1080p 短边的 ~8.9%。太小 ⇒ 落在物体内部均匀色块上，对运动不敏感；太大 ⇒ 混入无关背景，被场景边缘主导 | 小目标漏检 → 下调；大面积均匀物体不触发 → 上调 |
| `anchorGridSide` | `Int` | **8** | 采样点/边 | 64 采样点，配合 3×3 盒平均，成本与抗混叠的折中 | 一般不动；改动须同时复核 `contentThresholdLuma`（采样密度变化会移动噪声底）|
| `minReAnchorIntervalMs` | `Double` | **300.0** | ms | **D-15.2 明文允许：「固定间隔只作下界」。** 这是下界不是节流器 —— 负载自适应节流仍由 §16.2.3 的单在途批次机制承担。300 ms 上限 ≈ 3.3 批次/秒，N=3 时 decode 占用 ≈ 191 ms/批次 ⇒ 峰值占空比 ≈ 64%，留出余量给 background refresh 的 encode | 手抖误触发 → 上调；刷新明显迟钝 → 下调，但不得低于一个批次的实测耗时 |
| `reAnchorAcceptIoU` | `Double` | **0.5** | 无量纲 | §17.3.3 的否决门。0.5 是「同一物体的两次分割」与「两个不同物体」之间一个宽松但明确的分界；宁可放过（更新一个略有偏差的 mask）也不要误杀（把正确刷新挡掉） | 出现「mask 悄悄跳到别的物体」→ 上调；出现「物体明明还在但从不刷新」→ 下调 |
| `reAnchorConsistencyGateEnabled` | `Bool` | **true** | — | §12 的 C-6 纪律（单变量回退）：新增机制必须能被单独关闭，以便 Debugger 做单变量对照 | 仅用于对照实验，不得以关闭状态发布 |

**关于 ⑤ 手抖误触发的三层防御（三者叠加，缺一都不够）：**
1. 3×3 盒平均消掉 1–2 px 抖动的采样混叠（信号层）
2. `contentThresholdLuma = 8.0` ≈ 4× 噪声底（阈值层）
3. `minReAnchorIntervalMs = 300` 给出触发频率硬上界（速率层）—— **这一层是真正的成本保险**：即使前两层被某种高频纹理击穿，最坏情况也只是 3.3 批次/秒，不会出现每帧 fire 的灾难形态

**关于 ⑥ 优雅降级：** `latestCameraBuffer == nil`、基址锁定失败、`canonicalPoint` 越界钳制后窗口退化 —— 任一情况下 `signature` 返回 `nil`，`checkAndFireReAnchor` **视同「无漂移」直接返回**（不 fire、不更新基线、不记为失败）。选择「静默不触发」而非「保守触发」，理由与 §16.6.3 同源：不触发的代价是 mask 多陈旧一会儿（已知且有界的 Phase 4A 限制），错误触发的代价是一次错位的 decode 加一次主线程 post。

### 17.5 修订后的 DriftDetector 接口（取代 §16.1.1）

**核心结论：新信号无法表达为两个 `FrameGeometry` 快照的纯函数。** `FrameGeometry` 携带的全部字段（origW/origH/mirrored/rotation/letterboxOffset/scale）都是标定量，按 §17.1.2 全部与位姿无关 —— 这不是巧合，而是 `FrameGeometry` 的设计本意（它是**坐标变换的参数**，不是**画面内容的描述**）。因此接口必须改变。

#### 17.5.1 新接口（伪代码，接口级，非可编译 Swift）

```
// Interaction/DriftDetector.swift

/// 一个锚点邻域在某一帧上的内容签名。值类型，不可变。
struct AnchorSignature {
    // G×G 个盒平均后的亮度采样，行主序。
    // 实现要求：定长内联存储或复用的 scratch buffer，不得每帧堆分配（见 §17.6 / D-3）
    samples: [UInt8]
}

enum DriftDetector {

    // --- 可调参常量（§17.4）---
    static var contentThresholdLuma: Double = 8.0
    static var anchorWindowPx: CGFloat      = 96.0
    static var anchorGridSide: Int          = 8
    static var minReAnchorIntervalMs: Double = 300.0
    static var forceDriftForTesting: Bool   = false      // §17.7

    /// 采样一个锚点邻域的内容签名。
    /// ⚠️ videoQueue only —— 它读 `latestCameraBuffer`（videoQueue 所有）。
    /// 失败（buffer 不可用 / 锁基址失败）返回 nil，调用方视同无漂移。
    static func signature(from buffer: CVPixelBuffer,
                          atCanonical point: CGPoint) -> AnchorSignature?

    /// 两个签名之间的去均值平均绝对差，单位 luma level。
    /// 纯函数，无副作用，可在任意队列调用。
    static func divergence(from anchor: AnchorSignature,
                           to current: AnchorSignature) -> Double

    /// §16.1.1 的具名入口，语义保留、签名改变。
    static func hasDrifted(from anchor: AnchorSignature,
                           to current: AnchorSignature) -> Bool {
        forceDriftForTesting || divergence(from: anchor, to: current) > contentThresholdLuma
    }

    /// mask 一致性否决门（§17.3.3）。步长子采样的 IoU。
    /// 纯函数；调用方在 decoderQueue 上使用。
    static func alphaIoU(_ a: [UInt8], _ b: [UInt8],
                         width: Int, height: Int, stride: Int = 4) -> Double
}
```

⛔ **旧的 `hasDrifted(from: FrameGeometry, to: FrameGeometry) -> Bool` 与 `drift(from:to:) -> Drift` 一并删除**，不保留兼容重载 —— 保留一个恒返回 false 的重载是最容易被误调用的形态（A-4 家族：断言不得绑定在一个休眠的谓词上）。

#### 17.5.2 状态存放位置与队列归属（必须精确，因为这是接口变化的实质）

| 状态 | 存放位置 | 谁写 | 谁读 | 同步 |
|------|---------|------|------|------|
| 每实例内容基线 `anchorSignature: AnchorSignature?` | **`TapInstance` 新增字段** | videoQueue（`checkAndFireReAnchor`：首次播种 + 每次批次开始时更新）| videoQueue（同一函数）| `TapInstanceManager` **既有的 `lock`**（该 lock 已保护 `mask` / `maskAlpha` / `requestGen` 等全部可变字段）—— **不新增锁** |
| `lastReAnchorFireMs: Double` | `CameraManager` 私有字段 | videoQueue | videoQueue | 无需同步（单队列私有），与既有的 `tapModeFrameCount` / `reAnchorNoEmbeddingSkips` 同类 |
| `ReAnchorLoop.lastAnchorGeometry` | 保持存在，但**降级为遥测** | 同前 | 同前 | 同前 |

**⛔ `ReAnchorLoop.lastAnchorGeometry` 不再是漂移判据的输入。** 保留它的唯一理由是 `[REANCHOR]` 日志与 C4 诊断可能需要「批次开始时的几何」。若 Builder 认为无用，可直接删除；**不得**继续用它做任何判定。§16.4.2「批次开始即更新基线」的**时机决定完整保留**，只是基线的**内容**从 geometry 变成 signature —— 理由一字不变（触发批次的那一帧才是正确的比较基准；推迟到批次结束会让中间帧的累积漂移再触发一次冗余批次）。

#### 17.5.3 `checkAndFireReAnchor` 的修订后条件序（取代 §16.2.1 的条件 5）

顺序经过刻意安排，把**最便宜的门放在最前面**，使 `.tapToSegment` 下绝大多数帧在做任何像素工作之前就返回：

```
1. currentMode == .tapToSegment                      // 既有
2. !isAppBackgrounded                                // 既有（Builder 增补，保留）
3. batch = tapInstances.drawableInstances(); !batch.isEmpty   // 既有（§17.9 已追认）
4. 【新】now - lastReAnchorFireMs >= minReAnchorIntervalMs    // ← 时间下界，放在采样之前
5. embeddingCache != nil                             // 既有（§16.4.1）
6. 【新】对 batch 中每个实例：
       cur_i = signature(latestCameraBuffer, inst.canonicalPoint)
       若 cur_i == nil        → 视同无漂移，return
       若 inst.anchorSignature == nil → 播种该实例基线，continue（本帧不 fire）
       d_i = divergence(inst.anchorSignature!, cur_i)
   maxDivergence = max over i of d_i
   guard forceDriftForTesting || maxDivergence > contentThresholdLuma else { return }
7. 节流认领 + embedding 快照（stateLock 内，一次获取）  // 既有，§17.9 已追认
8. 更新 lastReAnchorFireMs；把本批次各实例的 cur_i 写回其 anchorSignature（批次开始即更新基线）
9. 派发 per-instance decode                            // 既有
```

**关于第 6 步「取 max 而非 per-instance 触发」的裁决：** 批次语义保持 §16.2.3 不变（一次 fire 刷新全部可绘制实例）。理由：单在途批次的节流是按批次计的，若允许 per-instance 独立触发会退化成「实例各自排队」，正是 D-15.2 拒绝的形态。代价是没漂移的实例也会被重新 decode 一次 —— 这对它们无害（内容没变 ⇒ 新 mask ≈ 旧 mask ⇒ 一致性门必然放行），只是浪费一次 decode。**若 Debugger 实测发现这部分浪费显著（N=3 时最坏 2/3 的 decode 是冗余的），可提案改为「只 decode 越阈的实例」；本次不做，因为它会让批次大小可变，而 §16.2.3 的计数器语义与 `batchId` 追认都建立在批次大小固定的前提上。记为保留项 R20。**

**日志格式修订（§16.2.5 的最小改动）：**

```
[REANCHOR][inst#%d] drifted %.1flum → qwait: %.1fms decode: %.1fms
```

⚠️ **`qwait:` 与 `decode:` 两个字段的名称、格式、相对位置逐字符不变** ⇒ §16.9.3 给出的三条 grep 命令（`grep -o 'qwait: [0-9.]*'` / `decode`）**全部继续有效，Debugger 无需改脚本**，D-4 判据不受影响。只有前缀里的 `%.1fpt/%.1fdeg` 换成 `%.1flum`（该实例自身的 `d_i`，不是批次的 max —— 每个实例报自己的量值才对调参有用）。

新增否决行（与既有失败行区分开，便于统计接受率）：

```
[REANCHOR][inst#%d] rejected — mask IoU %.2f < %.2f, keeping previous mask
```

---

### 17.6 Builder 变更清单（对照已建成的实现）

**保留（一行都不要动）：**

| 组件 | 状态 |
|------|------|
| `ReAnchorLoop` 节流状态机（`isReAnchoring` / `pendingCount` / `beginBatch` / `completeUnit` / `reset`）| ✅ 全部保留 |
| `batchId` 世代号机制 | ✅ 保留并**升格为契约**（§17.9.1）|
| `resetReAnchorState()` 挂到三个清池点 | ✅ 保留 |
| `reAnchorDecode` 的 decode 路径：`SAMDecoder.decode` → 数值哨兵 → 256 空间点变换 → `buildTapAlpha` → `iou_pred >= 0.1` 门 → `updateMask` → `compositeLayers` → `traceOutline` → 主线程发布 | ✅ 全部保留（R3 冻结区，逐位不变）|
| `[REANCHOR]` 日志在任何质量门控**之前**发出 | ✅ 保留（这是 §16.8 被动采集能成立的前提）|
| 失败策略 `reAnchorKeepStaleMask` | ✅ 保留，并被 §17.3.3 的否决门复用 |
| 双重 `requestGen` 检查（派发时快照 + `updateMask` 内原子校验）| ✅ 保留 |
| T3 取在 `decoderQueue.async` 调用点之外 | ✅ 保留 |
| `capturedEmbedding` 在 `stateLock` 内快照、且先取 embedding 后认领批次的顺序 | ✅ 保留（§17.9.3 追认）|
| `batch = drawableInstances()` | ✅ 保留（§17.9.2 追认，且在新信号下更必要）|
| 不新增队列 / 不新增锁 / 不调 encoder / 不动 `canonicalPoint` / 不动 `SAMDecoder.swift` / 不动 `MaskRenderer.swift` | ✅ 继续 INVIOLABLE |

**替换：**

| # | 现状 | 改为 | 依据 |
|---|------|------|------|
| B-1 | `DriftDetector.drift(from: FrameGeometry, to: FrameGeometry) -> Drift` 及 `hasDrifted` 的 FrameGeometry 重载 | **删除**，换成 §17.5.1 的 `signature` / `divergence` / `hasDrifted(AnchorSignature)` / `alphaIoU` | §17.1 / §17.5 |
| B-2 | `translationThresholdPt = 10.0` / `rotationThresholdDeg = 3.0` | **删除**，换成 §17.4 六个常量 | §17.4（单位改变，不可迁移）|
| B-3 | `DriftDetector.Drift` 结构体（translationPt / rotationDeg / exceedsThreshold）| 改为携带 `divergenceLuma: Double` + `exceedsThreshold: Bool`。**它「必须在检查时刻捕获、不得在闭包内重算」的设计理由完整成立，保留该形态** | §16.2.5 原论证不变 |
| B-4 | 漂移判定读 `reAnchor.lastAnchorGeometry` | 改读 per-instance `TapInstance.anchorSignature`；`lastAnchorGeometry` 降级为遥测或删除 | §17.5.2 |
| B-5 | `checkAndFireReAnchor` 的条件序 | 按 §17.5.3 重排，**时间下界 `minReAnchorIntervalMs` 必须排在像素采样之前** | §17.4 / §17.5.3 |
| B-6 | 日志前缀 `drifted %.1fpt/%.1fdeg` | `drifted %.1flum`。**`qwait:` / `decode:` 字段逐字符不变** | §17.5.3 |

**新增：**

| # | 内容 | 位置 | 要求 |
|---|------|------|------|
| B-7 | `TapInstance.anchorSignature: AnchorSignature?` 及 `TapInstanceManager` 上对应的读写方法 | `Interaction/TapInstanceManager.swift` | 用**既有的 `lock`**；不新增锁；不改动 `drawableInstances` 的过滤条件 |
| B-8 | `CameraManager.lastReAnchorFireMs` | `CameraManager` 私有字段，videoQueue 独占 | 与 `tapModeFrameCount` 同类，无需同步 |
| B-9 | 一致性否决门 `alphaIoU` 调用 | `reAnchorDecode` 中 `buildTapAlpha` 之后、`updateMask` 之前 | 失败走既有 `reAnchorKeepStaleMask`；受 `reAnchorConsistencyGateEnabled` 单开关控制（C-6 单变量回退）|
| B-10 | 否决日志行 | 同上 | 格式见 §17.5.3 |

**两条实现约束（对应 D-3 无内存累积验收）：**
1. `AnchorSignature.samples` 每帧构造 ≤3 个、每个 64 字节。**优先使用定长内联存储或 videoQueue 独占的复用 scratch buffer**，避免每帧堆分配。分配不等于累积，D-3 不会因此失败，但这是免费的卫生。
2. `alphaIoU` **禁止**为子采样构造中间数组；就地双指针遍历。

**明确不要求 Builder 做的事（防止范围蔓延）：** 不实现任何 tracker；不引入 CoreMotion / Vision；不改 `FrameGeometry.invertViewPoint`（§2 变换链继续冻结）；不改 tap 路径任何一行；不改 `.segmentation` 路径任何一行。

### 17.7 `DriftDetector.forceDriftForTesting` 的处置

**裁决：保留，但重新定性并附加使用纪律与移除条件。**

**保留的理由：** Builder 在信号被证伪、修订未落地的窗口期里引入它，使循环机制（节流状态机、`batchId` 孤儿单元、qwait、decode、保留旧 mask 降级）在真机上仍可测量 —— 这是**正确的工程判断**，而且它保住了三项本会被阻塞的产出：§16.8 的 ISSUE-P4-DECODE 被动采集、§16.9.2 的 D-4（qwait < 50 ms）R4 检验、以及节流机制本身的正确性验证。它与 `CameraManager.forceSlowPath` 同类，是既有惯例。

**重新定性（这一条是纪律的核心）：**

> `forceDriftForTesting` 打开时产生的数据，**对「机制」有效，对「行为」无效**。
>
> - ✅ 有效：qwait、decode、批次计数器、节流是否死锁、内存是否累积、主线程是否掉帧、`[REANCHOR]` 日志链路是否通畅
> - ❌ 无效：D-1 系列（刷新是否发生在该发生的时候）、否决门的接受率、误触发率、任何关于「信号好不好」的结论
>
> 任何引用 `[REANCHOR]` 数据的报告，**必须标注该会话 `forceDriftForTesting` 的开关状态**。这是 §9.2 「延迟数字必须标注工况」纪律在本项目的又一次应用；不标注的数据视为不可用。

**移除条件（合取，全部满足才移除）：**
1. §17.3 的信号已落地并在真机上被证实能自然触发（Debugger 在 `forceDriftForTesting == false` 的会话中采到 ≥20 条 `[REANCHOR]` 行）；
2. §17.8 修订后的验收准则 D-1a/D-1b/D-1c 全部判定完成（无论 PASS/FAIL）；
3. Phase 4 冻结之前。

**在移除之前必须满足的约束：** 默认 `false`；不得暴露在任何面向用户的 UI 上；不得在 release 配置下被任何代码路径置为 `true`。若第 1 条在两次真机会话后仍不满足（新信号也不触发），**不得靠这个开关掩盖，必须再次上交 Architect** —— 那意味着 §17.3 也选错了，处理方式与本次相同。

---

### 17.8 Debugger 验收准则修订（取代 §16.9.1 的 D-1）

#### 17.8.1 D-1「缓慢平移 3 秒，mask 随目标物体移动，偏差 ≤ 15 px」— ⛔ 撤销

**撤销理由不是「新信号做不到」，而是「该判据与 §16.7 自相矛盾，在任何信号下都不可能通过」**（§17.1.4）。`canonicalPoint` 冻结 ⇒ re-anchor 产出的 mask 锚在**画面坐标**上，不锚在**物体**上 ⇒ 相机平移时 mask 的正确行为恰恰是**不跟着物体走**。15 px 判据度量的是能力 C，而 Phase 4A 交付的是能力 A+B。

**这不是把不方便的线挪走**（§15 对自己立的规矩）：撤销的同时，能力 C 被完整保留为 `ISSUE-P4-TRACK`（§17.2.0），且下面三条新判据**比原判据更难通过** —— 原判据只要求 mask 位移正确，新判据要求「该刷新时刷新、不该刷新时不刷新、错刷时能自我否决」三件事同时成立。

#### 17.8.2 替代判据

| # | 条目 | 操作 | 判据 |
|---|------|------|------|
| **D-1a**（该刷新时刷新）| 静止相机 + 变化目标 | 手持稳定对准一个会变形/小幅移动的目标（人的手、被轻推的物体），tap 之，保持相机不动，让目标在锚点上小幅变化 3 秒 | 出现 ≥1 条 `[REANCHOR]` 且**未**被否决；mask 目视上跟上了目标的新形状。`forceDriftForTesting` **必须为 false** |
| **D-1b**（不该刷新时不刷新）| 全静止 | tap 之后相机与场景都保持静止 10 秒 | `[REANCHOR]` 行数 **= 0**。出现即为误触发 ⇒ 上调 `contentThresholdLuma` 后重测，并记录调后值 |
| **D-1c**（错刷时自我否决）| 相机平移离开目标 | tap 一个物体，然后缓慢平移相机直到该物体完全离开锚点 | mask **不得**悄悄变成另一个物体的 mask。合法结果二选一：(i) 出现 `rejected — mask IoU ...` 行且 mask 保持原样；(ii) 无 `[REANCHOR]` 触发。**若 mask 跳到了别的物体上 ⇒ FAIL**，且这是本次修订最重要的一条 |
| **D-2**（原样保留）| 快速甩动 | 同 §16.9.1 | mask 不消失；旧位置保留视为 PASS |
| **D-3**（原样保留）| 内存 | 同 §16.9.1 | 30 s 无累积，与 ~590 MB ±15 MB 基线一致 |
| **D-4**（原样保留）| qwait | 同 §16.9.2 | `[REANCHOR]` 中 qwait max < 50 ms；≥50 ms ⇒ R4 证实，上报并暂停合入 |
| **D-5**（原样保留）| V-3 | 同 §16.9.2 | 自然使用中不出现 ≥5 次慢路径 |
| **D-6**（新增）| 接受率 | 统计一次自然使用会话 | `rejected` 行数 / `[REANCHOR]` 行数。**这是观测指标，不设通过线**（§13 裁决一 (a)：不得把观测指标写成验收目标）。用途：接受率 ≈ 100% ⇒ 门形同虚设，考虑上调 `reAnchorAcceptIoU`；接受率 ≈ 0% ⇒ 刷新从未生效，等价于 re-anchor 没上线 |

#### 17.8.3 「15 px / 3 s」这个测试形态还对不对

**不对，而且它错在两个独立的层面，值得记下来：**

1. **它测的是没交付的能力**（上文已述）。
2. **即使能力 C 落地，15 px 也不是一个好判据。** 它需要「物体真实中心」这个**外部真值**，而验收方法是目视 —— 这正是 A-5 的形态（用没校准的量定判据）。能力 C 立项时，正确的判据形态应当是**相对的、可自证的**：例如「re-anchor 后的 mask 与 re-anchor 前的 mask 的 IoU ≥ X」，或者「锚点始终落在 mask 内部」—— 后者是一个布尔量，无需真值，且直接对应用户能感知的失败（锚点标记跑到 mask 外面）。**记入 `ISSUE-P4-TRACK` 的立项要求。**

#### 17.8.4 `ISSUE-P4-TRACK` 重开条件（合取）

1. Phase 4A 的 A+B 已验收且稳定（D-1a/D-1b/D-1c 通过，D-6 接受率落在合理区间）；
2. ISSUE-P4-DECODE 已结案 —— 因为 tracker 会新增每帧 ANE/GPU 负载，在 decode 倒挂的自变量未定之前引入它会同时污染两个问题（§17.2.1 对 Vision 的否决理由 1）；
3. 「tap 语义」的架构裁决先行：用户 tap 的是**一个点**还是**一个物体**。锚点可移动意味着 `canonicalPoint` 不再是不可变的用户意图记录，§16.7 的第二条禁令要正式撤销并给出替代不变量（例如 `originalCanonicalPoint` 冻结 + `trackedPoint` 可变）；
4. 判据按 §17.8.3 第 2 点重写，不得沿用 15 px。

### 17.9 Builder 三处自主决定的裁定

#### 17.9.1 `batchId` 世代号 — ✅ **追认，并升格为契约条款**

Builder 的论证成立且**我在 §16.2.3 确实留了这个洞**。复核：§16.2.3 只写了「C4 时 `isReAnchoring = false`、`pendingCount = 0`」，但没处理**已经排在 `decoderQueue` 上、无法取消的在途闭包**。这些闭包终将执行并调用 `completeUnit`：

- 若 C4 后没有新批次：计数器被减成负数 ⇒ `pendingCount == 0` 的释放条件**永远不再成立** ⇒ `isReAnchoring` 永久为 true ⇒ **节流永久死锁**（这恰恰是 §16.2.3 自己警告过的失败形态，而我给的复位方案并没有封住它）；
- 若 C4 后已经开了新批次：孤儿单元提前把**新批次**的计数器减到零 ⇒ 新批次的槽位被提前释放 ⇒ 单在途批次不变量被破坏。

`batchId` 单调自增 + `completeUnit(batch:)` 校验 + `reset()` 自增，使孤儿单元的 `completeUnit` 成为 no-op，两个失效路径同时封死，且不引入新状态机、不引入锁。**这是对契约缺陷的正确修补，不是范围蔓延。** 升格为 §16.2.3 的正式条款（以 §17 为准）。

**记录：这是本次同一份契约里的第二个缺陷**（第一个是 §16.3.1）。二者的共性是我在写 §16 时只推演了「正常路径」，没有推演「不可取消的异步工作在状态被重置之后落地」这一类竞态。⇒ **长期约定 A-8（不可取消工作的复位纪律）：任何「复位/清空」操作的设计，必须显式回答「此刻已经在飞、无法撤回的工作落地时会做什么？」。合格的答案只有两种 —— 让它变成 no-op（世代号/令牌），或让它的副作用幂等。答「它会自然结束」不合格。**

#### 17.9.2 批次成员取自 `drawableInstances()` 而非全部实例 — ✅ **追认**

Builder 的理由（首次 tap decode 尚在飞的实例没有 mask 可刷新，且 re-anchor 会与那次 tap 争同一实例槽位）成立。核实 `TapInstanceManager.swift:329`：`drawableInstances()` 过滤 `maskAlpha != nil` 并按 `createdAt` 排序，语义正确。

**在新信号下它从「优化」升为「必要条件」**，两条新增理由：
1. 没有 mask 的实例也没有 `anchorSignature` 基线（基线在实例首次可绘制后才播种），把它算进批次会让 `maxDivergence` 的取值范围里混入未定义项；
2. §17.3.3 的一致性否决门需要**旧 alpha** 作为比较对象；`maskAlpha == nil` 的实例无从比较，门会退化为无条件放行 —— 正好在最该保守的情形（一个尚未成形的实例）上最激进。

追认为契约条款。

#### 17.9.3 `capturedEmbedding` 在 `stateLock` **内**快照（偏离交接指令）— ✅ **追认 Builder，交接指令有误**

Builder 的三条论证我逐条核实，全部成立：

1. **数据竞争是真实的。** `CameraManager.swift:215` 的注释白纸黑字：「stateLock guards: isEncoding, isDecoding, **embeddingCache（written on encoderQueue, read on videoQueue）**」。`checkAndFireReAnchor` 跑在 videoQueue。锁外读 `embeddingCache` 就是跨队列无同步读一个正在被另一队列写的引用型字段 —— 不是理论风险，是 UB。
2. **§10.4 要求 A 被误读了。** 该条禁止的是**在锁内做重活**（编解码、合成、遍历像素），目的是压低锁持有时间；它从不禁止在锁内做 O(1) 的引用读取。`embeddingCache?.embedding` 是一次已存在对象的 retain，无分配、无拷贝。§15 实测 `lock` 段 66 个样本 < 0.05 ms 已经把这条的收益上界钉死了 —— **为省一次 retain 而制造一个数据竞争，是拿正确性换一个已被观测证明不存在的性能。**
3. **一致性论证成立。** `drainPendingTaps` / `scheduleEncoder` / `warmupDecoderIfPossible` 三个既有调用点都在锁内读它。让 re-anchor 独树一帜会制造一个「同一字段有两种同步约定」的陷阱，这类不一致是最难在 code review 中发现的。

**同时追认 Builder 未被要求、但明确写在注释里的第四点：块内顺序「先取出 embedding，后认领批次槽位」。** 若顺序颠倒，当 embedding 在两次获取之间变空时，槽位已被认领却提前 return ⇒ 一个**永不释放的认领** ⇒ 又一次永久节流死锁（A-8 的同族形态）。Builder 自行识别并规避了它。追认为契约条款。

> **长期约定 A-9（交接指令与同步不变量冲突时的优先级）**：上层交接/契约中关于「在锁外做某事」的指示，若与被访问字段的**既有同步归属**冲突，**以同步不变量为准，并上交**。判断依据是**写者所在的队列**，不是操作看起来有多轻。自检问法：**「这个字段是谁写的？他和我在同一个队列上吗？」** Builder 本次即按此处理（实现正确的一侧 + 在注释里写明偏离理由 + 上交），记录为推荐处理方式，与 Day 5「上交而非擅自决定」并列。

#### 17.9.4 三项裁定小结

| # | Builder 决定 | 裁定 | 是否升格为契约 |
|---|-------------|------|--------------|
| 1 | `batchId` 世代号 | ✅ 追认 | 是（补 §16.2.3 的洞）|
| 2 | `drawableInstances()` 批次成员 | ✅ 追认 | 是（新信号下为必要条件）|
| 3 | `capturedEmbedding` 锁内快照 + 先取 embedding 后认领 | ✅ 追认，交接指令有误 | 是 |

**三项全部追认。Builder 在本轮的判断质量高于契约本身**：它发现了 §16.3.1 的证伪、堵上了 §16.2.3 的死锁洞、拒绝了一条会引入数据竞争的指令，并且在每一处都选择了「实现正确的一侧 + 写明理由 + 上交」而不是沉默绕过。

---

### 17.10 保留项与风险

| # | 项 | 状态 |
|---|----|------|
| **R6** | 场景运动脱节 | **仍未关闭。** Phase 4A 只把「陈旧度无上界」变成「陈旧度有界且内容一致」，物体走出锚点时 mask 依旧留在原地（只是不会再变成别的东西）。R6 的完整解法是 `ISSUE-P4-TRACK` |
| **约定 A-1（TTL 适用性）** | 时间型 TTL 是否可重开 | **不可。** 自检问法「它过期之后谁把它变回来？」的答案现在是「**内容变了、且新 mask 与旧 mask 一致时，re-anchor 会**」—— 这是一个**条件性**刷新循环，不是无条件的。§11 的 mask TTL 撤销**维持不变**，Phase 4A 不重开 |
| **R20**（新）| 批次内冗余 decode | N=3 时最坏 2/3 的 decode 是对未漂移实例的冗余刷新。本次不优化（会让批次大小可变，冲击 §16.2.3 计数器与 `batchId` 语义）。Debugger 若观测到显著成本，提案改为「只 decode 越阈实例」 |
| **R21**（新）| 一致性否决门是新增机制，零真机数据 | `reAnchorAcceptIoU = 0.5` 是**工程判断，非文献值、非实测值**。D-6 接受率是它的唯一观测窗口。若接受率落在两个极端，该常量必须先调参再谈结论 |
| **R22**（新）| 去均值只对**加性**亮度偏移免疫 | 自动曝光若发生**增益**变化（乘性），去均值挡不住，可能产生误触发。缓解层是 `minReAnchorIntervalMs`。若 D-1b 在明暗剧变场景下失败，下一步是改用归一化互相关而非继续上调阈值 |
| **R23**（新）| `ISSUE-P4-TRACK` | 已立项，P2，重开条件见 §17.8.4。**不得在 Phase 4B（Pin & Annotation）排期内夹带实现** |
| **ISSUE-P4-DECODE** | 状态不变 | §16.8 的被动采集设计**完全不受本次修订影响**（`[REANCHOR]` 日志在质量门之前发出，decode 字段照常产生）。唯一变化：数据现在可以在 `forceDriftForTesting == false` 的自然会话中采集，比 §16.8 设想的更干净 |

### 17.11 Deliverable 确认

- [x] §17.1 缺陷陈述：⛔ SUPERSEDED 标记（§16.3.1 / §16.1.1 实现体 / §16.1.2 阈值表 / §16.3.2 论断）+ 赋值点级证据 + 根因 + 长期约定 A-7 + **第二个更深的缺陷（§16.7 冻结锚点 vs D-1 要求跟随，自相矛盾）**
- [x] §17.2 范围重划（能力 A/B/C 三分，C 另立 `ISSUE-P4-TRACK`）+ 五个候选信号按六条判据评估 + 逐条否决理由
- [x] §17.3 采纳信号：锚点邻域内容散度（定义 / 赋值点核实 / 计算步骤 / 三个设计要点）+ 配套的 mask 一致性否决门及其不违反 R3 / A-5 的逐条论证
- [x] §17.4 修订阈值：六个具名常量（单位改为 luma level，明示不可迁移）+ 手抖三层防御 + 降级行为
- [x] §17.5 修订接口：明示无法表达为两个 FrameGeometry 的纯函数 + 新接口 + 状态存放位置与队列归属 + 条件序 + 日志格式（qwait/decode 字段逐字符不变）
- [x] §17.6 Builder 变更清单：保留 10 项 / 替换 6 项（B-1…B-6）/ 新增 4 项（B-7…B-10）+ 两条实现约束 + 明确不要求做的事
- [x] §17.7 `forceDriftForTesting`：保留 + 重新定性（机制有效/行为无效）+ 标注纪律 + 三条合取移除条件
- [x] §17.8 验收准则修订：D-1 撤销及理由 + D-1a/D-1b/D-1c 替代 + D-6 观测指标 + 15 px 判据为何本身也是坏判据 + `ISSUE-P4-TRACK` 四条重开条件
- [x] §17.9 Builder 三处自主决定：全部追认 + 升格为契约 + 长期约定 A-8（不可取消工作的复位纪律）/ A-9（交接指令与同步不变量冲突时以不变量为准）
- [x] §17.10 保留项 R20–R23 + R6 / A-1 / ISSUE-P4-DECODE 状态

---

END OF PHASE 4 DAY 2–3 ARCHITECT REVISION（§17 Re-anchor 漂移信号修订）

---

# Phase 4 — Day 2–3 验收后裁决（§18 补救裁决）

日期：2026-08-17
触发：Debugger 按 §17.8.2 修订后的判据完成真机验收（debug_report §34），结果 **6 PASS / 2 FAIL / 1 观测项 / 1 判别失效**。两条 FAIL（D-4、D-1c）经复核**均为设计层缺陷，不是实现缺陷** —— Builder 忠实实现了 §16+§17，两条失败都直接来自我写下的条款。
性质：**修订而非改写**。§16 / §17 全文保留，被本节推翻的条款逐条加 SUPERSEDED 标记（§15 确立、§17 沿用的纪律）。

> **本节的自我定位**：§17 是「§16 选错了信号」的修订，§18 是「§16+§17 把批次语义和比较基准都定错了」的修订。同一份契约连续第三次出缺陷，这件事本身必须记录，不得淡化 —— §18.9 给出共性归因。

---

## §18 D-4 / D-1c 补救裁决 + 判据修订 + 排期裁定

### 18.1 D-4 / R4 补救裁决

#### 18.1.1 先确认事实边界（不重复 §34，只钉住裁决所依赖的四条）

| # | 事实 | 出处 |
|---|------|------|
| F1 | 自变量已隔离为**批内实例数**，不是 re-anchor 本身：单实例批次 n=125，qwait max **1.5 ms**（与 Phase 3 tap 同分布）；N=2 max 135.0；N=3 max **189.90** | §34.3.1 / §34.3.2 |
| F2 | 批内**首个**单元恒为 ~0.2 ms ⇒ 调度器无问题，成本 100% 来自串行 `decoderQueue` 上排在前面的单元 | §34.3.2 |
| F3 | §16.2.3 的 191.1 ms 算术是**被实测确认的下界**（实测 mean 205.2 / max 244.6） | §34.3.3 |
| F4 | **decode 优化解决不了 D-4**：即使 ISSUE-P4-1 的 ~30 ms 全部回收，N=3 的 qwait max 仍约 80 ms > 50 ms | §34.3.5 第 4 条 |

#### 18.1.2 一条 §34 未提、但改变全部账目的事实：**re-anchor 的绝大多数 decode 在数学上是空转**

这是本轮裁决的核心，先给论证再给方案。

**论证（已对源码核实，2026-08-17）：**

1. `reAnchorDecode` 的 decode 调用是 `decoder.decode(embedding: embedding, point: prompt, tapIndex: capturedGen)`。`tapIndex` **只进日志**，不进模型输入。
2. `prompt` 由 `PointPromptBuilder.buildPointPrompt(canonicalPoint:origSize:inputSize:)` 生成。`canonicalPoint` 被 §16.7 冻结（re-anchor 不得修改）；`origSize` 来自 `lb.origW/origH`，其变化会先触发 C4 清池 ⇒ **在一个实例的生命周期内，`prompt` 是常量**。
3. `SAMDecoder` 的存储属性只有 `boxModel` / `multimaskPointModel` / `boxModelLoadFailed` 三个**模型惰性缓存**，不参与输出计算；`decode` 是一次 `MLModel.prediction`，无跨调用状态。
4. `MaskRenderer` 唯一的存储属性是 `private let inputSize: CGFloat = 1024.0`（常量）；`buildTapAlpha` 是对 `(lowResMask, origW, origH, tapPoint256, iouPreds)` 的纯函数。

⇒ **对同一实例，`(embedding, canonicalPoint)` 不变时，整条 `decode → 数值哨兵 → buildTapAlpha` 链路逐位输出相同。**

**后果，用本轮实测数据结算：**

- 自然会话（测试 E，~89 s）中 `embeddingCache` 的更新次数 = background refresh 次数 = **15 次**（§34.8.2，`age ≥ 5000 ms` 门控）；`encode=own` = 0，re-anchor 自身不产生 embedding（§16.7 已真机验证）。
- 同期 re-anchor decode = **91 次**。
- 一个实例在同一 embedding 世代内的第 2 次及以后的 re-anchor decode，**其产物与屏幕上已有的 mask 逐位相同**。
- 上界估算：15 个 embedding 世代 × 最多 3 个实例 = **≤45 次**可能产生新结果的 decode；**其余 ≥46 次（≥50%）是可证明的空转**。加上 §34.5.3 已量化的 R20 冗余（17/91 单元自身 `d_i` ≤ 8.0），两类浪费**部分重叠但都是真的**。

**这一条同时解释了 §34.4.3 的一个未解现象：** 为什么 216 次比较中一次都没有 `rejected`？因为其中相当一部分比较的是**两个逐位相同的 alpha**，IoU 恒为 1.0。否决门不仅问错了问题（§34.4.4），它还被喂了大量恒等输入。

⇒ **长期约定 A-13（重复计算的输入核验）**：在为一个计算安排「周期性重复执行」之前，必须先回答「**它的输入在两次执行之间真的会变吗？**」。对固定输入的纯函数重复求值是一次**有成本的 no-op**。自检问法与 A-7 同族：A-7 问「这个量是被测出来的还是被算出来的」，A-13 问「**这个输入在我这个周期尺度上会变吗**」。

⇒ **长期约定 A-12（派生产物的新鲜度上界）**：一个派生产物**不可能比它最陈旧的输入更新鲜**。声称某个刷新循环把陈旧度变成有界之前，必须指出该循环**最慢的那个输入**，并用它来陈述界。本项目的实例：re-anchor 解码所依赖的 `embeddingCache` 由 background refresh 以 **5000 ms** 门控更新 ⇒ **能力 A 的真实陈旧度界是 ~5 s，不是 `minReAnchorIntervalMs` 的 300 ms**。§16 与 §17 都没有说出这句话，两次都把节流周期误当成了新鲜度界。

#### 18.1.3 裁决：D-4 的判据线 **不动**，改为消除违反该线的条件

§34.3.5 第 1 条正确地指出：50 ms 这条线在 §16.2.5 / §15.6.2 中**没有推导过程**。既然如此，我有两条路：给它补一个推导然后据此挪线；或者不碰线，改设计。

**裁决：不碰线。** 理由有三条，且第三条是决定性的：

1. **把线从 50 挪到 190 以上就是 §15 明令禁止的形态**（「把不方便的线挪走」），而 §17.8.1 撤销 15 px 判据时我已经为「什么样的撤销才合法」立过标准：撤销必须使**新判据更难通过**，且旧目标被完整保留另立条目。本次不满足该标准 —— 挪线只会让判据更容易通过。
2. **50 ms 虽无推导，但它约束的对象是对的。** qwait 度量的是「decoderQueue 被占用的程度」，而这个量的真正危害不是 re-anchor 自己晚到（没人在等它，晚 190 ms 完全落在能力 A 的陈旧度界之内），而是**它对 tap 的外部性**：tap 与 re-anchor 共用同一条串行队列。§34.3.6 已经严谨地声明「本会话没观测到 tap 被堵住」**不构成 tap 安全的证据**（M-15.3 同型）。⇒ 在这个外部性被专门测量之前，任何放宽都是无依据的。
3. **不需要挪线。** §18.1.2 的确定性论证表明：造成违规的那部分工作**本来就不该存在**。消除它之后，批次恒为单实例，而单实例批次的 qwait 有 **n=125、max 1.5 ms** 的实测支撑 —— D-4 由构造满足，无需任何判据修订。**当一条线之所以被违反是因为做了不必要的工作，正确的动作是删工作，不是改线。**

#### 18.1.4 补救 RE-1：embedding 世代门（消除空转）

**规则：一个实例在同一个 embedding 世代内，最多被 re-anchor 解码一次。**

接口级规格（伪代码，非可编译 Swift）：

```
// 新增：embedding 世代号
// 位置：CameraManager 现有的 embeddingCache 写入点（encoderQueue），与 embeddingCache 同一 stateLock 保护
embeddingGeneration: UInt64            // 每次 embeddingCache 被赋一个「新计算出的」embedding 时 += 1
                                       // 注：仅在 encode 完成写回时自增；置 nil 不自增

// 新增：每实例已消费的世代
TapInstance.lastReAnchorEmbeddingGen: UInt64?     // 初始 nil；由 TapInstanceManager 既有 lock 保护（同 anchorSignature，B-7 形态）

// checkAndFireReAnchor 的资格判定（在 §17.5.3 第 5 步之后、第 6 步像素采样之前）：
eligible = batch.filter { $0.lastReAnchorEmbeddingGen != currentEmbeddingGeneration }
guard !eligible.isEmpty else { return }        // 全部实例都已消费当前世代 ⇒ 本帧无事可做，且不做任何像素采样
```

- 世代号在**成功派发**该实例的 decode 时写入 `lastReAnchorEmbeddingGen`（与 §16.4.2「批次开始即推进基线」同一时机、同一理由），**不等 decode 返回**：这是节流量，不是正确性量，提前推进只会少刷一次，不会多刷。
- tap 路径**不读不写**该字段（tap 走 `requestGen`，语义正交）。
- C4 / `clearAll()` 时实例消失，字段随之消失，无需额外复位。

**RE-1 的性质必须说清楚：它不是一个折衷，是一次无损删除。** 被它删掉的每一次 decode，其产物都与屏幕上已有的 mask 逐位相同（§18.1.2 的四条核实）。**没有任何一个在 RE-1 之前会出现的画面，在 RE-1 之后不会出现。** 这与 §13 为 A-1 立的 E-3「否决式改动」是同一形态，基线可比性由构造保证。

**收益（用本轮数据结算）：** 91 → ≤45 次 decode，decoderQueue 占空比从 13%（11.9 s / 89 s）降到 ≤6%；且 §17.5.3 第 6 步的像素采样在「全实例已消费当前世代」时**整帧跳过**，把 §17.3 的 <50 µs/帧 进一步压到接近零。

#### 18.1.5 补救 RE-2：批次大小恒为 1（消除批内累积）

**RE-1 降低了频率，但没有削掉最坏情况**：一个新 embedding 落地、三个实例同时既越阈又未消费该世代时，仍会是一个 3 单元批次 ⇒ qwait 仍会回到 190 ms。D-4 是 max 判据，必须结构性封死。

**规则：一次 fire 只刷新一个实例。批次大小固定为 1。**

选择规则（按序，全部为确定性规则，无随机）：

```
candidates = batch
    .filter { $0.lastReAnchorEmbeddingGen != currentEmbeddingGeneration }   // RE-1 资格
    .filter { divergence(from: $0.anchorSignature, to: current_i) > contentThresholdLuma }  // 自身越阈（不再取批次 max）
guard let pick = candidates.min(by: { $0.lastReAnchorFireMs ?? -inf < ... }) else { return }
// 平局时取 slotIndex 较小者（primary 优先），保证完全确定
```

- **每实例记录自己上次被刷新的时刻**（`TapInstance.lastReAnchorAtMs`），选「最久未刷新」的那个 ⇒ **公平轮转，不会饿死任何实例**。
- `minReAnchorIntervalMs = 300` 的语义不变（fire 之间的全局下界）。
- 每实例刷新周期 = 300 ms × 同时越阈的实例数 ≤ **900 ms**（N=3）。而按 A-12，真实的新鲜度界由 embedding 的 5000 ms 主导 ⇒ **900 ms 的轮转周期完全落在 5 s 的界之内，能力 A 不受任何实质损失。** 这是 RE-2 成本极低的原因，也是它必须与 RE-1 一起裁决的原因：单看 RE-2 会以为在牺牲刷新率，实际上被牺牲的部分早已被 embedding 的更新节奏吞掉了。

**RE-2 顺带关闭 R20。** §17.5.3 第 6 步的「取 max 触发 ⇒ 未越阈实例被搭车重解码」在新规则下**结构性不可能发生**：候选集已按每实例自身的 `d_i` 过滤，且只取一个。§34.5.3 量化的 18.7%（多实例批次内 30.9%）冗余归零。

**RE-2 需要显式修订的既有条款：**

| 被修订条款 | 原文要旨 | 本节裁定 |
|---|---|---|
| **§16.2.3** | 「批次大小 = `tapInstances.count`」，`pendingCount` 与 `batchId` 语义建立在批次大小固定的前提上 | ⛔ **部分 SUPERSEDED**：批次大小改为**恒为 1**。注意这**加强**而非削弱了原前提 —— 计数器与 `batchId` 世代号机制（§17.9.1 追认）**逐行保留、无需改动**，`beginBatch(count: 1)` 即可 |
| **§17.5.3 第 6 步「取 max 而非 per-instance 触发」** | 一次 fire 刷新全部可绘制实例；理由是 per-instance 独立触发会退化为「实例各自排队」，即 D-15.2 拒绝的形态 | ⛔ **SUPERSEDED**。原理由**本身没错，但它防的是另一件事**：D-15.2 拒绝的是「多个批次同时在途」，而 RE-2 是「一个批次里只有一个单元」。单在途批次的不变量**完全保留**（`isReAnchoring` 机制不动）⇒ 不会退化成各自排队。我当时把「批次内并行」与「批次间并发」混为一谈了 |
| **§17.5.3 保留项 R20** | 「不做，因为会让批次大小可变」 | ✅ **关闭**。RE-2 让批次大小变为**固定的 1**，不是可变 —— 我当初担心的「可变批次冲击计数器语义」在这个方向上不成立 |

#### 18.1.6 §16.7 禁令的处置：**不需要放宽，一条都不动**

任务书要求我在需要放宽「禁止新增 DispatchQueue 或锁对象」时显式提出。**明确回答：本补救不需要放宽 §16.7 的任何一条。** 逐条核对：

| §16.7 禁令 | RE-1 / RE-2 是否触碰 |
|---|---|
| 禁止调用 SAMEncoder / `refreshTapEmbeddingIfNeeded` / 任何 encoder 路径 | ❌ 不触碰（RE-1 只**读**世代号，encode 的触发权仍完全在 background refresh） |
| 禁止修改 `canonicalPoint` | ❌ 不触碰（RE-1 的确定性论证正是建立在它冻结之上） |
| **禁止新增 DispatchQueue 或锁对象** | ❌ 不触碰。`embeddingGeneration` 与 `embeddingCache` 同生命周期、同 `stateLock`（§17.9.3 已追认的「锁内 O(1) 读」形态原样复用）；`lastReAnchorEmbeddingGen` / `lastReAnchorAtMs` 用 `TapInstanceManager` **既有的 `lock`**（与 B-7 的 `anchorSignature` 完全同型） |
| 禁止修改 `SAMDecoder.swift` / `MaskRenderer.swift` | ❌ 不触碰 |
| 禁止修改 R3 禁令参数 / `buildTapAlpha` | ❌ 不触碰（RE-1/RE-2 全部作用在 decode **派发之前**） |
| 禁止新增第二份 SAMEncoder 实例 | ❌ 不触碰 |

**为什么这条「不需要放宽」值得单独写一段**：一个直觉上的替代方案是「给 re-anchor 一条独立的低优先级队列，让它不与 tap 争 `decoderQueue`」。⛔ **明确否决**，三条理由：

1. 它**放大**而不是解决问题。ANE/GPU 是单一物理资源，把 re-anchor 挪到另一条队列只是把排队从软件队列移到硬件仲裁，`decode` 绝对耗时不会下降，反而引入 CoreML 并发预测的争用 —— 而 ISSUE-P4-DECODE（三条假说的自变量全是 GPU/ANE 占用状态，§17.2.1）**尚未结案**，此时新增一个并发执行上下文会同时污染两个问题。
2. 串行 `decoderQueue` 目前**免费提供了 tap 与 re-anchor 的互斥**，而 §16.5.1 的 `requestGen` 优先级机制建立在此之上。并行化后需要重新论证写回顺序，那是比本次补救大一个量级的改动。
3. 真正的问题是**工作量**（§18.1.2：一半以上是空转），不是**调度**（F2：批内首单元恒为 0.2 ms，调度器无问题）。**给一个不该存在的工作换一条更宽的路，是把浪费制度化。**

#### 18.1.7 RE-1 + RE-2 之后 D-4 的预期，以及一条**不得省略**的新验收

- 批次恒为 1 ⇒ qwait 预期落在 **max ≤ 2 ms**（实测支撑：测试 B/C 段 n=125，单实例 qwait max **1.5 ms**）。D-4 判据 50 ms 由构造满足。
- ⚠️ **但按 M-15.3，我不得据此宣布 R4 关闭。** 上述预期的实测支撑来自测试 B/C 段 —— 那一段是**单实例在屏**的工况；RE-2 之后是**三实例在屏、但每批只解一个**，两者的 `decoderQueue` 负载形态不同（后者仍有三倍的 fire 竞争同一条队列）。**必须实测。**
- ⚠️ 更重要的是 §34.3.6 点名的那个**从未被测过的条件**：tap 落在 re-anchor 占用窗口内时的 tap 延迟。RE-2 把最坏占用窗口从 205 ms 压到约 60 ms（单次 decode + post），但**窗口仍然存在**，而它才是 D-4 真正要保护的对象（§18.1.3 理由 2）。⇒ **新增验收 D-7（见 §18.3.4），且这一条不是可选项**：D-4 由构造满足这件事，恰恰使 D-4 丧失了作为「tap 是否安全」的判别力，必须由 D-7 顶上。

**这是本轮我要求自己遵守的同一条纪律的第三次应用**（§15.6.2 拒绝关闭 R4 → §34.3.4 证实其正确 → 本节拒绝据预期关闭 R4）。**一个由设计保证的界，在被专门制造的条件下测量之前，仍然只是一个预期。**

---

### 18.2 D-1c 补救裁决：比较基准改为 tap 时刻的原始 alpha

#### 18.2.1 诊断的采纳（复核结论，不重述证据）

§34.4.4 的诊断我逐条对源码核实，**全部成立**：

- `CameraManager.swift:2135` 的 `let previousAlpha = instance.maskAlpha` 取的是**当前屏上的 alpha**；
- 该字段唯一的写者是 `tapInstances.updateMask(...)`（`:2254`），而**一次成功的 re-anchor 自己就会调用它**；
- ⇒ 第 k 次比较的基准是第 k−1 次 re-anchor 自己的产物。**相邻步 IoU ≥ 0.5 的一条链，对 `IoU(M_0, M_k)` 没有任何蕴含**；
- `alphaIoU` 实现正确（就地双指针、`union == 0` 返回 1.0「无证据不否决」），`previousAlpha` 不可能为 nil（`drawableInstances()` 已过滤）。⇒ **门没有算错，它问错了问题。**

**这条缺陷的归属是明确的：§17.3.3 原文写的是「新 alpha 与该实例『原有 alpha』的 IoU」，而『原有 alpha』这个措辞在实现上唯一可指的就是 `instance.maskAlpha`。Builder 的实现与我的文字逐字对应。缺陷在我的措辞里 —— 我写的时候脑子里想的是「tap 时刻那一张」，落到纸上却是「当前那一张」，而这两者只有在「re-anchor 从不成功」时才相同。**

⇒ **长期约定 A-14（自反基准禁令）**：当一个门控用「历史值」作为接受新值的判据时，必须显式回答「**这个历史值会不会被该门控放行的写入所更新？**」。若会，则该门控约束的是**相邻步**，不是**端到端**，而相邻步约束对端到端偏离**没有任何蕴含**。自检问法：「**如果这个门连续放行 100 次，最后一次的输入还剩多少原始语义？**」

#### 18.2.2 裁决：RE-3 —— 冻结的 tap 原点 alpha 作为唯一比较基准

**规则：每个实例保留一份 tap 时刻产生的 alpha（`originAlpha`），由 tap 路径写入，re-anchor 路径永不写入。一致性否决门比较 `IoU(originAlpha, newAlpha)`。**

接口级规格（伪代码，非可编译 Swift）：

```
TapInstance:
    var originAlpha: [UInt8]?          // tap 路径产生的 alpha；re-anchor 永不写
                                       // 由 TapInstanceManager 既有 lock 保护（同 anchorSignature，B-7 形态）

写入点（唯一）：tapDecodeWithPoint 成功、调用 updateMask 时
    —— 即：当且仅当 requestGen 因用户 tap 而自增后的那次写回，才同时写 originAlpha
    —— re-anchor 的 updateMask 调用**只写 maskAlpha，不写 originAlpha**

否决门（取代 §17.3.3 的比较对象，位置与开关不变）：
    iou = alphaIoU(instance.originAlpha, built.alpha, width:, height:)
    if reAnchorConsistencyGateEnabled && iou < reAnchorAcceptIoU {
        reAnchorRejectUpdate(slot:, iou:)      // 复用既有「保留旧 mask」分支
        return
    }
    // originAlpha == nil（理论上不可达，drawableInstances 已过滤）⇒ 视同不可比较，按 alphaIoU 既有口径返回 1.0，不否决
```

**这条规则确立的不变量，可以用一句话陈述并被 Debugger 直接检验：**

> **一个实例在其整个生命周期内显示过的每一张 mask，与用户那一次 tap 所产生的 mask 的 IoU，都 ≥ `reAnchorAcceptIoU`。**

对照旧规则能陈述的不变量（「相邻两张 mask 的 IoU ≥ 0.5」）—— 后者是一个**对用户不可感知、也不可检验**的命题；前者直接对应 D-1c 要防的那件事。**这是本次修订最实质的收获：把一个无法陈述用户意义的不变量，换成一个能陈述的。**

#### 18.2.3 恢复语义（§34.4.5 要求必须一并给出）

§34.4.5 正确地预警：基线推进（`anchorSignature`，批次开始时推进，与否决无关）与比较基准推进（原 `previousAlpha`，仅成功时推进）条件不一致 ⇒ 一旦发生否决可能进入吸收态。**RE-3 把比较基准从「会推进」改成「永不推进」，因此必须正面回答吸收态问题。**

**裁决：吸收态是正确的终态，予以接受；恢复由三条路径提供，全部无需新机制。**

| # | 恢复路径 | 触发 | 语义 |
|---|---|---|---|
| **REC-1** | **自动恢复：相机转回来** | 用户平移离开又平移回来 ⇒ 锚点下重新是原物体 ⇒ `IoU(originAlpha, newAlpha)` 回升 ≥ 0.5 ⇒ 门放行，刷新恢复 | **这是冻结基准的免费红利，也是本裁决相对「链式基准」最有力的一条**：链式基准下「离开再回来」不会恢复（基准早已跟着爬走了），冻结基准下它**自动**恢复。恢复的判据与否决的判据是同一个，无需任何额外状态 |
| **REC-2** | **用户重新 tap** | 用户对该实例（或该位置）再 tap 一次 | `requestGen` 自增 ⇒ tap 路径写回 ⇒ **`originAlpha` 被重写为新的 tap 产物** ⇒ 新的原点，新的不变量。这是「用户改变了意图」的正确表达 |
| **REC-3** | **FIFO 淘汰 / C4 清池** | 实例池满后新 tap 挤出最旧实例；或几何签名变化 | 实例连同 `originAlpha` 一并消失，无残留状态 |

**为什么「否决后停止刷新」是正确终态，而不是需要被修掉的病：**

1. 它正是 §17.2.0 定义的能力 B。当锚点下面已经不是用户选的东西时，**唯一正确的动作就是不再更新**。「停止刷新」不是失败模式，是**该场景下的正确输出**。
2. 代价是**可见且可恢复的**：mask 停在原处不再更新，用户看得见；一次 tap 即可恢复（REC-2）。而旧行为的代价是**不可见且不可恢复的**：用户选中的杯子悄悄变成了桌面，用户不知道自己在看什么。**§17.2.0 已经裁定过这个取舍方向（「A 若无 B 则是净退化」），本节只是把它执行到底。**
3. `anchorSignature` 与 `originAlpha` 推进条件不一致这件事，在 RE-3 下**不再是问题而是分工**：内容散度决定**何时尝试**（它必须跟着画面走，否则停在旧内容上会反复空转），原点 alpha 决定**是否接受**（它必须冻结，否则就是 A-14 的自反基准）。**两个量的职责不同，推进条件本就不该相同。** §34.4.5 把它记为「未被数据检验的次生结构」是恰当的；本节把它从「结构性隐患」重新定性为「有意的职责分离」，并把理由写进契约，避免将来有人「顺手把它们对齐」。

#### 18.2.4 本裁决的真实成本（不掩饰）

**冻结原点的代价是：合法的大幅形变会被否决。**

能力 A 的样板场景（D-1a：静止相机 + 变形目标，如手的开合、人转身）中，物体可以合法地偏离 tap 时刻的形状很远。此时 `IoU(originAlpha, newAlpha)` 会跌破 0.5，门会否决一次**本应接受**的刷新，实例进入 REC 之前的冻结态。

**为什么仍然选它 —— 因为不存在第三个选项，且两类失败的可检出性相差一个量级：**

| 方案 | 失败形态 | 用户能否察觉 | 能否恢复 |
|---|---|---|---|
| 链式基准（现状） | mask 悄悄爬到别的物体上 | ❌ 察觉不到（D-1c 录屏中 mask 是**连续爬**过去的，不是跳） | ❌ 用户不知道要恢复什么 |
| **冻结原点（RE-3）** | 大幅形变时 mask 冻住 | ✅ 看得见（mask 明显不跟形状了） | ✅ 一次 tap（REC-2）或转回来（REC-1） |
| 「高阈值棘轮」（每步 ≥0.9 才推进原点） | 仍是链式，只是每步更紧 | ❌ 同第一行 | ❌ 同第一行 |

**第三行必须单独否决，因为它是最容易被提出的折衷方案：** 把原点在「高置信度」时推进，看起来能兼顾形变跟随与端到端约束。⛔ **否决。** §34.4.4 的论证逐字适用：**任何允许基准推进的方案都是链式方案，阈值只改变每步的紧度，不改变「相邻步约束对端到端无蕴含」这个结构。** 定量：D-1c 录屏中 10 s 的迁移在 300 ms 下界下约 33 步，0.9^33 ≈ 0.03 —— 一个「每步 ≥0.9」的棘轮足以让 mask 端到端偏离到几乎无重叠。**紧度不是种类。**

⇒ 记为新保留项 **R25**（§18.7）：合法大幅形变被否决 ⇒ 实例 mask 冻结。**本轮无数据**，D-1a 重测必须专门包含一个大幅形变目标以量化该边界。

#### 18.2.5 `reAnchorAcceptIoU = 0.5` 在新基准下的处置

- 数值**保持 0.5 不变**，但**它的含义已经改变**：旧基准下 0.5 是「相邻两次刷新的相似度」，新基准下是「与用户原始选择的相似度」。**同一个数字，不同的量。**
- ⇒ **R21 的零数据状态不因本次修订而改善，反而必须重新计时**：§34.6.1 的判断（「在比较基准被裁决之前对 0.5 做任何调参都是给错误的问题找答案」）在本节裁决后**其前置条件才刚刚被满足**。D-6 现在第一次成为一个**良定义**的观测窗口。
- **明令：在 D-6 于 RE-3 落地后重测出接受率之前，不得调整 `reAnchorAcceptIoU`。** 调参方向仍按 §17.4 的表述，但必须以重测读数为输入。

#### 18.2.6 §17.3.3 的处置：机制留、基准改、安全声明撤回

| §17.3.3 的组成部分 | 处置 |
|---|---|
| `alphaIoU` 的实现与口径（stride 4、就地双指针、`union==0` 返回 1.0） | ✅ **保留，一行不动**（§34.4.4 第 1 条已复核正确） |
| 门的位置（`buildTapAlpha` **之后**、`updateMask` **之前**）| ✅ **保留**（`buildTapAlpha` 零触碰，R3 不受影响） |
| 否决落回既有 `reAnchorKeepStaleMask` 分支 | ✅ **保留** |
| `reAnchorConsistencyGateEnabled` 单开关（C-6 单变量回退）| ✅ **保留**，且**仍不得以关闭状态发布** |
| 否决日志行格式 | ✅ **保留**（格式不变，D-6 提取脚本继续有效） |
| **比较对象 = 「该实例原有 alpha」（实现为 `instance.maskAlpha`）** | ⛔ **SUPERSEDED by RE-3**：改为冻结的 `originAlpha` |
| **「没有它，新信号会让产品变差」这一安全性声明** | ⚠️ **撤回**（详见 §18.6）：该声明在本轮被证伪 —— 门存在、且实现正确，产品**仍然**变差了。声明的重新成立以 D-1c 重测通过为条件 |

**§17.3.3 中「为什么不违反 R3 禁令」的五条逐条论证，在 RE-3 下全部继续成立**（比较对象换了一个数组，门的性质、位置、作用方向、对 §16.8 采集的影响均未变），无需重新论证。

---

### 18.3 排期裁定：Day 2–3 是否关闭 / Phase 4B Day 4 是否开工 / 九个复选框

#### 18.3.1 「是否值得保留 re-anchor」—— 先回答这个，再谈排期

任务书明确允许我判定「重新划范围」而非硬凑补救。我认真评估过三个鹰派选项，逐条给否决/采纳理由：

| 选项 | 裁定 | 理由 |
|---|---|---|
| **(i) 整体推迟 Phase 4A，直接进 Phase 4B** | ❌ 否决 | 立项动机（R6 / A-1「它过期之后谁把它变回来？」）在 Phase 3 冻结时是**无解**的，Phase 4B 的 Pin 会把这个无解状态**持久化到磁盘上**（一个 Pin 保存的缩略图若来自一张已经漂到别的物体上的 mask，错误就从瞬态变成了长期资产）。⇒ **能力 B 是 Phase 4B 的前置条件，不能推迟到它之后。** |
| **(ii) 只保留 primary 实例的 re-anchor（N 恒为 1）** | ❌ 否决 | RE-2 用**轮转**达到了同样的 qwait 界，却不牺牲 slot 1/2。在 A-12 给出的 5 s 新鲜度界之下，900 ms 的轮转周期是免费的（§18.1.5）⇒ 没有理由付出「二级实例永不刷新」这个代价 |
| **(iii) 门控 N ≤ 2** | ❌ 否决 | 它把一个**结构性**问题（串行队列上的批内累积）换成一个**条件性**问题（N=3 时静默降级），且 N=2 实测 qwait max 已达 135 ms，本就不满足 D-4。**用一条没有推导的实例数门控去救一条没有推导的延迟门控，是两个未定义相乘。** |
| **(iv) 保留，但按 §18.1 / §18.2 补救后重新验收** | ✅ **采纳** | 补救后的 re-anchor 是一个**比 §16/§17 设想的小得多、但可陈述、可检验**的特性：「**当 embedding 更新了、且锚点处画面确实变了、且新结果仍认得出是用户当初选的那个东西时，用最新 embedding 在原锚点重解一次。**」这句话里每一个从句都对应一个已裁决的门（RE-1 / §17.3 信号 / RE-3），且三个门加起来仍然不新增队列、不新增锁、不碰冻结文件 |

**必须诚实记录的一点：** 补救后的 re-anchor 交付量远低于 §16 立项时的想象。§16 想的是「每 300 ms 刷新全部实例」，实际能交付的是「每个 embedding 世代刷新每个实例最多一次，约 5 s 一轮，且语义偏离时自动停住」。**这个缩水不是补救造成的，是 A-12 一直如此而我没看见** —— §16 与 §17 都在用节流周期冒充新鲜度界。**把它写清楚，比让它继续隐身更有价值。**

#### 18.3.2 Day 2–3：**不关闭**

- tasks.md D-4 条目的明文是「⛔ ≥50 ms ⇒ R4 证实，**上报并暂停合入**」。上报已由 §34 完成，裁决由本节完成，**「暂停合入」维持有效**。
- D-1c 是 §17.8.2 自己标注的「本次修订最重要的一条」，FAIL 未修复。
- ⇒ **Day 2–3 保持开启，并入一个新的收尾区块「Day 3.5 — Re-anchor 补救（§18）」**：Builder 落地 RE-1/RE-2/RE-3，Debugger 执行 §18.3.4 的重测集。Day 2–3 的关闭条件见 §18.3.5。

**「暂停合入」的具体执行形态（避免分支长期分叉）：** 不要求 revert，不要求拉长分支。要求 Builder 增加一个**特性总开关**：

```
DriftDetector.reAnchorEnabled: Bool = false      // 默认 OFF，直到 §18.3.5 的关闭条件全部满足
```

- 语义：`checkAndFireReAnchor` 的**第 0 条**，OFF 时立即 return，`.tapToSegment` 的行为逐位回到 Phase 3 冻结态。
- 与 `reAnchorConsistencyGateEnabled` 的区别必须写在注释里：后者是**安全半边**的对照开关，§17.4 明令「不得以关闭状态发布」；前者是**整个特性**的合入闸门，**当前明令必须以关闭状态发布**，直到本节的关闭条件满足。二者语义相反，不得混用。
- 这是 C-6 单变量纪律在特性层的应用，也让 Debugger 的重测能做「开/关」对照而不必换构建。

#### 18.3.3 Phase 4B Day 4：**准许开工**，但有一条硬隔离

**裁定：Phase 4B Day 4（PinStore 数据模型 + 本地存储）准许立即开工，不被 Day 2–3 阻塞。**

依据是耦合面分析 —— Day 4 的三方任务与 re-anchor 的交集为空：

| Day 4 任务 | 触及的文件/资源 | 与 re-anchor 的耦合 |
|---|---|---|
| Architect：裁决 PinStore 存储方案（CoreData vs JSON） | 文档 | 无 |
| Builder：`Pin` 结构体 + `PinStore` CRUD（异步写） | 新文件，`Shared/` 或新 `Persistence/` | 无：不触 `CameraManager` / `decoderQueue` / `DriftDetector` / `TapInstanceManager` / 冻结文件 |
| Debugger：10 条 Pin 写读 < 50 ms + 重启持久 | 单元测试级 | 无：不需要相机会话 |

**硬隔离条件（三条，全部必须遵守）：**

1. **Day 4 不得引用 `TapInstance` 的任何 re-anchor 相关字段**（`anchorSignature` / `lastReAnchorEmbeddingGen` / `lastReAnchorAtMs` / `originAlpha`）。Pin 的数据来源只能是 `maskAlpha` / `canonicalPoint` / `FrameGeometry` 这三个 Phase 3 已冻结的量。
2. **Day 4 不得新增任何在 videoQueue / decoderQueue 上执行的工作。** PinStore 的写入按 tasks.md 原文「异步执行，不阻塞主线程」，但**必须使用自己的 I/O 上下文，不得复用 `decoderQueue`** —— 磁盘写入排进解码队列会重新制造本节刚刚消除的那类累积。注：这一条**不是**放宽 §16.7 的「禁止新增队列」—— §16.7 约束的对象是 **re-anchor 路径**，PinStore 是一条与之无关的新子系统，其队列归属属 Day 4 Architect 裁决范围（§18.3.6 已排期）。
3. **Day 4 的产物不得在 `reAnchorEnabled == true` 的构建上验收。** 两个特性的交互（Pin 保存的是一张正在被 re-anchor 刷新的 mask）属 Day 5 的范围，须在 Day 2–3 关闭之后单独设计。**这一点必须提前记下**：Pin 与 re-anchor 的交互是一个真实的未裁决问题 —— 「保存 Pin 的那一刻 mask 正好被 re-anchor 替换」的竞态，以及「Pin 保存后该实例是否还应继续被 re-anchor 刷新」的语义 —— 记为新保留项 **R27**。

**优先级排序（给 Builder 的明确顺序）：** §18 补救（P0）→ Phase 4B Day 4（P1）。Builder 完成补救构建并交付 Debugger 后，**不必等待重测结果**即可开始 Day 4；重测与 Day 4 并行。

#### 18.3.4 补救后的重测集（Debugger）

**必测（5 条）：**

| # | 条目 | 判据 | 为什么必须重测 |
|---|---|---|---|
| **D-1c'** | 平移离开目标 | mask 不得变成别的物体。合法结果：(i) 出现 `rejected` 行且 mask 保持原样；(ii) 无触发。**且必须同时验证 REC-1**：平移离开后再**平移回来**，刷新应自动恢复 | 本轮 FAIL 的直接对象；REC-1 是 RE-3 相对链式基准的核心优势，未验证等于未交付 |
| **D-4'** | qwait | `[REANCHOR]` qwait max < 50 ms，**且必须在三实例在屏的工况下采集** | §18.1.7：现有 n=125 的支撑来自单实例在屏，条件域不同（M-15.3） |
| **D-7（新）** | **tap 与 re-anchor 的争用** | 三实例在屏 + re-anchor 活跃 + **刻意高频 tap**（≥15 次，间隔 <1 s），统计 `[D7'] qwait` 的 max 与 `[D7'] total` 的 p95 | §34.3.6 明确指出该条件「基本没有出现过」，而 D-4 由构造满足后**丧失了对 tap 安全性的判别力**。判据：`[D7'] total` p95 不得超过 §33.2 Release 快路径 p95（97.30 ms）的 2 倍，即 **< 195 ms**；此线的推导为「tap 最坏情况 = 自身 total + 至多一个在途 re-anchor 单元的 decode+post ≈ 97 + 61 + 17 ≈ 175 ms」，留 ~11% 余量。**这是本项目第一条给出推导的延迟门控线** |
| **D-6'** | 接受率 | `rejected` / `[REANCHOR]`。**观测指标，仍不设通过线**。R21 的唯一输入 | §18.2.5：RE-3 之后 D-6 才第一次良定义 |
| **D-3'** | 内存 | 判据按 §18.5 修订（同会话单调性 + N=0→3 ≤ +31 MB 参照）。**额外允许 `originAlpha` 的 +2 MB/实例**（1080p 下 origW×origH 字节，COW ⇒ 首次 re-anchor 成功前与 `maskAlpha` 共享，之后分裂），N=3 上限 **+6 MB** | RE-3 是本轮唯一新增常驻内存的改动，必须落账 |

**复测（2 条，成本低、条件已变）：**

| # | 条目 | 变化 |
|---|---|---|
| **D-1a'** | 该刷新时刷新 | 判据不变，但**必须包含一个大幅形变目标**（手的开合、人转身），以量化 §18.2.4 的 R25 边界。另需确认 RE-1 之后触发频率下降不影响「跟上新形状」的目视结论 |
| **D-1b'** | 静止 10 s 零触发 | 判据不变，但**必须留原始 console 日志**（§34.1.1 指出本轮该条只有用户叙述），**且必须新增一个曝光剧变场景**（镜头扫过窗户 / 开关灯）以首次检验 R22（去均值只对加性偏移免疫，乘性增益挡不住） |

**不重测（2 条）：** D-2（甩动，PASS，补救不改变失败降级路径）、D-5（V-3，PASS，§16.7 禁令已真机验证且补救只减少 decode 不增加 encode）。

#### 18.3.5 Day 2–3 的关闭条件（合取，全部满足）

1. RE-1 / RE-2 / RE-3 三项落地，编译通过，且 Builder 逐条声明未触碰 §16.7 七条禁令与 R3 禁令参数；
2. D-1c' **PASS**（含 REC-1 自动恢复验证）；
3. D-4' **PASS**（三实例在屏工况下 qwait max < 50 ms）；
4. **D-7 PASS**（`[D7'] total` p95 < 195 ms）；
5. D-3' PASS，D-6' / D-1a' / D-1b' 完成读数（D-6 无通过线，只需有读数）；
6. `reAnchorEnabled` 由 `false` 改为 `true` 的动作，须在上述五条全部满足后由 Architect 单独批准，**不得由 Builder 自行翻转**。

#### 18.3.6 Architect 自身的下一步

- 本节完成后，我立即进入 **Phase 4B Day 4 的 Architect 条目**（PinStore 存储方案裁决：CoreData vs JSON），它与补救无耦合，不占用 Builder 的关键路径。
- ISSUE-P4-DECODE 的判别实验**不在本轮排期内**，其处置见 §18.4。

#### 18.3.7 九个复选框的处置（我在 tasks.md 上的动作范围）

> 纪律：只动 Debugger 的九个框与 Architect 自己的条目；**Builder 的框一个都不碰**（§17 同纪律）。失败项**不留白**，就地记录 FAIL 与去向。

| # | 判定 | tasks.md 动作 |
|---|---|---|
| **D-1a** | ✅ PASS | **勾选**，附实测（216 条自然触发，`d_i` 0.6–125.1 lum；A-7 首次应用即命中） |
| **D-1b** | ✅ PASS（证据等级低） | **勾选**，附 §34.1.1 caveat：仅用户现场观察，无原始日志；并注明 D-1b' 须补采原始日志 + 曝光剧变场景（R22） |
| **D-1c** | ⛔ FAIL | **不勾选**，就地记录 FAIL + 诊断（自反基准）+ 去向（§18.2 RE-3 / D-1c'） |
| **D-2** | ✅ PASS | **勾选** |
| **D-3** | ✅ PASS | **勾选**，同时就地修订判据文本（删 ~590 MB，改为 §18.5 口径） |
| **D-4** | ⛔ FAIL | **不勾选**，就地记录 FAIL（max 189.90 ms）+ R4 CONFIRMED + 去向（§18.1 RE-1/RE-2 / D-4'） |
| **D-5** | ✅ PASS | **勾选** |
| **D-6** | 📊 已执行，**读数作废** | **不勾选**。理由：观测动作确实做了，但 §34.6.1 已证明该读数（0/216）在错误的比较基准下**不构成对 R21 的任何输入**。勾选会让后来者误以为 R21 已有数据。就地记录「已执行 / 读数作废 / 须在 RE-3 后重测」 |
| **ISSUE-P4-DECODE 被动采集** | ⚠️ 判别失效 | **不勾选**。就地记录：§16.8.2 判据表作废、假说 (a) 实测贡献 +11.50 ms（n=8, d=1.19）「部分成立非主因」、issue 保持 OPEN、替代协议见 §18.4 |

**另加三处（非九框）：**
- Day 2–3 Architect 条目下追加一条 §18 修订说明（不取消勾选，形态同 §17）；
- Day 2–3 Debugger 区块头部追加「§18 重测集」说明与 D-7 新条目；
- Phase 4B Day 4 区块头部追加「准许开工 + 三条硬隔离」说明。

---

### 18.4 §16.8 判别设计的修订：判据表作废，采集前提部分保留

#### 18.4.1 我先接受一处对自己的纠正

§16.8 的框架是我写的，本轮我给 Debugger 的口径是「按判据表读数」。**§34.7 拒绝了这个框架并给出了更好的分析，该拒绝正确，我采纳。** 具体地：

- 我要求的读数是二选一（≈36.4 ⇒ (a) 成立；≈63.7 ⇒ 排除 (a)）；
- §34.7.2 用行序划出 15 个「encode 在飞」窗口，做出了我没有设计的**分组对照**：并发组 n=8 mean **71.97 ms** vs 非并发组 n=83 mean **60.48 ms**，差 **+11.50 ms**（Welch t=2.55，Cohen d=1.19）；
- ⇒ 正确结论是 **(a) 部分成立、非主因**（解释 27.3 ms 中的 11.5 ms），而我的判据表**只允许写「排除 (a)」**。

**⇒ 若按我的表结案，会得到一个与数据方向相反的结论。** 这不是「表不够细」，是**表的形状错了**。

#### 18.4.2 §16.8 的四项主张逐条裁定

| §16.8 主张 | 裁定 |
|---|---|
| 「`[REANCHOR]` decode 字段可直接提取，无需额外 session」 | ✅ **保留成立**。216 个样本、零额外成本、字段格式逐字符未变 |
| 「n ≥ 20 `[REANCHOR]` 行即可支撑初步判断」 | ⛔ **SUPERSEDED**。n 必须**按工况格子计**，不是按总数计。216 条里落在决定性格子（与 encode 并发）的只有 **8 条** |
| **§16.8.2 的三行判据表** | ⛔ **作废（VOID）**，三条独立理由全部成立（§34.7.2/.3/.4） |
| 「ISSUE-P4-DECODE 可由被动采集结案」 | ⛔ **不成立**。ISSUE-P4-DECODE **保持 OPEN** |

**三条作废理由，逐条采纳并各自升格为一条通用纪律：**

1. **假说不互斥，而表是二选一。** (a) 争用 / (b) 局部性 / (c) DVFS 是**可叠加**的物理机制，观测量是它们的**和**。均值 61 既可能是 (a)=0,(b)+(c)=27，也可能是 (a)=11,(b)+(c)=16 —— 表把两者都读成「排除 (a)」。
   ⇒ **长期约定 A-10（可叠加机制的判据形态）**：当候选机制在物理上**可叠加**时，判据必须写成**分解**（每条机制贡献多少），不得写成**二选一**。二选一表只在机制互斥时合法，而「互斥」这件事本身必须先被论证，不能默认。
2. **(b) 从未有过正向检验。** §16.8.1 里 (a) 有预测值、(c) 有判别方式，而 (b) 的判别方式一栏写的是「同上比较」—— (b) **只被定义为 (a) 的补集**，在设计上无法被证实，只能被「剩下来」。更严重的是它的 36.4 ms 参照点本身是脏的：`forceSlowPath` 同时清空 `embeddingCache` **且**挂起 background refresh（§33.4.4，`CameraManager.swift:77–80`）⇒ 那个参照是「无争用**且**热 embedding」的复合工况。**整条比较轴的原点是混杂的，采多少数据都不会收敛。**
   ⇒ **长期约定 A-11（残差不是证据）**：每一条候选假说都必须有**自己的正向检验**。仅以「其余假说被排除」为依据认定的假说，其地位是**记账方式，不是证据**。且用于分离两条假说的参照工况，必须只在**一个**自变量上与被测工况不同 —— 若参照点同时改变两件事，它不能分离任何东西。
3. **(c) 的判别在本数据上不可执行。** (c) 的判别方式原文是「观察时序相关性」，而全部 2338 行日志**没有任何时间戳**（§34.1.5）。这不是「做了没结论」，是「做不了」。§34.7.4 用行序邻近度做的最好替代给出 ~6 ms 的弱单调梯度（n=7），方向与 (c) 一致但量级远小于 27 ms。
   ⇒ **纪律**：任何以「时序相关性」为判别方式的假说，其埋点必须**先**具备时间戳。判别方式与埋点能力的匹配性，属契约作者的责任，不是 Debugger 的。

#### 18.4.3 替代判别设计（采纳 §34.7.6，定为契约；但**不在本轮排期**）

**采纳 §34.7.6 的五条最小可行设计，逐条定为 ISSUE-P4-DECODE 重开时的前置契约：**

| # | 内容 | 归口 | 本轮是否执行 |
|---|---|---|---|
| 1 | **日志行加单调时钟前缀**（`PerfLogger.nowMs()` 量级，非 wall-clock） | Builder | ✅ **本轮执行**（见下） |
| 2 | 拆开 `forceSlowPath` 的两件事 ⇒ 新增 `suspendRefreshOnly`（只 early-return `refreshTapEmbeddingIfNeeded`，**不清缓存**） | Builder | ✅ **本轮执行**（见下） |
| 3 | **2×2 析因**（{refresh 挂起/活跃} × {embedding 刚生成/已放置数秒}），**每格 n ≥ 20** | Debugger | ❌ 推迟 |
| 4 | 同一次运行内**交错**采样四格，不按格分段（避免 DVFS/热漂移混入主效应） | Debugger | ❌ 推迟 |
| 5 | (c) 单独判定需**受控的 encode→decode 间隔扫描**（0/100/500/2000 ms） | Debugger | ❌ 推迟 |

**为什么第 1、2 项本轮执行而 3–5 推迟：**

- 第 1、2 项是**埋点与开关**，成本极小（一个字符串前缀、一个 early-return 分支），且**永久消除一整类不可判定性** —— 时间戳不仅服务于 ISSUE-P4-DECODE，它同时让 §34.1.5 记录的「行序不是时序」这个结构性缺口对**未来所有分析**关闭。这是本轮性价比最高的一项改动，**不做它等于每一轮都重新交一次学费**。
- 第 3–5 项是**采集协议**，需要专门的真机 session。ISSUE-P4-DECODE 的优先级排序：**低于 D-4/D-1c 补救（P0），也低于 Phase 4B Day 4–6（P1）**。理由：(i) 它是一个 27.3 ms 的效率问题，不是正确性问题；(ii) RE-1 落地后 re-anchor 的 decode 总量降半，该 issue 的实际影响进一步缩小；(iii) §17.8.4 把它列为 `ISSUE-P4-TRACK` 的重开前置，而 `ISSUE-P4-TRACK` 本就是 P2。
- ⇒ **ISSUE-P4-DECODE 定级：P2（由 P1 下调），重开时机为 Phase 4B 完成之后**；本轮只买期权（埋点+开关），不行权（实验）。

**本轮 Builder 须执行的两项（编号接续 §17.6 的 B 系列）：**

| # | 内容 | 要求 |
|---|---|---|
| **B-11** | 为 `perfLog` / `diagLog` 输出加单调时钟前缀 | 格式建议 `[t=%.1f]`（`PerfLogger.nowMs()`，进程启动起算）。⚠️ **必须加在行首，不得插入 `[REANCHOR]` / `[D7']` / `[SEG]` 等既有 tag 与其字段之间** —— §16.9.3 与 §33 的全部 grep（`grep -o 'qwait: [0-9.]*'` 等）依赖字段的**相对**位置，行首前缀不破坏它们 |
| **B-12** | 新增 `CameraManager.suspendRefreshOnly: Bool = false` | 语义：为 true 时 `refreshTapEmbeddingIfNeeded` 立即 early-return，**不清空 `embeddingCache`**。与既有 `forceSlowPath` **并存且正交**，不得合并、不得互相蕴含。默认 false，不暴露于 UI |

---

### 18.5 D-3 判据的参照点更正

#### 18.5.1 采纳 §34.8.1：`~590 MB ±15 MB` 是错配的比较对象

§16.9.1 与 §17.8.2 的 D-3 判据都写着「与 Phase 3 `~590 MB ±15 MB` 基线一致」。**该参照错配，本节予以更正。** §34.8.1 的对照成立：

| | `~590 MB` 基线（§27.6） | Day 2–3 实测 |
|---|---|---|
| 场景 | **Phase 2 ⇄ Phase 3 快速切换 ×5**，切换后 3 s 平台 | `.tapToSegment` **单模式常驻**，无模式切换 |
| 驻留模型 | 切换过程中两条 pipeline 先后驻留 | 仅一套 |
| 测的是什么 | **模式切换是否泄漏** | **re-anchor 是否累积** |
| 量级 | 582–597 MB | 177.7–374.5 MB |

⇒ 观测值比该「基线」低 200–400 MB。**若照字面执行，会得出「内存异常偏低」这个荒谬读数。**

#### 18.5.2 修订后的 D-3 判据（取代 §16.9.1 D-3 / §17.8.2 D-3 的判据文本）

> **D-3'：re-anchor 连续运行 ≥30 s，内存不得单调增长。**
> - **主判据（充要）：同一会话内的单调性。** 取 `Pre=…|Mem=` 采样序列，比较**第一四分位均值**与**第四四分位均值**：后者不得高于前者 **+30 MB**。本轮实测 311.8 → 209.5（**−102.3 MB**）⇒ PASS。
> - **辅助参照（同工况，非达标线）：** §27.7 第 4 项的 **N=0→3 增量 = +31 MB**。用于判断多实例占用是否偏离已知量级；**不得**作为绝对值达标线（绝对基线随 session 的冷启动次数与 CoreML 缓存累积浮动，§27.7 已记录 484 vs 304 MB 的同类差异）。
> - **RE-3 追加允量：** `originAlpha` 为每实例增加约 **2 MB**（1080p 下 origW×origH 字节；COW ⇒ 首次 re-anchor 成功写回前与 `maskAlpha` 共享，之后分裂），N=3 上限 **+6 MB**。该增量计入预期，不计入超标。
> - ⛔ **`~590 MB ±15 MB` 参照删除。** 它是 Phase 2⇄3 模式切换工况的平台值，与 `.tapToSegment` 常驻工况不可比。

#### 18.5.3 一处对我自己归因的更正

§17.8.2 之后我在口头交接中把本轮 374.5 MB 峰值归因于「FIFO 池行为」。**该归因错误，§34.8.1 已定位到行并推翻：** 峰值出现在测试 E 第 378 行，**紧跟**第 370 行的 `[TAP] background embedding refresh 9566.76 ms`（冷启动首次 encode，9.6 s，对照 warm mean 730.8 ms），此刻 `pool=[…] n=1` —— **只有一个实例**。三实例在屏的时段（第 487–728、1522–1565 行）内存反而稳在 **337–343 MB**，池满时**没有**创出新高。

⇒ 正确归因：**模型冷启动 / 首次 ANE 编译的一次性瞬态**（同类形态在装载阶段已出现过一次：`afterEncoder=673.8 MB → settled +2s: 114.2 MB`）。**与 FIFO 池、与 re-anchor 均无关。**

⇒ **纪律记录**：把一个峰值归因给「最近新增的那个特性」是最省事也最常见的错误。§34 的做法（先定位到行、再看该行的上下文与实例数）是正确形态。**归因必须落到行，不能落到直觉。**

---

### 18.6 §17.3 成立 / §17.3.3 的安全声明被证伪 —— 两半分开搬运

§34.5.2 要求这两半的结论「必须分开搬运，不得笼统写成『§17 通过 / 未通过』」。**采纳，并在此给出正式的分离表述**，因为将来引用 §17 的人最可能犯的错就是拿一个整体判定去代替这两个方向相反的结论。

#### 18.6.1 分离表述（引用 §17 时必须整体搬运本表）

| §17 的两半 | 判定 | 依据 | 引用时的正确说法 |
|---|---|---|---|
| **§17.3 信号选择**：锚点邻域内容散度作为触发信号 | ✅ **成立（真机验证通过）** | 216 条自然触发（`forceDriftForTesting=false` 三重证据）；`d_i` 跨 **0.6–125.1 lum**，有量纲、宽分布、随场景变化；静止 10 s 零触发；阈值 8.0 无需调参（噪声底 <8.0，真实运动 median 15.8–25.7，无贴线抖动病态形态） | 「§17 换掉死信号这一步是对的，新信号在真机上是活的」 |
| **§17.3.3 安全声明**：「一致性否决门是采纳新信号的**必要条件**；没有它新信号会让产品变差」 | ⛔ **被证伪** | 门**存在**、实现**正确**（`alphaIoU` 已复核）、开关**打开**、每次都**实际执行了比较**（`previousAlpha` 不可能为 nil）—— 而产品**仍然变差了**：mask 在 ≥3 个不相关物体间迁移，0/216 否决 | 「§17 给出的安全机制在**当时那个比较基准下**不成立；机制本身留用，基准已由 §18.2 RE-3 更换」 |

**必须防止的两种误读，各写一句：**

- ⛔ 误读 A：「§17.3.3 是多余的机制，可以删。」——**错。** §17.2.0 的论证（A 若无 B 则是净退化）**完全没有被证伪，反而被本轮实测强化**：D-1c 的录屏正是「没有生效的 B」所导致的那个后果。机制必须留，且必须修好基准。
- ⛔ 误读 B：「§17 整体不通过，应回退。」——**错。** 回退 §17 意味着回到 §16.3.1 的死信号（`translationDrift ≡ 0`），那是一个**永不触发**的循环，能力 A 与 B 都归零。**本轮的两条 FAIL 都发生在 §17 之上，不是 §17 造成的退步。**

#### 18.6.2 A-7 的地位

§17.1 立的 A-7（信号存在性核验：从赋值点出发判断「这个量是被测出来的还是被算出来的」）**首次应用即命中**（§34.5.2）。予以确认，无修订。

**但本轮暴露了 A-7 的一个盲区，须补一句：** A-7 检验的是**触发信号**是否活着，它对**门控基准**是否自反（A-14）、对**重复计算的输入**是否会变（A-13）一无所知。三者是同一族自检问法的三个不同对象：

| 约定 | 对象 | 自检问法 |
|---|---|---|
| **A-7** | 触发信号 | 这个量是被测出来的，还是被算出来的？ |
| **A-13** | 周期性计算的输入 | 这个输入在我这个周期尺度上会变吗？ |
| **A-14** | 门控的历史基准 | 这个基准会被本门控放行的写入更新吗？ |

**三条应合并为一次检查表**，在任何「循环 / 门控 / 周期任务」的契约完成前逐条走一遍。§16 与 §17 各只覆盖了其中零到一条，代价是三轮修订。

---

### 18.7 保留项与风险状态更新

| # | 项 | 本节裁定 |
|---|----|------|
| **R4**（decode 堆积） | ⛔ **CONFIRMED**（§34.3.4，qwait max 189.90 ms，自变量 = 批内实例数 ≥2）。**本节不关闭。** RE-2 使批次恒为 1，从设计上消除了触发条件，但按 M-15.3，**由设计保证的界在被专门制造的条件下测量之前仍只是预期** ⇒ 关闭条件 = D-4' **与** D-7 同时 PASS。⚠️ 注意 D-4' 单独 PASS **不足以**关闭：D-4 由构造满足后已丧失对 tap 安全性的判别力（§18.1.7） |
| **R20**（批次内冗余 decode） | ✅ **关闭。** RE-2 的候选集按每实例自身 `d_i` 过滤且只取一个 ⇒ 未越阈实例被搭车重解码在结构上不可能发生。§34.5.3 量化的 18.7%（多实例批次内 30.9%）归零。**关闭依据是机制，不是测量**，故 D-4' 重测时若仍出现 `d_i ≤ contentThresholdLuma` 的 `[REANCHOR]` 行，即为 RE-2 未正确落地的**签名**，须上报 |
| **R21**（`reAnchorAcceptIoU = 0.5` 零数据） | **OPEN，且重新计时。** §34.6.1 的判断被采纳：0/216 这个读数**不构成对 R21 的任何输入**，因为它来自错误的比较基准。RE-3 之后 D-6' 才第一次成为良定义的观测窗口。⛔ **在 D-6' 出读数之前，不得调整该常量。** 数值保持 0.5 |
| **R22**（去均值只对**加性**亮度偏移免疫） | **OPEN，本轮仍未被检验。** D-1b PASS 但该场景是「全静止」，**从未包含曝光剧变**；且 §34.1.1 指出 D-1b 连原始日志都没有。⇒ D-1b' **必须**新增一个乘性增益场景（镜头扫过窗户 / 开关灯）。若届时失败，处置方式按 §17.10 R22 原文：**改用归一化互相关，而不是继续上调阈值** |
| **R23**（`ISSUE-P4-TRACK`） | 状态不变，P2。**新增一条重开前置**：§17.8.4 条件 1 原文是「Phase 4A 的 A+B 已验收且稳定」—— 本轮 B 未交付 ⇒ 该条件**未满足**，`ISSUE-P4-TRACK` 距离重开又远了一步。且 §17.8.4 条件 2（ISSUE-P4-DECODE 结案）随 §18.4 的降级而进一步推后 |
| **R24**（新） | **D-4 的 50 ms 线仍无推导。** 本节选择不动线而改设计（§18.1.3），因此这条线**当前不构成约束**（由构造满足）。但它的无推导状态未被消除：**若将来任何改动重新引入多单元批次，必须先给出推导再谈是否满足**，不得直接沿用 50，也不得直接挪线。可用的推导锚点：D-7 的 195 ms 线是本项目第一条给出推导的延迟门控线（§18.3.4），50 ms 若要保留应从同一个 tap 预算反推 |
| **R25**（新） | **冻结原点基准会否决合法的大幅形变** ⇒ 该实例 mask 冻结，直到 REC-1（转回来）或 REC-2（重新 tap）。**本轮零数据。** D-1a' 必须包含大幅形变目标以量化边界。若实测显示常见形变即被否决，候选处置：下调 `reAnchorAcceptIoU`（R21 的调参窗口），**不得**改回可推进的基准（§18.2.4 已否决棘轮方案） |
| **R26**（新） | **能力 A 的真实陈旧度界是 embedding 的刷新周期（~5 s），不是 `minReAnchorIntervalMs`（300 ms）。** 这不是 RE-1 引入的限制，是 A-12 一直如此而 §16/§17 都没说出来。⇒ 对外描述 re-anchor 的能力时**必须用 5 s 这个数**。若将来需要更紧的界，唯一的杠杆是 background refresh 的 5000 ms 门控，**而那属于 encoder 排期，不属于 re-anchor** |
| **R27**（新） | **Pin 与 re-anchor 的交互未裁决。** 两个具体问题：(i) 保存 Pin 的瞬间 mask 正被 re-anchor 替换的竞态；(ii) 一个已被 Pin 固定的实例是否还应继续被 re-anchor 刷新（Pin 的语义是「冻结这一刻」还是「持续跟踪这个东西」）。⇒ 归 Phase 4B Day 5 的 Architect 裁决，**Day 4 不得夹带**（§18.3.3 硬隔离第 3 条） |
| **R6**（场景运动脱节） | 状态不变，仍未关闭。补救不改变这一点：`canonicalPoint` 仍冻结，物体走出锚点时 mask 仍留在原地 —— 但 RE-3 之后它**不会再变成别的东西**，这正是能力 B 的全部内容 |
| **约定 A-1**（TTL 适用性） | **不可重开时间型 TTL**，判断不变。自检问法「它过期之后谁把它变回来？」的答案现在收窄为：「**当 embedding 更新了、锚点处画面变了、且新结果仍认得出是用户选的那个东西时，re-anchor 会 —— 周期约 5 s。**」比 §17.10 的表述更窄、更准确 |
| **ISSUE-P4-DECODE** | **OPEN**，判据表作废（§18.4），优先级 **P1 → P2**，重开时机为 Phase 4B 完成之后。本轮只执行前置埋点 B-11 / B-12 |
| **V-3 / G-5 / D-15.1** | ✅ **维持**（§34.8.2：慢路径 1 次、`encode=own` 0 次）。补救只减少 decode、不新增 encode ⇒ 无需重测 |
| **§16.7 七条禁令** | ✅ **全部维持 INVIOLABLE，本节未放宽任何一条**（§18.1.6 逐条核对），且「禁止调 encoder」已获真机验证（15 次 encode 全部来自 background refresh） |
| **R3 禁令参数 / 冻结文件** | ✅ 未涉及。`SAMDecoder.swift` / `MaskRenderer.swift` / `buildTapAlpha` 零触碰 |
| **M-15.3**（无效的证明只在被测过的条件域内有效） | ✅ 被 §34.3.4 完整证实，并在本节第三次应用（拒绝据预期关闭 R4） |

---

### 18.8 Builder 变更清单（编号接续 §17.6）

**新增（5 项）：**

| # | 内容 | 位置 | 要求 |
|---|------|------|------|
| **B-13** | `embeddingGeneration: UInt64` + `TapInstance.lastReAnchorEmbeddingGen: UInt64?`（**RE-1**） | 世代号与 `embeddingCache` 同 `stateLock`（encoderQueue 写、videoQueue 读，§17.9.3 形态）；实例字段用 `TapInstanceManager` **既有 lock**（同 B-7） | 仅在 encode 成功写回新 embedding 时自增；置 nil 不自增。tap 路径不读不写 |
| **B-14** | `TapInstance.lastReAnchorAtMs: Double?` + 单实例选择规则（**RE-2**） | `checkAndFireReAnchor` 第 6 步 | 候选 = 「未消费当前世代」∩「自身 `d_i > contentThresholdLuma`」；取 `lastReAnchorAtMs` 最小者，平局取 `slotIndex` 小者。`beginBatch(count: 1)`，**节流状态机 / `batchId` / `pendingCount` 逐行不动** |
| **B-15** | `TapInstance.originAlpha: [UInt8]?`（**RE-3**） | 写入点**唯一**：tap 路径成功 `updateMask` 时。re-anchor 的 `updateMask` **只写 `maskAlpha`，不写 `originAlpha`** | 否决门比较对象由 `instance.maskAlpha` 改为 `instance.originAlpha`；门的位置、开关、日志行、失败分支**全部不变** |
| **B-16** | `DriftDetector.reAnchorEnabled: Bool = false`（特性总开关） | `checkAndFireReAnchor` 第 **0** 条 | ⚠️ **必须以 false 发布**，直到 §18.3.5 关闭条件满足且 Architect 单独批准。注释须写明与 `reAnchorConsistencyGateEnabled`（必须以 true 发布）语义相反 |
| **B-11 / B-12** | 日志单调时钟前缀 / `suspendRefreshOnly` 开关 | 见 §18.4.3 | B-11 前缀**必须在行首**，不得插入既有 tag 与字段之间（保护 §16.9.3 / §33 的全部 grep） |

**删除 / 替换（1 项）：**

| # | 现状 | 改为 |
|---|------|------|
| **B-17** | §17.5.3 第 6 步的 `maxDivergence = max over i of d_i` + 「一次 fire 刷新全部可绘制实例」 | 逐实例 `d_i` 独立判定 + 单实例选择（B-14）。⛔ 取 max 的逻辑**删除**，不保留 |

**保留（一行都不要动）：** §17.6「保留」表的全部 10 项继续有效；额外明确保留 `ReAnchorLoop` 节流状态机、`batchId` 世代号、三处清池点、T3 位置、`[REANCHOR]` 日志在质量门**之前**发出、`drawableInstances()` 批次成员、`capturedEmbedding` 锁内快照 + 先取 embedding 后认领的顺序。

**明确不要求做的事：** 不新增队列、不新增锁、不引入 tracker / CoreMotion / Vision、不改 `canonicalPoint`、不改 tap 路径的**判定逻辑**（B-15 只在既有 `updateMask` 成功路径上多写一个字段）、不改 `.segmentation` 路径任何一行、不动 `SAMDecoder.swift` / `MaskRenderer.swift` / `buildTapAlpha` / R3 禁令参数。

---

### 18.9 同一份契约第三次出缺陷 —— 共性归因

必须记录，不得淡化。三次缺陷及其发现者：

| # | 缺陷 | 出处 | 发现者 |
|---|------|------|--------|
| 1 | §16.3.1 的漂移信号是标定量，`translationDrift ≡ 0` | §16 | Builder（实现时上交） |
| 2 | §16.2.3 未处理「不可取消的在途闭包在复位后落地」⇒ 节流永久死锁 | §16 | Builder（自行修补并上交） |
| 3 | §17.3.3 的比较基准自反 + §17.5.3 的批次语义把「批内并行」当成了「批次间并发」 | §17 | Debugger（真机验收） |

**共性：三次都是「我推演了正常路径，没有推演该量/该基准/该输入在时间上如何演化」。**

- 缺陷 1：没问「这个量**会不会变**」（⇒ A-7）
- 缺陷 2：没问「已经在飞的工作**落地时**会做什么」（⇒ A-8）
- 缺陷 3a：没问「这个基准**会不会被自己放行的写入更新**」（⇒ A-14）
- 缺陷 3b：没问「这个输入**在我这个周期尺度上会不会变**」（⇒ A-13）

四条自检问法形状相同：**都是在问一个量随时间的行为，而不是它在某一时刻的值。** ⇒ **契约层纪律 M-18.1：任何涉及「循环 / 门控 / 周期任务 / 复位」的契约，在 Deliverable 确认之前，必须对其每一个输入量、每一个基准量、每一个被复位量，逐个走一遍 A-7 / A-8 / A-13 / A-14 四问，并把答案写进契约正文。** 四问全部只需要读赋值点，成本是分钟级；本项目为跳过它们付出的代价是三轮修订加一次真机验收失败。

**同时记录一条正面结果**（与 §34.10 一致）：三次缺陷全部由**下游**发现并上交，且每一次上交都附带了源码级证据与不擅自改设计的处置。§17.9.4 说「Builder 在本轮的判断质量高于契约本身」，本轮该评价扩展到 Debugger —— §34.7 不仅执行了我的判别设计，还**证伪了它并给出了更好的替代**（+11.50 ms 的并发分组对照是我没有设计的分析）。**这条上下游关系本身是当前项目最可靠的一项资产，其价值高于任何单条技术裁决。**

---

### 18.10 Deliverable 确认

- [x] §18.1 D-4 / R4 补救：确定性论证（decode+buildTapAlpha 对固定输入是纯函数）⇒ **RE-1 embedding 世代门**（无损删除 ≥50% 空转）+ **RE-2 批次恒为 1**（轮转选择，消除批内累积）；**判据线 50 ms 不动**及三条理由；**§16.7 一条都不放宽**及「独立低优先级队列」方案的三条否决理由；长期约定 **A-12 / A-13**
- [x] §18.2 D-1c 补救：**RE-3 冻结 tap 原点 alpha**；可陈述的端到端不变量；**恢复语义 REC-1/2/3**（吸收态定性为正确终态，REC-1 是冻结基准的免费红利）；成本坦白（R25）；「高阈值棘轮」折衷方案的定量否决；长期约定 **A-14**
- [x] §18.3 排期：三个鹰派选项逐条否决 + 保留 re-anchor 的理由；**Day 2–3 不关闭**（`reAnchorEnabled=false` 作为「暂停合入」的执行形态）；**Phase 4B Day 4 准许开工** + 三条硬隔离；重测集（D-1c' / D-4' / **新增 D-7 tap 争用，带推导的 195 ms 线** / D-6' / D-3' / D-1a' / D-1b'）；关闭条件六条；**九个复选框逐条处置**
- [x] §18.4 §16.8 修订：判据表 **VOID**；四项主张逐条裁定；三条作废理由升格为 **A-10（可叠加机制须写分解不得写二选一）/ A-11（残差不是证据；参照工况只能差一个自变量）** + 时间戳纪律；替代设计采纳 §34.7.6；**ISSUE-P4-DECODE 降为 P2**，本轮只执行 B-11 / B-12 两项前置埋点
- [x] §18.5 D-3 参照更正：删除 `~590 MB ±15 MB`；改为**同会话单调性（Q1→Q4 ≤ +30 MB）+ §27.7 的 N=0→3 = +31 MB 同工况参照**；RE-3 的 +2 MB/实例允量；更正我自己对 374.5 MB 峰值的 FIFO 误归因
- [x] §18.6 §17 两半分开搬运：§17.3 信号 ✅ 成立 / §17.3.3 安全声明 ⛔ 证伪（机制留、基准改、声明撤回）+ 两种误读的显式防护 + A-7 盲区与 A-7/A-13/A-14 三问表
- [x] §18.7 保留项：R4 **CONFIRMED 不关闭**（关闭条件 = D-4' ∧ D-7）/ R20 **关闭** / R21 **OPEN 重新计时，禁止提前调参** / R22 **OPEN 仍未检验，D-1b' 须加曝光剧变场景** / R23 推后 / 新增 **R24–R27** / R6 / A-1 收窄表述 / V-3 维持 / §16.7 与 R3 维持
- [x] §18.8 Builder 变更清单 **B-11 … B-17**（新增 5 / 替换 1 / 保留全部 §17.6 项）
- [x] §18.9 三次缺陷共性归因 + 契约层纪律 **M-18.1**（Deliverable 前对每个输入量/基准量/被复位量走 A-7/A-8/A-13/A-14 四问）

---

END OF PHASE 4 DAY 2–3 ARCHITECT REMEDY RULING（§18 D-4 / D-1c 补救裁决）

---

# Phase 4B — Day 4 Architect 裁决（§19 PinStore 存储方案）

日期：2026-08-17
依据：tasks.md Phase 4B Day 4 Architect 条目 + §18.3.3 三条硬隔离 + §3（几何链）/ §4（TapInstanceManager）/ §5（线程模型）/ §14（Phase 3 冻结）/ §17.2.0（能力 A/B/C 划分）/ §18（RE-1/2/3、§16.7 禁令、ISSUE-P4-TRACK）+ debug_report §33（延迟基线）/ §35（`originAlpha` 实为 **64 KB/实例**，不是 2 MB）
源码核实：`Interaction/FrameGeometry.swift`、`Interaction/TapInstanceManager.swift`、`Interaction/PointPromptBuilder.swift`、`Detection/CameraManager.swift:1117–1360`、`Shared/SAMConfiguration.swift`

> ⚠️ **本节不是 re-anchor 补救章节。** STOP RULE（tasks.md，用户裁定 2026-08-17）禁止为 re-anchor 新开第 5 轮补救；§19 是 **Phase 4B Day 4 的存储裁决**，与 D-1c'' / D-6'' 无耦合，不引用、不修改、不延期 Day 2–3 的任何条目。

---

## §19 PinStore 存储方案裁决 + Pin schema + 重访契约

### 19.1 存储后端裁决

#### 19.1.1 先把数量与字节量算出来，再选后端

裁决依据 tasks.md 写的是「Phase 4 预估 Pin 数量 + 是否需要跨设备同步」。这两个量必须先落成数字，否则 A/B 之争是口味之争。

**每条 Pin 的字节量（推导，非估计）：**

| 组成 | 依据 | 字节 |
|------|------|------|
| `id` (UUID 字符串) | 36 字符 | 36 B |
| `canonicalPoint` (2×Double) | §2.3 Canonical 像素空间 | 16 B |
| 几何快照（origW/origH/rotation/mirrored/promptSpace/encoderInputSize + 3 个诊断量） | §19.2.3 | < 120 B |
| `tag` / `note` | 上限 64 / 2000 字符（§19.2.5） | ≤ 2.1 KB（典型 0） |
| 时间戳 ×2（epoch 秒 Double） | | 16 B |
| **JSON 元数据小计（典型）** | | **≈ 0.4–0.6 KB，上限 3 KB** |
| mask PNG（256×256 8-bit 灰度，值域 {0,255}） | §35.6.3 的 256×256 不变量；二值图 PNG 压缩率极高 | **典型 1–3 KB，上限 ~8 KB** |

⇒ **典型每 Pin ≈ 2–4 KB，最坏 ~11 KB。**

**数量：** Phase 4B 的 Pin 由人工产生 —— 长按 → 弹 sheet → 输入标签 → 保存。单次使用产出个位数，重度用户跨月累积数百。

| N | 元数据总量 | blob 总量 | 全量载入解码 |
|---|---|---|---|
| 10 | 6 KB | 30 KB | < 1 ms |
| 100 | 60 KB | 300 KB | ~2 ms |
| 1,000 | 0.6 MB | 3 MB | ~20 ms |

**设计目标定为 N ≤ 1,000；Day 7 的 50 Pin 压测在此目标内。** N ≥ 10,000（例如将来「自动为每个 YOLO 检测建 Pin」）**不在本裁决射程内，届时须重新立项裁决，不得把本方案往上拉伸。**

#### 19.1.2 Day 6 实际需要的查询

tasks.md Day 6 的 PinList 只要三件事：按 `createdAt` 排序、按 `tag` 筛选、按 `id` 取单条。在 N ≤ 1,000 的**常驻内存数组**上，这三件事分别是一次 sort（~0.1 ms）、一次 filter（微秒级）、一次字典查找。**没有任何一条查询需要数据库。**

#### 19.1.3 iCloud 同步：**不在 Phase 4 范围内**（明确裁定）

四条理由，第三条是决定性的：

1. App 内没有账号 / 身份模型，引入同步等于引入一整套本项目没有的概念。
2. 同步立刻带来冲突解决设计（两台设备编辑同一个 `tag`），与本 App 的价值主张无关。
3. **决定性：Pin 的语义是设备与场景局部的。** §17/§18 反复确立「Pin 的价值在于**可重新 decode**，不是截图」。一条 Pin 是指向某个物理场景中某个像素位置的可重解指针；把它同步到一台从未见过该场景的设备上，能交付的**只剩缩略图** —— 恰恰就是我们宣布 Pin 不是的那个东西。**同步一个场景绑定的对象，语义上是空操作。**
4. 设备迁移这个真实需求已由 iCloud **备份**覆盖（Application Support 默认参与备份），无需同步。

#### 19.1.4 裁决：**候选 C —— Manifest JSON + Sidecar Blob（采纳 B 的精神，拒绝 B 的形态；拒绝 A）**

```
<Application Support>/Pins/
├── manifest.json          ← 全部元数据，单文件，原子写
├── manifest.v1.bak        ← 迁移前备份（仅迁移时产生）
└── masks/
    ├── <uuid>.png         ← 256×256 8-bit 灰度 mask，一 Pin 一文件，不可变
    └── …
```

**为什么不是候选 A（CoreData）：**

| 反对理由 | 说明 |
|---|---|
| 引用语义与本项目的纪律冲突 | `NSManagedObject` 是 context 绑定的引用类型、非 Sendable。本项目的**全部**跨队列纪律建立在值类型快照上（§17.9.3 `capturedEmbedding` 锁内快照、`tapGeometryMirror`、`tapInstances.snapshot()`）。把一族 context 绑定对象放进来，等于把本项目已经踩过三次的「跨队列未同步读」缺陷类重新请回来 |
| `.xcdatamodeld` 是二进制产物 | 本项目的契约靠评审存活；一个不可 diff 的 schema 产物会让 schema 变更绕过评审 |
| `viewContext` 是主线程 I/O 的诱饵 | 与 Carry-over Constraint「写入必须异步，不得影响实时推理帧率」同向相反 |
| 收益为零 | 谓词查询 / faulting / 分页在 N ≤ 1,000、总量 ≤ 3 MB、数据集常驻内存时**没有可兑现的收益** |

**为什么不是候选 B 的字面形态（单个 JSON 文件装下一切）：** 把 `thumbnail: Data` 塞进 JSON 意味着 base64，体积 +33%，且每次读取都要让 JSONDecoder 过一遍全部二进制载荷，每次写入都要重排全部二进制载荷。**blob 和元数据的变更频率不同**（mask 不可变，`tag`/`note` 可变），放在一起就是把不可变数据绑上可变数据的写放大。拆开之后：改一个 tag = 重写 ≤ 60 KB 的 manifest，不碰任何 blob。

**为什么不是 SwiftData：** 部署目标是 iOS 17.0，SwiftData 可用，且工程里有 Xcode 模板残留 `JudgeE2/Item.swift`（`@Model final class Item`，全工程无引用）。仍然拒绝，理由与拒绝 CoreData 的第 1 条**完全相同且更强**（`@Model` 强制引用类型 + context 限定 + iOS 17.0 期的已知缺陷）。⇒ **B-26：Builder 删除 `Item.swift`**，以免将来有人以为工程里已经有一层存储。

#### 19.1.5 本裁决关闭掉的路（明写，避免将来被当成疏漏）

- ⛔ 关闭：谓词下推查询、faulting / 分页载入、note 全文检索（N 大时才有意义）。
- ⛔ 关闭：`NSPersistentCloudKitContainer` 的一行式同步。
- ✅ 保留的回退路径：`PinStore` **定义为 protocol**，`FilePinStore` 是唯一实现。若将来同步成为需求，替换的是 store 层单文件，且 N ≤ 1,000 可一次性全量迁移。**这条 protocol 边界是本裁决唯一的对冲成本，且成本为零。**

---

### 19.2 `Pin` schema（逐字段，含不变量）

> 本节取代 tasks.md Day 4 Builder 条目里的 `struct Pin` 草案。草案有**四处不成立**，逐条记在 §19.2.7。

#### 19.2.1 分层：运行时模型 vs 持久化记录

```
TapInstance（Phase 3 冻结，内存）
      │  仅取三个量：maskAlpha / canonicalPoint / createdAt   ← §18.3.3 硬隔离 1
      ▼
Pin（运行时值类型，UI 与 PinStore 之间流通）
      │  一一映射，无逻辑
      ▼
PinRecordV1 + PinGeometryV1（持久化 DTO，Codable，schemaVersion 绑定）
      │  manifest.json
      ▼
masks/<uuid>.png（sidecar blob，不可变）
```

**规则 P-A：持久化 DTO 与运行时结构必须是两个类型。** 不得让 `FrameGeometry` 直接 `: Codable` —— 那会把持久化格式钉死在一个属于几何链（§3）的运行时结构上，将来 `FrameGeometry` 增删字段就会**静默**改变磁盘格式。转换是两个显式的纯函数（B-20）。

#### 19.2.2 `PinRecordV1` 字段表

| 字段 | 类型（持久化形态） | 可变 | 定性 | 说明 |
|---|---|---|---|---|
| `id` | String（UUID lowercased） | ❌ 冻结 | **契约** | 主键，同时是 blob 文件名的 stem |
| `pointX` / `pointY` | Double / Double | ❌ **冻结** | **契约** | `canonicalPoint`，**Canonical 像素空间（origW×origH）**，见 §19.2.4 |
| `geometry` | `PinGeometryV1` | ❌ 冻结 | 见 §19.2.3 | 拍摄时几何快照 |
| `maskFile` | String?（`masks/<id>.png`） | ❌ 冻结 | 降级用 | nil = 无视觉降级物，Pin 仍合法（重解路径不需要它） |
| `maskWidth` / `maskHeight` | Int / Int | ❌ 冻结 | **契约** | v1 必须是 256 / 256。**显式持久化，使「mask 空间不变量被改动」可被检出而不是被误读** —— 这正是 ISSUE-P4-GATE（§35）的成因（维度前提错在规格、注释、调用点三处而自洽） |
| `maskNonZero` | Int | ❌ 冻结 | 自校验 | mask 空间内非零像素数。使 blob 的 roundtrip 可自证，并为重访相似度提供 §35.7.2 式的独立上界 |
| `iouPredAtCreation` | Double? | ❌ 冻结 | **仅诊断** | 创建该 mask 的 tap 的 `iou_pred`。⛔ 禁止参与任何门控或排序 |
| `tag` | String? | ✅ | 用户数据 | ≤ 64 字符（§19.2.5） |
| `note` | String? | ✅ | 用户数据 | ≤ 2000 字符 |
| `createdAt` | Double（epoch 秒） | ❌ 冻结 | 契约 | PinList 默认排序键 |
| `updatedAt` | Double（epoch 秒） | ✅ | 契约 | 每次成功变更后写。**tasks.md 草案漏了此字段，而 Day 6 的 AnnotationView 明写要显示「修改时间」** |
| `previewFile` | String? | — | **保留位** | ⛔ **Day 4/5 必须恒为 nil。** 见 §19.2.6 |

**显式不持久化（写进注释，不是留白）：** embedding、`mask: MLMultiArray`、`color` / slot / `isPrimary`（呈现态，重访时按 §3.3 重新分配）、`requestGen`、`viewPoint`（view 坐标随布局与设备而变，跨会话无意义），以及 `TapInstance` 的**全部** re-anchor 字段（`anchorSignature` / `lastReAnchorEmbeddingGen` / `lastReAnchorAtMs` / `originAlpha`）—— §18.3.3 硬隔离 1，不得引用。

#### 19.2.3 `PinGeometryV1`：哪些字段够用来重解，哪些只是诊断

源码核实（`PointPromptBuilder.buildPointPrompt`）：重解一次 point prompt 在数学上**只需要** `origSize` 与 `inputSize`；`scale` / `padX` / `padY` 由这两者在函数内部自行推出。

| 字段 | 类型 | 角色 | 判据 |
|---|---|---|---|
| `origW` / `origH` | Double | **重解数学（必需）** | `buildPointPrompt(origSize:)` |
| `promptSpace` | Int | **重解数学（必需）** | `SAMConfiguration.pointPromptSpace`（当前常量 1024）。⚠️ **`FrameGeometry` 里没有这个量** —— 这就是为什么 tasks.md 草案的 `frameGeometry: FrameGeometry` **不足以重解**（§19.2.7 第 2 条） |
| `rotationDeg` | Double | **兼容性判定（必需）** | 不参与数学（`canonicalPoint` 已在反演之后），但旋转不同 ⇒ Canonical 空间的语义不同 ⇒ 同一坐标指向不同东西 |
| `mirrored` | Bool | **兼容性判定（必需）** | 同上（前后摄切换） |
| `encoderInputSize` | Int | **诊断** | 1024/768 AB 开关。**因为 embedding 从不持久化，重解永远用当前 embedding**，所以它对重解没有约束力，只用于事后归因 |
| `letterboxScale` / `letterboxPadX` / `letterboxPadY` | Double? | **诊断** | ⛔ **禁止参与重解数学。** 它们是 `origW/origH/promptSpace` 的冗余函数，一旦被读进变换就构成第二条变换路径，违反 §2.3「变换路径唯一」 |

#### 19.2.4 `canonicalPoint` 是 Pin 的身份 —— 冻结不变量

> **不变量 PIN-1：`pointX` / `pointY` 在 Pin 创建时写入，此后任何代码路径不得改写。**
> 与 §16.7 对 `TapInstance.canonicalPoint` 的规定**同一条规则、同一理由**：它是「用户指的是哪儿」这个事实本身。编辑 `tag`/`note` 不得触碰它；重访不得「校正」它；将来的 tracking（`ISSUE-P4-TRACK`，能力 C）若需要移动锚点，那是**新建一条 Pin**，不是改写这条。
>
> 推论：`PinStore.update` 的签名只允许提交可变字段（`tag`/`note`），**不得**提供整条记录覆盖式的 `save(_ pin:)` 语义去改冻结字段。若 Builder 保留 tasks.md 的 `save(_:)` 单入口，则 store 必须在写入前对已存在的 `id` 校验冻结字段一致性，不一致 ⇒ 返回错误而不是静默接受。

#### 19.2.5 字段长度：拒绝，不截断

`tag` > 64 / `note` > 2000 ⇒ `PinStore` 返回 `.failure(.fieldTooLong(field:))`，**不得静默截断**。理由是 §10.4 要求 C 的同一条：静默地把用户的数据改小，与静默清除 `tagProcessing` 属同一失败类。UI 层负责在输入处就限制，store 层是最后一道断言。

#### 19.2.6 缩略图是什么、以及它**永远不能**是什么

**裁决：持久化的视觉物是 `masks/<id>.png` —— 256×256、8-bit 灰度、值域严格 {0,255}、`maskAlpha` 的逐字节无损序列化。不是 128×128。**

- 128×128 在本系统里**不对应任何不变量**。真正的不变量是 **256×256**（`MaskRenderer.AlphaResult.alpha`、`compositeLayers` 的 `guard layer.alpha.count == 256*256`、`TapInstance.maskAlpha`，见 §35.6.3）。往 128 缩放要对二值图做重采样，产生中间灰度，再随便挑一个阈值二值化回来 —— **凭空引入一个从未被裁决过的参数**，而这正是 §35 那类缺陷的温床。
- 体积上 128 也换不来什么：二值 mask 的 PNG 在 256² 下典型 1–3 KB。为省 1 KB 引入一个重采样阈值，是坏交易。
- UI 需要的 128 pt 列表缩略图由显示层在渲染时缩放（`UIImage` / `contentsGravity`），**零持久化成本** —— 与 §35 的 D-13 同向（让 GPU 去缩放，别在 CPU 上预烘一个尺寸）。

> **不变量 PIN-2（缩略图的用途边界）。** `masks/<id>.png` 是**降级静态回退物 + 列表可视物**，仅此两用。它**永远不得**：
> 1. ⛔ 作为 `mask_input` / `has_mask_input` 喂回 decoder 作先验。当前 point prompt 契约是「零先验」（`PointPromptBuilder`：`maskInput` 全零、`hasMaskInput = 0.0`，model_plan §A.1）。改动它是模型 I/O 契约变更，须 ML_Vision + Architect 裁决，**不得在 Day 5/6 夹带**。
> 2. ⛔ 被当作「该物体此刻存在」的证据。它只证明**当时**存在过。
> 3. ⛔ 与实时 mask 同形态合成上屏。静态回退必须在视觉上与活跃 mask 可区分（§19.4.4）。
> 4. ⛔ 参与任何门控判据。

#### 19.2.7 tasks.md 的 `struct Pin` 草案：四处不成立

| # | 草案 | 裁定 |
|---|---|---|
| 1 | `canonicalPoint: CGPoint // SAM 输入坐标（1024×1024）` | ❌ **注释错误，且是危险的那类错误。** 源码：`FrameGeometry.invertViewPoint` 产出的是 **Canonical 像素空间（origW×origH，如 1080×1920）**，→1024 空间的映射发生在 `PointPromptBuilder` **内部**（§2.3「归一化仅在 PointPromptBuilder 内部执行」）。若 Builder 按注释办事，Pin 会存下一个坐标空间不明的数，重解时不知道该不该再乘一次 scale |
| 2 | `frameGeometry: FrameGeometry` | ❌ **不足以重解。** `FrameGeometry` 不含 `promptSpace`，也不含 `encoderInputSize`；且它是运行时结构，不得直接 Codable（规则 P-A）。改为 `PinGeometryV1`（§19.2.3） |
| 3 | `thumbnail: Data // 128×128 mask PNG` | ❌ 改为 256×256 无损（§19.2.6），且字段改为**文件引用**而非内联 Data（§19.1.4） |
| 4 | 缺 `updatedAt` | ❌ 补上。Day 6 AnnotationView 明写要显示「修改时间」 |
| 5 | `CGPoint` / `CGFloat` 直接 Codable | ⚠️ 不接受。`CGPoint` 在 Apple 平台上合成为**无键的双元素数组**，那是实现细节，不是可依赖的磁盘格式。持久化必须是显式的 `pointX` / `pointY` Double 键（§19.5） |

---

### 19.3 写入路径与线程归属

#### 19.3.1 队列裁定：新增**恰好一条** serial queue

```
pinStoreQueue = DispatchQueue(label: "pin.store.io", qos: .utility)   // serial
```

**与 §16.7「禁止新增队列」的关系：** 不构成放宽。§16.7 约束的对象是 **re-anchor 路径**（§18.1.6 逐条重申过一条都不动），而 tasks.md §18.3.3 硬隔离 2 已把 PinStore 的队列归属**显式交给本次裁决**，同时禁止复用 `decoderQueue`。三条理由支持新建而非复用：

1. **不得复用 `decoderQueue`（`qos: .userInitiated`）** —— 磁盘写入排进解码队列会重造 §18.1 刚消除的那类队内累积（`qwait` 当前 0.14–0.60 ms，是 R4 未触发的唯一证据，不能拿它冒险）。
2. **不得复用 `videoQueue` / `sessionQueue` / `encoderQueue`** —— 前两者是实时路径，第三者是单 encoder 槽位。
3. `.utility` **严格低于** `decoderQueue` 的 `.userInitiated` 与实时捕获队列 ⇒ CPU/IO 争用时系统调度天然让位于推理。

**serial 即互斥：** 该队列同时充当 store 的唯一临界区，**不新增任何锁**。内存索引只在此队列上变更。

#### 19.3.2 读路径：内存索引，磁盘只在启动时读一次

```
启动 → pinStoreQueue.async { 读 manifest → 解码 → 建内存索引 → GC 孤儿 blob }
                                                    │
                                                    ▼ MainActor
                                            @Published pins: [Pin]（快照，值类型）
                                            @Published isLoaded: Bool
```

- `fetch(id:)` / `fetchAll()` **服务于内存索引**，不触磁盘。⇒ Day 4 的「读取 < 50 ms」由构造满足（这正是 §19.6 要重写该判据的原因）。
- blob **懒加载**：`masks/<id>.png` 只在 UI 真要显示它时读，读完可进一个上限 32 条的 LRU（≤ 256 KB）。全量载入时**不读任何 blob**。
- ⛔ 禁止在 App 启动的主线程上同步读盘。UI 在 `isLoaded == false` 期间显示载入态，**不得**显示「暂无 Pin」（把「还没读完」画成「没有数据」是同一类静默失败）。

#### 19.3.3 写路径：blob 立即写、manifest 合并写

| 对象 | 时机 | 形态 | 理由 |
|---|---|---|---|
| `masks/<id>.png` | 创建 Pin 时立即写 | 独立文件，一次成型后**不可变** | 不可变 ⇒ 无并发写、无覆盖竞态、无需合并 |
| `manifest.json` | 置脏 + **≤ 250 ms 合并窗**后 flush | 全量重写，`Data.write(options: [.atomic])`（临时文件 + rename） | 一次 tag 编辑只写 ≤ 60 KB；突发 N 次保存只重写一次 |

**强制 flush 点（三处，缺一不可）：** `scenePhase → .background`、`willTerminate`、以及**任何一次成功写入的 250 ms 合并窗到期**。前两处必须在返回前完成（同步等待 `pinStoreQueue` 上的 flush，此时无实时推理在跑，代价可接受）。

#### 19.3.4 失败必须可见

- 所有变更型 API 以 `Result<Void, PinStoreError>` **在主线程**回调；`PinStore` 另发布 `lastWriteError`。
- ⛔ 禁止 `try?` 吞掉写入错误。⛔ 禁止「UI 已显示保存成功、盘上没有」。UI 的成功态必须由回调驱动，不得乐观预渲染。
- 日志 tag `[PIN]`，行首带 B-11 单调时钟前缀。**成功与失败必须在日志上同形可辨** —— §35.9 的教训：一条恒真的「安全默认」分支在日志上与正常放行同形，把整个门变成了 no-op 而不留痕迹。

#### 19.3.5 不得扰动 tap 延迟 —— 保证的具体形式

§33 快路径预算：`total` mean **80.97 ms** / p95 **97.30**，其中 `decode` **63.70**（ANE/GPU）+ `post` **16.69**（后处理 + 主线程跳），其余各段合计 < 1 ms（`lock` 66 样本全 < 0.05 ms）。

PinStore 与该路径的**共享面**：

| 资源 | 共享？ |
|---|---|
| `stateLock` | ❌ 不触碰 |
| `TapInstanceManager` 的 lock | ❌ 不触碰（Day 5 的创建路径只**读**一次实例快照，在主线程手势回调里，见 B-27） |
| `decoderQueue` / `videoQueue` / `encoderQueue` | ❌ 不投递任何工作 |
| ANE / GPU | ❌ 不使用（PNG 编码是 CPU；`post` 段不含 PinStore） |
| 主线程 | 仅一次小数组的 `@Published` 变更，由用户动作触发，不在 tap 路径上 |
| CPU / 存储带宽 | ✅ 唯一共享面 —— 一次保存 = 一次 ≤ 8 KB blob 写 + 一次 ≤ 60 KB 原子写，`.utility` |

> **约束 PIN-3：`CameraManager` 中任何在 `videoQueue` / `decoderQueue` / `encoderQueue` 上执行的闭包内，不得出现任何 `PinStore` 符号。** 这是可被静态检查的（§19.6 P-5），比事后测延迟更早、更便宜。

---

### 19.4 重访 / 重解契约

> tasks.md Day 6：「点击 PinList 中某条 Pin → 加载 `frameGeometry` → 用 `canonicalPoint` 重新 decode → **恢复活跃 mask**」，验收「重访 decode 成功率 ≥ 80%（在相近场景下）」。
> **该表述按字面不成立**，与 §16.3.1（把标定量当漂移信号）、§17.1.4（锚点冻结与「随物体移动」自相矛盾）**同一缺陷类**：判据依赖一个系统里没有任何量能支撑的事实。**A-7 适用**（这个量是被测出来的，还是被算出来的？）。本节给出可实现的替代契约。

#### 19.4.1 缺陷陈述（先说清楚哪儿不成立）

1. **「恢复」这个词不成立。** embedding **不持久化**（§19.4.2）。重解使用的是**当前场景的 embedding**。所以这个操作的真实语义是：*在一个记住的像素位置上，对**此刻**镜头里的东西重新分割一次*。它可能是同一个物体，也可能是别的东西 —— 系统**没有任何存储量**能分辨这两种情形。
2. **「decode 成功率」几乎恒为 1。** `SAMDecoder` 对任何合法点都会返回**某个** mask；能失败的只有几何不兼容、无 embedding、超时这些结构性原因。把它当 80% 的判据，测的不是重访质量。
3. **「在相近场景下」是未经核验的外部前提** —— 与 §17.8.3 批评过的形态相同：判据需要一个测试者主观提供的 ground truth。

#### 19.4.2 embedding：**永不持久化**（不变量 PIN-4）

- 体积：`[1,256,64,64]` fp32 ≈ **4 MB / Pin**（fp16 亦 2 MB），是元数据的三个数量级。
- 语义：embedding 是**某一帧画面**的编码。持久化它 = 把「重新分割当前场景」变成「重新分割三天前那一帧」—— 那等价于一张截图，只是多花了 4 MB 和一次 decode。**§17/§18 反复确立的「Pin 不是截图」在这里被具体兑现。**
- ⇒ 重访时若没有当前场景的有效 embedding，走的是**与普通 tap 完全相同的慢路径**（encode + decode，§33 慢路径 mean 804.5 ms / p95 931.0），并复用 Tier 1 的持续脉冲 + 12 s 超时 UI（§15.3 D-15.1，🔒 FINAL）。**不引入任何重访专属的等待 UI。**

#### 19.4.3 准入门（重解前评估，全部合取）

| 门 | 条件 | 不满足时 |
|---|---|---|
| **G1 几何兼容** | 当前 `rotationDeg` **且** `mirrored` **且** `promptSpace` 与 Pin 记录**逐位相等** | ⛔ **拒绝重解**（见 §19.4.5 唯一允许的例外） |
| **G2 模式** | 当前 `AppMode == .tapToSegment` | 拒绝并提示切换模式（不得自动切模式） |
| **G3 相机就绪** | `currentFrameGeometry() != nil` | 拒绝，提示稍后重试（复用既有 "camera not ready" 文案） |
| **G4 池容量** | 重解产生的实例受 §3.2 **完全相同**的 FIFO(max=3) 规则约束 | 正常 FIFO 淘汰，无特例 |

**G1 的理由：** `canonicalPoint` 是 Canonical 空间里的坐标，而 Canonical 空间的**语义**由 rotation + mirrored 定义（`FrameGeometry.invertViewPoint` 的 Step 2/3）。竖持时的 (300, 900) 与横持时的 (300, 900) 指向画面上完全不同的地方，两者之间**没有可用的重映射** —— 差的不是坐标轴，是画面内容本身。

> ⛔ **不设「相似度门」。** 不得用 `IoU(存储 alpha, 新 alpha)` 去否决重解。理由：重访时相机本来就移动过，低 IoU 是**合法结果**而非错误；再加上 §35 的教训（一个未经真实执行验证的门比没有门更坏）。IoU **计算并展示、并记入日志，但不参与任何判定**（§19.4.6）。

#### 19.4.4 用户实际看到什么（三种结局，措辞受约束）

| 结局 | 触发 | 呈现 |
|---|---|---|
| **R-A 重解完成** | G1–G4 通过且 decode 产出 mask | 一个**普通的活跃实例**上屏，走 §3.3/§3.4 的常规配色与层次。文案必须是「**在该位置重新分割**」一类；⛔ **禁止**出现「恢复」「找回你的物体」等宣称物体同一性的措辞 |
| **R-B 拒绝重解** | G1 不满足（换了持机方向 / 前后摄） | 打开**静态查看器**显示 `masks/<id>.png`，并明写原因：「该 Pin 记录于<竖/横>持、<后/前>置摄像头，当前不匹配」。⛔ 不得静默降级成一张图而不给原因 |
| **R-C 重解失败** | 超时 / 无相机帧 / decoder 返回空 | 复用既有 Requirement C 失败横幅（§10.4 C），**再**提供静态回退入口 |

**R-A 的静态回退与活跃 mask 必须视觉可辨**（PIN-2 第 3 条）：静态图不得与活跃 mask 同形态合成上屏。

#### 19.4.5 唯一允许的坐标重映射：同向同宽高比、仅分辨率不同

若 `rotationDeg`、`mirrored`、`promptSpace` 全等，仅 `origW/origH` 与当前不同**且宽高比相等**（`|W'/H' − W/H| < 1e-3`，源于用户切换了拍摄分辨率），允许**唯一一种**重映射：

```
pointX' = pointX × (origW_now / origW_pin)
pointY' = pointY × (origH_now / origH_pin)
```

- 这是相似变换，语义保持。**除此之外不得有第二种重映射。**
- 重映射后**仍须**做 §2.3 Step 5 的边界裁剪。
- 重映射**不写回** Pin（PIN-1：`pointX/pointY` 冻结）；它是重解时的一次性派生量。
- 宽高比不等 ⇒ 视野被裁过 ⇒ 落到 R-B 拒绝。

#### 19.4.6 实现约束：重访必须表达为一次「合成 tap」

> **约束 PIN-5：重访不得新建第二条 decode 入口。** 它必须最终调用既有的 `CameraManager.handleTap(canonicalPoint:viewPoint:)`（或一个只做 G1/G4 校验 + 坐标派生、随后转调它的薄封装 `handleTap(fromPin:)`）。

理由 —— 免费继承 Phase 3 的**全部**不变量：`tapGeneration` / `requestGen` 排序、`inFlightTaps` 记账、快慢路径判定、park/drain、超时、Requirement C 失败可见性、几何变更清池、FIFO、「tap 落在已有 mask 内 ⇒ 提升为 primary 不重解」。任何平行的第二条路径都要把这些逐条重写一遍，而本项目已有三次记录表明**重写不变量就是缺陷的产地**（§18.9）。

**日志（Day 6，仅记录不判定）：**
```
[PIN] revisit id=<uuid8> geo=(ok|remapped|refused) path=(fast|slow) iou=<0.00–1.00|n/a> origin=<N>px new=<N>px
```
`iou` 在 **256×256 存储空间**内、`stride: 1` 全遍历计算（§35 第 2 项：真实全遍历仅 65,536 次 ≈ 30–60 µs，`stride: 4` 会把判据变成掷硬币）。`origin` / `new` 两个面积给出 IoU 的独立上下界，使该字段自身可交叉校验。

#### 19.4.7 对 Day 6 验收判据的建议（本节**不代 Day 6 裁决**，仅指出并给替代）

- ⛔ 「重访 decode 成功率 ≥ 80%（在相近场景下）」—— 建议作废，理由见 §19.4.1。
- ✅ 建议替代（自校验、无需外部 ground truth）：**同会话、机位不动**，对同一 Pin 连续重访 10 次，`[PIN] revisit` 行的 `iou` 中位数 ≥ 0.7 且 10/10 均为 `geo=ok`。机位不动这一条**由测试者控制、不由测试者判定**，判据本身只读日志里的数。
- ✅ 建议追加：切到横持后重访 ⇒ 必须 10/10 落到 R-B 且给出原因文案（负向路径必须被测，否则 G1 与 §35 的门同命）。
- 以上两条须由 Architect 在 **Day 5/Day 6 裁决时**正式确立（Day 5 还要一并裁 **R27**：Pin × re-anchor 交互、以及「Pin 的语义是冻结这一刻还是持续跟踪这个东西」）。**Day 4 不得夹带。**

---

### 19.5 Schema 迁移

Pin 跨 App 版本存活，**版本策略现在就定死，不留到出问题时再补。**

#### 19.5.1 信封格式

```json
{
  "schemaVersion": 1,
  "writtenAt": 1755400000.0,
  "pins": [ { … PinRecordV1 … } ]
}
```

版本号在**信封层**（单个数字），不在每条记录上：整个 manifest 一次原子重写，不存在混版本状态。

#### 19.5.2 三条规则

1. **加法优先。** 新增字段一律 `Optional` 且带默认值 ⇒ 纯加法演进**不需要**写迁移代码，只需 `schemaVersion` +1。
2. **删除 / 改名 / 改语义 ⇒ 必须写一步编号迁移。** 迁移链是一串纯函数 `[RecordVN] -> [RecordVN+1]`，按序执行，**不得跳版**。迁移前先把原文件复制为 `manifest.v<N>.bak`（保留最近 1 份），迁移后整体重写一次。
3. **前向版本拒绝写入。** 若 `manifest.schemaVersion > 当前支持版本`（用户降级安装），**以只读模式打开并显式告知**，⛔ 绝不重写 —— 否则一次降级会截断用户的全部数据。

#### 19.5.3 编码形态的硬性规定（防止「格式静默漂移」）

| 项 | 规定 | 理由 |
|---|---|---|
| 坐标 | 显式 `pointX` / `pointY` 的 `Double` 键 | ⛔ 不依赖 `CGPoint` 的合成 Codable（Apple 平台上是无键双元素数组，属实现细节） |
| 浮点 | 一律 `Double`，不用 `CGFloat` | `CGFloat` 的位宽随平台而定 |
| 时间 | epoch 秒 `Double` | ⛔ 不用本地化字符串，不依赖 `Date` 的默认编码策略（它随 `JSONEncoder.dateEncodingStrategy` 而变，改一次全局配置就静默换了磁盘格式） |
| 枚举 | 一律显式 `String` rawValue | 防止序号漂移 |
| `id` | UUID 的 lowercased `uuidString` | 同时是文件名，必须与文件系统大小写行为解耦 |
| `JSONEncoder` | 显式设置 `outputFormatting`、`dateEncodingStrategy`、`keyEncodingStrategy`，**不依赖任何默认值** | 默认值是 SDK 的、不是我们的 |

#### 19.5.4 blob 的版本化

blob 的格式契约由记录里的 `maskWidth` / `maskHeight`（v1 = 256/256）承载，而**不是**由 `schemaVersion` 隐含。载入时若读到非 256×256 ⇒ 记录标记为 `degraded`，blob 不用于任何合成，元数据仍完全可用（重解不需要 blob）。—— 这条正是 §35 的直接教训：维度前提必须随数据走，不能只活在注释里。

#### 19.5.5 目录位置

- `Application Support/Pins/`。**不是** `Documents`（会随 `UIFileSharingEnabled` 暴露给「文件」App），**不是** `Caches`（系统可回收，Pin 是用户数据，丢失不可接受）。
- Application Support 默认参与 iCloud **备份** ⇒ 换机需求由备份覆盖（§19.1.3 第 4 条），**不设** `isExcludedFromBackup`。
- 目录以 `withIntermediateDirectories: true` 创建；创建失败 ⇒ store 进入显式 `unavailable` 状态并向 UI 报错，⛔ 不得静默降级成「内存态 Pin」（那会让用户以为保存成功）。

---

### 19.6 Day 4 验收判据（交 Debugger，取代 tasks.md 两条）

> tasks.md 原判据：「PinStore 写入 10 条 Pin，读取耗时 < 50 ms」「App 重启后 Pin 数据持久」。
> 两条都**由构造满足**、失败不了 —— 读取走内存索引（§19.3.2），持久化走原子写。按 §17.8.3 的教训，判据要么可能失败，要么没有信息量。以下五条中 **P-1 / P-3 / P-4 为门控，P-2 / P-5 为必报**。

**P-1｜Roundtrip 逐字段保真（门控，自校验，无需外部 ground truth）**
写入 10 条各字段互不相同的合成 Pin（含：空 `tag` / 64 字符 `tag` / 含 emoji 与换行的 `note` / `maskFile == nil` 的 1 条 / 面积极小的 mask 1 条）。**冷启动**后 `fetchAll`：
- 条数 = 10；
- 每条**每个**字段与写入值相等（Double 逐位相等 —— 全链路无算术，不允许出现 epsilon 借口；字符串逐字节相等）；
- 有 blob 的记录，PNG 解回的 `[UInt8]` 与写入的 `maskAlpha` **逐字节相同**，且重数的非零像素数 == 记录里的 `maskNonZero`。
- PASS = 10/10 全字段；任何不符 **必须点名到字段**。

**P-2｜读取延迟 + 线性度（必报，含一条可失败的线）**
测 N=10 与 N=50 的**冷启动首次** `fetchAll`（含读盘 + 解码 + 建索引）与**热** `fetchAll`。
- 报告四个数；
- 门控线只有一条：`t(N=50) < 4 × t(N=10)`（线性度）。绝对值 50 ms 保留为**报告项**，不作达标线 —— 它测不出东西。
- 若线性度超标 ⇒ 存在意外的 O(N²) 或每条一次文件打开（例如误在全量载入时读了 blob，违反 §19.3.2）。

**P-3｜终止耐受（门控，三种杀法）**
- (i) 切后台后从切换器强杀 ⇒ 已确认的写入**一条不丢**；
- (ii) 一次 `save` 回调返回成功后**立刻** SIGKILL（Xcode Stop）⇒ 该条必须在盘上；
- (iii) 在 10 条连续保存的**中途** SIGKILL ⇒ 允许丢失未确认的写入，但重启后 store **必须能成功载入**，条数 ∈ [0,10]，且每条载入的记录都通过 P-1 的字段检查。
- (iii) 是唯一真正检验原子写的一条；原判据「重启后数据持久」检验不到它。

**P-4｜不扰动 tap 延迟（门控，同会话 A/B）**
`.tapToSegment`，**`reAnchorEnabled == false`**（§18.3.3 硬隔离 3）：
- A 臂：无任何 PinStore 活动，采 ≥ 30 次快路径 tap；
- B 臂：同一会话、同一热态，脚本每 500 ms 保存 1 条 Pin，采 ≥ 30 次快路径 tap；
- 读 §33 的六段埋点 `total`。门控：`mean(B) − mean(A) ≤ 5 ms` **且** `p95(B) − p95(A) ≤ 10 ms`。
- ⛔ **不得**与 §33 历史值 80.97 / 97.30 直接比 —— 那是另一个 session（§18.4 A-11：参照工况只能差一个自变量）。§33 只用于判断量级是否离谱。

**P-5｜热路径静态隔离（必报，零成本）**
对 `CameraManager.swift` 做静态检查：`videoQueue` / `decoderQueue` / `encoderQueue` 上执行的闭包内**不出现任何 PinStore 符号**（约束 PIN-3）。这条在写代码当天就能查，比事后测延迟早得多。

**Day 4 出场条件：** P-1 ∧ P-3 ∧ P-4 通过，P-2 / P-5 已报告。

---

### 19.7 Builder 变更清单（编号接续 §18.8 的 B-17）

**Day 4 范围内（B-18 … B-26）：**

| # | 内容 | 位置 | 要求 |
|---|------|------|------|
| **B-18** | `protocol PinStore` + 唯一实现 `FilePinStore` | 新文件 `JudgeE2/Persistence/PinStore.swift`、`FilePinStore.swift`（新目录，不进 Interaction / Detection） | 接口：`load()`、`fetchAll() -> [Pin]`、`fetch(id:) -> Pin?`、`create(_:completion:)`、`update(id:tag:note:completion:)`、`delete(id:completion:)`、`loadMaskImage(id:completion:)`。变更型 API 全部 `Result<Void, PinStoreError>` **主线程**回调（§19.3.4）。⛔ 不提供能改写冻结字段的覆盖式 `save` —— 若保留 `save(_:)` 名字，必须在写前校验冻结字段一致性，不一致返回错误（PIN-1） |
| **B-19** | `PinRecordV1` / `PinGeometryV1` Codable DTO | 同上 | 逐字段按 §19.2.2 / §19.2.3；显式 `pointX`/`pointY`；epoch 秒；显式 `JSONEncoder` 配置（§19.5.3）。`previewFile` 建字段但**恒 nil** |
| **B-20** | 两个纯映射函数 | 同上 | `PinGeometryV1(from: FrameGeometry, promptSpace: SAMConfiguration.pointPromptSpace, encoderInputSize: SAMConfiguration.encoderInputSize)` 与反向 `(origSize, promptSpace)`。⛔ **不得**给 `FrameGeometry` 加 `: Codable`（规则 P-A）。⛔ 反向映射**不得**读 `letterboxScale/PadX/PadY`（§19.2.3） |
| **B-21** | `pinStoreQueue`（serial, `.utility`）+ 合并写 | `FilePinStore` | blob 立即写；manifest 置脏 + ≤ 250 ms 合并窗 + `Data.write(options:[.atomic])`；三处强制 flush（background / willTerminate / 合并窗到期）。**不新增锁** |
| **B-22** | mask ↔ PNG 编解码 | 新文件 `MaskPNGCodec.swift` | `[UInt8]`(256×256, 值 {0,255}) ↔ 8-bit 灰度 PNG，**无插值、无重采样、无阈值参数**。DEBUG 下断言编码→解码逐字节一致。⛔ 不得引入 128×128（§19.2.6） |
| **B-23** | 内存索引 + `@Published pins` / `isLoaded` | `FilePinStore` | 索引只在 `pinStoreQueue` 上变更，向主线程发布**值类型快照**；启动异步载入；载入时 GC 孤儿 blob（文件存在但 manifest 无对应记录）；blob 懒加载 + ≤32 条 LRU；⛔ 全量载入不读 blob |
| **B-24** | 错误面 + `[PIN]` 日志 | 同上 | `PinStoreError`：`.unavailable` / `.fieldTooLong(field:)` / `.notFound` / `.frozenFieldMutation(field:)` / `.io(underlying:)` / `.schemaTooNew(found:supported:)`。日志行首带 B-11 单调时钟前缀；**成功/失败在日志上同形可辨**（§19.3.4） |
| **B-25** | 迁移脚手架 | 同上 | 信封 `schemaVersion`；有序迁移链数组（v1 时为空数组，但**结构现在就建**）；前向版本 ⇒ 只读 + 报错，⛔ 不重写；迁移前落 `manifest.v<N>.bak` |
| **B-26** | 删除 `JudgeE2/Item.swift` | 工程 | Xcode 模板残留 `@Model final class Item`，全工程零引用。确认工程**未链接** SwiftData / CoreData（§19.1.4） |

**Day 4 只做接口、不做实现（规格先落、Day 5/6 再建）：**

| # | 内容 | 归属 |
|---|------|------|
| **B-27** | `makeRecord(from instance: TapInstance, geometry: FrameGeometry) -> PinRecordV1` —— **只读** `maskAlpha` / `canonicalPoint` / `createdAt` 三个量（§18.3.3 硬隔离 1），⛔ 一个 re-anchor 字段都不读 | Day 5 |
| **B-28** | 重访入口 `handleTap(fromPin:)` —— 薄封装：G1–G4 校验 + §19.4.5 派生坐标 + 转调既有 `handleTap(canonicalPoint:viewPoint:)`。⛔ **不得**新建第二条 decode 路径（PIN-5） | Day 6 |

**明确不要求做的事（照抄 §18.8 的纪律并扩展）：**
不新增第二条队列、不新增锁、不动 `SAMDecoder.swift` / `MaskRenderer.swift` / `buildTapAlpha` / R3 禁令参数、不动 `.segmentation` 路径任何一行、不动几何链（§3）与 `PointPromptBuilder` 的零先验契约、不引用 `TapInstance` 的任何 re-anchor 字段、不向 `videoQueue`/`decoderQueue`/`encoderQueue` 投递任何工作、不在 Day 4 触碰 UI（PinCreationSheet / PinList / AnnotationView 全部属 Day 5/6）。

---

### 19.8 保留项与风险

| # | 项 | 状态 |
|---|---|---|
| **R27** | Pin × re-anchor 交互（保存瞬间 mask 正被替换的竞态；已 Pin 的实例是否继续被刷新；Pin 的语义 = 冻结这一刻 vs 持续跟踪这个东西） | **OPEN，归 Day 5 Architect 裁决**（§18.3.3 硬隔离 3）。Day 4 产物只在 `reAnchorEnabled == false` 上验收 |
| **R28** 🆕 | **重访的物体同一性无法核验。** 系统没有任何存储量能判断重解出的 mask 是否还是当初那个物体（§19.4.1）。当前处置：不做宣称、不设门、只记 IoU。真要解决，需要场景描述子或 tracking ⇒ 属 `ISSUE-P4-TRACK`（能力 C）辖区 | **OPEN，P2，不占 Phase 4B 排期** |
| **R29** 🆕 | `previewFile`（相机 RGB 裁剪预览）能显著改善 PinList 的可识别度（纯二值剪影常认不出是什么），但 RGB 帧**不在** §18.3.3 硬隔离 1 允许的三个数据源内 | **保留位已建、恒 nil。Day 5 与 R27 同批裁决**，届时需一并裁隐私与体积（~6 KB/Pin） |
| **R30** 🆕 | N ≥ 10,000（例如「自动为每个检测建 Pin」）会使全量重写 manifest 的形态失效 | **超出本裁决射程。** 届时须重新立项，⛔ 不得把本方案向上拉伸 |
| **A-15** 🆕（长期约定） | **一个不变量的数值必须随数据一起持久化，不能只活在注释里。** ISSUE-P4-GATE（§35）的成因是 256×256 这个维度前提同时错在规格、注释、调用点三处而自洽 —— 因为没有任何一处**数据**承载它。⇒ 凡持久化 blob，其形状/单位/值域必须作为字段与之同存（本裁决的 `maskWidth`/`maskHeight`/`maskNonZero`） | 立 |
| **A-16** 🆕（长期约定） | **由构造满足的验收判据没有信息量。** 「读取 < 50 ms」在内存索引上不可能失败。判据必须是**可能失败**的：改成线性度（相对）、逐字段相等（自校验）、同会话 A/B（工况只差一个自变量）。承 §17.8.3 与 §18.4 A-11 | 立 |

---

### 19.9 Deliverable 确认

- [x] §19.1 后端裁决：字节量与数量**推导**（典型 2–4 KB/Pin，N ≤ 1,000 ⇒ ≤ 3 MB）⇒ **候选 C：Manifest JSON + Sidecar Blob**；逐条否决 CoreData（引用语义 / 二进制 schema / 主线程 I/O 诱饵 / 收益为零）、否决候选 B 的字面形态（base64 写放大）、否决 SwiftData；**iCloud 同步不在 Phase 4 范围**（决定性理由：Pin 是场景绑定对象，同步过去只剩截图）；对冲手段 = `PinStore` protocol 边界
- [x] §19.2 schema 逐字段 + 不变量 **PIN-1**（`canonicalPoint` 冻结，同 §16.7）/ **PIN-2**（缩略图四条禁令，含「不得回喂 decoder 作 mask 先验」）；缩略图裁定为 **256×256 无损**而非 128×128（256 是系统里真正的不变量）；tasks.md 草案**五处**不成立逐条裁定（1024 空间注释错误、`FrameGeometry` 不足以重解、128×128、缺 `updatedAt`、`CGPoint` 合成 Codable）
- [x] §19.3 线程：新增**恰好一条** `pin.store.io`（serial, `.utility`）—— 与 §16.7 无冲突的三条理由；serial 即互斥、**不新增锁**；读走内存索引、blob 懒加载；blob 立即写 + manifest ≤250 ms 合并原子写 + 三处强制 flush；失败可见（承 §10.4 C）；**共享面表**证明与 §33 的 tap 预算无交集；约束 **PIN-3**（静态可查）
- [x] §19.4 重访契约：指出 tasks.md 表述属 §16.3.1 / §17.1.4 同一缺陷类（**A-7 适用**）；**PIN-4** embedding 永不持久化及其语义理由；准入门 G1–G4；⛔ 不设相似度门（承 §35 教训）；三种结局 R-A/R-B/R-C 与**受约束的措辞**（禁用「恢复」）；唯一允许的坐标重映射；**PIN-5** 重访必须表达为合成 tap，不得建第二条 decode 入口；给 Day 6 的替代判据（建议，不代裁）
- [x] §19.5 迁移：信封 `schemaVersion`；加法优先 / 编号迁移链 / **前向版本只读拒写**；编码形态六条硬性规定（防格式静默漂移）；blob 形状随数据走（承 **A-15**）；目录 = Application Support（非 Documents / 非 Caches），参与备份
- [x] §19.6 Day 4 验收：**P-1** 逐字段 roundtrip（自校验）/ **P-2** 线性度（绝对 50 ms 降为报告项）/ **P-3** 三种杀法（含写入中途 SIGKILL —— 唯一真正检验原子写的一条）/ **P-4** 同会话 A/B 不扰动 tap（≤5 ms mean、≤10 ms p95，⛔ 不与 §33 跨 session 比）/ **P-5** 热路径静态隔离
- [x] §19.7 Builder 清单 **B-18 … B-26**（Day 4）+ **B-27 / B-28**（规格先落，Day 5/6 实现）+ 明确不要求做的事
- [x] §19.8 保留项：**R27** 归 Day 5；新增 **R28**（物体同一性不可核验）/ **R29**（`previewFile` 保留位）/ **R30**（N≥10k 出射程）；长期约定 **A-15**（不变量须随数据持久化）/ **A-16**（由构造满足的判据没有信息量）

---

END OF PHASE 4B DAY 4 ARCHITECT RULING（§19 PinStore 存储方案）

---

## §20 Phase 4A Day 2–3 关闭记录（STOP RULE 第 4 轮，2026-08-17）

> 本节是 Day 2–3 的**关闭记录**，不是第 5 轮补救裁决。依据：tasks.md 区块末尾的 **⏱️ STOP RULE**（用户裁定，2026-08-17），其效力高于本区块内其他条目。
> 本节**不新增任何 re-anchor 补救机制**、不新增常量、不放宽 §16.7 七条禁令、不触碰 R3 禁令参数、不改动 `SAMDecoder.swift` / `MaskRenderer.swift`。

### 20.1 第 4 轮真机数据与两条 PASS 判定

#### 20.1.1 采集口径（先声明，再用）

| 项 | 值 |
|---|---|
| 设备 / 构建 | iPhone 11（A13）/ Release，`.tapToSegment` |
| 会话 | **单次**，约 **73 s**，一条 `[t=]` 时间轴（段内可做时间推理）|
| 开关 | `reAnchorEnabled = true`、`reAnchorConsistencyGateEnabled = true`、`forceDriftForTesting = false` |
| 常量 | `reAnchorAcceptIoU = 0.5` / `contentThresholdLuma = 8.0` / `minReAnchorIntervalMs = 300`（**本轮零调参**）|
| 埋点 | B-18…B-21b 已落地（维度 `maskAlphaSide=256`、`stride=1`、接受分支追加 `iou:` / `origin:` / `new:`）|
| 样本 | `[REANCHOR]` **11 个单元**，其中 **5 个 rejected（45 %）** |
| 结束方式 | `Message from debugger: killed`（用户自 Xcode 停止）—— **不是崩溃**，不构成失败签名 |

#### 20.1.2 全部 11 条读数（本节唯一的一手数据）

| # | t (ms) | iou | origin px | new px | 面积比 | 判定 |
|---|---|---|---|---|---|---|
| 1 | 18022.1 | 1.00 | 482 | 482 | 1.00× | 接受（**逐位相同的 no-op**，TAP#1 后首次）|
| 2 | 19719.4 | **0.02** | 482 | 22910 | 47.5× | **否决** |
| 3 | 25354.9 | **0.03** | 482 | 14498 | 30.1× | **否决** |
| 4 | 30991.1 | 0.57 | 482 | 481 | 1.00× | 接受 |
| 5 | 36523.0 | 0.59 | 482 | 487 | 1.01× | 接受 |
| 6 | 46735.0 | 1.00 | 491 | 491 | 1.00× | 接受（**no-op**，TAP#4 后首次）|
| 7 | 48495.5 | **0.03** | 491 | 9851 | 20.1× | **否决** |
| 8 | 54134.4 | 0.89 | 491 | 490 | 1.00× | 接受 |
| 9 | 60001.4 | **0.02** | 491 | 18108 | 36.9× | **否决** |
| 10 | 65620.6 | 0.86 | 491 | 487 | 0.99× | 接受 |
| 11 | 71611.4 | **0.25** | 491 | 847 | 1.7× | **否决** |

会话内的 tap：TAP#1（t=16690，area 482，total 292.0 ms，post 227.0 = 首 tap 一次性成本，同 §33.2.4 / §35.9.2 第 12 项族）；TAP#2（t=41854，area 6329，`iou_pred=0.512 stab=0.05`）；双击 `clearAll`（t=43352，清 2 个实例）；TAP#4（t=44853，area 491，total 77.9 ms）。

⚠️ **TAP#2 是一次 tap 质量事件，不是 re-anchor 事件**：`iou_pred=0.512 / stab=0.05` 是模型自评在 tap 时刻就已给出的差 mask，与否决门无关（它随后被 `clearAll` 清掉）。**不得**把它计入 re-anchor 的账。

#### 20.1.3 判定一：**D-6'' PASS** —— `iou:` 字段第一次是真实读数

- 11 条读数中 **9 条 ≠ 1.00**，取值域 **0.02 – 0.89**，连续分布、跨两个数量级。
- 余下 2 条 1.00 **是真读数不是旧缺陷**：它们都紧跟一次 tap，是**同一 embedding 世代内的重复 decode**，其输出按 §18.1.2 已确立的纯函数性质**逐位相同** ⇒ IoU 必然为 1.00。这两条同时给出 `origin == new`（482/482、491/491），与该解释一致；而 §34/§35 的恒 1.00 缺陷会在 `origin` 与 `new` 相差 20–47 倍时也打印 1.00 —— 本轮第 2、3、7、9 条恰恰**没有**这样打印。⇒ **`iou:` 与面积比上界互相校验通过**（§35.7.2 设计该字段的目的即在于此）。
- ⇒ **R21 首次获得真实输入：否决率 45 %（5/11）。**

#### 20.1.4 判定二：**D-1c'' PASS** —— 无迁移

- 录屏确认 mask 全程稳定在 ~490 px 的目标上，**未发生迁移**。
- 四次**粗迁移**（面积 20–47 倍）全部被门拦下 —— 这正是 §34.4 与 §35.6 里产生 D-1c FAIL 的那一种失效形态（§34：431→10935→719；§35：同型）。**同一失效形态，这一次被拦住了。**
- **REC-1 得到确认**：第 2、3 条被否决之后，第 4 条（481 px）、第 5 条（487 px）自动接受；第 9 条被否决之后，第 10 条（487 px）自动接受。⇒ **物体回到锚点下，刷新自动恢复，无需重新 tap。** 这是 §18.2 把比较基准冻结为 tap 原点所换来的红利，本轮第一次被观测到（§35.9.1 条件 2 曾明确指出它「未被验证」）。

#### 20.1.5 本轮取代 §34 / §35 的哪些判定

| 被取代的判定 | 出处 | 本轮判定 | 为什么可以取代 |
|---|---|---|---|
| **D-1c FAIL**（mask 在 ≥3 个物体间迁移，0/216 否决）| §34.4 | ✅ **D-1c'' PASS** | §34 的系统用的是**自反基准**（比上一次刷新自己的产物），已由 RE-3 更换 |
| **D-1c' FAIL**（431→10935→719，0/76 否决，REC-1 未验证）| §35.6 | ✅ **D-1c'' PASS**，且 **REC-1 已验证** | §35 的系统里门**从未执行过一次比较**（ISSUE-P4-GATE，维度实参 1080×1920 vs 256×256）；B-18/B-19 修复后本轮是**第一个真正带门的系统** |
| **D-6 读数作废**（0/216 = 0 %，「不构成对 R21 的任何输入」）| §34.6 / §18.3.7 | ✅ **D-6'' PASS**，45 % | 同上：那个 0 % 不是「阈值太松」，是「门没跑」|
| **D-6' 读数结构性作废**（0/76，三个世界不可区分）| §35.7 | ✅ **D-6'' PASS** | B-21 把 `iou` 移到门**外**计算并打印 ⇒ §35.7.1 表里的 (a)/(b)/(c) 三个世界现在在日志上可区分 |
| **§18.2 RE-3「既未证实也未证伪」** | §35.10 | ✅ **证实** | 冻结原点基准在 11 条读数上工作正常，且 REC-1 被观测到 |
| **§17.3.3 一致性否决门「从未生效」** | §35.10 | ✅ **生效** | 45 % 否决率 + 拦下四次粗迁移 |

#### 20.1.6 本轮证据的边界（必须与结论同时搬运）

1. **n = 11，单会话、单设备。** 45 % 这个点估计的二项 95 % CI 约为 **[21 %, 72 %]** —— **有信息量的不是这个数值本身，而是它同时远离 0 % 与 100 %**（这正是 D-6 立项时写明的两个病态端点）。⛔ 不得把 45 % 当作可外推的稳态否决率。
2. **本轮没有 qwait / decode / 内存读数交到裁决层**，故本节对 R4、ISSUE-P4-DECODE、D-3' 的处置**不建立在新测量上**（见 §20.4）。
3. **D-1c'' 的正面依据是录屏 + `origin`/`new` 面积列**，不是对「mask 是否仍是同一物体」的独立真值标注 —— 系统里没有这个真值（同 R28）。它证明的是「**没有发生粗迁移**」，不是「mask 始终精确正确」。
4. 2/11 的 1.00 是 no-op，**不携带关于门的判别信息**；严格地说，门在本轮被真正考验了 9 次。
5. **`0.25 @ 1.7×` 这一条单独有价值**：它说明门**不是一个面积过滤器**。若把门换成便宜的「面积比在 [1/k, k] 内即放行」，这一条会被放行。⇒ 任何以面积比替代 IoU 的「简化」提案，在本数据上已被**预先否决**。

---

### 20.2 关闭裁决

#### 20.2.1 STOP RULE 触发的是「通过」分支

STOP RULE 的期限是「第 4 轮真机，仅此一轮」，只测两条：**D-1c''** 与 **D-6''**。二者**均 PASS**（§20.1.3 / §20.1.4）⇒ 命中表中第一行：

> ✅ **通过** | Day 2–3 **关闭**；`reAnchorEnabled = true` 发布；进 Phase 4B

**裁决（本节即为 §18.3.5 条件 6 所要求的 Architect 单独批准）：**

1. **Phase 4A Day 2–3 关闭**（2026-08-17）。
2. **`DriftDetector.reAnchorEnabled` 以 `true` 发布**，「暂停合入」（§18.3.6 / tasks.md D-4 条目原文）**即刻解除**。
3. **`reAnchorConsistencyGateEnabled` 仍必须以 `true` 发布**（§17.4 原文，不变）。两个开关语义相反，**不得混用**。
4. **进入 Phase 4B。** Day 4 已在并行进行（§18.3.3），本裁决解除其硬隔离第 3 条的前提（见 §20.3.4）。

#### 20.2.2 §18.3.5 六条关闭条件的处置

STOP RULE 明文「效力高于本区块内其他条目」⇒ **它取代 §18.3.5 的合取门作为关闭依据**。但取代不等于抹掉：六条各自的**事实状态**必须如实记录，未满足的部分转为保留项，不得因关闭而消失。

| # | 条件（§18.3.5）| 事实状态 | 处置 |
|---|---|---|---|
| 1 | RE-1/2/3 落地、编译通过、Builder 声明未触碰 §16.7 与 R3 | ✅ **满足** | §35.9.1 已确认；ISSUE-P4-GATE 修复（B-18…B-21b）后再次 `BUILD SUCCEEDED`，Builder 逐条声明未动 R3 禁令参数与两个冻结文件 |
| 2 | **D-1c' PASS**（含 REC-1 验证）| ✅ **满足（以修订判据 D-1c'' 计）** | §20.1.4：无迁移 + 四次粗迁移被拦 + **REC-1 首次被观测到**。D-1c' 与 D-1c'' 判据实质相同，D-1c'' 是它在「门真正执行」前提下的重述 |
| 3 | **D-4' PASS**（**三实例在屏** qwait max < 50 ms）| ⛔ **未满足，且本轮未执行** | 本轮 11 条 qwait 全部 ≤ **0.2 ms**，但**全程单实例**（TAP#2 短暂产生 `#2`，随即被 `clearAll` 清除）。§35 已把 qwait 的自变量钉死为**批内实例数**，单实例域的读数对三实例域**无判别力**（M-15.3）⇒ **不得计为 D-4' 通过。** 处置见 §20.4.2（R4 重新划范围）|
| 4 | **D-7 PASS**（`[D7'] total` p95 < 195 ms）| ✅ **满足** | §35.5.1：p95 **108.3 ms**。附采集边界（争用条件欠压，期望碰撞 ≈0.95 次）随 R4 一并转出 |
| 5 | D-3' PASS，且 D-6' / D-1a' / D-1b' **完成读数** | ✅ **满足**（一处残留）| D-3' PASS（§35.8.4）；本轮内存 208→232 MB、峰值 ~398 紧邻 background refresh、**无单向增长**，与 §35 同向（但同为单实例工况，只作旁证）。D-6' 的读数由 **D-6''** 补齐（§20.1.3）。D-1a'/D-1b' 有读数。**残留：D-1a' 后半句在 §35 是恒真命题（门未执行）**，本轮门已执行使该空洞在结构上消失，但**未在大幅形变目标上重测** ⇒ **R25 仍无直接数据**（§20.4.4）|
| 6 | 翻 `true` 须 Architect 单独批准 | ✅ **本节即该批准** | 见 §20.2.1 第 2 条 |

⇒ **四条满足、一条满足带残留、一条未满足（第 3 条）。** 若按 §18.3.5 的合取门，Day 2–3 仍不能关闭；**关闭的依据是 STOP RULE，不是合取门。** 这一句必须原样保留在记录里 —— 把未满足的条件说成满足，是本项目多次明令禁止的「把不方便的线挪走」（§15 / §18.1.3）。

#### 20.2.3 `reAnchorEnabled` 取值来历的澄清（消除一处悬疑）

Builder 在执行本次翻转时报告：打开文件时该字段**已是** `true`，并提出「暂停合入期间的构建产物是否可能是 enabled」的疑虑。

**该疑虑不成立，账目干净：**

- 上一轮 Builder 确实将其设回 `false`（§18.3.5 条件 6 / builder_progress 的「回滚」行，已验证）；
- 随后是**用户**按第 4 轮真机测试步骤**手动**改为 `true` 用于采集。

⇒ **「暂停合入」期间的构建产物确为 disabled；第 4 轮数据确为 enabled 下采集。两侧都无污染，§35.9.2 第 10 项就此结清。** Builder 本轮在该字段上的实际改动只是**把与实际状态矛盾的注释重写**（把「以 false 发布、翻转须批准」改为发布态说明），不是一次行为变更。

当前发布值（经 Builder 确认，`BUILD SUCCEEDED`）：

| 常量 | 值 | 依据 |
|---|---|---|
| `reAnchorEnabled` | **`true`** | 本节 §20.2.1 |
| `reAnchorConsistencyGateEnabled` | `true` | §17.4（禁止以关闭状态发布）|
| `reAnchorAcceptIoU` | `0.5` | **本轮不调**，理由见 §20.4.1 |
| `contentThresholdLuma` | `8.0` | §34.5.2 数据支持，本轮不调 |
| `forceDriftForTesting` | `false` | §17.7 |

---

### 20.3 能力交付声明

#### 20.3.1 能力 A（有界刷新）—— **已交付**

- **机制**：RE-1 embedding 世代门 + RE-2 批次恒为 1（单在途、丢弃而非排队）。
- **实测**（§35.3 / §35.4.1）：单元速率 1.02/s → **0.403/s（−60.5 %）**，优于 §18.1.4 的 ≥50 % 预测；re-anchor↔re-anchor 堆积 max 从 189.90 ms 降至 **0.6 ms**；decoderQueue 占空比 13.4 % → **3.0 %**。
- **本轮再次确认陈旧度界**：11 个单元中有 7 个相邻间隔落在 **5.53 – 5.99 s**（median ≈ 5.64 s），与 background refresh 的 embedding 周期一致 ⇒ **R26 的「真实陈旧度界 ≈ 5 s」第二次获实测确认**（§20.4.5）。
- **对外必须使用的措辞**：re-anchor 保证 mask **不会比最近一次 embedding 刷新更陈旧（≈5 s）**，⛔ 不得用 `minReAnchorIntervalMs = 300 ms` 描述该能力（A-12 / R26）。

#### 20.3.2 能力 B（语义保持）—— **已交付**

- **机制**：一致性否决门，基准为**冻结的 tap 原点** `originAlpha`（RE-3），`alphaIoU` 在 256×256 空间上 `stride=1` 全遍历（B-18/B-19）。
- **实测**：否决率 **45 %（5/11）**，拦下四次 20–47 倍面积的粗迁移与一次 1.7 倍的细迁移；**REC-1 自动恢复被观测到**。
- **「已交付」的精确含义（不得放大）**：
  - ✅ 交付的是 —— **mask 不会在用户不知情的情况下变成另一个物体**；被否决时保留用户原始选择的那张 mask；目标回到锚点下时刷新自动恢复，无需重新 tap。
  - ⛔ **没有**交付 —— 「mask 始终精确贴合目标」。目标离开锚点期间 mask **冻结在原地**（吸收态，§18.2 已定性为**正确终态**）。合法的大幅形变也可能被否决（R25）。这些是能力 C 的辖区。
- ⇒ **§17.2.0 的能力三分在此结账：A + B = Phase 4A 的交付物，两者均已交付。** §18.6.1 的「§17.3.3 安全声明被证伪」按 §35.9.2 第 9 项 (ii) 重述为：§34/§35 实际运行的是「**有 A、无 B**」的系统，其观测到的迁移反而是 §17.2.0「A 若无 B 是净退化」这一论证的纯净实证 —— 本轮是「**有 A、有 B**」的系统首次上机，迁移消失。

#### 20.3.3 能力 C（目标跟随）—— **维持推迟，归 `ISSUE-P4-TRACK`**

- 状态：**P2，不占 Phase 4B 排期**（R23 原文不变）。
- **§17.8.4 的四条重开条件一字不改**，其当前状态如下（这是状态更新，不是条件修改）：

| # | 条件 | 状态 |
|---|---|---|
| 1 | Phase 4A 的 A+B 已验收且稳定（D-1a/D-1b/D-1c 通过，D-6 接受率落在合理区间）| ✅ **本节起满足**（§18.7 曾记「未满足」，因当时 B 未交付；D-6'' 的 45 % 恰是「合理区间」的定义）|
| 2 | ISSUE-P4-DECODE 已结案 | ⛔ **未满足**（P2，重开时机为 Phase 4B 之后，§18.4）|
| 3 | 「tap 语义」的架构裁决先行（tap 的是一个点还是一个物体；§16.7 第二条禁令须正式撤销并给出替代不变量）| ⛔ **未满足**（未立项）|
| 4 | 判据按 §17.8.3 第 2 点重写，⛔ 不得沿用 15 px | ⛔ **未满足**（未重写）|

⇒ **合取不成立 ⇒ `ISSUE-P4-TRACK` 不重开。** ⛔ **不得在 Phase 4B 内以任何形式夹带 tracking 实现**（R23 原文）。

#### 20.3.4 §18.3.3 硬隔离第 3 条的解除与转出

原文：「**Day 4 产物不得在 `reAnchorEnabled == true` 的构建上验收**」，理由是 Pin × re-anchor 的交互未裁决（R27）。

- 本节把 `reAnchorEnabled` 发布为 `true` ⇒ **该条隔离在字面上已不可执行**（不存在 disabled 的发布构建）。
- **裁决**：硬隔离第 1 条（不得引用 `TapInstance` 的 re-anchor 字段）与第 2 条（不得在 videoQueue/decoderQueue 新增工作）**继续有效，不受本节影响**；第 3 条**解除**，其保护对象**全部转入 R27**，由 **Phase 4B Day 5 的 Architect 裁决**处理（Pin 保存瞬间 mask 正被替换的竞态；已 Pin 的实例是否继续被刷新）。
- ⚠️ **在 R27 裁决之前**，Day 4 的验收判据（§19.6 P-1…P-5）**不得引用任何 re-anchor 产生的 mask 状态**；若 Debugger 在 Day 4 验收中观测到 Pin 与 re-anchor 的交互异常，**记录并上报 R27，不得就地处置**。

---

### 20.4 保留项处置

#### 20.4.1 R21（`reAnchorAcceptIoU = 0.5` 零真机数据）—— ✅ **关闭**，并留下一条经验上界

R21 的原文范围是：「一致性否决门是新增机制，**零真机数据**；0.5 是工程判断非实测值；**D-6 接受率是它的唯一观测窗口**」。该窗口本轮首次产出读数 ⇒ **R21 的立项条件已清偿，关闭。**

**关闭时随之落账的三条数据（R21 的最终内容）：**

1. **否决率 45 %（5/11，95 % CI ≈ [21 %, 72 %]）。** 按 D-6 自身的读数规则：既非 ≈100 %（门形同虚设），亦非 ≈0 %（刷新从未生效）⇒ **落在合理区间，`reAnchorAcceptIoU` 无需调参。** ⛔ 调参禁令（§18.2.5「D-6' 出读数前不得调整」）**随本条关闭而解除**，但见第 2 条的双向夹逼 —— 解除禁令不等于存在调参空间。
2. **⚠️ 0.5 的余量比设计时假设的薄得多（本项目对该常量的第一条经验约束）。** 同一物体、面积几乎未变（482→481、482→487）的两次合法刷新，IoU 只有 **0.57 / 0.59**。⇒ **同一物体的轻微平移已经把 IoU 推到 0.6 附近。**
   - **上界推论**：`reAnchorAcceptIoU` **不得高于 ≈0.55**，否则本轮 5 条合法接受中会有 2 条被误否决（40 %）。⛔ 「接受率偏高就上调阈值」这条 D-6 立项时写下的处置路径，**在本数据上已被关闭** —— 上调空间几乎为零。
   - **下界侧**：0.25 @ 1.7× 被否决（§20.1.6 第 5 条）⇒ 下调也会很快牺牲判别力。**0.5 处在一个窄区间的中部，而不是像设计时假定的那样处在一个宽区间的保守端。**
3. **判别力的经验形态**：迁移事件 IoU **0.02 – 0.25**；同物体刷新 **0.57 – 0.89**；同世代 no-op **1.00**。三簇之间存在空隙（0.25 ↔ 0.57），这是 0.5 目前工作的**真正原因**，也是它唯一的余量来源。⇒ **任何会压缩这条空隙的改动**（换基准、换分辨率、换 stride、引入形变容忍）**必须先重测这三簇，不得沿用 0.5。**

⇒ 交叉引用：本条第 2 点同时是 **R25 的首条间接数据**（§20.4.4）与 **R28 重访 IoU 的首条量纲**（§20.4.6）。

#### 20.4.2 R4（decode 请求密集进入串行 `decoderQueue`）—— **重新划范围**，不按原判据关闭

R4 自 §10.5 立项、§15.6.2 判「未触发非已排除」、§34.3 转 CONFIRMED、§18.7 定关闭条件为「D-4' ∧ D-7」。**本节既不宣布它已排除，也不让它继续以现有形态挂在一个已关闭的区块上。**

**逐块结账（R4 被拆成三个可分别处置的部分）：**

| 部分 | 内容 | 状态 | 依据 |
|---|---|---|---|
| **R4-a** | re-anchor ↔ re-anchor 的队内累积 | ✅ **关闭**（实测 + 结构双重）| RE-2 使批次恒为 1、单在途 ⇒ 累积在结构上不可能；§35.4.1 实测 50/52 ∈ 0.1–0.6 ms，§34 的 189.90 ms 机制不复存在；本轮 11/11 ≤ 0.2 ms |
| **R4-b** | re-anchor 排在一次 **tap** 之后（re-anchor 让路）| ✅ **关闭，且方向与 R4 相反** | §35.4.2 逐行反推：两个越线样本（63.8 / 26.6 ms）均排在 tap 的 decode 之后。R4 要防的是 **tap 受损**；这是 **tap 优先** 的证据。⇒ 它不属于 R4 的风险面，**不应计入 D-4'** |
| **R4-c** | **tap 排在 re-anchor 之后**（tap 受损，R4 的真正风险面）| ⚠️ **未被测量，但已由构造定界** | §35.5.1：`[D7'] qwait` max **0.5 ms**，没有一次 tap 等过队列；§35.5.2 同时声明争用条件欠压（期望碰撞 ≈0.95 次）。**界**：单在途 + 批次恒为 1 ⇒ tap 最坏只等**一个** re-anchor 的 decode ≈ 61 ms，无界排队在结构上不可能 |

**裁决：**

1. **R4 以原形态结项**（原文描述的失效 = 「多个实例的 decode 请求更密集地进入串行队列 ⇒ **无界排队/堆积**」）。**无界排队现已在结构上不可能**，R4-a / R4-b 均已清偿。
2. **⛔ 不得表述为「R4 已排除」。** R4-c 从未在被专门制造的条件下测到过（M-15.3 三次援引，本节是第四次）。本节的处置是**把一个未测量的风险换成一个已知的算术界**，不是把它测掉了。
3. **残留以「有界的已知量」形态转出，不再作为保留项跟踪**：tap 最坏额外等待 ≈ **61 ms**（一次 re-anchor decode），叠加 §33.2 快路径 p95 97.3 ms ⇒ **≈158 ms < D-7 的 195 ms 判据线**（该线是本项目唯一有推导的延迟门控线，§18.3.4）。⇒ 该残留**已被 D-7 的预算覆盖**，无需独立编号。
4. **重开触发条件（写死，任一命中即自动重开 R4，且必须先按 R24 给出 50 ms 线的推导，⛔ 不得沿用 50 也不得挪线）：**
   - (i) 任何改动重新引入**多单元批次**或**多个在途 re-anchor**；
   - (ii) 任何改动在 `decoderQueue` 上新增第三类工作（Pin / tracking / 任何后台 decode）；
   - (iii) `[D7'] qwait` 在任何一次会话中出现 **> 5 ms** 的样本（当前观测上界 0.5 ms，留一个数量级余量）。
5. **D-4 / D-4' 的复选框处置**：⛔ **D-4 不勾选**。本轮 qwait 全部 ≤ 0.2 ms 但**全程单实例**，而 D-4' 明文要求三实例在屏；把单实例读数计为 D-4' 通过，正是 M-15.3 禁止的形态。tasks.md 上按「**未执行 —— STOP RULE 取代，去向见 §20.4.2**」记录。
6. **R24 维持 OPEN**（50 ms 线仍无推导）。它当前不构成约束（由构造满足），但在上述重开条件命中时立即成为阻塞项。

#### 20.4.3 R22（去均值只对**加性**亮度偏移免疫）—— **OPEN，降级 P2，转出 Day 2–3**

- **状态不变：仍未被检验。** §35.8.2 已证明它在当前埋点下**原理上不可判定**（曝光/AE 不进日志、未触发时的散度不进日志）；本轮亦无曝光剧变场景。⇒ 三轮真机之后 R22 的数据量仍是零。
- **⚠️ 但严重性本节下调，理由是能力 B 已交付**：R22 的失效后果是**误触发**。在 §34/§35 的无门系统里，一次误触发 = 一次不受约束的 mask 替换（可能换成别的物体）；**门生效后**，误触发的后果被限制为 **一次被否决或被接受的 decode** —— 内容未变 ⇒ 新 mask ≈ 原点 ⇒ 门放行且结果无害；内容真变了 ⇒ 那本就该刷新。⇒ **R22 的后果从「语义损坏」降为「一次浪费的 decode（≈55 ms，且被 `minReAnchorIntervalMs=300` 限流）」** ⇒ **P1 → P2**。
- **处置不变**（§17.10 原文）：若将来检验失败，正解是**改用归一化互相关**，⛔ **不得继续上调 `contentThresholdLuma`**。
- **Owner / 目标阶段**：Builder（`baseLuma` 埋点，§35.9.2 第 6 项规格）+ Debugger（重测），**与 ISSUE-P4-DECODE 同批**，时机为 **Phase 4B 完成之后**。⛔ **不构成继续做 Day 2–3 的理由。**

#### 20.4.4 R25（冻结原点会否决合法的大幅形变）—— **OPEN，首获间接数据，严重性上调**

- **直接数据仍为零**：D-1a'（大幅形变目标）本轮**未执行**；§35.8.3 的「结构性不可测」已随门生效而消失，但**没有人再测一次**。
- **⚠️ 但 §20.4.1 第 2 点是它的第一条间接数据，方向不利**：同一物体、面积几乎未变、仅轻微平移 ⇒ IoU 已降到 **0.57 / 0.59**。手的开合、人转身这类**大幅形变**几乎必然落到 0.5 以下 ⇒ **R25 从「理论边界、零数据」升为「大概率真实存在的边界」**。
- **表现形态**（用户可感知）：大幅形变的目标其 mask **冻结在形变前的形状**，直到 REC-1（形变回来）或 REC-2（重新 tap）。**这是能力 B 的既定代价，不是缺陷**（§18.2 已把吸收态定性为正确终态）。
- **处置**：⛔ **不得**改回可推进的基准（§18.2.4 已定量否决棘轮方案）；⛔ **不得**为它下调 `reAnchorAcceptIoU`（§20.4.1 已证明下调很快牺牲判别力，且 45 % 的否决率不支持放松）。**真正的解法是能力 C（tracking），不是调参。**
- **Owner / 目标阶段**：Architect，**并入 `ISSUE-P4-TRACK` 的立项材料**（R23，P2）。⛔ **不构成继续做 Day 2–3 的理由。**

#### 20.4.5 R26（能力 A 的真实陈旧度界 ≈ 5 s）—— ✅ **关闭，转为能力 A 的对外规格**

- §35.3.1 首次实测确认（单实例触发间隔 median 5710 / 5702 ms；三实例簇间隔 median 5775 ms）；**本轮第二次独立确认**（7/10 个相邻间隔 ∈ 5.53–5.99 s，median ≈ 5.64 s，与 background refresh 周期一致）。
- 两次独立会话、两种实例数、同一个数 ⇒ **不再是推演，是已确立的系统属性。R26 从保留项转为 §20.3.1 的对外措辞规范**（描述 re-anchor 能力时必须用 ≈5 s）。
- 唯一能收紧它的杠杆仍是 background refresh 的 5000 ms 门控，**那属于 encoder 排期，不属于 re-anchor**（原文不变）。

#### 20.4.6 R28（重访的物体同一性无法核验）—— **OPEN，状态不变，但首次获得可用量纲**

- **状态不变**：系统里没有任何存储量能判断重解出的 mask 是否还是当初那个物体（§19.4.1）；§19.4 的处置（**不做宣称、不设门、只记 IoU**）**维持不变**。
- **新增的输入**：本轮给出了同一会话内 `alphaIoU` 的经验分簇（迁移 0.02–0.25 / 同物体 0.57–0.89 / no-op 1.00）⇒ **Day 6 重访时记录的那个 IoU 第一次有了可解读的量纲**。
- **⚠️ 但它恰恰强化了「不设门」的裁决**：同一物体在**同一会话内、几秒之内、轻微平移**就已经掉到 0.57；跨会话重访（不同时刻、不同位姿、不同光照）的同物体 IoU **只会更低** ⇒ 任何 ≥0.5 的重访门都会把大量真实重访判成失败。⇒ **§19.4「⛔ 不设相似度门」由本轮数据获得正面支持，不再只是谨慎。**
- **Owner / 目标阶段**：Architect，Day 6 重访契约执行时引用；真解法属 `ISSUE-P4-TRACK`（能力 C）。

---

### 20.5 两条观察归档

> ⛔ **本节两条均 NOT 构成继续做 Day 2–3 的理由。** 它们被记录、被指派归口、被排到未来阶段，**不在本区块内处置**。按 STOP RULE：「不得以『再调一个参数就好了』『再补一次埋点就能定位』为由延长本区块。」

#### 20.5.1 OBS-1 / **R31（新）**：RE-1 的 no-op 缺口 —— 每次 tap 之后的**第一个** re-anchor 必然空转

**事实（两轮独立确认）：**

- `lastReAnchorEmbeddingGen` **只在一次 re-anchor 派发时被写入，tap 路径从不写它**。
- ⇒ tap 之后的第一次 re-anchor 看到的世代号是**上一个实例留下的**（或初值）⇒ 世代门必然放行 ⇒ 该次 decode 与 tap 自己那次 decode **同世代、同 `canonicalPoint`** ⇒ 按 §18.1.2 的纯函数性质**逐位相同**。
- **本轮观测**：#1（482→482，iou 1.00）与 #6（491→491，iou 1.00），**2/11 = 18 % 的单元是可证明的空转**。两者的下一个单元分别在 **+1.70 s / +1.76 s** 出现（远短于 5.6 s 的世代周期）—— 这正是「no-op 用掉了本世代的名额，真正的刷新要等下一个 embedding 世代」的签名。

**定性：**

- **正确性影响为零。** 逐位相同 ⇒ 门必然放行 ⇒ mask 无变化、无迁移风险；它也**不会**掩盖真实漂移（下一个世代照常触发）。
- **成本**：每次 tap 之后浪费一次 decode（本轮 mean ≈53 ms，在 `decoderQueue` 上）。这是 §18.1 声称「删除 ≥50 % 空转」时**未被删干净的那一部分**。
- **修法一行**：让 tap 路径在建立实例 / 更新 mask 时也写入 `lastReAnchorEmbeddingGen`。

**归口与排期：**

| 项 | 值 |
|---|---|
| 编号 | **R31**（保留项）|
| Owner | **Builder**（实现，一行）+ **Architect**（复核：写入点必须与 embedding 世代号的读取点在同一队列上 —— A-9）|
| 目标阶段 | **Phase 4B 完成之后，与 ISSUE-P4-DECODE 重开同批**（它减少的正是 decode 总量，两者共用同一次测量）|
| 优先级 | **P3**（效率问题，非正确性问题）|
| ⛔ | **不得在 Phase 4B 内夹带**；**不构成第 5 轮 re-anchor 补救** |

#### 20.5.2 OBS-2：`reAnchorAcceptIoU = 0.5` 的余量比设计假设薄 —— 已落账于 R21

- 事实与推论见 **§20.4.1 第 2 点**（同物体轻微平移即 0.57 / 0.59 ⇒ 上界 ≈0.55，上调空间几乎为零）。
- 本条**不另立保留项**：R21 已关闭，而这条约束是 R21 关闭记录的**永久内容**，任何将来触碰该常量的人必须先读它。
- **Owner**：Architect。**触发时机**（无排期，被动触发）：(i) `ISSUE-P4-TRACK` 立项时重写判据（§17.8.3 第 2 点建议的 IoU 型判据会直接继承这条量纲）；(ii) §19.4 的重访 IoU 记录；(iii) 任何压缩 §20.4.1 第 3 点那条「0.25 ↔ 0.57 空隙」的改动。
- ⛔ **不构成继续做 Day 2–3 的理由**：本轮否决率 45 % 落在合理区间，**没有任何证据要求现在动这个常量**。

---

### 20.6 回顾：四轮的代价，以及 gate 缺陷特有的教训

**四轮的形状**：§16 契约（信号是算出来的，不是测出来的 ⇒ 死信号）→ §17 换信号 + 范围重划（同时发现锚点冻结与「跟随物体」自相矛盾）→ §18 补救（RE-1/2/3：空转、批内累积、自反基准）→ ISSUE-P4-GATE 修复（门从未执行）→ 第 4 轮通过。

前三轮的教训已分别落为 **A-7**（信号存在性核验）/ **A-8**（复位与在途工作）/ **A-9**（交接指令 vs 同步不变量）/ **A-12 – A-14** / **M-18.1**（Deliverable 前的四问检查表）。**本节不复述它们**，只补 gate 缺陷**这一次**教出来的、上述条目一条都抓不到的东西 —— §35.9.3 已如实指出：本缺陷发生时 A-7 过（信号是活的）、A-13 过（输入会变）、A-14 过（基准已冻结），**四问检查表全部通过，门仍然从未执行**。

#### A-17（新，长期约定）：**安全默认分支必须在可观测面上与正常分支可区分**

`alphaIoU` 的 `else { return 1.0 }` 是一条**设计意图正确**的「无证据不否决」路径。缺陷不在它存在，而在**它的输出与「判断之后放行」在日志上完全同形** ⇒ 一个恒真的守卫把整个机制变成 no-op，**且不留任何痕迹**，两轮真机（216 + 76 个单元）都没能发现。

> **自检问法：「这个门在什么输入下会返回『放行』而不做任何判断？那条路径在日志/指标上，和『判断之后放行』长得一样吗？」**

- 与 A-7 / A-13 / A-14 的区别：那三条审问的是**输入量**；A-17 审问的是**输出路径的可观测性**。
- 落地形态（本项目已执行）：§35.7.2 要求 `iou` 在门**外**计算、`originAlpha == nil` 时打印 `n/a` 而不是省略该字段；并要求同时打印 `origin:` / `new:` 使 `iou` **可被面积比上界交叉校验** —— 本轮 §20.1.3 正是靠这条校验才能断言「1.00 这两条是真读数」。
- ⇒ **推论（对 Builder 的通用要求）**：凡新增「安全回退值」，必须同时给出一个能把它与正常值区分开的可观测量。

#### A-18（新，长期约定）：**缓冲区的形状必须与缓冲区同行，不得由调用方各自推导**

缺陷的物理形态是：`alpha` 是 **256×256 = 65 536** 字节的裸 `[UInt8]`，而它的形状以**两个 `Int` 实参**的方式独立旅行；调用点传了 `1080×1920`。**规格、注释、调用点三处自洽地错在一起** —— 三处同时错不是巧合，而是**一个事实没有任何单一载体时的必然结果**。

> **接口规则：裸数组 + 另行传入的 width/height 是一种可以自洽地错在多处的接口形态。形状必须作为类型或结构体字段与数据同行。**

- 与 **A-15** 的关系：A-15 说「不变量的数值必须随**持久化数据**一起存」（§19，PIN blob 的 `maskWidth`/`maskHeight`/`maskNonZero`）。**A-18 是同一条原则的内存侧**：A-15 管落盘，A-18 管在途。两者应成对引用。
- 本项目的兑现：B-18 新增 `DriftDetector.maskAlphaSide = 256`（`static let`，**非可调参数**），使调用点**无法第二次自行推导维度**。这是正确方向的最小形态；更彻底的形态（把 alpha 与其形状包成一个值类型）**不在本阶段排期**，⛔ 也不得作为重开 Day 2–3 的理由。

#### M-20.1（新，方法学）：**激活证据先于质量证据**

D-6 被设计为一个**质量指标**（接受率高低说明阈值松紧）。但 `rejected = 0` 这一个读数同时是**「门跑了但都放行」**与**「门根本没跑」**的表现 ⇒ 该指标在最需要它的那一档上**不具判别力**，于是它连续两轮（0/216、0/76）都被当成「阈值太松」来解读，而真相是机制从未运行。

> **规则：一个新机制的第一轮验收，第一条判据必须是「它执行了几次」，而不是「它执行得怎么样」。**
> 具体要求：任何新增的门/过滤器/否决机制，必须能报出「**已执行的比较次数**」，而不能只报「**被否决的次数**」。

- 与 **A-16**（由构造满足的判据没有信息量）的区别：A-16 管**不可能失败**的判据；M-20.1 管**无法区分「没跑」与「跑了且通过」**的判据。两者都是「判据本身没有信息量」的子形态，但自检问法不同。
- 与 **A-11**（残差不是证据）同族：都要求每个断言有自己的**正向**证据。

#### 关于 STOP RULE 这个工具本身（一条给排期的记录）

四轮里 Phase 4B **一行未动**。用户设下的期限没有改变任何技术判据，但它**改变了验收的形状**：从「再修一点再测一次」收窄为「**两条判据、一轮、二选一**」。而恰恰是这一轮拿到了三轮里最干净的数据（11 条可交叉校验的读数，胜过前两轮的 216 + 76 条不可解读的日志）。

> **记录**：判据的数量与迭代的次数不是同一个维度上的量。**前三轮的问题从来不是测得不够多，而是每一轮都在测一个还没被证明在运行的系统**（M-20.1）。⇒ 期限之所以有效，是因为它强制了一次收窄，而不是因为它施加了压力。

---

### 20.7 本节对既有条目的状态变更声明

| 条目 | 原状态 | 本节 | 依据 |
|---|---|---|---|
| **Phase 4A Day 2–3** | 不关闭，「暂停合入」 | ✅ **CLOSED（2026-08-17）** | STOP RULE 通过分支（§20.2.1）|
| **`DriftDetector.reAnchorEnabled`** | `false`（暂停合入）| ✅ **`true`（发布态）** | §20.2.1 第 2 条；本节即 §18.3.5 条件 6 所需的批准 |
| **能力 A（有界刷新）** | 已交付 | ✅ **维持已交付**，陈旧度界 ≈5 s 第二次确认 | §20.3.1 |
| **能力 B（语义保持）** | ⛔ 未交付（§34/§35）| ✅ **已交付** | §20.3.2 |
| **能力 C（目标跟随）** | 推迟，`ISSUE-P4-TRACK` P2 | **维持推迟**；§17.8.4 四条重开条件**一字未改**，其中条件 1 由本节起满足，2/3/4 仍未满足 ⇒ 合取不成立 | §20.3.3 |
| **D-1c**（原判据）| ⛔ FAIL（§34.4）| ✅ **以修订判据 D-1c'' 关闭** | §20.1.4 / §20.1.5 |
| **D-1c'**（§18 重测集）| ⛔ FAIL（§35.6）| ✅ **由 D-1c'' 取代并通过**，REC-1 首次被验证 | §20.1.4 |
| **D-6 / D-6'** | 读数两次作废 | ✅ **D-6'' 产出真实读数**，45 % | §20.1.3 |
| **D-4 / D-4'** | ⛔ FAIL（§34.3 189.90 ms）/ 字面 FAIL（§35.4 63.8 ms）| **本轮未执行**（无三实例工况）⇒ **不勾选**；R4 按 §20.4.2 重新划范围结项 | §20.2.2 条件 3 |
| **D-7 / D-3' / D-1a' / D-1b'** | §35 已判定 | **状态不变**；D-1a' 后半句的空洞随门生效而在结构上消失，但**未重测** ⇒ R25 无直接数据 | §20.2.2 条件 5 |
| **ISSUE-P4-DECODE 被动采集** | 判别设计作废，issue OPEN P2 | **状态不变，不勾选**（本轮无可用于假说判别的 decode 分组：全程单实例、n=11）| §20.4.2 / §18.4 |
| **§17.3.3 一致性否决门** | ⛔ 从未执行（ISSUE-P4-GATE）| ✅ **生效**（45 % 否决率，拦下四次粗迁移）| §20.1.3 / §20.1.4 |
| **§18.2 RE-3（冻结原点基准）** | 既未证实也未证伪 | ✅ **证实**（含 REC-1）| §20.1.4 |
| **R4** | CONFIRMED，关闭条件 = D-4' ∧ D-7 | **重新划范围**：R4-a / R4-b 关闭，R4-c 未测量但已由构造定界并被 D-7 预算覆盖；⛔ 不得表述为「已排除」；四条重开触发条件写死 | §20.4.2 |
| **R21** | OPEN，零数据，禁止调参 | ✅ **关闭**（45 % 否决率）；调参禁令解除但**上界 ≈0.55**，实际无调参空间 | §20.4.1 |
| **R22** | OPEN，原理上不可判定 | **OPEN，P1 → P2**（能力 B 交付后误触发的后果降为一次浪费的 decode）；转出，与 ISSUE-P4-DECODE 同批 | §20.4.3 |
| **R24**（50 ms 线无推导）| OPEN，由构造满足 | **状态不变**；R4 重开触发条件命中时立即成为阻塞项 | §20.4.2 |
| **R25** | 零数据，结构性不可测 | **OPEN，首获间接数据**（0.57/0.59 ⇒ 大幅形变几乎必然被否决）；严重性由「理论边界」升为「大概率真实」；并入 `ISSUE-P4-TRACK` 立项材料 | §20.4.4 |
| **R26** | 实测确认一次 | ✅ **关闭**，转为能力 A 的对外措辞规范（≈5 s）| §20.4.5 |
| **R27**（Pin × re-anchor）| OPEN，归 Day 5 | **OPEN，责任加重**：§18.3.3 硬隔离第 3 条本节解除，其保护对象全部转入 R27 | §20.3.4 |
| **R28**（重访同一性不可核验）| OPEN，P2 | **OPEN，状态不变**；首获量纲；§19.4「⛔ 不设相似度门」由本轮数据获正面支持 | §20.4.6 |
| **R31** 🆕 | — | RE-1 的 no-op 缺口（2/11 = 18 % 单元可证明空转），P3，Builder，与 ISSUE-P4-DECODE 同批 | §20.5.1 |
| **§16.7 七条禁令 / R3 禁令参数 / 冻结文件** | INVIOLABLE | ✅ **维持**：本节未放宽任何一条；`SAMDecoder.swift` / `MaskRenderer.swift` 未触碰 | 全节 |
| **§18.3.3 硬隔离 1 / 2** | 有效 | ✅ **继续有效**（第 3 条解除）| §20.3.4 |

### 20.8 边界声明

- 全部一手数据来自 **iPhone 11 / A13 单机、单次 ~73 s 会话、n = 11 个 re-anchor 单元**；跨设备不得外推（§10.5 R5）。
- 本轮**全程单实例**（TAP#2 短暂产生的 `#2` 随即被 `clearAll` 清除）⇒ **一切与「批内实例数」相关的量（qwait、批次占用、多实例内存）在本轮均无判别力**（M-15.3）。
- 本节**未做任何调参**；六个 §17.4 常量保持初始值。
- 本节**未新增 re-anchor 机制、未新增队列、未新增锁、未修改任何 Swift 源文件**；未勾选或修改 Builder 的任何 tasks.md 条目；未修改 `debug_report.md` / `builder_progress.md`。
- 本节**不是第 5 轮补救**：它不诊断、不提出修复、不要求重测。§20.4 / §20.5 中所有未清偿项**一律转出到 Phase 4B 之后或 `ISSUE-P4-TRACK`**。

### 20.9 Deliverable 确认

- [x] §20.1 第 4 轮数据（11 条读数全表）+ **D-6'' PASS**（9/11 ≠ 1.00，0.02–0.89；2 条 1.00 经 `origin`/`new` 交叉校验确认为同世代 no-op）+ **D-1c'' PASS**（无迁移、四次粗迁移被拦、**REC-1 首次验证**）+ 取代 §34.4 / §34.6 / §35.6 / §35.7 四条判定的对照表 + 六条证据边界（含 45 % 的 CI 与「门不是面积过滤器」）
- [x] §20.2 关闭裁决：STOP RULE 通过分支 ⇒ **Day 2–3 CLOSED**、`reAnchorEnabled = true` 发布、本节即条件 6 的批准；**§18.3.5 六条逐条如实处置**（4 满足 / 1 带残留 / **1 未满足且不掩饰**，关闭依据是 STOP RULE 而非合取门）；`reAnchorEnabled` 取值来历澄清（暂停期产物确为 disabled，第 4 轮数据确采自 enabled，**两侧无污染**）
- [x] §20.3 **能力 A 与 B 均已交付**，并精确界定 B 交付了什么、没交付什么；**能力 C 维持推迟**，§17.8.4 四条重开条件一字未改（条件 1 现已满足，2/3/4 未满足 ⇒ 不重开）；§18.3.3 硬隔离第 3 条解除并转入 R27
- [x] §20.4 保留项：**R21 关闭**（45 %；并留下 0.57/0.59 的经验上界 ≈0.55）/ **R4 重新划范围**（a、b 关闭，c 未测量但由构造定界 + D-7 预算覆盖，⛔ 不得称「已排除」，四条重开触发条件）/ **R22 OPEN 降 P2** / **R25 OPEN 首获间接数据、严重性上调** / **R26 关闭**转对外规范 / **R28 OPEN** 获量纲且强化「不设门」
- [x] §20.5 两条观察归档：**R31**（RE-1 no-op 缺口，2/11，Builder，Phase 4B 之后与 ISSUE-P4-DECODE 同批，P3）+ **OBS-2**（0.5 的余量，已落账于 R21，Architect，被动触发）；**两条均显式标注不构成继续做 Day 2–3 的理由**
- [x] §20.6 回顾：**不复述** A-7/A-8/A-9/M-18.1；新增 **A-17**（安全默认必须可区分 —— 采纳 §35.9.3 的数据层建议并立约）/ **A-18**（形状随缓冲同行，A-15 的内存侧）/ **M-20.1**（激活证据先于质量证据）+ 一条关于 STOP RULE 作为工具的排期记录

---

END OF PHASE 4A DAY 2–3 CLOSURE RECORD（§20）

---

# Phase 4B — 启动前烟测记录维护（§21 记录落账，非裁决轮）
# 2026-08-18
# 触发：debug_report.md §36（Phase 4B 启动前烟测，用户预先裁定处置为「记录，不修」）
# 范围：只做**登记维护** —— 落账 §36 支持的读数与状态变更，更正一处措辞，新增两个保留项编号
# ⛔ 本节不是第 5 轮 re-anchor 补救，不是重新裁决轮

## §21 §36 烟测的记录落账（2026-08-18）

> **本节是一次记录维护，不是一次决策轮。** tasks.md 的 ⏱️ **STOP RULE 及其禁止事项继续有效**；Phase 4A Day 2–3 **维持 CLOSED**（§20），本节不重开它、不重开 §20 已关闭的任何条目、不提出任何修复方案、不要求任何重测。
> 本节**不新增 re-anchor 机制、不新增常量、不新增队列、不新增锁**，不放宽 §16.7 七条禁令，不触碰 R3 禁令参数，不改动 `SAMDecoder.swift` / `MaskRenderer.swift`，不修改任何 Swift 源文件。
> 遵守 §15 确立的纪律（§17.0 / §18.0 重申）：**裁决与封层表不得原地改写。** §20 全文一字未改；本节以「补充 + 指向」的形态落账。唯一的原地更新是 **§10.5 的 R4 状态跟踪行**（保留项登记表是状态跟踪器，§18 已确立可原地追加）。

### 21.1 证据等级 —— 先声明，再用

§36 的一手证据是**用户粘贴的会话转录，磁盘上没有本轮的日志文件**（§36.9）。这一句必须与本节的每一个数字一起搬运：

| 项 | 本轮（§36）| 对照 |
|---|---|---|
| 证据形态 | **粘贴转录，无归档日志文件** | §34 / §35 有归档日志与完整性交叉校验 |
| n 的含义 | **「用户粘贴了多少」**，不是「系统产生了多少」 | §34.1.1 式的完整性校验**本轮无法执行** |
| 可回溯性 | **任何数字都不可回溯到原始行** | — |
| 证据等级 | **低于 §35，与 §20 同级** | — |

**转录缺失的字段及其后果（逐条，⛔ 不得被静默忽略）：**

| 缺失字段 | 后果 |
|---|---|
| `Mask logits range` / `iou_preds` / `Invalid input tensor …` | **§34.11 / §35.11 的 A13 ANE fp16 已知失效签名对照本轮做不了。** ⛔ 本节**不得**、也未被任何一处表述为「decode 输出健康」 |
| `drifted %.1flum` | **本轮零 `d_i` 读数** ⇒ 与散度信号有关的任何论断（含 §21.5）只能是**源码演绎**，不是数据读出 |
| `[CACHE] background refresh triggered` | 世代边界由单元间距 + RE-1 的结构性约束**反推**，非直接观测 |
| `Pre=…|Mem=` | 无内存读数 ⇒ D-3' 的门后复核结构上不可做 |
| `[D7'] total/post/decode` | 无端到端读数 ⇒ 本轮**不产生任何 D-7 判据输入** |
| `FPS=` / `Frame inference time` | 无帧率读数 ⇒ §36.6.2 次项 A 的 ≤31 % 上界**不可核实** |

⇒ **引用规则（写死）：本节落账的任何数字，在被引用时必须带「⚠️ 转录来源、无归档日志」这一句。** ⛔ 不得把一个转录来源的测量当作具有 §35 那一级归档溯源性的读数使用。这与 §9.2 的工况纪律（延迟数字必须标注冷/热态、并发负载、n）同族：**证据来源是数字的一部分。**

### 21.2 R4-c 首次获得测量值 —— 落账，**不关闭**

§20.4.2 把 R4 拆成 R4-a / R4-b / R4-c 三块结项时，R4-c 一行的状态逐字是「⚠️ **未被测量，但已由构造定界**」。§36.4 给出了它的第一组数字。**采纳 §36.4.6 的建议措辞**，以补充形态落账如下（⛔ §20.4.2 的表格与四条重开触发条件**一字未改**）：

#### 21.2.1 R4-c 状态与依据（补充 §20.4.2 表格中 R4-c 一行，不替换原文）

> **状态：** ⚠️ **首次获得测量（§36.4，2026-08-18），条件仍未压满，构造界不变**
>
> **依据：** §35.5.1：`[D7'] qwait` max 0.5 ms，同时声明争用条件欠压（期望碰撞 ≈0.95）。**§36.4 首次在「三实例在屏 + 刻意高频 tap（37 次）」下取样：n=32，max 4.9 ms / p95 3.5 ms / mean 0.45 ms，仅 2 个样本 > 1 ms。max 那一条经时间戳反推确为一次真实的 tap-排在-re-anchor-之后 的碰撞（TAP#34 @ t=74288.2 紧随 inst#2 的 re-anchor 单元 @ t=74282.5），即 R4-c 的形态本身。该值距重开触发条件 (iii) 的 > 5 ms 线仅 0.1 ms —— 按实测记录该余量，⛔ 不表述为「通过」。** ⚠️ 该轮期望碰撞仅 ≈0.31（低于 §35 的 ≈0.95，因高频 tap 抑制 re-anchor，§36.6 / 本节 §21.7）⇒ **4.9 ms 不是尾部估计**；反推显示该 tap 等的是 re-anchor 单元的 post-decode 尾段（≈10 ms）而非 decode 本身 ⇒ **界（≈61 ms）不变，测量已补。** **界仍被 D-7 的 195 ms 预算覆盖**（≈158 ms）。⚠️ 证据来源为粘贴转录，无归档日志（§21.1）。

#### 21.2.2 §20.4.2 第 4 点 (iii) 的状态注记（⛔ 条件本身一字不改）

> 〔状态注记，§36.4 / §21.2，2026-08-18〕**当前观测上界由 0.5 ms 更新为 4.9 ms**；该条件写下时假定的**一个数量级**余量已被压缩到 **2 %**。⛔ 条件文本、触发值（**> 5 ms**）、以及命中后的处置（**R4 自动重开 + 必须先按 R24 给出 50 ms 线的推导**）一字不改。

#### 21.2.3 三条必须与这个数字同行的限定

1. **⛔ 不写「通过」，写余量。** 实测 max **4.9 ms** vs 触发线 **> 5 ms** ⇒ 余量 **0.1 ms**。(iii) 的设计意图是一根**绊线**，不是一条合格线；「未越线」不等于「已证明安全」。
2. **4.9 ms 是一次相位走运的单次抽样，不是分布尾部。** 该 tap 等的是 re-anchor 单元的 **post-decode 尾段（≈10 ms）**，不是它的 **decode（≈55 ms）**。若同一次 tap 早到约 10 ms 落进 decode 期间，等待就是构造界量级的 **≈61 ms**。⇒ **构造界既没有被这次测量推翻，也没有被它证实；界仍然是界。**
3. **「用高频 tap 制造争用」这个协议是自相矛盾的。** 本轮期望碰撞 **≈0.31**，**低于** §35 的 ≈0.95 —— 刻意压满的那一轮，争用反而更弱。机制见 §21.7（tap 抑制 re-anchor 4.9×）。⇒ **M-15.3 的第五次援引，本节自我适用：可以说「R4-c 第一次拿到非零碰撞样本」，⛔ 不可以说「R4-c 在压满条件下最坏 4.9 ms」。**

#### 21.2.4 R4 的状态：**不变**

- **R4 维持 §20.4.2 的处置**：以原形态结项 + 四条重开触发条件监视。**本节补的是 R4-c 的第一条测量值，不是它的关闭依据。**
- ⛔ **仍不得表述为「R4 已排除」**（§20.4.2 第 2 点的禁令继续有效）。
- **R24 维持 OPEN**，状态不变；一旦任一重开触发条件命中，它立即成为阻塞项。
- **§20.4.2 第 3 点的残留账目不受本轮任何数据挑战**：tap 最坏额外等 ≈61 ms + §33.2 快路径 p95 97.3 ms ≈ **158 ms < 195 ms**。4.9 ms 仅占 D-7 判据线的 **2.5 %**，且本轮 32 次 tap 中 30 次的 qwait ≤ 0.4 ms ⇒ **对用户不可感知**。
- **一条协议记录（不排期、不执行）**：§36.7.4 ISSUE-2 指出，真正能压满 R4-c 的采集协议是**让全部 tap 落在已有 mask 内**（走 promote ⇒ 不新建实例 ⇒ 不重播散度基线 ⇒ re-anchor 不被抑制），配合持续晃动相机抬高 `d_i`。⇒ **此条仅在 R4 因 §20.4.2 四条触发条件之一被重开时才有意义**，登记于此是为了届时不必重新发现它。⛔ **本节不提议执行，STOP RULE 禁止。**

### 21.3 那 0.1 ms 的余量属于 `decoderQueue` —— §19.3 的队列裁决首次获得实测依据

这是 §36 唯一一条对 **Phase 4B** 有直接约束力的结论，本节予以采纳并落账。

#### 21.3.1 余量的物理含义

「离 5 ms 还有 0.1 ms」不是一个抽象数字。它的物理含义是：**`decoderQueue` 上目前恰好只有两类工作**（tap decode、re-anchor 单元），而在这个配置下**一次 tap 的排队时间已经能摸到 4.9 ms**。⇒ **再放第三类工作上去，它的每一次执行都直接加在 tap 的 `qwait` 上。**

⇒ 与 §20.4.2 重开触发条件 (ii)（「任何改动在 `decoderQueue` 上新增第三类工作（Pin / tracking / 任何后台 decode）」）合起来看：若 Phase 4B 的 PinStore 把 blob 写盘 / manifest 合并写 / 索引重建放上 `decoderQueue`，**(ii) 与 (iii) 会同时命中** —— (ii) 由构造命中，(iii) 由 IO 抖动命中（一次 flush 的量级远大于 0.1 ms）。

#### 21.3.2 §19.3 裁决的依据升级：从纯构造论证 → 构造论证 + 一条实测

§19.3.1 裁定 PinStore 新增**恰好一条** `pin.store.io`（serial, `.utility`），其三条理由中的第 1 条（「不得复用 `decoderQueue`」）当时的支撑是：**`qwait` 当前 0.14–0.60 ms，是 R4 未触发的唯一证据，不能拿它冒险** —— 那是一个**构造 + 谨慎**的论证，**没有测量支持**。

> **本节补入的实测依据（§36.4 / §21.2）：** 在 `decoderQueue` 只有两类工作、且没有任何新增 IO 的条件下，tap 的 `qwait` 已经取到 **4.9 ms**，距 §20.4.2 触发条件 (iii) 仅 **0.1 ms**。⇒ **把 Pin IO 放进 `decoderQueue` 会把这条余量整块吃掉，且是可预期地、而非概率性地吃掉。**
> ⇒ **§19.3.1 的队列裁决维持不变，理由等级由「构造论证」升为「构造论证 + 一条实测支持」。** ⚠️ 该实测的证据来源为粘贴转录、无归档日志（§21.1）—— 它**加强**该裁决，但不足以反过来支撑任何「因为余量只有 0.1 ms，所以必须做 X」的新工作立项。

#### 21.3.3 Phase 4B 约束：**维持原样，不新增约束，理由记录在案**

- **PIN-3**（热路径闭包内禁止出现 `PinStore` 符号，§19.3 定为**可静态核查**）**已经是**该约束的正确形态 ⇒ **不新增第二条约束。** 本节只把它的**理由**由构造升级为实测：PIN-3 现在有一个可引用的经验数字（0.1 ms 余量）。
- §18.3.3 **硬隔离第 2 条**（不得在 videoQueue / decoderQueue 上新增工作）**继续有效**（§20.3.4 只解除了第 3 条）。本节为它补一条实测理由，**不改其文本、不扩其范围**。
- ⇒ **Day 4 / Day 5 的 Builder 交付中，「PinStore 的任何工作不得进入 `decoderQueue`」继续以 PIN-3 的静态可核查形态执行。** 这不是一条新要求，是一条已有要求的**依据更新**。

### 21.4 R21 —— **维持关闭，数字不改**，唯一增量是第三个合法接受样本

§36.3 的 78 % / 84 % 否决率**不对 R21 构成任何改变**，本节逐条确认 §36.3.4 的判断并落账：

#### 21.4.1 为什么 84 % 不动 R21：它是**场景属性**，不是**参数属性**

- **口径先统一**：§20 的 45.5 % 分母是「到达门的单元」（11 条全部到达门）⇒ **可比的数字是 84.0 %（21/25）vs 45.5 %（5/11）**，⛔ **不是 78 %（21/27）vs 45 %** —— 78 % 用的是全部派发单元（含 2 条未到门的 decode 失败），与 §20 不同分母，**不可比**。
- **决定性的量化论据（§36.3.3）**：**21 条否决中每一条的面积比上界都 ≤ 0.199**（最大者 1028→5162 = 0.199）。⇒ **把 `reAnchorAcceptIoU` 设在 `[0.20, 0.60)` 内的任何值，本轮 27 条决策一条都不会改变。**
- ⇒ **该数字对阈值不携带任何信息。** 它度量的是「手持行走 60 s 期间锚点脱离目标的比例」。⛔ **不得读作「阈值偏紧」**，⛔ **不得读作「门过于激进」**，⛔ **不得把 84 % 写进 R21 的关闭记录当作新的稳态否决率** —— §20.4.1 第 1 点的 45 %（CI ≈[21 %, 72 %]）是在**桌面近景、相机基本不动**的场景下取得的，两者不可合并为一个「稳态否决率」。

#### 21.4.2 唯一的增量（本节对 R21 关闭记录的全部改动）

> **§20.4.1 第 2 点（上界 ≈0.55）新增第三个同类样本，§20.4.1 第 3 点（经验空隙）随之加宽：**
> - **合法接受样本由 2 个增至 3 个：0.57 / 0.59（§20.1.2 第 4、5 条）+ `0.60`（§36.3.1，449→301，面积比 0.67×）。** 方向一致 ⇒ **上界 ≈0.55 的结论不变**；若把阈值上调到 0.60 以上，本轮这一条会被误否决。
> - **判别力经验空隙由 `0.25 ↔ 0.57` 加宽为 `0.15 ↔ 0.60`**（本轮：迁移 0.01–0.15 / 合法接受 0.60 / no-op 0.99–1.00）。⇒ **0.5 同时落在两个不同场景各自的空隙内部** ⇒ §20.4.1 第 3 点「空隙是 0.5 能工作的真正原因」由**第二个场景独立支持**。
> - ⚠️ 三个样本的证据等级不一致：0.57 / 0.59 来自 §20（同为转录级），0.60 来自 §36（粘贴转录、无归档日志）。

⇒ **R21 维持 ✅ 关闭。** ⛔ 不重开、不建议调参、§20.4.1 三点的原有数字一个未改。§20.4.1 第 2 点末句（「『接受率偏高就上调阈值』这条处置路径在数据上已被关闭」）**由第三个样本进一步加强**。

#### 21.4.3 顺带：与 R21 相邻但不改状态的两条

- **R28**（重访物体同一性不可核验）：§36 的第二个场景（跨大幅位移的 IoU 域 0.01–0.15）**再次强化** §19.4「⛔ 不设相似度门」的裁决。**R28 状态不变（OPEN，P2）**，§20.4.6 的处置一字不改。
- **R31**（RE-1 的 no-op 缺口）：§36.3.5 在 **N=3** 上首次观测到该签名（三个实例的第一条 `[REANCHOR]` 中 2 条为可证明的逐位相同 no-op，1 条为「近 no-op」0.99 不满足严格签名、不计数）；量级 2–3/27 = 7.4–11.1 %，与 §20.5.1 在 N=1 上的 2/11 = 18 % 相符（本轮世代数多、tap 数少）。⇒ **R31 状态不变**：P3，Owner Builder，与 ISSUE-P4-DECODE 同批，Phase 4B 之后。⛔ **不构成任何现在动手的理由。**

### 21.5 RE-2 的轮转次序异常 —— **追认为符合规则，不是缺陷**

§36.2.3 观测到位置 15/16 连续两次 inst#0，且相位从 `1 2 0` 切换为 `0 1 2`。**本节采纳「符合选择规则」的判定**，理由是它与 RE-2 的设计语义一致：

1. **`min(by:)` 作用于 `candidates`，不作用于 `eligible`。** 排序前已按每个实例**自己的** `d_i` 过滤（`exceedsThreshold`）⇒ 「最久未刷新优先」只在**同一帧内多个已越阈实例**之间决定先后。⇒ 它**既不能**让一个未越阈的实例插队，**也不能**阻止一个刚刷新过的实例在它是**唯一**候选时立刻再被选中。
2. **这正是 R20 关闭时确立的语义**：候选过滤在设计上**优先于**轮转排序，冲突时前者胜出（「实例不得搭邻居的便车」）。⇒ 观测到的现象是该优先级的正常输出。
3. **两种相位都是该规则的不动点**：若某世代按 `(1,2,0)` 服务，下一世代的 `lastReAnchorAtMs` 排序仍是 1<2<0 ⇒ 继续 `(1,2,0)`；`(0,1,2)` 同理。⇒ **规则只保相位，不选相位；相位由「哪个实例在世代内最先越阈」设定。** 一次重新起相是正常输出，不是状态损坏。
4. **公平性未受影响：9/9/9。** 且公平性**本来就不由排序保证** —— 它由 RE-1 的「每个 embedding 世代每个实例恰好一票」保证（27 单元 = 9 世代 × 3 实例，计数闭合，无一多无一少）。⇒ **RE-2 的价值是「批次恒为 1」（消除队内累积），不是「公平」；公平是 RE-1 的副产品。这一点本节第一次写清楚。**
5. **decode 失败不扰动轮转状态**：`markReAnchorDispatched` 在**派发时**调用 ⇒ 失败单元与成功单元一样消耗世代票并推进 `lastReAnchorAtMs`。实证：含两次失败的世代 G4 与其后的 G5 次序均为 `1 2 0`，相位到 G6 才切换。⇒ 排除。

> ⚠️ **证据形态必须随判定同行：第 3 点（以及「唯一候选」这个成因）是从源码演绎得出的，不是从数据读出的。** 本轮转录不含 `drifted %.1flum` 字段 ⇒ **零 `d_i` 读数**，无法直接验证「G6 首个触发帧上只有 inst#0 越阈」。⇒ 该判定的强度是「**与源码一致且排除了其余已知路径**」，不是「已被观测证实」。⛔ 引用时不得升格为实测结论。

⇒ **本节关闭的是一个疑点，不是一条判据**：「RE-2 的轮转从未在**候选多于一个且门在跑**的构建上执行过」这一缺口 —— **已覆盖**（27 单元 / 9 世代 / 三实例全程在屏 / 门确实在算，见 §21.6.2）。⛔ **D-4 / D-4' 仍不勾选**，见 §21.9。

### 21.6 §16.6.1 静默降级路径 —— **状态由「可达性无实证」变为「已观测、行为已验证」**

#### 21.6.1 落账

§36.5 记录两条 `decode failed — keeping stale mask (no mask region at point)`（`t=60716.4` inst#1 / `t=61613.5` inst#0，同一 embedding 世代 G4，相隔 897.1 ms；该世代第三个实例同时刻成功）。失败率 2/27 = 7.4 %（n=2，⛔ 不得外推）。§34 全日志该行为 0，§35 亦无 ⇒ **本轮是第一次**。

**行为与 §16.6.1 契约逐条相符**（§36.5.3）：保留旧 mask ✅ / 静默降级只记一行 ✅ / 下一次成功 re-anchor 自动替换 ✅（两个失败实例均在下一世代 +5.36 s 按正常节奏恢复，**无额外延迟**）/ **D-2「甩动时降级为旧帧而非消失」第一次有直接实例** ✅。

**最有价值的一条是间接实证：§16.2.3 警告过的「永久节流死锁」没有发生。** 两次失败之后 re-anchor 又正常跑了 **15 个单元 / 28 s** ⇒ 批次计数器在失败退出路径上确实递减了（`finishReAnchorUnit` 的 `defer` 兜底）。⇒ **这是 A-8（任何复位/退出路径必须回答「已在飞、无法撤回的工作落地时会做什么」）在真机上第一次被正面验证** —— 该约定此前只有一次反面来源（§16.2.3 我自己留的洞，由 Builder 的 `batchId` 世代号补上）。

#### 21.6.2 顺带落账（均不改变任何条目状态）

- **ISSUE-P4-GATE 的修复在 N=3 上首次被校验**：25/25 条 `iou` 全部满足恒真面积比上界 `min/max`（§36.3.2）⇒ 门确实在做真实比较，第三次独立确认、**首次在多实例上**。⇒ **§20.7「§17.3.3 一致性否决门 ✅ 生效」的判定得到扩域支持，状态不变。**
- **R26（陈旧度界 ≈5 s）第三次实测落在同一个数**（世代周期 median 5604.6 ms）。**R26 已由 §20.4.5 关闭，本节不改变其状态**，仅记录同向样本。
- **一条形态观察（记录，不作结论）**：多数否决条目的 `iou` 贴近其面积比上界 ⇒ 新 mask **包住**了原 mask ⇒ **冻结锚点下的「迁移」以吞并为主，不以位移为主**。无物体同一性真值（R28）⇒ 不升格为结论。
- **§36.5.4 的「失败单元在派发时已推进散度基线与世代票，但没有写回 mask」**：这正是 §16.4.2 已经写明并接受的权衡（提前推进最多损失一次刷新机会，滞后推进则会让 decode 期间流逝的帧再触发一次冗余刷新）。**本轮实测损失为 0。** ⇒ **契约预期行为，⛔ 不构成任何修改理由。**
- ⚠️ **本轮未观测到另外两条失败分支**（`decode returned nil` / `corrupt decode (iou_pred …)`）；且转录不含 `Mask logits range` / `iou_preds` 行 ⇒ **§34.11 / §35.11 的 A13 ANE fp16 已知失效签名对照本轮无法执行**（§21.1）。⛔ **不得写作「decode 输出健康」。**

### 21.7 tap 抑制 re-anchor 4.9× —— **对外措辞裁定：§20.3.1 维持，并补一条措辞禁令**

#### 21.7.1 事实

| | 段 1（三实例，**零 tap**，行走 ~60 s）| 段 2（三实例，**37 次刻意连击**）|
|---|---|---|
| 单元速率 | **0.646 /s** | **0.131 /s** |
| 倍数 | — | **↓ 4.9×** |

两次独立会话、四个时段方向与量级一致（§35.5.2：0.57 → 0.18 /s，3.2×）。**§35.5.2 当时只能把它作为 D-7「争用条件欠压」的解释提出；§36 是它第一次作为被单独测量的现象出现。** 机制（已对源码核实）：主项是新 tap ⇒ `addInstance` ⇒ 新实例 `anchorSignature = nil` ⇒ **该槽位的散度时钟从零重启**，叠加 N=3 池 + FIFO + 三次 `clearAll` ⇒ 槽位持续翻新，很少有实例活到锚点内容累积 8.0 lum；次项是播种帧的**整帧否决**与 `clearAll` 后池空的结构性不触发。

📌 抑制不只体现在**次数**上，也体现在**每次的信息量**上：段 2 那四次还能触发的 re-anchor，`iou` 为 1.00 / 0.96 / 0.99 / 0.99，面积变化 ≤2 % ⇒ **几乎都是 no-op。**

#### 21.7.2 裁定：**§20.3.1 的对外措辞不需要修改**，但需要一条禁令

- ✅ **§20.3.1 的措辞规范维持不变，一字不改。** 它说的是「mask 不会比最近一次 embedding 刷新更陈旧（**≈5 s**）」—— **这是一个陈旧度上界，不是一个频率。** 抑制之所以不削弱这个上界，是因为**抑制它的那个事件（一次新 tap）本身就产出了一张全新的 mask**，比一次 re-anchor 刷新更强。⇒ **抑制路径上不存在「更陈旧的 mask」。**
- ⛔ **新增措辞禁令（本节的实质增量）：不得把 re-anchor 描述为「每 ~5 s 刷新一次」这样的固定节拍。** 同一对会话内速率跨度就是 **0.131 – 0.646 单元/s（4.9 倍）**，自变量是 **tap 活动**与**锚点内容变化**，**不是时钟**。⇒ 对外只允许陈述**上界**（≈5 s），⛔ 不允许陈述**频率**、**周期**或**节拍**。
- **长期约定的一个实例（不新立约定）**：这与 A-1（时间型 TTL 只允许作用于「过期后可自动重建」的资源）同源 —— **一个事件驱动的机制，其可观测速率不是它的规格；它的规格只能写成对用户可见量的界。** 记为 A-1 的一条应用注记。

#### 21.7.3 新增保留项 **R33**：陈旧度上界在极端连击下不由世代周期严格保证

- **内容**：播种帧的整帧否决（`guard !seededThisFrame …`）会让**别的槽位**的实例陪着被否决一帧。理论上，在极端连击下，一个**没有被 tap 过的老实例**可能被其它槽位的播种帧反复推迟 ⇒ 它的陈旧度上界**严格来说不再由 embedding 世代周期保证**。
- **⚠️ 注意范围**：§21.7.2 的论证（「抑制它的事件本身产出了新 mask」）**只覆盖被 tap 的那个实例**，不覆盖同池中的其它实例。**这是 §20.3.1 措辞的一个严格意义上的洞** —— 本节把它写下来而不是让它藏在「抑制无害」这句话里。
- **量级**：本轮实测代价为 **0**（段 2 的四次刷新 `iou` 均 ≥0.96）；上界估算为 ≤~31 % 的检查帧、每帧推迟 ~250 ms（⚠️ **不可核实**，转录无 `FPS=` 行）⇒ **远低于 ≈5 s 规格的余量。**
- **处置**：**OPEN，P3，Owner Architect（对外措辞侧）。** ⛔ 不构成任何现在动手的理由；⛔ 不构成重开 Day 2–3 的理由。若将来 §20.3.1 的 ≈5 s 需要写进用户可见文案或对外规格，**此条必须先被结清**（要么给出覆盖多槽位的论证，要么把措辞降为「典型 ≈5 s」）。

### 21.8 `tap inside existing mask → promote` 与 REC-2 的交叉面 —— **裁定 + 一处措辞更正 + 新保留项 R32**

§36.7.4 ISSUE-1 报了一个交叉面而没有给判定：**promote 路径不写 `originAlpha` ⇒ 点进一个被冻结的 mask 不会触发 REC-2**，而那恰恰是用户在 mask 冻住时最自然的动作。本节给出裁定。

#### 21.8.1 裁定一：**这不是未规范行为，也不是规格冲突**

`tap inside existing mask → promote to primary, no re-decode` 是 **Phase 3 §3.2 的封层契约**（`architect_output.md:148` 交互表逐字；`tasks.md:686` Builder 条目；`TapInstanceManager` 注释标注「locked §3.2 contract」；**§19 已把它列入 Pin 继承的不变量之一**）。⇒ **它是被规范的、被复用的、被显式继承的。** §16–§20（re-anchor 契约族）没有提到它，是因为它不属于 re-anchor 的范围，**不是因为它没有规范**。

⇒ 两份规格各自内部一致：§3.2 说「点进已有 mask ⇒ 提升 primary，不重解」；§18.2 说「冻结态由 REC-1 或 REC-2 退出」。**冲突不在规格之间，在于 REC-2 的可达性描述不精确。**

#### 21.8.2 裁定二（本节的实质动作）：**更正 REC-2 的可达性措辞**

§18.2 的 REC-2 一行原文是「用户对该实例（**或该位置**）再 tap 一次」。⚠️ **「或该位置」这半句在实现上不成立**：若该位置落在任何一个现存实例的 alpha 区域内（`alpha[ty*256+tx] > 0`），该 tap 走 promote 路径并在 `addInstance` 之前 `return`，**不会到达 `updateMask(..., recordOrigin: true)`** —— 而那是全项目 `originAlpha` 的**唯一**写入点。

> **更正（⛔ 不原地改写 §18.2，按 §15 纪律以本节为准并互相指向）：**
> **REC-2 的准确可达条件 = 一次会新建实例的 tap，即 tap 点落在所有现存实例的 mask alpha 之外。** 落在任一现存 mask 内部的 tap 走 §3.2 的 promote 路径 ⇒ 只翻 `isPrimary` 标志 ⇒ **不安装新原点，不退出冻结态。**
> ⇒ §20.4.4（R25）中「直到 REC-1（转回来）或 REC-2（重新 tap）」一句，其中 REC-2 按本条理解；§18.2 的 REC-2 描述**在此范围内被本节取代**。

**为什么这是措辞更正而不是缺陷修复**：被更正的是**文档对一条已实现且已封层行为的描述**，被描述的代码一行未改、也不需要改才能让描述成立。A-15 的同族（不变量必须与数据同行）在文档侧的形态：**恢复路径的描述必须与它唯一的写入点同行。**

#### 21.8.3 裁定三：产品层的问题是真的，**登记为 R32，不在本节设计任何修复**

更正措辞之后，剩下一个**真实的产品问题**，本节明确不回避：

> **当一个实例的 mask 因大幅形变被门冻结（R25 的既定代价）时，用户最自然的动作是「点一下那张还显示着的 mask」，而该动作在当前契约下是一次 no-op（只翻 primary 标志）。真正能恢复的动作是「点在旧 mask 之外」—— 这一点用户无从得知。**

- **登记为新保留项 R32。** **OPEN，P2，Owner Architect。**
- **为什么它必须是一个保留项而不是「已知取舍」**：§18.2 把冻结吸收态定性为**正确终态**，其正当性明确建立在「代价**可见且可恢复**，一次 tap 即可恢复」之上（§18.2 逐字）。⇒ **可恢复性是该定性的前提之一**；本节证明该前提在最自然的交互路径上**不成立**。⇒ 它不是一个被接受过的取舍，是一个**此前没被发现的前提缺口**。⚠️ 但该定性本身**不因此翻转**：REC-1（转回来）与「点在旧 mask 之外」两条恢复路径都真实存在且已被观测（REC-1 见 §20.1.4）。⇒ **降级为「恢复路径存在但不可发现」，不是「不可恢复」。**
- ⛔ **本节不设计修复。** STOP RULE 禁止 re-anchor 补救轮；且任何候选处置（让 promote 在实例处于冻结态时改走重解、或在 promote 路径补写 `originAlpha`、或纯 UI 侧提示）都会**同时触碰** §3.2 封层契约与 §19 已声明由 Pin 继承的不变量 ⇒ **属裁决，不属记录。**
- **零实测**：本轮无该场景数据（段 2 四次刷新 `iou` 均 ≥0.96 ⇒ 没有任何被冻结的 mask）。R32 与 **R25**（大幅形变被否决，`ISSUE-P4-TRACK`）**共享同一个前置条件** —— 没有 R25 的场景就没有 R32 的场景 ⇒ **两者应同批取数。**
- **归口建议（不是裁决，见 §21.10 交回用户的决定）**：R32 的自然归口是 **Phase 4B Day 5 的 Pin 交互裁决**（与 R27 同批）—— 因为 Pin 的命中判据与 promote **是同一个**（见下一条），Day 5 无论如何都要裁「点中已有 mask 时发生什么」。

#### 21.8.4 R27 增加一个面（不新增编号）

§36.7.4 ISSUE-3：**若 Pin 的交互是「点中已有 mask 即 Pin 该实例」，它与 promote 用的是同一个命中测试**（`alpha[idx] > 0`）。§19 声明 Pin 复用 tap 路径并继承 §3.2 不变量，但**「同一次点击落在已有 mask 内时，是 promote 还是 Pin，抑或两者都做」在 §19 中未见裁决。**

⇒ **登记为 R27 的一个新面**（Pin × 既有交互的优先级），**不新增编号**，归 **Phase 4B Day 5 的 Architect 裁决**，与 R27 原有两问（保存瞬间的替换竞态 / 已 Pin 的实例是否继续被刷新）同批。⇒ **Day 5 的 Pin 交互裁决现在有三问，且 R32 与其中第三问共用同一个命中判据 ⇒ 一并裁最经济。**

#### 21.8.5 §36.7 的另一条落账（不改判任何条目）

`[TAP#13] tap inside existing mask (gen #12) — promote to primary, no re-decode` 这行日志在 §34 / §35 / §20 的任何归档日志里**都没出现过**，本轮**首次被直接观测**。⇒ **§34.4.2 当时那条「那 4 次是 promote 或失败 tap」的推断获得直接确认。** ⚠️ 该确认**不改变 §34 的任何判定** —— §34.4.2 当时刻意没有让 D-1c 的判定依赖那两帧（这是当时正确的做法，本节予以记录）。

### 21.9 本节对既有条目的状态变更声明

| 条目 | 原状态 | 本节 | 依据 |
|---|---|---|---|
| **Phase 4A Day 2–3** | ✅ CLOSED（§20）| ✅ **维持 CLOSED，本节不重开** | STOP RULE 继续有效 |
| **R4 / R4-c** | R4 以原形态结项 + 四条重开触发条件；R4-c「未被测量，但已由构造定界」| **状态不变。R4-c 首次获得测量值**（n=32，max 4.9 ms / p95 3.5 / mean 0.45）；⛔ 不表述为「通过」，⛔ 不得称「R4 已排除」；构造界 ≈61 ms 不变 | §21.2 |
| **§20.4.2 第 4 点 (iii)** | 触发线 > 5 ms，当时观测上界 0.5 ms | **条件文本、触发值、命中处置一字不改**；仅追加状态注记：观测上界 0.5 → **4.9 ms**，余量由一个数量级压缩到 **2 %** | §21.2.2 |
| **R24**（50 ms 线无推导）| OPEN，由构造满足 | **状态不变**；触发条件命中时立即成为阻塞项 | §21.2.4 |
| **§19.3.1 队列裁决**（`pin.store.io`）| 裁决成立，依据为**纯构造论证** | **裁决不变，依据等级升为「构造 + 一条实测」** | §21.3.2 |
| **PIN-3 / §18.3.3 硬隔离第 2 条** | 有效，可静态核查 | **文本不变、范围不变**；理由由构造升级为实测（0.1 ms 余量）⇒ **不新增约束** | §21.3.3 |
| **R21** | ✅ 关闭（45.5 %；上界 ≈0.55；空隙 0.25↔0.57）| **维持关闭，三点原有数字一个未改。** 唯一增量：合法接受样本 2 → **3**（新增 `0.60`），空隙加宽为 **0.15↔0.60**。⛔ 84 % 是场景属性，不得写入 R21 作为稳态否决率 | §21.4 |
| **RE-2 轮转次序** | 从未在「候选 >1 且门在跑」的构建上执行过 | ✅ **疑点关闭**：轮转已执行、无饥饿、9/9/9；位置 15/16 判定为**符合选择规则，非缺陷**。⚠️ 判定来自**源码演绎**，非数据（零 `d_i` 读数）。**公平性由 RE-1 的每世代一票保证，非由排序保证**（本节首次写清） | §21.5 |
| **§16.6.1 静默降级路径** | 已实现，**可达性无实证** | ✅ **已观测、行为已验证**（2 条，与契约逐条相符；节流槽位正常释放 ⇒ §16.2.3 的死锁未发生 ⇒ **A-8 首次正面验证**）| §21.6 |
| **D-2**（甩动降级为旧帧）| §34.8.3 只能证明「路径没被用到」 | **首次有直接实例**；判据状态不变（不重新勾选） | §21.6.1 |
| **§17.3.3 一致性否决门** | ✅ 生效（§20.7）| **状态不变，扩域支持**：25/25 面积比上界自洽，**首次在 N=3 上校验** | §21.6.2 |
| **R26**（≈5 s 陈旧度界）| ✅ 关闭，转对外措辞规范 | **状态不变**；第三次实测落在同一个数（median 5604.6 ms）| §21.6.2 |
| **§20.3.1 对外措辞** | ≈5 s 陈旧度**上界** | **一字不改**；⛔ **新增措辞禁令：不得描述为「每 ~5 s 刷新一次」的固定节拍**（实测速率跨度 4.9 倍，自变量是 tap 活动与内容变化，不是时钟）| §21.7.2 |
| **R33** 🆕 | — | **OPEN，P3，Architect。** 播种帧整帧否决 ⇒ 未被 tap 的老实例的陈旧度上界在极端连击下**不由世代周期严格保证**；实测代价 0，上界不可核实；**§20.3.1 若要进入用户可见文案，此条须先结清** | §21.7.3 |
| **REC-2 可达性描述** | §18.2：「对该实例（**或该位置**）再 tap 一次」| **措辞更正**：REC-2 只能由**会新建实例的 tap**（落在所有现存 mask alpha 之外）触发；落在已有 mask 内走 §3.2 promote ⇒ 不写 `originAlpha` ⇒ 不退出冻结态。§18.2 该描述在此范围内**由本节取代** | §21.8.2 |
| **§3.2 promote 规则** | 🔒 Phase 3 封层契约，§19 列为 Pin 继承的不变量 | ✅ **维持封层，一字不改**；本节确认它**不是**未规范行为 | §21.8.1 |
| **R32** 🆕 | — | **OPEN，P2，Architect。** 冻结态的恢复路径**存在但不可发现**（最自然的「点一下那张 mask」是 no-op）⇒ §18.2「代价可见且可恢复」这一定性的一个**前提缺口**（定性不翻转，降级为「不可发现」）。与 **R25** 共享前置场景 ⇒ 同批取数。⛔ 本节不设计修复 | §21.8.3 |
| **R25** | OPEN，首获间接数据，并入 `ISSUE-P4-TRACK` | **状态不变**（本轮零直接数据：段 1 是迁移非形变，段 2 无形变目标）；**新增与 R32 的绑定** | §21.8.3 |
| **R27**（Pin × re-anchor）| OPEN，归 Day 5 | **OPEN，增加第三问**（点中已有 mask 时 Pin 与 promote 的优先级；两者命中判据相同）。**不新增编号** | §21.8.4 |
| **R28** | OPEN，P2，首获量纲 | **状态不变**；§19.4「⛔ 不设相似度门」由第二个场景（IoU 0.01–0.15）再次强化 | §21.4.3 |
| **R31** | P3，Builder，与 ISSUE-P4-DECODE 同批 | **状态不变**；N=3 上首次观测（2–3/27 = 7.4–11.1 %），量级与 N=1 的 18 % 相符 | §21.4.3 |
| **R22** | OPEN，P2，仍零数据 | **状态不变**；本轮转录连 `drifted …lum` 都没有 ⇒ 数据量仍是零 | §21.1 |
| **D-4 / D-4'** | 未执行，**不勾选**（§20.2.2 条件 3）| ⛔ **仍不勾选。** §36.2.4 落账的三实例 `[REANCHOR] qwait` max 0.5 ms / mean 0.14（n=27）**是一个读数，不是一次验收**：它采自一次「记录，不修」的烟测而非 D-4' 的采集协议，证据等级为转录级，且 **STOP RULE 禁止本区块内的任何 re-anchor 后续工作**。⇒ **读数落账，判据保持未执行**（形态同 §20 的 ⊘ 结项）| §21.9 本行 |
| **ISSUE-P4-DECODE** | OPEN，P2，判别设计作废 | **状态不变，不勾选**；本轮无可用于假说判别的分组，且转录不含 decode 分项字段 | §21.1 |
| **A13 ANE fp16 失效签名** | §34.11 / §35.11 的对照 | **本轮无判断**（转录缺三个签名字段）。⛔ 不得写作「decode 输出健康」 | §21.1 |
| **§16.7 七条禁令 / R3 禁令参数 / 冻结文件** | INVIOLABLE | ✅ **维持**：本节未放宽任何一条；`SAMDecoder.swift` / `MaskRenderer.swift` 未触碰 | 全节 |
| **STOP RULE** | 已满足并结束，禁止事项继续有效 | ✅ **继续有效，本节未触碰其文本** | 全节 |

### 21.10 交回用户的一个决定（本节不代裁）

> **R32（冻结态恢复路径不可发现）应当何时被裁？**
>
> - **选项 A（我的建议）——** 归 **Phase 4B Day 5**，与 **R27** 同批裁。理由：Day 5 无论如何都要裁「点中已有 mask 时发生什么」（R27 第三问与 promote 用**同一个**命中判据），R32 与它共用同一个决策点 ⇒ **合并裁决零边际排期成本**，且它是一次 **Pin 交互裁决**，⛔ **不是** STOP RULE 所禁止的 re-anchor 补救轮。
> - **选项 B ——** 并入 `ISSUE-P4-TRACK`，与 **R25** 一起处理。理由：没有 R25 的场景就没有 R32 的场景，能力 C 落地后两者可能一并消失。代价：Day 5 仍要在不知道 R32 结论的情况下裁 R27 第三问。
> - **选项 C ——** 明确记为 **Phase 4A 范围之外**，不再跟踪。⚠️ 我不建议：R32 是 §18.2「代价可见且可恢复」这一定性的前提缺口，除名等于把一个已知的前提缺口静默化。
>
> ⛔ **无论哪个选项，本节都不设计修复。** 若用户认为需要一轮针对该交叉面的补救，**须由用户明确要求** —— STOP RULE 规定 Architect 只能提出请求，不得自行启动。**本节即该请求的载体，且我不请求现在启动。**

### 21.11 边界声明

- **⚠️ 本节全部数字的一手证据是用户粘贴的会话转录，磁盘上无归档日志文件**（§21.1）。证据等级低于 §35，与 §20 同级。**引用本节任何数字必须带这一句。**
- 全部数据来自 **iPhone 11 / A13 单机**、`.tapToSegment`、**Release**；跨设备不得外推（§10.5 R5）。两次独立 app 运行、两条独立 `[t=]` 时间轴，**段内可做时间推理，跨段不可以**。
- 本节**未做任何调参**；六个 §17.4 常量与 §20.2.3 发布值表逐条一致。
- 本节**未新增机制、未新增队列、未新增锁、未修改任何 Swift 源文件**；**未修改** `debug_report.md` / `builder_progress.md`；**未勾选、未取消、未新增 tasks.md 任何复选框**；**未触碰 STOP RULE 文本**。
- 本节**未原地改写任何裁决或封层表**（§15 纪律）。唯一的原地更新是 **§10.5 的 R4 状态跟踪行**（追加一条 ➡️ 状态更新，指向本节）。
- 本节**不是第 5 轮 re-anchor 补救**：不诊断以求修复、不提出修复方案、不要求重测、不重开任何已关闭条目。

### 21.12 Deliverable 确认

- [x] §21.1 证据等级先声明：**粘贴转录、无归档日志**，n 的含义、不可回溯性，以及六个缺失字段各自的后果（含「⛔ 不得写作 decode 输出健康」）；写死引用规则
- [x] §21.2 **R4-c 首次测量落账**（采纳 §36.4.6 建议全文）+ (iii) 状态注记（观测上界 0.5 → 4.9 ms，余量压缩至 2 %，**条件一字不改**）+ 三条必须同行的限定（余量非通过 / 相位走运的单次抽样非尾部 / 协议自相矛盾）+ **R4 状态不变**
- [x] §21.3 0.1 ms 余量归属 `decoderQueue` ⇒ **§19.3.1 队列裁决依据由纯构造论证升为「构造 + 一条实测」**；PIN-3 与硬隔离第 2 条**不新增约束**，只更新理由
- [x] §21.4 **R21 维持关闭、数字不改**；统一分母（84.0 % vs 45.5 %，⛔ 非 78 % vs 45 %）；84 % 是**场景属性**（21 条否决面积比上界全 ≤0.199 ⇒ 阈值 [0.20,0.60) 内任取不改变一条决策）；唯一增量 = 第三个合法接受样本 **0.60**，空隙加宽为 **0.15↔0.60**
- [x] §21.5 RE-2 轮转次序**追认为符合规则、非缺陷**；标注判定为**源码演绎非数据**；首次写清「公平由 RE-1 保证、非由排序保证」
- [x] §21.6 **§16.6.1 静默降级路径状态变更**（可达性无实证 → 已观测已验证）；**A-8 首次正面验证**（节流槽位释放，无死锁）；顺带落账 ISSUE-P4-GATE 在 N=3 的校验、R26 第三次同向样本
- [x] §21.7 **§20.3.1 对外措辞维持一字不改**，新增**措辞禁令**（不得写成固定节拍）；新增 **R33**（陈旧度上界在极端连击下的严格意义缺口，P3）
- [x] §21.8 promote × REC-2：裁定**非未规范行为、非规格冲突**；**更正 REC-2 可达性措辞**（只能由会新建实例的 tap 触发）；新增 **R32**（恢复路径存在但不可发现，P2，§18.2 定性的前提缺口）；**R27 增加第三问不新增编号**；⛔ 不设计修复
- [x] §21.9 状态变更声明（含 **D-4' 读数落账但判据仍不勾选**）+ §21.10 **交回用户的归口决定（三选项，建议 A）** + §21.11 边界声明

---

END OF PHASE 4B SMOKE-TEST RECORDS PASS（§21）

## §22 Phase 4B Day 5 — PinStore 补救裁决 + Pin × 既有交互（promote / re-anchor）裁决（2026-08-21）

> 本节回应 debug_report §37（Day 4 PinStore 静态验收）的移交清单，以及 tasks.md Day 5 区块顶部登记的 R27（三问）与 R32。
> 本节**不重写** §19 / §21 一个字；凡有冲突，本节明示取代哪一句，其余原文继续有效（§15 纪律）。
> 本节**不写可编译 Swift**——全部以接口级规格 + 伪代码给出，Builder 据此实现。
> 本节**未打开** `SAMDecoder.swift` / `MaskRenderer.swift`；R3 禁令参数、`buildTapAlpha`、`canonicalPoint` 零触碰；未要求 `DriftDetector.swift` / `ReAnchorLoop.swift` / 一致性门任何一行改动——见 §22.2.6 明确声明的一处例外裁定与一处明确拒绝裁定。

### 22.1 §37 静态发现的补救裁决（A 组）

#### 22.1.1 §19.3.2 与 §19.3.4 的冲突 —— 裁定：§19.3.4 优先，取代 §19.3.2 的发布时机

**裁定：** 「失败必须可见 / ⛔ 禁止 UI 显示保存成功但盘上没有」（§19.3.4）是**绝对约束**，优先于 §19.3.2 对 `pins` 的描述。§19.3.2 原文「blob 懒加载」「内存索引只在此队列上变更」等机制性描述继续成立，但它**从未裁决过** `@Published pins` 的发布时机可以早于 flush——这是一处未言明的空白，不是两条条款的平等冲突。ISSUE-D4-3（P0-3）的成因是 Builder 在这处空白上选择了「早发布」，而 §19.3.4 的字面语义要求「晚发布」。

**具体修改（取代 §19.3.2 中「blob 懒加载」段落之后一句未明写的隐含时机，B-32）：**

```
现状（P0-3）：
    markDirtyAndPark() { manifestDirty = true; park(completion); publishSnapshot(); scheduleFlush() }
    flushNow() 成功 → releasePending(.success)      // pins 早已发布
    flushNow() 失败 → releasePending(.failure)      // pins 不回滚，仍是「盘上没有」的那条

裁定后：
    markDirtyAndPark() { manifestDirty = true; park(completion); scheduleFlush() }   // 不再在此发布
    flushNow() 成功 → manifestDirty = false; publishSnapshot(loaded: true); releasePending(.success)
    flushNow() 失败 → releasePending(.failure)      // 不发布，pins 维持上一次成功落盘的快照
```

- **不新增锁、不新增队列**：改动只是把 `publishSnapshot()` 这一行从 `markDirtyAndPark` 移到 `flushNow` 的成功分支，两处都已在 `pin.store.io` 上。
- **代价（明示，不掩饰）：** 新建/编辑/删除一条 Pin 后，`pins`（因而任何绑定它的列表 UI）最多延迟 ≤250 ms（合并窗）才更新，且必须等到一次**真实**落盘成功。这与「Result 回调延迟到 flush 落地才触发」（Builder 已对回调正确实现）是**同一时机**、同一等待——现在 UI 的两条通路（回调、`@Published pins`）第一次真正同形。
- **副作用（正确且是本条修复的一部分）：** 一次写失败后，`records`（队列私有状态）仍保留该次未落盘的变更（ISSUE-D4-4 描述的现状不变），但因为 `pins` 现在只在 `flushNow` 成功时发布，**下一次任何一次成功的 flush**（无论由哪次调用触发）都会把它一并带上并正确发布——不会出现「UI 显示了一条盘上没有的记录」，也不会出现「盘上有了但 UI 永远不显示」。ISSUE-D4-4（flush 失败无重试触发器）**仍然存在，不在本条修复范围内**，见 §22.1.10。
- **B-32（Builder，P0，Day 5 UI 落地前必须完成）：** 按上述伪代码把 `publishSnapshot` 调用点从 `markDirtyAndPark` 移到 `flushNow` 的 `.success` 分支。`load()` 内的 `publishSnapshot(loaded: true)` 调用点**不受影响、不移动**——那是载入结果的发布，不是「保存成功」的发布，§19.3.4 管的是后者。

#### 22.1.2 ISSUE-D4-1（P0-1，manifest 解码/迁移失败后仍可写）—— 裁定

**裁定三点，缺一不可（B-30，Builder，P0，Day 5 UI 落地前必须完成）：**

1. **`storeUnavailable` 必须在 `stage=decode`（`FilePinStore.swift:186-193`）与 `stage=migrate`（`:172-181`）两条 catch 分支内置为 `true`，作为该分支返回前的最后一步。** 这是唯一必需的行为改动——两处分支目前都缺这一行，而 `create`/`update`/`delete` 已经通过 `availabilityFailure()` 正确地在 `storeUnavailable == true` 时拒绝写入（§19.5.5 原文的「⛔ 不得静默降级成内存态 Pin」在 create 目录失败的路径上是对的，只是没有覆盖「读不动」这条姊妹路径——§37.4.1 指出的正是这个不对称）。补上这一行后，「解码失败 ⇒ 下一次 create 仍能写 ⇒ 覆盖 manifest ⇒ 下次启动 GC 删光 blob」这条两步不可逆链条在**第一步**就被切断。
2. **两条 catch 分支都必须尝试把失败读到的原始字节做一次取证备份**（`try?`，尽力而为，不改变返回路径）：`stage=decode` 写 `manifest.corrupt-<schemaVersion 或 unknown>.bak`；`stage=migrate` 复用已有的 `backupManifest`（该函数已被验证不会静默失败，见 §37.4.4）。**理由**：`.bak` 目前只在「迁移成功执行」这一条路径上产生（§37.4.4 已核实其正确性），而**最需要取证的恰恰是解码失败**这条路径——它今天完全没有备份。这不是为了自动恢复（v1 没有恢复机制），是为了让「一次读取故障」在未来可被人工排查，而不是随下一次 flush 消失。
3. **`storeUnavailable == true` 必须立刻可被 UI 观测，不必等到用户发起第一次写入才发现。** 在上述 catch 分支置位 `storeUnavailable` 的同时，主线程发布 `lastWriteError = .unavailable`（复用已有的 `@Published lastWriteError`，不新增 published 属性）。Day 5/6 UI（PinCreationSheet、PinList）必须在 `lastWriteError == .unavailable` 时展示一条持久横幅并禁用一切写入触发的 UI（长按建 Pin、编辑、删除），而不是让用户按下保存后才第一次看到失败。

#### 22.1.3 ISSUE-D4-2（P0-2，`create` 先于 `load` 的静默丢弃）—— 裁定：结构性消除，不是检测

**裁定：可达，且必须在 Day 5 接线前被结构性地消除，而不是靠调用方守规矩。**

`load()` 已经是幂等的（由 `loadStarted` 布尔 + `queue.sync` 守卫）。**裁定（B-31，Builder，P0，Day 5 阻塞项）：`create` / `update` / `delete` / `loadMaskImage` 四个入口，在各自方法体的第一行、进入各自的 `queue.async` 之前，都必须先调用 `self.load()`。**

```
func create(...) {
    load()                     // 新增：幂等，若已 load 过则整个函数体是一次 queue.sync 判断后立即返回
    queue.async { [weak self] in
        ... 原有逻辑不变 ...
    }
}
// update / delete / loadMaskImage 同样处理
```

**为什么这就足够、且不需要新状态机：** `load()` 自身向 `queue` 发起的 `queue.async`（真正做载入工作的那个闭包）与 `create` 自己的 `queue.async` 都是在**同一个调用线程**上、**按程序顺序**提交给**同一个串行队列**的——串行队列保证先提交的闭包先执行。所以只要 `create` 在提交自己的闭包之前先调用一次 `load()`（从而先提交 load 的闭包，如果它还没被提交过的话），`installRecords` 的 `removeAll` 就一定先于 `create` 的写入执行，P-2 描述的「已回调 `.success` 的记录被后到的 `load()` 抹掉」在结构上不再可能发生——不是靠「调用方记得先调 load()」这条约定，是靠**入口自己保证**。
**不引入新锁、新队列、新状态字段**——这是这个方案被选中的原因，相较于「给 store 加一个四态生命周期状态机」（§37.7.2 第 12 项曾提议）的方案，本方案是四行调用，成本更低、且从根源上消除了需要状态机去分辨的那种时序。

#### 22.1.4 blob LRU 容量修正（ISSUE-D4-8）—— 裁定：条目数不变，修正文档数字

**裁定：LRU 上限维持 32 条（`blobCacheCapacity` 不改），修正的是 §19.3.2 与代码注释里错误的字节量描述。**

- **理由**：该缓存服务的是 UI 懒加载显示（Day 6 PinList 滚动、AnnotationView 打开），32 条能覆盖一屏到一屏半的列表滚动窗口而不抖动；4 条（若按 256 KB 真实上限反推）会在正常滚动速度下持续抖动式地驱逐再重读。2 MiB 常驻在 D-3/§34.8 已确认的内存预算量级上无关紧要，且没有任何实测数据显示它构成压力——「按更小的容量数字削足适履」没有收益，只有滚动体验的代价。
- **`create` 主动预热缓存（`cacheBlob` 在写入后立即调用）——裁定：保留，不算违反「懒加载」。** 理由：`create` 调用时 `alpha` 已经在手（是它自己刚写盘的字节），把已经在内存里的值顺手放进缓存，不是「提前读取」，不产生任何额外磁盘 I/O 或额外解码——它与「全量载入不得读 blob」（防的是 O(N) 次磁盘打开）是两件不同的事。PinCreationSheet 保存后大概率立刻要显示同一张缩略图，预热避免了一次多余的 `loadMaskImage` 磁盘往返。
- **B-33（Builder，P3，措辞修正，无行为改动）：** 把 `FilePinStore.swift:96-99` 的注释「≤ 32 entries (≈ 256 KB)」改为「≤ 32 entries (≈ 2 MiB decoded, 65,536 B × 32)」；§19.3.2「读完可进一个上限 32 条的 LRU（≤ 256 KB）」一句由本条**取代**为「读完可进一个上限 32 条的 LRU（≤ 2 MiB，缓存的是解码后的 `[UInt8]` 而非 PNG 字节）」。

#### 22.1.5 P-4 前置条件重述 —— 裁定：采纳 §37.6.2 建议措辞，正式取代 §19.6 P-4 首行

**裁定（取代 §19.6 P-4 原文第一行「`.tapToSegment`，`reAnchorEnabled == false`（§18.3.3 硬隔离 3）」）：**

> **P-4 前置条件（Day 5 起生效）：** `.tapToSegment`，A/B 两臂须在**同一会话、同一构建**上采集；`reAnchorEnabled` 取当前发布值（`true`，§20.2.1），**两臂之间只差 PinStore 活动这一个自变量**。理由：§20.3.4 已解除硬隔离第 3 条，原前置条件字面不可执行；A-11（参照工况只能差一个自变量）在「同会话 A/B」形态下已经满足——re-anchor 在两臂中同时开启，它不是本次比较的自变量。**若两臂的 re-anchor 单元速率差异显著（参照 §20.3.1：单元速率量级 0.13–0.65/s，随 tap 活动与内容变化而非时钟），报告必须记录两臂各自的 `[REANCHOR]` 单元计数作为工况证据**，以便一次「B 臂比 A 臂慢」的结果能被归因到 PinStore 而非归因到两臂恰好撞上了不同的 re-anchor 负载。

- 该判据的**其余条款**（A 臂无 PinStore 活动、B 臂脚本 500ms 存一条、门控线 `mean ≤5ms / p95 ≤10ms`、⛔ 不与 §33 跨 session 比）**一字不改**。
- **B-29 fixture 需要提供的驱动能力见 §22.1.6。**

#### 22.1.6 B-29：调试专用 fixture —— 裁定归属与范围

**裁定：`#if DEBUG` 编译期隔离，落在 `Persistence/` 目录内一个新文件（例如 `PinDebugFixture.swift`），编译进 App target；不新建测试 target，不修复 `JudgeE2Tests` 的空 Sources phase。**

**为什么不是测试 target：** ① `JudgeE2Tests` 今天 Sources phase 为空、未参与编译，把它接起来是一项与 Persistence 无关的独立工程任务，超出本节裁决射程；② P-3 的三种杀法本质是「杀掉宿主 App 进程后检查它重启的行为」，这天然是**宿主 App 内**的可控场景，而不是一个独立测试进程能覆盖的东西（测试 target 杀的是测试进程，不是被测 App 本身）；③ Release 不存在、无 UI 入口，符合 §37.6.1 建议的「不是新功能」定性。

**B-29（Builder，P1，Day 5 提供，供 Debugger 在 Day 5/Day 6 执行 P-1…P-4 时使用）范围：**

```
#if DEBUG
enum PinDebugFixture {
    // (a) 按参数构造合成 PinRecordV1 + 合成 maskAlpha —— 覆盖 P-1 的 5 类边界值
    //     （空 tag / 64-UTF16 字符 tag / emoji+换行 note / maskFile==nil / 极小面积 mask）
    static func makeSyntheticRecord(tag: String?, note: String?,
                                     maskAreaPx: Int?, includeMaskFile: Bool) -> (PinRecordV1, [UInt8]?)

    // (b) 可设定间隔的批量保存驱动 —— 供 P-4 的 B 臂（500ms 一条）与 P-3(iii)
    //     （连续 N 条保存中途可被杀）复用同一个驱动
    static func batchSave(store: PinStore, count: Int, intervalMs: Int)
}
#endif
```

- **触发方式：** 通过 launch argument / 环境变量在 `#if DEBUG` 的 `JudgeE2App.init()` 内读取并调用（例如 `-PinFixtureBatch 10:500`），**不新增任何 UI 入口**，与 Day 5/6 的 UI 工作完全解耦，不占用 Day 5 UI 的接线顺序。
- **约束（沿用 §37.6.1 原文）：** 该 fixture ⛔ 不得引用 `TapInstance` 或任何 re-anchor 字段，⛔ 不得向 `videoQueue`/`decoderQueue`/`encoderQueue` 投递任何工作——它只调用 `PinStore` 协议上已有的 `create`/`flush`，不触碰 CameraManager。

#### 22.1.7 编译器 actor 隔离标注（ISSUE-D4-BUILD-1）—— 裁定：显式标注，不接受「运行时已经够安全」

**裁定：`pin.store.io` 的队列纪律必须获得编译器背书，而不是继续依赖人工复核（§37.4.4 已经做过一次，但 A-15 的精神——不变量必须随数据/结构同行，不能只活在注释或人工复核记录里——同样适用于并发隔离标注）。**

**具体标注方案（B-34，Builder，P1）：**

| 对象 | 标注 |
|---|---|
| `PinRecordV1` / `PinGeometryV1` / `PinManifestV1` / `PinStoreError` / `PinIntegrityError` / `PinCoders` / `MaskPNGCodec` 七个纯值类型/枚举 | 整个类型声明标 `nonisolated`（它们不持有任何跨线程可变状态，本就不该继承模块默认的 `@MainActor`） |
| `FilePinStore` 的主线程专属表面：`pins` / `isLoaded` / `lastWriteError`（三个 `@Published`）与 `fetch(id:)` | 显式标 `@MainActor`（这与它们的实际契约——§19 协议注释「main thread only」——完全对应，本来就该是这个隔离域） |
| `FilePinStore` 的其余部分（`queue` 私有的十个状态量、`load`/`create`/`update`/`delete`/`loadMaskImage`/`flush` 等方法体、以及全部 `private` helper） | 标 `nonisolated`——它们的真实执行域是 `pin.store.io`，从未在主线程同步执行过 |
| `fetchAll()` | `@MainActor`（读 `pins`，与上面同组） |

- **效果**：13 条 warning 中 Swift 6 模式下会变 error 的那批（`PinStoreError`/`PinManifestV1` 的隐式 `@MainActor` 推断）消失；`publishSnapshot` 内 `Pin.init(record:)` 那条（`:595`，本节唯一一条不带「Swift 6 才 error」措辞的）也随 DTO 类型标 `nonisolated` 一并消失。**这不改变任何运行时行为**（§37.3.2 已确认 Swift 5 模式下 actor 推断不影响 GCD 执行域），只是让编译器现在能看见并核对本项目一直用人工复核维持的那条纪律。
- **口径要求（B-34 的一部分）：** Builder 交付时必须附带**可复现的构建命令 + 输出**（两个配置、全新 derivedDataPath），而不是一句「零 warning」结论——采纳 §37.3.2 的 RECOMMENDATION，正式确立为要求：**Day 5 起，涉及 warning 数量的自评必须附带可复现命令。**

#### 22.1.8 P-1 两处歧义 —— 裁定

**空 tag / note 的持久化表示：canonical 表示为 `nil`，不是 `""`。**
- 理由：`update` 已经这样做（`tag.isEmpty ? nil : tag`），且 `nil` 与字段本身的 `String?` 类型语义一致（「没有标签」比「有一个空字符串标签」更准确地表达用户意图）。
- **裁定（B-35 的一部分，Builder，P1，阻塞 P-1 执行）：`create` 必须与 `update` 采用同一条归一化规则**——在 `FilePinStore.create` 写入 `records[record.id]` 之前，对 `record.tag`/`record.note` 做同样的 `isEmpty ? nil : $0` 归一化（或等价地，把这条归一化前移到 `PinFactory.makeRecord` 内，使 `create` 永远收不到 `""`）。两处选一即可，但**不得两处都不做、也不得只做一处**——这正是 ISSUE-D4-13 描述的现状。

**64 字符的计数口径：canonical 为 UTF-16（`utf16.count`），不是字素簇（`String.count`）。**
- 理由：① UITextField/UITextView 系家族对「字符数上限」的传统实现口径是 UTF-16 code unit，Day 5 的 SwiftUI `TextField` 若要做输入时限制，最容易复用、最不易与 store 侧脱节的就是这个口径；② 一个按字素簇计数的上限对 JSON/磁盘占用没有可预测的上界（组合表情序列可以是 1 个字素簇、十几个 UTF-16 单元），而 §19.1 的字节量推导本来就依赖一个可预测的上界；③ 实现成本相同（一次 `.count` 属性访问的区别）。
- **裁定（B-35 的一部分）：** `PinFieldLimits` 新增一个居中函数 `static func length(of s: String) -> Int { s.utf16.count }`；`FilePinStore.fieldLengthFailure` 与 Day 5 的 UI 输入限制器（`TextField` 的 `onChange`/`.onReceive` 限制逻辑）**必须共用这同一个函数**，不得各自实现一遍计数。

#### 22.1.9 P-4 埋点补充建议（供 Architect 与 Debugger 参考，不强制立即执行）

§37.5.4 指出 P-4 若失败将难以归因（主线程争用无埋点）。**裁定：不在本节强制要求新增 `[PIN] flush ms=` 埋点**，理由是它是一项独立于本节裁决范围的小改动，留给 Debugger 在实际执行 P-4 前视需要提出（若提出，走正常 B 编号流程，不需要再次经过 Architect 裁决——这是一条纯遥测埋点，不涉及任何契约）。

#### 22.1.10 未在本节处置的 §37 发现 —— 明确声明，不静默略过

以下条目 §37.7.2 已给出移交，本节**不在 Day 5 这一批裁决**，状态与优先级维持 §37.7.2 原表不变，留待后续批次：ISSUE-D4-4（flush 失败无重试触发器）、ISSUE-D4-5（NaN/Inf 永久写入停摆）、ISSUE-D4-6（delete 的 blob/manifest 顺序窗口）、ISSUE-D4-7（`record.uuid ?? UUID()` 静默造假 id）、ISSUE-D4-9/D4-10/D4-11/D4-12（读路径不校验/日志缺条数/`maskFile` 装饰性）、ISSUE-D4-15（前向版本路径的第三态 UI 需求）、ISSUE-D4-16（`id` 未校验为合法 UUID）、ISSUE-D4-17（三处强制 flush 目前只接一处——**这条例外**：它不是「留待后续」，是 Day 5/6 UI 接线本身就要做的事，见 §22.1.11）、§19.1.4 的措辞更正（改一个 tag 的重写量级）、真机构建验证、`JudgeE2 copy/` 工程卫生。**理由：** 本节的射程是 §37 移交清单中标为「P0，Day 5 阻塞项」与「P1，阻塞 P-1/P-4 执行」的条目（22.1.1–22.1.8）；P2/P3 级、或与 Day 5 UI 落地本身自然耦合（会在实现时被动触及）的条目不需要额外裁决即可处理，**不代表被忽略**，Builder 与 Debugger 应按 §37.7.2 原有优先级顺序继续处理。

#### 22.1.11 三处强制 flush 的接线 —— 明确要求随 Day 5 UI 一并完成

**裁定：B-21 已实现的 `flush(waitUntilDone:)` 必须在 Day 5 接入以下两处（第三处「合并窗到期」已经存活）：** `scenePhase → .background`、`willTerminate`。这不是一项新设计——§19.3.3 原文「三处强制 flush，缺一不可」已经裁定过，本条只是把它列为 Day 5 UI 落地的**显式前提**，因为 P-3(i)（切后台强杀后已确认写入不丢）在当前状态下结构性必挂（ISSUE-D4-17）。**归入 B-21 的收尾，不新编号。**

---

### 22.2 Pin × 既有交互（R27 三问 + R32）的统一裁决（B 组）

> 本节把 R27（i/ii/iii）与 R32 当作**一个交互设计**统一裁决——理由见任务书：四问共用同一个命中判据（`alpha[idx] > 0`）与同一个决策点（「点中已有 mask 时发生什么」）。

#### 22.2.1 出发点：Pin 创建绑定的手势是长按，不是点击——这决定了 R27(iii) 的答案

Builder 的 Day 5 草案已经把 Pin 创建绑定到**长按**（≥0.5s）：「长按 tap anchor marker → 固定 Pin」。这不是本节的新发明，是既定输入。§3.2 的 promote 规则（「点击已有 mask 内部 → 提升为 primary，不重新 decode」）与 `handleTap` 的现有实现（`CameraManager.swift:1232-1260`）针对的都是**普通点击**（tap）。

**裁定一（回答 R27(iii)）：长按与点击在同一次触摸事件上互斥，「同一次点击既是 promote 又是 Pin」这个场景不存在。**

- iOS 手势识别的标准语义：长按识别器要求按住达到最短时长（此处 0.5s）才触发；若在此之前抬手，事件只满足点击识别器，长按识别器不触发。两者作用于**同一次 down-up**，但只有其一能被判定成立——不是「谁优先」的裁决问题，是手势识别本身的互斥结构。**⇒ 不需要，也不得，在业务逻辑层面额外裁决两者的优先级**——那会是在解决一个不存在的冲突。
- 因此 R27(iii) 的准确形态是：给定「长按落点是否在已有 mask 内」与「点击落点是否在已有 mask 内」各自独立的行为，而不是给定同一事件的双重解释。裁定见下。

#### 22.2.2 完整决策树（Builder 必须实现的形态）

```
onTouch(location, duration):
    if duration ≥ 0.5s:                                   // 长按 —— Pin 创建/编辑通道
        target = hitTestExistingInstance(location)         // 与 promote 完全相同的映射 + 判据：
                                                             //   canonical→256 空间, alpha[idx] > 0
        if target != nil:
            openPinCreationSheet(for: target)               // 不 promote，不改 isPrimary，不 decode
        else:
            // 落在所有现存 mask 之外 —— 长按不是 §3.2 addInstance 的触发源，no-op
            return

    else:                                                   // 普通点击 —— 既有 §3.2 / handleTap 契约，一字不改
        // 完全复用 CameraManager.swift:1180-1260 现有逻辑：
        //   落在已有 mask 内 → promoteToPrimary，no re-decode（不触发任何 Pin 副作用）
        //   落在所有 mask 外 → addInstance + decode（FIFO 规则不变）
    // 双击：不变，clearAll()（C1）
```

- **长按的命中测试复用 promote 已有的映射代码**（`CameraManager.swift:1237-1248` 的 canonical→256 空间换算 + `alpha[idx] > 0`）。Builder 应把这段映射抽成一个私有 helper 供两处调用，避免复制一份——这是实现质量要求，不是新契约。
- **长按命中已有 mask 时不调用 `promoteToPrimary`，不改变 `isPrimary`。** Pin 创建是一个与「谁是当前主要展示实例」正交的动作；顺带把长按的目标提升为 primary 会是一个未被要求的副作用。
- **长按落在所有现存 mask 之外时是 no-op，不新建实例。** `addInstance` 的触发源在 §3.2 是「新 tap」，长按不在其列；本节不扩展这条规则。
- **PIN-5 不受影响：长按路径完全不调用 `handleTap`。** PIN-5 约束的是**重访**（Day 6，`handleTap(fromPin:)`），Pin 的**创建**（Day 5）从一开始就不是一次 decode——它是对一个已经在屏、已经 decode 过的 `TapInstance` 的只读快照（B-27 `makeRecord` 的三个字段），不需要、也不得经过 `handleTap`。

#### 22.2.3 R27(i)：保存瞬间竞态 —— 裁定：单次锁下快照即可，不需要新同步

**裁定：`PinFactory.makeRecord`/`maskAlpha` 读取 `instance.maskAlpha` / `canonicalPoint` / `createdAt` 三个量，必须在 `TapInstanceManager` 现有的 lock（保护 `originAlpha`/`anchorSignature` 等字段的同一把锁，§18.2.2 已确认其形态同 B-7）下一次性完成，产出一份值类型快照。不新增锁，复用已有的那一把。**

- **为什么不需要更多：** `maskAlpha` 是 `[UInt8]`（值类型，COW），在锁内读出即是一次完整、不可能撕裂的快照——无论 re-anchor 的 `updateMask` 在锁外等待还是已经写完，读到的必是**完整的某一版本**，不存在半写状态。所以「保存的瞬间 mask 正被替换」这个场景不产生任何数据损坏风险，产生的只是一个语义问题：拿到的是替换前还是替换后的那一版。
- **对这个语义问题的裁定：不做特殊调解，接受「锁内快照那一刻恰好是哪个版本就是哪个版本」。** 理由：re-anchor 本身的契约（§16.6.1、§18.2）已经确立「mask 会在用户不知情的情况下小步刷新」是这个系统的正常行为（能力 A 的定义本身），Pin 在这一点上不比屏幕上任何一次普通渲染更特殊——用户看着屏幕做长按手势时，他看到的就是即将被快照的那一版；系统没有义务、也没有机制去证明「用户长按时脑子里想的是哪一像素级版本」。
- **必须实现的一条纪律（新不变量 **PIN-6**，B-27 的补充要求）：** 长按打开 `PinCreationSheet` 时的那一次锁内快照，**必须是唯一的一次**——`PinCreationSheet` 展示的缩略图预览与用户按下「保存」时最终写入 `PinStore.create` 的 `maskAlpha`，**必须是同一份字节数组**，不得在用户点「保存」的那一刻重新去读一次 `instance.maskAlpha`（那会引入第二次竞态窗口，且会造成「预览和保存的不是同一张图」这个用户可感知的不一致）。`PinCreationSheet` 打开时锁定的快照应作为该 sheet 的构造参数持有到保存或取消。
- **不构成需要触碰 re-anchor 代码的理由**——本条裁决完全在 Pin 侧（B-27 的读取纪律 + Day 5 sheet 的状态持有方式）落地，`TapInstanceManager` 的锁是**复用**，不是新增或修改其语义。

#### 22.2.4 R27(ii)：已 Pin 的实例是否继续被 re-anchor 刷新 —— 裁定：继续刷新，Pin 是快照，不是绑定

**裁定：是。已被 Pin 的 `TapInstance` 继续接受 re-anchor 的正常刷新（RE-1/RE-2/RE-3、C1–C6 全部不变），不因为它被 Pin 过而获得任何特殊状态。**

三条理由：

1. **Pin 从定义上就是快照，不是引用。** §19.2.1/§19.4.2/PIN-1/PIN-2 已经反复确立「Pin 不是截图，但它的 mask blob 和 canonicalPoint 是一次性冻结的」——`masks/<id>.png` 一旦写入即不可变，`pointX/pointY` 冻结。这些裁决的前提本来就是「持久化的东西与屏幕上继续变化的东西是两个不同的物」。R27(ii) 问的其实是「要不要在这两者之间建一条新的绑定」，而 §19 从头到尾没有建这条绑定,也没有必要建。
2. **建立这条绑定需要触碰 re-anchor 自己的代码，本节明确拒绝。** 让 re-anchor「知道」某个实例被 Pin 过并因此改变刷新行为（例如停止刷新、或降低优先级），意味着要给 `TapInstance` 或 `DriftDetector` 引入一个 Pin 感知的标志位——这既违反 §18.3.3 硬隔离第 1 条的对称精神（Pin 不得读 re-anchor 字段；反过来 re-anchor 也不该被要求读 Pin 状态），也是对 `DriftDetector.swift`/`ReAnchorLoop.swift` 的一次实质性改动。**任务书明令：本节的 R27/R32 裁决不得要求这类改动；若裁决结果看起来需要，必须停下明说，交还用户——这正是这种情况，本节因此选择不这样裁。**
3. **不绑定在产品语义上也是对的：** 若用户 Pin 住一个物体后相机继续对着它，用户大概率希望屏幕上继续看到跟随的实时反馈（这是 re-anchor 存在的意义），而不是画面从此冻结成一张静态图——冻结是 Pin **磁盘记录**的属性，不该是**屏幕体验**的属性。

**推论（UI 侧，Day 5 必须遵守）：**
- 一旦长按保存成功，屏幕标记变为 📌（Builder 草案已定）。但该 📌 **装饰的是 `TapInstance`，不是 `Pin` 记录**——若这个实例后续被 FIFO 淘汰（C2）、被清空（C1/C3）、或几何变化被清空（C4），📌 随实例一起消失，这与任何其他 tap marker 的生命周期完全一致，**不得**为了让 📌「看起来更持久」而给它引入独立于 `TapInstance` 生命周期的展示状态。已保存的 Pin 记录本身完好无损地留在磁盘上，只是不再能通过实时画面看到——找回它是 Day 6 PinList 的职责，不是 Day 5 实时视图的职责。
- 这条推论**关闭了 R27(i)(ii) 之间的一个潜在耦合**：既然实例继续被刷新且 Pin 只是某一时刻的快照，「保存瞬间被替换」不再是一个需要与「是否继续刷新」联合考虑的复合问题——两问独立回答，互不影响对方的裁决。

#### 22.2.5 R32：冻结态恢复路径不可发现 —— 裁定：确认定性、追加一条 Pin 侧纯 UI 缓解，拒绝任何触碰 re-anchor/promote 的方案

**裁定一（复核并采纳 §21.8.3 的定性，不翻转）：** 冻结吸收态（§18.2 的正确终态）「代价可见且可恢复」这一定性的**可恢复性**部分存在前提缺口——最自然的「点一下那张还显示着的 mask」是 no-op（走 promote，不写 `originAlpha`）。真正有效的恢复路径（REC-1 转回来 / 点在旧 mask 之外新建实例并触发 REC-2）**存在且已被观测**（§20.1.4 REC-1），只是不可发现。**降级为「恢复路径存在但不可发现」，不是「不可恢复」——本节维持这一定性，不重新论证。**

**裁定二（本节的实质增量，范围严格限定在 Pin 自己的新 UI 内）：** `PinCreationSheet` 必须携带一条**静态、无条件展示**的提示文案，大意为：「若画面内容与当前实际物体不符，可点击该物体在画面中的其他位置以重新框选」（措辞由 Day 5 Builder / UI 文案定稿，语义须包含「点在别处」这一动作）。

- **为什么是无条件展示，而不是仅在检测到冻结态时才显示：** 判断「这个实例当前是否处于冻结吸收态」需要读取 `originAlpha` 与当前 `maskAlpha` 的差异信号（或等价的 re-anchor 侧派生状态）——而这正是 §18.3.3 硬隔离第 1 条明令禁止 Pin 子系统读取的东西（`originAlpha` 是被列名禁止的 re-anchor 字段之一）。**本节不裁决放宽这条隔离**，所以「按状态条件显示提示」这个更好的方案不在本节的可实现范围内。**无条件静态提示**是唯一不需要读取任何 re-anchor 字段就能实现的缓解:它不查询、不判断、不触碰 `TapInstance` 除三个已授权字段外的任何状态,纯粹是 `PinCreationSheet`（Day 5 新建的 UI 文件）里的一行固定文案。
- **代价明示：** 该提示在实例并未处于冻结态时也会显示，是多余信息。裁定认为，相对于放弃这条缓解、任由 R32 描述的可发现性问题继续存在，这个代价可以接受；且措辞刻意写成通用的操作指引（「点在别处可重新框选」）而非诊断性宣称（不说「当前已冻结」），避免在没有依据时做出系统状态判断。
- **不构成对 REC-2 可达性措辞的修改**——§21.8.2 对 REC-2 的更正维持不变，本条只是在 Pin 的 UI 里教会用户已经存在的那条路径怎么走，不改变任何判定逻辑。

**裁定三（明确拒绝、交还用户的部分）：** 任何**依状态显示**的提示、任何让 promote 在冻结态下改走重解、任何让 promote 补写 `originAlpha` 的方案，**本节均不设计、不实现、不建议现在做**——它们都需要读取 re-anchor 派生状态或修改 `CameraManager.swift` 中 promote 分支/`originAlpha` 唯一写入点，触及 §3.2 封层契约与/或 §18.3.3 硬隔离第 1 条。**按任务书指示，本节在此明确停下并交还用户**：若用户认为「按状态显示提示」或更深的修复值得做，需要用户明确指示一次新的、范围受限的裁决（且仍须遵守 STOP RULE——不得把它包装成「只是加个只读信号」而实质打开 re-anchor 补救轮）。

**R32 状态更新：** 维持 **OPEN，P2，Owner Architect**（§21.8.3 原状态不变）——本节交付的是一条 Pin 侧缓解（B-36），不是 R32 的关闭。与 **R25** 共享前置场景的绑定（§21.8.3）不变。

#### 22.2.6 本节新增/更正的不变量与保留项汇总

| # | 内容 | 状态 |
|---|---|---|
| **PIN-6**🆕 | Pin 创建的 `maskAlpha` 快照必须在 `TapInstanceManager` 既有锁内一次性读出，且 `PinCreationSheet` 的预览与最终保存必须复用同一份快照，不得在保存时二次读取 | 立，§22.2.3 |
| **R27(i)** | 保存瞬间竞态 | **裁定：单次锁内快照即可，无数据撕裂风险；语义上不做特殊调解**（PIN-6） |
| **R27(ii)** | 已 Pin 实例是否继续被刷新 | **裁定：继续刷新，不新增 Pin↔TapInstance 绑定**；📌 标记装饰 `TapInstance`，随其生命周期消失，与磁盘上的 Pin 记录无关 |
| **R27(iii)** | 点中已有 mask 内是 promote 还是 Pin | **裁定：手势时长互斥，冲突不存在**；长按 = Pin 通道（复用 promote 的命中判据但结果只开 sheet，不 promote、不 decode）；普通点击 = 既有 §3.2/`handleTap` 契约，一字不改 |
| **R32** | 冻结态恢复路径存在但不可发现 | **状态不变：OPEN，P2，Architect**；本节交付 Pin 侧无条件静态 UI 提示（B-36）作为缓解，**明确拒绝**依状态显示或触碰 promote/`originAlpha`/re-anchor 代码的方案，交还用户 |
| **B-36**🆕 | `PinCreationSheet` 追加静态提示文案（缓解 R32 可发现性问题） | Builder，P2，Day 5 |

### 22.3 Builder 变更清单（编号接续 §19.7 的 B-28）

| # | 内容 | 归属 | 优先级 |
|---|---|---|---|
| **B-29** | `#if DEBUG` PinDebugFixture：合成记录构造 + 可设间隔批量保存驱动，经 launch argument 触发 | Persistence 目录，App target 内 `#if DEBUG` | P1，Day 5 |
| **B-30** | `storeUnavailable` 在 decode/migrate 失败分支置位 + 取证 `.bak` + 立即发布 `lastWriteError = .unavailable` | `FilePinStore.swift` | **P0，Day 5 UI 前阻塞** |
| **B-31** | `create`/`update`/`delete`/`loadMaskImage` 入口首行调用 `self.load()`（幂等），结构性消除 P0-2 | `FilePinStore.swift` | **P0，Day 5 阻塞** |
| **B-32** | `publishSnapshot` 调用点从 `markDirtyAndPark` 移至 `flushNow` 成功分支 | `FilePinStore.swift` | **P0，PinList 落地前阻塞** |
| **B-33** | LRU 容量注释/§19.3.2 措辞修正（≤256KB → ≤2MiB），行为不变 | `FilePinStore.swift` 注释 | P3 |
| **B-34** | Persistence 七个纯值类型标 `nonisolated`；`FilePinStore` 主线程表面标 `@MainActor`，其余标 `nonisolated`；交付附可复现构建命令+输出 | `Pin.swift` / `PinRecordV1.swift` / `MaskPNGCodec.swift` / `FilePinStore.swift` | P1 |
| **B-35** | `create` 的 tag/note 归一化对齐 `update`（`""`→`nil`）；`PinFieldLimits.length(of:)` 统一为 `utf16.count`，store 与 Day 5 UI 共用同一函数 | `FilePinStore.swift` / `Pin.swift` / Day 5 UI | **P1，阻塞 P-1 执行** |
| **B-36**🆕 | `PinCreationSheet` 追加 R32 缓解的静态提示文案 | Day 5 UI | P2 |
| （沿用 B-21） | 三处强制 flush 中「background / willTerminate」两处接线，随 Day 5 UI 完成 | Day 5 UI 接入点 | P1 |
| （沿用 B-27） | `makeRecord`/`maskAlpha` 实现：三字段只读 + PIN-6 单次锁内快照纪律 | Day 5 | P0（Day 5 主线） |

### 22.4 边界声明

- 本节**未修改**任何 Swift 源文件；**未打开** `SAMDecoder.swift` / `MaskRenderer.swift`；R3 禁令参数、`buildTapAlpha`、`canonicalPoint` 零触碰。
- 本节**未要求、也未建议**对 `DriftDetector.swift` / `ReAnchorLoop.swift` / 一致性否决门的逻辑做任何改动；§22.2.5 裁定三明确把「按状态显示提示」等更深修复**停下并交还用户**，未自行裁决。
- 本节**不新增队列、不新增锁**——B-31/B-32 是既有 `pin.store.io` 上的调用顺序与调用点调整；PIN-6 复用 `TapInstanceManager` 既有锁。
- 本节**未重开** Phase 4A Day 2–3（STOP RULE 继续有效）；未重新触发任何 re-anchor 重测。
- 本节**未原地改写** §19 / §21 —— 冲突处（§19.3.2 发布时机、§19.6 P-4 前置条件、§18.2 REC-2 相关的 R32 定性）均以「本节取代」的方式明示，原文保留可查。

### 22.5 Deliverable 确认

- [x] §22.1.1 §19.3.2/§19.3.4 冲突裁定：§19.3.4 优先，`publishSnapshot` 移至 `flushNow` 成功分支（B-32），代价与副作用明示
- [x] §22.1.2 P0-1 三点裁定：`storeUnavailable` 补位（decode+migrate 两条分支）+ 取证 `.bak` + 立即发布 `lastWriteError`（B-30）
- [x] §22.1.3 P0-2 裁定：四入口首行调用 `load()`，结构性消除而非检测（B-31）
- [x] §22.1.4 LRU 容量：条目数不变（32），修正文档字节量为 2 MiB；预热缓存判定为合法（B-33）
- [x] §22.1.5 P-4 前置条件重述，采纳 §37.6.2 建议措辞，取代 §19.6 P-4 首行
- [x] §22.1.6 B-29 归口：`#if DEBUG` 落 Persistence 目录，不新建测试 target，范围与触发方式给定
- [x] §22.1.7 actor 隔离标注方案（B-34）：七个值类型 `nonisolated`，`FilePinStore` 主线程表面 `@MainActor`
- [x] §22.1.8 P-1 两处歧义裁定：空值归一化为 `nil`（B-35）；字符计数口径为 UTF-16（B-35）
- [x] §22.1.9/10/11 P-4 埋点建议不强制；未处置项明确列出不静默略过；三处强制 flush 接线随 Day 5 UI 完成
- [x] §22.2 R27(i/ii/iii) + R32 统一裁决：手势时长互斥关闭 R27(iii)；PIN-6 单次锁内快照关闭 R27(i) 的数据风险面；R27(ii) 裁定不建 Pin↔TapInstance 绑定；R32 状态不变 + Pin 侧静态提示缓解（B-36）+ 明确拒绝触碰 re-anchor/promote 的方案并交还用户
- [x] §22.3 Builder 变更清单 B-29…B-36（接续 §19.7 的 B-28）
- [x] §22.4 边界声明：未碰冻结文件、未碰 re-anchor 代码、不新增队列/锁、未重开 Day 2–3

---

END OF PHASE 4B DAY 5 ARCHITECT RULING（§22 PinStore 补救 + Pin 交互裁决）

---

# Phase 4B — Day 6 (Architect) 两项裁决
# 2026-08-23
# 触发：(A) 用户在真机上点 PinList 行完成重访、mask 上屏，主诉「图钉不见了」；(B) §19.4.7 提出的替代验收判据自 Day 4 起一直未经裁决，Day 6 三条 Debugger 复选框因此悬空
# 输入：architect_output §19.4 全文 / §16 / §17 / §18.3.3 / §20.3 / §20.4.1 / §20.4.6 / §21.4.1 / §21.8 / §22；tasks.md Phase 4B Day 6 区块；debug_report §37 / §38 / §39；builder_progress Day 6 条目；源码 `Persistence/PinInterfaces.swift`、`UI/PinListView.swift`、`UI/ContentView.swift`、`Detection/CameraManager.swift`、`Interaction/FrameGeometry.swift`；2026-08-23 两次真机会话日志（转录等级）
# 范围：只裁 (A) 重访产物的身份显示边界与 G4 淘汰 UX、(B) Day 6 验收判据。⛔ 不重开 Phase 4A Day 2–3（STOP RULE 继续有效）；⛔ 不改动 re-anchor 任何逻辑与任何阈值；⛔ 不写可编译 Swift
# 本节以「本节取代」的方式明示与 §19.4 的冲突处，原文保留可查（§15 纪律）

## §23 Phase 4B Day 6 — 重访身份显示边界裁决（Ruling A）+ §19.4.7 验收判据裁决（Ruling B）（2026-08-23）

---

### 23.1 Ruling A — 重访产物可以显示什么样的 Pin 派生身份

#### 23.1.1 先把设备上真实发生的事说全 —— 是三条机制，不是两条

任务书列出两条（R-A 是「完全普通的活跃实例」故无 📌 无 tag；G4 无特例故重访会静默淘汰别的实例）。**读源码后必须补第三条，而且它才是主因：**

**重访产物在屏幕上没有任何锚点标记，连编号标记都没有。**

- `handleTap(fromPin:store:)` 唯一的动作是 `self.handleTap(canonicalPoint: derivedPoint, viewPoint: nil)`（`PinInterfaces.swift:291`）—— **`viewPoint` 传的是 nil**，因为本工程只有 view→canonical 的反演（`FrameGeometry.invertViewPoint`），**没有 canonical→view 的正向映射**，重访手上只有 canonical 坐标。
- `publishAnchorMarkersOnMain()` 的第一行是 `guard let vp = inst.viewPoint else { return nil }`（`CameraManager.swift:1133`）⇒ **`viewPoint == nil` 的实例不产生 `TapAnchorMarker`**。
- 同一个 nil 还关掉了在途反馈：`TapLoadingIndicator` 只在 `let p = cameraManager.lastTapViewPoint` 非 nil 时渲染（`ContentView.swift:63-65`），`TapRippleEffect` 的触发同样读 `lastTapViewPoint`（`:390`）。

⇒ **一次重访在画面上是「空间匿名」的**：解码期间没有原地脉冲、没有涟漪，解码完成后没有编号标记、没有 📌、没有 tag —— 只有屏幕顶部一句 "Re-segmenting at this location…"，和一张凭空出现的 mask。用户说「图钉不见了」是**字面准确的观察**，不是误读。

**定性（必须说清楚，因为它决定后面怎么裁）：**

| 机制 | 是否符合规格 | 判定 |
|---|---|---|
| 无 📌 / 无 tag | ✅ 符合 §19.4.4 R-A 的字面 | 规格的**边界没画到这里**，见 §23.1.2–23.1.4 |
| G4 可静默淘汰别的实例 | ✅ 符合 §19.4.3 G4 与 §3.2 | 规格正确，**呈现层欠一张回执**，见 §23.1.7 |
| **无任何锚点标记 + 无在途反馈** | ⛔ **不符合** §19.4.4「一个**完全普通**的活跃实例」 | **as-built 与契约的偏离**：普通 tap 实例是有标记的，重访产物严格少于「普通」。见 §23.1.5 |

⚠️ 第三条不是 Builder 的失误：在没有正向映射的前提下，传 nil 是它唯一能做的事。真正的缺陷在**契约**——§19.4.6 用「转调既有入口 ⇒ 免费继承全部不变量」论证 PIN-5 的正确性，却没有检查**新调用方是否供得起既有入口的全部参数**。见 §23.5 的 **M-23.1**。

#### 23.1.2 那条线到底在哪：**来源（provenance）可陈述，同一性（identity）不可陈述**

系统能以确定性说出的一句话：

> **「这张 mask 是在 Pin X 记录的坐标上、对**此刻**的画面重新解码得到的。」**

这句话的每一个成分都来自：读了哪条记录（磁盘事实）、读到什么值（字段事实）、代码走了哪条分支（执行事实）。三类都是可核验的。

系统**没有任何存储量**能支撑的一句话：

> 「这是 Pin X 当初框住的那个物体。」

理由不变（§19.4.1 / PIN-4 / R28）：embedding 永不持久化，重解用的是当前场景的 embedding；跨会话的物体同一性在本系统里**没有被测量的载体**。§20.4.6 进一步给了不利证据 —— 同一物体在同一会话内几秒之内轻微平移，IoU 就已掉到 0.57。

**裁定：显示落在第一句这一侧是允许的，落在第二句这一侧是禁止的。** §19.4.4 禁「恢复/找回」禁的正是第二句；它没有、也不应该禁第一句 —— **§19.4.4 把「不许宣称同一性」误落地成了「不许显示任何来源」，这是本节要修的那一处。**

#### 23.1.3 显示判定式（新不变量 **PIN-7**）—— 给 Builder / UI 的可执行判据

> **PIN-7：** 一个可见元素允许携带 Pin 派生信息，**当且仅当**把它翻译成一句陈述句之后，这句话的真值**只依赖**于以下三类事实：① 读取了哪条记录；② 读到的字段值是什么；③ 代码执行了哪条分支。
> 若它的真值还依赖「镜头前的东西是不是同一个物体」，⛔ **禁止**。
> 若它的真值只依赖上述三类事实、但**取值为假**，⛔ 同样禁止（这是一条独立的、更基本的禁令，见下表第 1 行）。

**逐项判定（本表即 Builder 的验收口径）：**

| 可见元素 | 翻译成的陈述句 | 真值依赖 | 判定 |
|---|---|---|---|
| 在重访产物上打 📌 | 「这张 mask 已被保存到磁盘」 | ①②（可判定） | ⛔ **禁止 —— 它是假的**。📌 由 `markInstancePinned` 在 `PinStore.create` 成功后置位（§22.2.4），其既定语义是「**这一张** mask 有一条磁盘记录」。重访产物的 mask 是**新解出来的**，磁盘上没有它；原 Pin 的 blob 是另一张图。打 📌 等于对存储状态说假话 |
| 裸 tag（marker 旁只写 `mug`） | 「这是 mug」 | 镜头内容 | ⛔ 禁止（同一性） |
| `From Pin “mug”` | 「本次分割是从名为 mug 的那条 Pin 发起的」 | ①② | ✅ 允许 |
| `Re-segmented at this Pin's saved point` | 「代码在该 Pin 的存储坐标上重跑了一次解码」 | ③ | ✅ 允许 |
| `Restored` / `恢复` / `找回` | 「你原来那个东西回来了」 | 镜头内容 | ⛔ 禁止（§19.4.4 原禁令，本节重申并扩大到图标） |
| `Tracking “mug”` | 「系统正在持续跟随 mug」 | 镜头内容 + 一项**未交付的能力** | ⛔ 禁止（能力 C 未交付，§20.3.3） |
| 把 IoU 数值放进用户可见 UI（哪怕叫「相似度 62%」） | 「新旧两张 mask 有 62% 的把握是同一个东西」 | 镜头内容 | ⛔ **禁止**。见 §23.1.6 对 §19.4.3「展示」一词的更正 |
| promote 结局上显示 "Re-segmenting…" | 「代码正在该位置重新分割」 | ③（可判定） | ⛔ **禁止 —— 它是假的**（promote 分支不解码）。见 §23.1.9 |

#### 23.1.4 裁定：重访产物的标记规格（**B-37**，P1）

**一、必须有锚点标记。** 重访产物是活跃实例，必须与普通 tap 实例一样在 `marker.viewPoint` 上有一个锚点标记；解码在途期间必须有原地脉冲。这是把 §19.4.4「一个完全普通的活跃实例」**兑现**，不是新增功能。

**二、坐标怎么来 —— 两条允许的路线，A1 为目标形态：**

- **A1（目标形态，推荐）：** 在 `FrameGeometry` 内新增 **canonical→view 的正向映射**，作为 `invertViewPoint` 的**逆函数**实现，而不是另写一套：Step 1 用 AVFoundation 文档化的对偶 `previewLayer.layerPointConverted(fromCaptureDevicePoint:)`；Step 2/3/4 是轴交换 / 翻转 / 缩放，逐条可逆。
  - ⚠️ **约束**：必须落在 `FrameGeometry.swift`（§2 坐标链的单一真源）内，⛔ 不得落在 UI 文件或 `PinInterfaces.swift`；注释必须写明「本函数是 `invertViewPoint` 的逆，不是第二条变换路径」。
  - 它的正确性由 **P-9**（§23.2.8）门控 —— 纯几何、零场景依赖、可失败。
- **A2（允许的 Day 6 权宜）：** 若 Builder 判断 A1 在本轮风险过高，可先把重访标记锚到**新 mask 在 view 空间的质心**（`MaskRenderer` 已持有该空间的映射）。
  - ⚠️ 采用 A2 时**必须**在 `builder_progress.md` 显式记录「重访标记的位置语义与普通 tap 标记不同（锚 mask 不锚 prompt 点）」，且解码在途期间仍无原地脉冲这一缺口必须一并写明。⛔ 不得默默采用。
  - A2 下 **P-9 不适用**，改为报告项。

**三、字形与标签（措辞受约束，口径同 §19.4.4 的严格程度）：**

- **字形**：重访产物用与 📌 **可区分**的字形（建议 SF Symbol `arrow.clockwise`，或文本字形 `↻`），沿用既有 chip 几何、既有 slot 配色、既有 primary 白环规则。⛔ **不得**使用 📌，⛔ 不得使用任何图钉形状的字符/符号 —— 理由见 PIN-7 表第 1 行（它会说一句假话）。
- **标签文本**：
  - ✅ 允许：`From Pin “<tag>”` ／ `From an untagged Pin`（tag 为空时）／ `Re-segmented at this Pin's saved point` ／ 在途文案 `Re-segmenting at this Pin's saved point…`
  - ⛔ 禁止：裸 tag；`<tag> again`；`This is <tag>`；`Your <tag>`；`Restored` / `Recovered` / `Got it back` / `恢复` / `找回` / `还原`；`Tracking` / `Following` / `Matched` / `Re-identified` / `Same object`；任何数字化的相似度/匹配度/置信度。
  - **硬性构词规则（便于复核）：tag 只允许出现在以 `From Pin` 开头的短语内，且必须带引号。** tag 一旦脱离 `From Pin` 前缀单独出现，即为违规。
- **展示时机**：字形随实例存活；文本标签沿用既有 `showsTag` 策略（点击时显示、数秒后收起），**并追加一次**：重访产物的 mask 首次上屏时自动显示一次该标签。理由 —— 用户说「图钉不见了」正是在那一刻，把连接关系补在那一刻最省。
- **生命周期**：与 📌 完全相同（§22.2.4）—— 装饰的是 `TapInstance`，随 C1/C2/C3/C4 一起消失。⛔ 不得为了让它「看起来更持久」而引入独立于 `TapInstance` 生命周期的展示状态。⛔ 不得写回 Pin 记录（PIN-1）。

#### 23.1.5 为什么不能「直接沿用 📌 + 原 tag」—— 把张力说透，不和稀泥

这是最省事的 UX 修法，本节**明确拒绝**，理由有两层，而且两层的强度不同：

1. **弱理由（同一性）**：📌 + 原 tag 读起来就是「你的 mug 回来了」。系统没有依据。这一层是 §19.4.4 已经裁过的，本节不重复论证。
2. **强理由（它是一句可判定的假话）**：📌 在本工程里**已经有一个确定的所指** —— `markInstancePinned` 只在 `PinStore.create` 成功之后被调用（`CameraManager.swift:1152`），所以 📌 的意思是「**屏幕上这一张 mask** 在磁盘上有记录」。重访产物的 mask 是刚解出来的、从未写盘的字节；给它打 📌，系统就在陈述一件它自己能查证为**假**的事。
   - 后果不是抽象的：用户据此以为「这张已经存好了」，实例被 FIFO 淘汰后什么都没留下 —— 这正是本项目最不该重犯的一类错误（§19.3.4「⛔ 禁止 UI 显示保存成功但盘上没有」的同形问题，只是搬到了标记上）。

⇒ **裁定：显示落在「来源」一侧（B-37 的 ↻ + `From Pin “…”`），不落在「同一性」一侧；📌 的所指保持唯一，不被稀释。**

#### 23.1.6 三处随之而来的措辞/语义更正

1. **§19.4.3 的「IoU 计算并**展示**、并记入日志」—— 本节更正为：「计算、**记入日志**、可进调试面；⛔ 不得进入任何用户可见 UI」。** 理由：一个成对 mask 的 IoU 一旦上了用户界面，无论叫什么名字，都会被读成同一性判断，而 §19.4.3 的整段立论正是「IoU 不参与任何判定」。原文的「展示」二字是本节发现的一处措辞漏洞，按 §15 纪律以本节为准。
2. **保存一个重访产物 ⇒ 新建记录，⛔ 不得回写原 Pin。** 重访产物可以被长按保存（Day 5 通道，§22.2.2），此时 `markInstancePinned` 置 📌 是**正确的**（它现在确实存了），↻ 装饰由 📌 取代。但写入的必须是一条**新的 `PinRecordV1`（新 UUID）**：`pointX/pointY/geometry/maskFile/createdAt` 全是冻结字段（PIN-1），且系统没有依据宣称两条记录描述同一个物体（R28）。⛔ 不得提供「更新原 Pin 的 mask」这种入口。
3. **§19.4.4 R-A 一行的「一个完全普通的活跃实例」** —— 本节取代为：「一个**普通的活跃实例（含锚点标记与在途反馈）**，外加一处**只陈述来源、不陈述同一性**的装饰（B-37，PIN-7）」。R-A 的其余文字（走 §3.3/§3.4 常规配色与层次、禁「恢复/找回」）**一字不改**。

#### 23.1.7 G4 淘汰 UX 的裁定：**规则不动，呈现层补一张回执**（**B-38**，P2）

**裁定一：⛔ 不设任何特例。** 「已 Pin 的实例不被淘汰」「重访不占池位」「重访优先淘汰非 Pin 实例」——三种方案**全部拒绝**。理由不是保守：任何一种都要让 FIFO 的淘汰顺序依赖 Pin 状态，也就是在 `TapInstance` 与 Pin 之间建一条绑定，而 **§22.2.4 已经裁定不建这条绑定**，且 §3.2 的池不变量（FIFO(max=3)，无特例）是 §19.4.6「免费继承」论证的地基之一。为一条提示去动池规则，是拿地基换装修。

**裁定二：呈现层补一张回执，且刻意**不做成重访专属**。** 淘汰发生时（**任何**淘汰，包括普通第 4 次 tap，不只是重访触发的那次），复用既有的瞬时横幅表面显示一条短提示。

- **为什么不做成重访专属**：同一个困惑在普通第 4 次 tap 上一模一样地存在；做成通用的，既修了重访这一处，又不产生「重访有特例」这个新面。**这是本条不违反 §3.2 的具体形式：它不读池的任何新状态、不改变任何淘汰决定，只是把池**已经做出**的决定说出来。**
- **实现面为零新增状态**：淘汰分支已经在同一个 `DispatchQueue.main.async` 块里读 `pinnedInstanceIDs` / `pinnedInstanceTags` 做清理（`CameraManager.swift:1362-1365`），「被淘汰的那个是不是已 Pin 的」这条事实**已经在手**。
- **措辞（受约束）：**
  - ✅ 允许：`Oldest selection removed — 3 on screen at a time.`；当被淘汰实例带 📌 时追加：`Its saved Pin is still in Pin List.`
  - ⛔ 禁止：`Pin deleted` / `Pin removed` / `Lost` / 任何暗示**磁盘记录**被删的措辞；`You can restore it`（「restore」词族本身受禁）。
  - 追加句的真值只依赖「磁盘上那条记录还在」——属 PIN-7 的 ①，允许。

#### 23.1.8 「重启后一个标记都没有」= 不是数据丢失，但入口要看得见（**B-39**，P2）

§22.2.4 的推论（📌 装饰 `TapInstance`，重启后自然没有）**完全正确，本节不翻转一个字**。但用户第二次把它读成数据丢失，说明缺的不是标记，是**入口的可见性**。

- **裁定：`ContentView` 导航栏的 "Pin List" 按钮在 `store.pins.isEmpty == false` 时携带一个条数徽标。**
- 真值只依赖磁盘记录条数（PIN-7 ①）⇒ 允许。它不在相机画面上画任何东西，因而不建立任何 Pin↔实例绑定，也不做任何几何宣称（一个存储的 `canonicalPoint` 在 G1 通过之前对当前画面**没有意义**，⛔ 因此**明确禁止**「把所有已存 Pin 常驻叠在相机画面上」这一类方案）。
- 措辞：✅ 徽标只显示数字，或 `Pin List (7)`；⛔ 禁止 `7 objects` / `7 tracked` / `7 saved objects`（对象词族即同一性宣称）。

#### 23.1.9 as-built 缺陷：重访落进已有 mask ⇒ promote —— **新结局 R-D**（**B-40**，P1）

**§19.4 从头到尾没有考虑重访与 §3.2 promote 规则的交叉面。** §19.4.6 把「tap 落在已有 mask 内 ⇒ 提升 primary 不重解」列为**免费继承的好处**，没有注意到它同时制造了 R-A/R-B/R-C 都盖不住的**第四种结局**。设备上这条路径必然可达（重访点常常落在别的实例的 mask 里，尤其在池已满的场景）。

**逐条后果（源码可核实）：**

| # | 后果 | 依据 |
|---|---|---|
| 1 | 屏幕显示 `Re-segmenting at this location…`，**而实际上没有发生任何解码** —— 一句可判定的假话（PIN-7 表末行） | promote 分支 `CameraManager.swift:1336-1347` 直接 `return myGen`；`handleTap(fromPin:)` 只看 `gen > 0` 就置 `lastTapWasRevisit = true`（`PinInterfaces.swift:292-304`） |
| 2 | **该次重访不产生任何 `[PIN] revisit` 日志行** —— `onTapMaskPlaced` / `onTapFailed` 在 promote 分支都不触发，`Join` 永远等不齐 | `PinInterfaces.swift:312-331` 的 `finish()` 需要 placement 或 didFail 之一 |
| 3 | `PinRevisitTracker` 的两个字典条目**永久泄漏**（`gen` 单调递增，不会有第二次机会释放），且闭包持有已从磁盘读回的 origin blob ⇒ **每次这样的重访泄漏约 64 KB** | `PinInterfaces.swift:151-152, 160-161`；`placed`/`failed` 只在两个 hook 内 `removeValue` |
| 4 | `lastTapWasRevisit` **在 promote 分支与 `failTap`（camera not ready）分支都没有被重置为 false** —— 重置只写在 `addInstance` 之后的主线程块里（`CameraManager.swift:1378`）⇒ 一次重访之后，**普通**屏幕 tap 若落进已有 mask，会显示重访文案 | 同上 |

**裁定：新增结局 R-D，与 R-A/R-B/R-C 并列，写入 §19.4.4 的结局表（本节取代该表，追加一行）：**

| 结局 | 触发 | 呈现 |
|---|---|---|
| **R-D 重访解析为 promote** | G1–G3 通过，但派生点落在某个现存实例的 mask alpha 内（§3.2 promote 分支） | 该实例被提升为 primary（既有行为，不变）。⛔ **禁止**任何 "Re-segment(ing)" 文案。✅ 允许的文案：`That spot is already covered by a selection on screen.`（可追加 `From Pin “<tag>”` 作为来源说明）。必须产出一行 `outcome=promote` 的重访日志（§23.2.4 的格式） |

**B-40 的具体要求（规格，非实现）：**
1. `CameraManager` 新增**第三个纯观测闭包** `onTapPromoted: ((_ gen: Int, _ promotedInstanceID: UUID) -> Void)?`，在 promote 分支已有的主线程块内触发；与既有两个 hook 同形、同纪律。
2. `lastTapWasRevisit = false` 的重置点移到**每一条**会返回 `gen > 0` 的路径都必经的位置（promote 分支、`failTap` 早退分支、正常分支三处覆盖）。
3. `PinRevisitTracker` 对每个注册的 `gen` 必须有**穷尽的释放点**：placed / failed / promoted 三者之一必到。
4. ⇒ 新不变量 **PIN-8**（§23.5）。

#### 23.1.10 本节明确拒绝、不设计、不建议现在做的方案

- ⛔ 让 FIFO 或 promote 感知 Pin 状态（任何形式的 Pin↔`TapInstance` 绑定）—— §22.2.4 已裁，本节维持。
- ⛔ 把已存 Pin 常驻叠加在相机画面上（§23.1.8）。
- ⛔ 用 IoU 或任何相似度在 UI 上表达「这大概率还是原来那个」（§23.1.6 第 1 条）。
- ⛔ 为重访新建等待 UI（§19.4.2 原禁令不变；B-37 要求的是复用既有原地脉冲，不是新建）。
- ⛔ 为重访产物提供「更新原 Pin 的 mask」入口（§23.1.6 第 2 条）。

---

### 23.2 Ruling B — §19.4.7 提出的验收判据裁决

#### 23.2.1 第一层理由：**§19.4.7 与 §19.4.3 自相矛盾**，这一层不需要任何设备数据

§19.4.3 写得毫不含糊：

> ⛔ **不设「相似度门」。** 不得用 `IoU(存储 alpha, 新 alpha)` 去否决重解 …… IoU **计算并展示、并记入日志，但不参与任何判定**。

§19.4.7 随后提出：`iou` 中位数 ≥ 0.7 作为验收线。

**一条验收复选框就是一个判定。** 同一个量，在准入时被判定为「不能判定任何事」，在验收时却被当成通过与否的依据 —— **理由不会因为使用时机不同而改变**：§19.4.3 拒绝它的根据是「重访时相机本来就动过，低 IoU 是合法结果而非错误」，这句话在验收台上一字不变地成立。

⇒ **裁定：§19.4.7 的「中位数 IoU ≥ 0.7」作废，且 ⛔ 不得以任何 IoU 阈值形态复活（换分位数、换阈值、换分辨率、换 stride 都不行）。** 这不是数据问题，是这一条**从写下的那一天起就与它上一节相冲突**。

#### 23.2.2 第二层理由：即便忽略上一层，这条线在**两个方向上同时是错的**

**方向一 —— 它会放过垃圾（Session 2 的反例）：** TAP#10 产出的是一张坏 mask（`iou_preds 0.545`、`stab=0.16`、面积 155 px，`gate iou_pred(selected)=0.545`），被存成 Pin `3620286D`。之后重访它报 **`iou=0.82`**。0.82 的含义是**解码器忠实地复现了自己先前的错误**，不是「重访工作正常」。

- ⚠️ 但要说准确：这条读数**并非全无信息**。155 px 只占 65,536 的 0.24 %，两个如此小的斑块要重合 82 %，派生坐标必须落得很准 ⇒ **它其实是坐标保真度的证据**。问题在于判据没有这么写 —— 它写的是一条听起来像质量的线，实际测到的是坐标 + 场景静止 + 解码器可复现性 + 两端 mask 质量的**混合物**。**判据说的和它测的不是一回事。**
- ⇒ 这条反例**正面支持** §19.4.3「不设相似度门」的裁决（任务书的判断成立），同时**否定** §19.4.7 把同一个量搬到验收台上的做法。

**方向二 —— 它会误杀正确的重访：** §20.4.1 第 3 点已确立本项目的 IoU 经验分簇：**迁移 0.02–0.25 / 同物体 0.57–0.89 / 同世代 no-op 1.00**。0.7 这条线**落在「同物体」簇的中间**。§20.4.6 更进一步：「跨会话重访的同物体 IoU **只会更低**」。⇒ 一次完全正确的、对着同一个物体的重访，合法地落到 0.6 是**预期之内**的。Session 1 的 `iou=0.61` 正是这个形态。

**方向三 —— 在它自己规定的条件下，这条线又太松：** §19.4.7 要求「同会话、机位不动」。机位真不动、embedding 复用同一份、派生点逐位相同 ⇒ 解码是确定性的，IoU 应当 ≈ **1.00**（这正是 §20.4.1「同世代 no-op 1.00」那一簇）。Session 2 的 0.98 就在这个位置。所以在「机位不动」这个前提下，0.7 松得毫无判别力；一旦前提不成立（0.61 那次），它又变成误杀。**一条在自己的前提下太松、脱离前提就太紧的线，测不出任何东西。**

#### 23.2.3 把教训立成长期约定 —— **A-19（场景相关量不得充当门控判据）**

> **长期约定 A-19：** 若一个量的取值由**镜头前的内容**决定，而内容不在测试协议的控制之下，则该量只能作为**报告项**，⛔ 不得作为**门控线**。把它写成门控线，等于把验收结果交给拍摄对象。
> **可门控的量必须满足其一：**(i) 取值由代码路径决定（走了哪条分支、日志是否完备、字段是否逐位保真）；(ii) 取值由测试者能完全控制的自变量决定（持机方向、前后摄、模式、是否强制慢路径）。

- **与 A-7 的分工**：A-7 问「这个量是被**测出来**的还是被**算出来**的」；**A-19 问「这个量的值是由谁定的 —— 代码，还是镜头前的东西」。** 两条都只需读赋值点，成本同为分钟级。
- **A-19 一眼就能抓到的两条**：tasks.md 原文「重访 decode 成功率 ≥ 80 %**（在相近场景下）**」—— 那个括号就是自供状，它把判据的成立条件外包给了一个不受控的变量；§19.4.7 的「中位数 IoU ≥ 0.7」同理。
- **一致性交叉检验**：A-19 独立地重新推导出了 §19.4.1 第 2 点的结论（「decode 成功率」不是可用判据），这是它成立的一个旁证。

#### 23.2.4 前置：日志行必须先改，判据才有东西可读（**B-41**，P1）

现行 `[PIN] revisit` 行（§19.4.6）在设备上暴露出三个洞：

1. **G2/G3 拒绝走的是另一种格式**（`[PIN] revisit id=… rejected reason=wrongMode`，`PinInterfaces.swift:214/223`）—— 与 §19.4.6 规定的格式不同构，无法用一条 grep 统计全部重访。
2. **promote 结局没有任何行**（§23.1.9 后果 2）。
3. **没有任何字段能回答「这次重访一共发起了几次」** ⇒ 「日志是否完备」本身不可判定。

**裁定（本节取代 §19.4.6 的日志行格式；`iou` 的计算口径——256×256 空间、`stride: 1` 全遍历、复用 `DriftDetector.alphaIoU`——一字不改）：**

```
[PIN] revisit id=<uuid8> seq=<N> outcome=(mask|promote|refused|failed) reason=(n/a|mode|cameraNotReady|orientation|camera|orientationCamera|promptSpace|aspect|decodeFailed|timeout) geo=(ok|remapped|refused|n/a) path=(fast|slow|n/a) pt=(<x.1>,<y.1>|n/a) pin=(<x.1>,<y.1>) iou=<0.00–1.00|n/a> origin=<N>px new=<N>px|n/a
```

- **`seq`**：会话内单调递增的重访序号，在 `handleTap(fromPin:)` **入口处、任何门之前**自增。这一个字段就让「发起次数 == 日志行数」变成可判定的（P-7 的门控依赖它）。
- **`outcome`**：`mask`=R-A ／ `promote`=R-D ／ `refused`=R-B ／ `failed`=R-C。**四种结局都必须走这一行格式**，不再有第二种格式。
- **`pt`**：本次实际送进 prompt 的派生 canonical 点；**`pin`**：记录里存的点。`geo=ok` 时两者必须相等，`geo=remapped` 时必须差一个 §19.4.5 的比例因子 ⇒ **坐标派生第一次变成可从日志直接核验的量**。
- **每次进入 `handleTap(fromPin:)` 必须恰好产出一行，且必须在某个终局产出。** ⛔ 不新增定时器 —— 既有 tap 超时已经会走 `failTap ⇒ onTapFailed`；唯一的缺口是 promote，由 B-40 的第三个观测钩子补上。

#### 23.2.5 **P-6｜重访准入门与结局路由（门控）**

> 取代 tasks.md「重访 decode 成功率 ≥ 80 %」的**正向与负向路径部分**。全部 5 例的取值都由代码分支与测试者可控的自变量决定 ⇒ 满足 A-19(i)/(ii)。

| # | 制造方式（测试者可完全控制） | 期望可观测（逐字段比对） |
|---|---|---|
| **C1 G2 模式门** | 切到非 `.tapToSegment` 模式，打开 Pin List，点一行 | 日志 `outcome=refused reason=mode`；屏上提示切换模式；⛔ **模式未被自动切换**（切完之后当前模式仍是原模式） |
| **C2 G1 朝向** | 竖持保存一条 Pin → 转横持 → 重访该 Pin | `outcome=refused reason=orientation`；打开**静态查看器**（灰度 + 虚线框 + "STATIC SNAPSHOT"），且原因文案**同时写出记录值与当前值**（portrait/landscape、front/back）；⛔ 不得是通用文案 |
| **C3 G1 摄像头** | 后摄保存一条 Pin → 切前摄（保持同一朝向）→ 重访 | `outcome=refused reason=camera`；同 C2 的静态查看器与具体原因文案 |
| **C4 R-A 正常** | 同朝向同摄像头，重访一条 Pin | `outcome=mask geo=ok`，且 `pt == pin` **逐字段相等**；屏上出现活跃 mask **且**出现锚点标记 + 来源装饰（B-37）；文案符合 §23.1.4 允许表 |
| **C5 R-D promote** | 先在画面上 tap 出一个覆盖该 Pin 坐标的实例，再重访同一条 Pin | `outcome=promote`；⛔ 屏上**不得**出现任何 "Re-segment(ing)" 文案；该实例变为 primary |

- **PASS = 5 例各执行 ≥1 次，5/5 的 `outcome`/`reason`/`geo`/`pt` 与预期逐字段相等，且 5/5 的屏上表面符合上表。** 任何不符**必须点名到 case 与字段**。
- ⛔ **不设 N=10 之类的次数下限。** G1/G2 是逐位比较的确定性分支，重复 10 次不增加任何信息 —— 这一条直接取代 §19.4.7「10/10 落到 R-B」的形态：**负向路径必须被测（§19.4.7 这一半是对的），但「必须被测」不等于「必须被测十遍」。**
- **明确不列为门控的两项（写明理由，不静默略过）：**
  - **G3 相机未就绪**：无法确定性制造（依赖首帧到达前的时间窗）。⇒ 报告项：若偶遇则记录，⛔ 不为它新增调试钩子。
  - **`geo=remapped`（§19.4.5 的重映射）**：**在当前构建里结构性不可达** —— 全工程只有一处 `session.sessionPreset = .high`（`CameraManager.swift:3108`）且从不改动，`origW/origH` 唯一可能变化的途径是切换前后摄，而那会同时翻转 `mirrored` ⇒ G1 先一步判 `refused`，永远走不到重映射分支。同理 `promptSpace` 是编译期常量，其拒绝分支在同一构建内不可达。⇒ 见保留项 **R35**。

#### 23.2.6 **P-7｜重访问责 + IoU 报告（必报，含一条可失败的完备性门）**

**门控（唯一一条，且当前 as-built 必挂）：**
> 一个会话内从 Pin List 发起的重访次数 R（人工计数）必须等于该会话 `[PIN] revisit` 行数，且行内 `seq` 恰好覆盖 1…R，**无缺号、无重号**。

- 这一条的取值完全由代码决定（A-19(i)），可失败，且**现在就会失败**（promote 结局无行，§23.1.9）。⇒ 它同时是 B-40/B-41 的回归门。
- 形式上属 **A-17 族**（安全/异常分支必须在可观测面上可区分）：一次没有留下痕迹的重访，与一次没有发生的重访，在日志上目前完全相同。

**必报（⛔ 不设任何达标线）：**
1. ≥8 次 `outcome=mask` 的重访，覆盖 **≥3 条不同 Pin**，且至少 1 次 `path=fast`、1 次 `path=slow`（用既有 Debug Options → **Force Slow Path** 制造 —— 这是测试者可控的自变量，满足 A-19(ii)）。
2. 逐行报告 `seq / geo / path / pt / pin / iou / origin / new`，并给出 IoU 的 **min / median / max**。
3. 报告**必须**附上这一句固定的解读框，逐字照抄：
   > 「本项目已确立的 IoU 分簇（§20.4.1 第 3 点）：迁移 0.02–0.25 ／ 同物体 0.57–0.89 ／ 同世代 no-op 1.00。**重访 IoU 落在哪一簇不构成任何通过或失败判定**（§19.4.3 / §23.2.1）。」
4. **反例制度化（本项目吃过一次亏，写进判据防止下一个人重蹈）：** 若某条重访 `iou ≥ 0.7`，而该 Pin 创建时的 `[TAP]` 行显示 `iou_pred(selected) < 0.6` **或** `stab < 0.3`，报告**必须**点名该条，并注明「**高 IoU 在此条上只说明解码器复现了自己**」。Session 2 的 `3620286D`（创建时 `iou_preds 0.545` / `stab 0.16` / 155 px，重访 `iou=0.82`）就是这个形态。

#### 23.2.7 **P-8｜AnnotationView 编辑 / 删除回归（门控）**

> 取代 tasks.md「AnnotationView 编辑后 Pin 数据正确更新」—— 原文的**意图正确**（这确实是 Day 6 唯一一条能真失败的写路径判据），但没有任何可执行口径。以下是口径。

**编辑（取 2 条已有 Pin，其中 1 条把 tag 改成空串）→ 保存 → 冷启动 → `fetchAll` 逐字段比对：**
- `tag` / `note` 等于输入；**空串必须回读为 `nil`**（B-35 的归一化，⛔ 不是 `""`）；
- 字符上限口径为 `PinFieldLimits.length(of:)`（UTF-16），store 与 UI 用的**是同一个函数**（B-35）；
- `updatedAt` **严格大于**编辑前的 `updatedAt`；`createdAt` **逐位不变**；
- **冻结字段逐位不变（PIN-1）**：`id` / `pointX` / `pointY` / `geometry` 的**全部**子字段 / `maskFile` / `maskWidth` / `maskHeight` / `maskNonZero`；
- blob 不变：重新 `loadMaskImage` 解回的 `[UInt8]` 与编辑前**逐字节相同**。

**删除（删 1 条）→ 冷启动：**
- 该 id 不出现在 `fetchAll`；
- `[PIN] load ok … orphans=` 字段为 **0**（blob 已随记录一并消失，没有留下孤儿）。

- **PASS = 上述每一条**；任何不符**必须点名到字段**。
- ⚠️ **与 §19.6 P-1 的关系**：方法相同（冷启动逐字段），但 **P-8 不等待 P-1，也不被 P-1 取代** —— P-1 至今**未执行**（§39.8），且 P-1 的输入是合成记录、P-8 的输入是真实 UI 操作，覆盖的是不同的写路径。
- ⚠️ **与 R34 的关系**：R34（PinStore 计数倒退，OPEN/P0）未澄清。**若 P-8 在冷启动后读到的条数与写入序列不符，⛔ 不得就地判 P-8 FAIL，而应按 R34 上报**（这是 §39.8 对 P-3 已经确立的同一条纪律）。

#### 23.2.8 **P-9｜canonical ↔ view 往返 ≤ 1 px（门控；仅当 B-37 走 A1 路线）**

- 对当前 geometry，取 canonical 空间的 9 点网格（四角、四边中点、中心，四角各向内收 1 px），对每点先 `forward`（canonical→view）再 `invertViewPoint`（view→canonical），要求回到原点的 **Chebyshev 距离 ≤ 1.0 px**。
- 四组工况：**竖持 × 后摄 / 竖持 × 前摄 / 横持 × 后摄 / 横持 × 前摄**（覆盖 rotation 分支与 `mirrored` 分支）。
- **PASS = 4 组 × 9 点全部 ≤ 1.0 px。**
- **这是本节唯一一条完全不依赖镜头前内容的判据**，也是 A-19 意义上「可门控量」的样板：它的值只由代码决定，它能失败，且失败必定指向一处真实的坐标缺陷。
- 若走 A2（mask 质心锚定），**本条不适用**，改为报告项（见 §23.1.4 二/A2 的记录要求）。

#### 23.2.9 Day 6 出场条件与执行顺序

```
B-40（R-D 路由 + 泄漏 + 标志复位） ─┐
B-41（统一日志格式 + seq + pt/pin）─┴→ P-6 / P-7 可执行
B-37（重访标记）────────────────────→ P-6 C4 的屏上部分 / P-9 可执行
（无前置）────────────────────────→ P-8 可执行
```
- **Day 6 出场条件：P-6 ∧ P-8 ∧（若走 A1）P-9 **通过**；P-7 的完备性门**通过**且其必报部分**已报告**。**
- ⚠️ **2026-08-23 两次会话的既有读数不能用来满足以上任何一条** —— 日志格式将由 B-41 改变，`seq`/`outcome`/`pt`/`pin` 四个字段当时不存在。**那两次会话交付的是 M-20.1 意义上的「激活证据」**（重访路径在真机上端到端跑通：G1/G2/G3 都真的执行过，R-A 与 R-B 都真的到达过，日志行带着真实数字落了地），**不是质量证据，也不是验收证据**。这一条区分必须随结论同行。

#### 23.2.10 相邻证据的落账（⛔ 不改任何数字、不改任何条目状态）

**一、一致性否决门自 ISSUE-P4-GATE 修复后首次在真机上开火，且开对了。**
- 读数：`[REANCHOR][inst#0] drifted 14.6lum → iou: 0.05 origin: 157px new: 2917px` ⇒ `rejected — mask IoU 0.05 < 0.50, keeping previous mask`。一次真实的 **18.6 倍**面积暴涨被否决。
- **落账为 §20.4.1 第 3 点分簇结构的第三次独立确认**：0.05 落在「迁移 0.02–0.25」簇内，与 §20（4 次 20–47 倍粗迁移）、§21 的读数同形。
- ⛔ **不动 `reAnchorAcceptIoU`，不动 45.5 %，不动 §20.4.1 的上下界推论。**
- **否决率两次会话合计约 1/50 ≈ 2 %，与 §20 的 45.5 %、§21 的 84 % 并列** ⇒ 直接落入 **§21.4.1 已经确立的判读**：「否决率是**场景属性**，不是**参数属性**」。⇒ **三次读数的离散本身就是该判读的证据，⛔ 不得取平均、不得当作趋势、不得据以调参。**
- **对 Ruling B 的作用**：它正是 A-19 的另一个实例 —— 一个由镜头前内容决定的比率，做报告项有信息，做门控线只会随场景摇摆。

**二、撤回一条我此前对用户做出的、错误的论断（本节正式记录，防止它被后续引用）：**
- **被撤回的论断**：「§RE-3 冻结 `originAlpha` 作基准 ⇒ re-anchor IoU **单调衰减** ⇒ 任何长寿实例最终必被永久否决。」
- **证伪它的数据**：Session 2 的同一实例序列 `1.00 → 0.98 → 0.95 → 0.97`；重读 Session 1 亦有回升 `0.67 → 0.70`。
- **准确的陈述（今后引用这一条）**：**冻结基准使 IoU 跟踪的是「相对**捕获时刻**的累计偏离量」——场景漂离时它下降，场景回到锚点时它回升。它不是时间的单调函数。**
- ⛔ **任何裁决、判据、保留项都不得建立在单调衰减前提上。**
- **自我批评（写下来才有用）**：这条错误论断当时就与 §20.1.4 已经**观测到**的 REC-1（自动恢复）相矛盾 —— 一个会自动恢复的机制在定义上就排除了单调衰减。**我在做那次分析时没有回头核对已归档的观测**。这与 §18.9 归因过的三次缺陷同族：**结论没有回到赋值点/观测记录去对账。** 撤回本论断后，R25 / §18.2「吸收态是正确终态 + REC-1/REC-2 退出」的原有定性**完全成立、不受影响**。

---

### 23.3 §19.4 经设备证据判定为**错误**或**欠规定**之处（清单，便于日后检索）

| # | 条款 | 定性 | 处置 |
|---|---|---|---|
| 1 | §19.4.4 R-A「一个**完全普通**的活跃实例」 | **欠规定** —— 「普通」二字同时决定了呈现层次（本意）与身份显示（未经审查），落地成「连锚点标记都没有」 | §23.1.4 取代该行；B-37 |
| 2 | §19.4 全篇未考虑**重访 × §3.2 promote** 的交叉面；§19.4.6 反而把 promote 列为「免费继承的好处」 | **错误（漏了一种结局）** —— 制造假文案、丢日志行、64 KB/次泄漏、标志污染普通 tap | §23.1.9 新增 **R-D**；B-40 |
| 3 | §19.4.6 的日志行格式 | **欠规定** —— 拒绝路径实际走了第二种格式；无 `seq` ⇒ 完备性不可判定；无 `pt/pin` ⇒ 坐标派生不可核验 | §23.2.4 取代该格式；B-41 |
| 4 | §19.4.7「中位数 IoU ≥ 0.7」 | **错误** —— 与 §19.4.3 自相矛盾；且两个方向同时错（放过 0.82 的垃圾、误杀 0.61 的正确重访、在自身前提下又太松） | §23.2.1–23.2.2 作废；P-6/P-7 取代 |
| 5 | §19.4.7「10/10 落到 R-B」 | **部分正确** —— 「负向路径必须被测」成立；「必须测十遍」不成立（确定性分支） | §23.2.5 保留意图、改为 5 例各 ≥1 次 |
| 6 | §19.4.5 唯一允许的坐标重映射 | **结构性不可达** —— 单一 `sessionPreset` 且从不改；`origW/origH` 唯一变化途径（切前后摄）必然先翻 `mirrored` ⇒ G1 先判 refused | §23.2.5 明确不写判据；登记 **R35** |
| 7 | §19.4.3 G1 的 `promptSpace` 维度 | **同构建内不可达**（编译期常量） | 同上，归 R35 |
| 8 | §19.4.3「IoU 计算并**展示**」 | **措辞漏洞** —— 「展示」可被读成「给用户看」，与该段整体立论冲突 | §23.1.6 第 1 条更正为「记入日志/调试面，⛔ 不入用户 UI」 |
| 9 | §19.4.6 PIN-5 的「免费继承全部不变量」论证 | **有洞** —— 继承只对「不依赖你供不起的那些参数」的不变量免费；`viewPoint: nil` 就是代价落地处 | §23.5 **M-23.1** |

---

### 23.4 Builder 变更清单（编号接续 §22.3 的 B-36）

| # | 内容 | 位置 | 优先级 |
|---|---|---|---|
| **B-37** 🆕 | 重访产物的锚点标记 + 来源装饰：① 补上锚点标记与在途原地脉冲（路线 A1 = `FrameGeometry` 内新增 `invertViewPoint` 的逆函数；A2 = mask 质心锚定，须显式记录语义差异）；② 字形用 ↻ 族，⛔ 不得用 📌；③ 标签严格遵守 §23.1.4 的允许/禁止表与「tag 只能出现在 `From Pin “…”` 内」的构词规则；④ 生命周期同 📌（随 `TapInstance` 消亡），⛔ 不写回 Pin | `FrameGeometry.swift`（A1）/ `PinInterfaces.swift` / `CameraManager.swift`（标记发布）/ `ContentView.swift` | **P1** |
| **B-38** 🆕 | FIFO 淘汰回执横幅（**对一切淘汰生效，非重访专属**）：复用既有瞬时横幅表面；被淘汰实例带 📌 时追加 `Its saved Pin is still in Pin List.`；措辞按 §23.1.7。⛔ 不改任何淘汰决定、不新增池状态 | `CameraManager.swift` 既有淘汰主线程块 / `ContentView.swift` | P2 |
| **B-39** 🆕 | "Pin List" 导航按钮加条数徽标（`store.pins.count`，为 0 时不显示）；措辞按 §23.1.8，⛔ 禁 object 词族 | `ContentView.swift` | P2 |
| **B-40** 🆕 | **R-D 路由修复（三项，缺一不可）**：① `CameraManager` 新增第三个纯观测闭包 `onTapPromoted(gen:promotedInstanceID:)`，在既有 promote 主线程块内触发；② `lastTapWasRevisit = false` 的复位覆盖**每一条**返回 `gen > 0` 的路径（promote / `failTap` 早退 / 正常）；③ `PinRevisitTracker` 对每个注册 `gen` 有穷尽释放点（placed/failed/promoted 三选一必到），消除 64 KB/次泄漏。R-D 文案按 §23.1.9 结局表 | `CameraManager.swift` / `PinInterfaces.swift` | **P1（阻塞 P-6/P-7）** |
| **B-41** 🆕 | 统一 `[PIN] revisit` 日志行为 §23.2.4 的单一格式（新增 `seq` / `outcome` / `reason` / `pt` / `pin`；四种结局同一格式，取消 `rejected reason=…` 那条异构行）。`iou` 的计算口径（256×256、`stride: 1`、复用 `DriftDetector.alphaIoU`）**一字不改**；⛔ 不新增定时器 | `PinInterfaces.swift` | **P1（阻塞 P-6/P-7）** |
| （追认） | **B-28 的签名扩展 `handleTap(fromPin:store:)` 予以追认。** 约束：`store` **只**用于读回 origin blob 供日志 `iou` 字段，⛔ 不得用于写、⛔ 不得在 `videoQueue`/`decoderQueue`/`encoderQueue` 上被引用（PIN-3 未被扩大触碰面，本节复核确认） | — | — |
| （追认） | `PinRevisitTracker` 作为两个（B-40 后为三个）观测钩子的**唯一安装者**予以追认，并升格为不变量 **PIN-8** | — | — |

---

### 23.5 新增不变量 / 长期约定 / 保留项汇总

| # | 内容 | 状态 |
|---|---|---|
| **PIN-7** 🆕 | **Pin 派生的可见元素只允许陈述来源（provenance），不得陈述同一性（identity）；且不得陈述可判定为假的事实。** 判定式与逐项裁定见 §23.1.3 | 立，§23.1.3 |
| **PIN-8** 🆕 | `onTapMaskPlaced` / `onTapFailed` / `onTapPromoted` 是**纯观测出口**：⛔ 不得有任何控制流读取它们或据其分支（否则就是伪装成钩子的第二条 decode 路径，违反 PIN-5）；且**每一个注册的 `gen` 必须在有限时间内被释放**，每一种 tap 结局都必须有对应的释放点。三个钩子的安装者全工程唯一 | 立，§23.1.9 / §23.4 |
| **A-19** 🆕 | **场景相关量不得充当门控判据。** 取值由镜头前内容决定、而内容不受测试协议控制的量，只能做报告项。可门控量必须由代码路径或测试者可控自变量决定。与 A-7 的分工：A-7 问「测出来的还是算出来的」，A-19 问「值由谁定 —— 代码还是镜头」 | 立，§23.2.3 |
| **M-23.1** 🆕 | **以既有入口表达新功能（PIN-5 型复用）时，若新调用方供不起既有入口的某个（可选）参数，必须在交付报告里显式列出该缺省及其可观测后果。**「转调既有入口 ⇒ 免费继承全部不变量」这条论证，**只对不依赖该参数的不变量成立**；`viewPoint: nil` 就是本项目为这个洞付出的第一次代价 | 立，§23.1.1 / §23.3 第 9 行 |
| **R35** 🆕 | **§19.4.5 的坐标重映射与 G1 的 `promptSpace` 维度在当前构建里结构性不可达** ⇒ 两段代码**永远无法被任何真机测试执行到**，其正确性只有推导、没有实证 | **OPEN，P3，Owner Architect。** 处置：代码保留（推导正确、成本为零），但 ⛔ 不得写进任何验收判据，⛔ 不得对外声称「支持切换分辨率后重访」。重开触发：任何引入第二个 `sessionPreset`、可变 `promptSpace`、或多分辨率采集的改动 |
| **R28** | 重访的物体同一性无法核验 | **OPEN，P2，状态不变。** 本节新增内容：§19.4「不设相似度门」的裁决**扩展到验收侧**（§23.2.1）——同一个量既不能准入判定，也不能验收判定；Session 2 的 0.82 反例是它的第一条正向实证 |
| **R32** | 冻结态恢复路径存在但不可发现 | **OPEN，P2，状态不变。** 本节未触碰 |
| **R34** | PinStore 计数倒退，根因未明 | **OPEN，P0，状态不变。** 本节未触碰；P-8 与它的交互纪律见 §23.2.7 末条 |
| **R21 / R25 / §20.4.1 全部数字** | — | **一字不改**（§23.2.10 只做落账） |

---

### 23.6 tasks.md 应做的修改（⛔ 本节不代改，交用户决定何时应用）

**一、Day 6 `## Debugger` 区块的三条复选框，整体替换为：**

```
## Debugger
- [ ] **P-6｜重访准入门与结局路由（门控）** —— architect_output.md §23.2.5：C1 模式门 / C2 朝向 / C3 摄像头 / C4 R-A 正常 / C5 R-D promote，各 ≥1 次，5/5 逐字段比对 outcome/reason/geo/pt 与屏上表面
- [ ] **P-7｜重访问责 + IoU 报告（必报，含一条门控）** —— §23.2.6：门控 = 「Pin List 发起次数 == [PIN] revisit 行数且 seq 覆盖 1…R 无缺号」；必报 = ≥8 次 R-A、≥3 条不同 Pin、fast/slow 各 ≥1；⛔ IoU 不设任何达标线，报告须附 §20.4.1 分簇解读框与高-IoU 反例点名规则
- [ ] **P-8｜AnnotationView 编辑 / 删除回归（门控）** —— §23.2.7：冷启动逐字段；空串回读为 nil；updatedAt 严格增而 createdAt 与全部冻结字段逐位不变（PIN-1）；blob 逐字节不变；删除后 orphans=0
- [ ] **P-9｜canonical↔view 往返 ≤1 px（门控，仅当 B-37 走 A1 路线）** —— §23.2.8：4 组工况 × 9 点网格全部 ≤1.0 px
  → **原三条的处置（§23 裁决，2026-08-23）**：
    · 「10 个 Pin 写入后检索延迟 < 100 ms」→ **作废**。与 §19.6 P-2 重复，且属同一缺陷类：读取走内存索引（§19.3.2），由构造满足、失败不了。其正确形态是 P-2 的线性度门 `t(N=50) < 4 × t(N=10)`，**P-2 至今未执行**，应在那里跟踪而不是在这里重开一条。
    · 「重访 decode 成功率 ≥ 80%（在相近场景下）」→ **作废，且 ⛔ 不得以任何 IoU 阈值形态复活**（§19.4.7 的「中位数 IoU ≥ 0.7」同样作废）。理由：与 §19.4.3「IoU 不参与任何判定」自相矛盾（§23.2.1）；真机反例证明高 IoU 可以只是「解码器复现了自己的错误」（§23.2.2）；括号里的「相近场景下」本身违反 A-19。由 P-6 / P-7 取代。
    · 「AnnotationView 编辑后 Pin 数据正确更新」→ **意图保留、形态被 P-8 取代**（原文无可执行口径）。
  → **Day 6 出场条件**：P-6 ∧ P-8 ∧（若 A1）P-9 通过；P-7 的完备性门通过且必报部分已报告。
  → ⚠️ 2026-08-23 两次真机会话的读数**不能**用来满足以上任何一条（日志格式将被 B-41 改变）；那两次交付的是 **M-20.1 意义上的激活证据**，不是验收证据。
```

**二、Day 6 `## Builder` 区块**：**不取消勾选**既有三条（Builder 按当时的 §19.4 实现无误；缺的是契约本身）。**追加**五条未勾选项：B-37 / B-38 / B-39 / B-40 / B-41，正文见 §23.4。

**三、Phase 4B 保留项登记处**：新增 **R35**（OPEN，P3，Architect），正文见 §23.5。

---

### 23.7 边界声明

- 本节**未修改**任何 Swift 源文件；未勾选 / 取消勾选 `tasks.md` 任何复选框；未改动 `tasks.md` / `builder_progress.md` / `debug_report.md`。
- **未打开** `SAMDecoder.swift` / `MaskRenderer.swift`；R3 禁令参数、`buildTapAlpha`、`canonicalPoint` 零触碰。
- **未要求、也未建议**对 `DriftDetector.swift` / `ReAnchorLoop.swift` / 一致性否决门的**逻辑或阈值**做任何改动；`reAnchorAcceptIoU = 0.5`、§20.4.1 的三簇与上下界推论、45.5 % / 84 % 两个历史否决率**一字未动**（§23.2.10 只是落账第三个场景）。
- **未重开** Phase 4A Day 2–3（STOP RULE 继续有效）；未重新触发任何 re-anchor 重测。
- **不新增队列、不新增锁**：B-40 的第三个观测钩子与 `PinRevisitTracker` 的释放点全部在既有主线程块内；B-37 的 A1 路线是 `FrameGeometry` 内一个纯函数。
- **未原地改写 §19**：冲突处（§19.4.3 的「展示」、§19.4.4 的 R-A 行与结局表、§19.4.6 的日志格式、§19.4.7 全节）均以「本节取代」明示，原文保留可查。
- 本节引用的两次真机会话为**转录等级证据**（同 §36/§38/§39 的口径），⛔ 不得被转述为归档日志等级；本节所有裁决**不依赖**这两次会话的任何具体数值 —— Ruling B 的第一层理由（§23.2.1）是纯文本自相矛盾，不需要数据。

### 23.8 Deliverable 确认

- [x] §23.1 Ruling A：补出第三条机制（重访产物**无任何锚点标记、无在途反馈**，源于 `viewPoint: nil`）并定性为 as-built 与 §19.4.4 的偏离
- [x] §23.1.2/23.1.3 身份显示的线：**provenance 可陈述 / identity 不可陈述**，落为可执行判定式 **PIN-7** + 逐项裁定表
- [x] §23.1.4 重访标记规格（B-37）：必须有标记；A1/A2 两条允许路线与各自的记录义务；字形 ⛔ 非 📌；允许/禁止措辞表 + 「tag 只能出现在 `From Pin “…”` 内」构词规则
- [x] §23.1.5 把张力说透：拒绝「📌 + 原 tag」的**强理由**是它会说一句可判定的假话，而不只是同一性宣称
- [x] §23.1.6 三处更正：IoU ⛔ 不入用户 UI；保存重访产物 ⇒ 新记录不回写；R-A 行取代
- [x] §23.1.7 G4 淘汰 UX：⛔ 不设特例（维持 §22.2.4 与 §3.2）；改在呈现层给**对一切淘汰生效**的回执（B-38），零新增池状态
- [x] §23.1.8 重启后无标记 ≠ 数据丢失：Pin List 条数徽标（B-39）；⛔ 拒绝把已存 Pin 叠在相机画面上
- [x] §23.1.9 **新结局 R-D**（重访解析为 promote）+ 四条可核实后果（假文案 / 丢日志行 / 64 KB 泄漏 / 标志污染普通 tap）+ B-40
- [x] §23.2.1 Ruling B 第一层：**§19.4.7 与 §19.4.3 自相矛盾**，不需要任何数据即可作废
- [x] §23.2.2 第二层：0.82 反例（复现自己的错误）+ 0.61 误杀 + 「在自身前提下应 ≈1.00」⇒ 阈值**三个方向同时错**
- [x] §23.2.3 **A-19**（场景相关量不得充当门控判据）+ 与 A-7 的分工 + 自洽性旁证
- [x] §23.2.4 日志格式取代（B-41）：`seq` 使完备性可判定，`pt/pin` 使坐标派生可核验，四结局单一格式
- [x] §23.2.5–23.2.8 **P-6 / P-7 / P-8 / P-9** 四条可直接执行的判据（每条都对自身走过 A-7 与 A-19），含明确不可判据化的两项（G3、`geo=remapped`）及其理由
- [x] §23.2.9 出场条件与执行顺序；⚠️ 既有两次会话属**激活证据**非验收证据
- [x] §23.2.10 相邻证据落账：否决门首次正确开火（0.05，第三次确认分簇）+ 否决率是场景属性（引 §21.4.1，不改数字）+ **正式撤回单调衰减论断**并给出准确陈述与自我批评
- [x] §23.3 §19.4 被设备证据判为错误/欠规定的 9 处清单
- [x] §23.4 Builder 变更清单 B-37…B-41 + 两处追认（`handleTap(fromPin:store:)` 签名、`PinRevisitTracker` 唯一安装者）
- [x] §23.5 PIN-7 / PIN-8 / A-19 / M-23.1 / R35 汇总；R21/R25/R28/R32/R34 状态**均不变**
- [x] §23.6 tasks.md 的逐条应改内容（含三条复选框的作废理由与替换文本）——**本节不代改**
- [x] §23.7 边界声明：未碰冻结文件、未碰 re-anchor 逻辑与阈值、不新增队列/锁、未重开 Day 2–3、证据等级如实声明

---

END OF PHASE 4B DAY 6 ARCHITECT RULING（§23 重访身份显示边界 + Day 6 验收判据）

---
